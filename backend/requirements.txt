fastapi>=0.115
uvicorn[standard]>=0.34
asyncpg>=0.30
pydantic>=2.10
PyJWT>=2.10
# Google IDトークン（RS256）の署名検証に必要。無いと PyJWT が RS256 を扱えない
cryptography>=44
httpx>=0.28
# Windowsにはtzdataが同梱されないためzoneinfoが失敗する（A-20検索のJST日付境界計算に使用）
tzdata>=2025.1
# A-61 アイコンアップロード（multipart/form-data）のパースに必要
python-multipart>=0.0.20
# チャンネルAIの応答生成（AIサポート10章）。Keirekiと同じ版指定
openai>=2.9,<3
# 層2ドキュメントQ&Aの索引化（Slice 3）。アップロードされたPDF/Wordからテキストを抽出する
pypdf>=5.1
python-docx>=1.1
# デスクトップ通知②（Web Push、2026-09-11）。VAPID署名・購読先への暗号化送信
pywebpush>=2.5
