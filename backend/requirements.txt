fastapi>=0.115
uvicorn[standard]>=0.34
asyncpg>=0.30
pydantic>=2.10
PyJWT>=2.10
# Google IDトークン（RS256）の署名検証に必要。無いと PyJWT が RS256 を扱えない
cryptography>=44
httpx>=0.28
# Windowsにはtzdataが同梱されないためzoneinfoが失敗する（A-20検索のJST日付境界計算に使用）
tzdata>=2025.1
