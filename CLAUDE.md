# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 概要

社内向けAIネイティブチャットシステム「Kogack」の設計ドキュメント一式と実装（新人社員向けAI開発研修の成果物）。既存Slackと併存しつつ、チャンネルにAIエージェントが常駐して質問回答・依頼対応・社内システム操作を行う社内Webシステム。設計フェーズ（`docs/`）は完成しており、2026-08-26より**実装フェーズに着手**した（`backend/` `frontend/`）。姉妹プロジェクトの社員経歴書管理システム「Keireki」（`c:\Users\pc-000688\Documents\keireki`、リポジトリ外）を技術構成・実装パターンのテンプレートとして踏襲する（詳細は「実装状況」節）。`docs/index.html` をブラウザで開くと全設計ドキュメントにアクセスできる。

技術構成（既存システム LCC CRM を踏襲、REQ-N-07）: React 19 + TypeScript + Vite + Tailwind CSS + react-router + SWR / FastAPI (Python) + Uvicorn + asyncpg / PostgreSQL (Supabase・全テーブルRLS有効) / Google OAuth 2.0 + セッションJWT（HttpOnly Cookie、許可ドメイン: kogasoftware.com） / OpenAI API（Chat Completions + Function Calling） / Google Drive API（文書参照・権限確認） / リアルタイム配信は3秒間隔ポーリング（WebSocket等は使わない簡易方式。LCC CRMに実装例がないためここだけ基本設計書で個別選定） / Supabase Storage / ホスティングはFly.io（アプリ kogack=本番 / kogack-staging=ステージング。LCC CRM踏襲の既定はKoyebだったが、2026-09-04にユーザーの意向で変更。「Keirekiからの意図的な相違点」参照）。

### ロール（3ロール制の実装表現）

グローバルなロールは **member / admin の2値のみ**。「チャンネル管理者（chadmin）」はチャンネル単位で付与されるため、users テーブルの3値カラムでは表現せず `channel_members.is_channel_admin` で表現する2階層構成（設計判断は基本設計書6.2節）。認可判定はサーバー側（`backend/auth_helpers.py` の `require_auth`/`require_roles`）で行い、フロントの表示制御は補助に留める方針（REQ-N-02）。

### 想定規模・現状のリリース計画

想定利用者200名。**2026年9月末に初期リリース**予定で、第0層（チャンネル・スレッド・DM・検索・ファイル共有）と第1層（AIのキャラクタ・スキル・振る舞い・反応設定）はほぼ全て実装、第2層（ドキュメントQ&A・閲覧権限フィルタリング）も核として実装するが、第3層（社内システムへの登録・変更操作）は座席予約システムの**参照のみ**を追加しスマートフォン対応とあわせて対象外とする（要件定義書3.2節）。**自分たちだけでは決めきれない事項は千田氏との別途協議として要件定義書8.2節に明記してあり、この一覧は今のところ2件（AI利用コスト上限到達時の挙動、初期リリースの先行運用対象部署・人数）**。

## ドキュメント構成（変更前に該当ドキュメントを参照すること）

| # | 内容 | ファイル | 状態 |
|---|---|---|---|
| 1 | 要求仕様書 | `docs/01_要求仕様書.html` | 完成 |
| 2 | 要件定義書 | `docs/02_要件定義書.html` | 完成（一部は千田氏との別途協議事項として明記） |
| 3 | 画面モックアップ（S-01, S-03〜S-08 + 補足01〜06） | `docs/画面モックアップ/00_一覧.html` | 完成 |
| 4 | 基本設計書 | `docs/04_基本設計書.html` | 完成 |
| 5 | 詳細設計書（総論＋4分冊） | `docs/05_詳細設計書.html`（総論: 1・2・5・7・9章）+ `docs/05-1_詳細設計書_DB設計.html`（3章）+ `docs/05-2_詳細設計書_API設計.html`（4・6章）+ `docs/05-3_詳細設計書_画面設計.html`（8・11章）+ `docs/05-4_詳細設計書_AIサポート.html`（10章） | 完成 |
| 6 | 操作マニュアル | `docs/06_操作マニュアル.html` | 未着手 |

詳細設計書は2026-08-24に総論＋4分冊へ分割した（章番号は分冊前のまま維持、改訂履歴・版数は総論に一本化。`docs/_sidenav.js` に5ファイルとも登録済み）。

`README.md` の成果物一覧は2026-08-24時点で `docs/index.html`（改訂履歴 Ver 1.6〜4.17）に追従済み。ドキュメントを追加・完成・分割させたときは両方を同時に更新すること（過去に3・4番の完成が反映漏れになっていたため）。

## ID体系（ドキュメント間の相互参照）

REQ-xx（要求）/ F-xx（機能）/ S-xx（画面）/ T-xx（テーブル）/ A-xx（API）。上流の要求IDが下流の設計まで追跡できる（トレーサビリティ）ことを確認しながら読む・書くこと。

## 規約

- **共通スタイル**: 設計書は `docs/assets/style.css` を、画面モックアップは `docs/画面モックアップ/mock.css` を使用。ページ個別のスタイルは作らない。
- **サイドナビ**: 各設計書は `<body>` 直下で `_sidenav.js` を読み込む（サブフォルダからは `../_sidenav.js`）。ドキュメントが完成したら `docs/_sidenav.js` の `DOCS` 配列に1エントリ追加する。**画面モックアップ本体（S-0x・補足0x）は固定サイドバーを持つためサイドナビを読み込まない**（リンクのみ張る）。`DOCS` 配列には一覧ページ（`画面モックアップ/00_一覧.html`）のみを掲載し、個々の画面は掲載しない。
- ドキュメント追加時は `docs/index.html` と `README.md` の成果物一覧も更新する（前述のとおり現状ここが滞っている）。
- **検討資料・計画資料は `検討資料/` へ**: AIが作成する提案・検討・判断根拠のドキュメントは正式な成果物（`docs/`）に混ぜず、リポジトリ直下の `検討資料/` に `YYYYMMDD_タイトル.html` で日付順に置き、`検討資料/README.md` の一覧に登録する。各資料の冒頭にステータス（検討中 / 採用 / 見送り）を明記する。`docs/index.html`・サイドナビには載せない。
- **検討資料は承認後も削除しない**: 内容が `docs/` の該当設計書へ取り込まれたら「採用／設計反映済み」に更新するが、ファイル自体は経緯の記録として残す。判断が後で覆った場合も検討資料自体は当時の記録のまま変更せず、`検討資料/README.md` の一覧側にその旨を追記する（例: 一度「見送り」とした非公開チャンネルを後日採用に変えた際の扱い）。
- **設計判断は基本設計書に callout として残す**: 「なぜその設計にしたか」「代替案との比較」は本文の表に埋め込まず、`<div class="callout">`（新規追加分は `callout diff-new`）に「設計判断（〜理由）:」の形式でまとめる（基本設計書6章に多数の実例あり）。理由を後から読んで判断の妥当性を検証できることを優先する。
- **改訂は複数ドキュメントを横断して同じコミットで一貫更新する**: 要求仕様書・要件定義書・基本設計書・画面モックアップのうち影響する全てを同時に改訂し、変更箇所は `class="diff-new"` で明示する。`docs/index.html` の改訂履歴テーブルに1行（版数・日付・内容・関連検討資料へのリンク）を追記する。
- **見送った機能のID（F-xx / T-xx / A-xx）は欠番のまま再利用しない**: 一度採番した後に対象外へ戻した場合でも、そのIDは空けたままにし、将来復活させる際は同じIDを再利用する（例: F-36個人宛リマインダーを対象外に戻した際、T-20・A-57〜A-60は欠番のまま）。
- 実装は 詳細設計書（総論 `docs/05_詳細設計書.html` ＋4分冊。テーブル定義は05-1、API詳細は05-2、画面項目定義・フロントエンドコンポーネント設計は05-3、AIプロンプト設計は05-4、権限制御は総論）に準拠すること。
- **実装は姉妹プロジェクトKeireki（`c:\Users\pc-000688\Documents\keireki`）のパターンをそのまま流用する**: 技術構成・ディレクトリ構成・`database.py`/`auth_helpers.py`/`google_auth.py`/`lib/api.ts`/`hooks/`/`components/ui/`の作り方は自分で選ばずKeirekiを参照する。ただし「実装状況」節の「Keirekiからの意図的な相違点」は必ず踏襲し、Keirekiのコードを無条件にコピーしないこと。
- **実装を進めたら関連ドキュメントを同じコミットで更新する**: 画面・API・テーブルを新規実装したら、この`実装状況`節に追記する。設計自体を変更した場合（設計書に無い挙動を実装する等）は、詳細設計書・基本設計書・画面モックアップ側もあわせて改訂する（前述の規約どおり）。起動方法・ポート・依存関係を変えたら`README.md`の起動手順も更新する。

## 現状のドキュメントから読み取れる主要な設計判断

- **DM（F-05）は一対一固定ではなくグループDM対応**。参加者は `direct_message_members`（T-17）の中間テーブルで管理し、開始時に固定（開始後の参加者追加・削除は対象外）。
- **非公開チャンネル（F-34）は新規テーブルを追加せず**、既存の `channels.is_public` と `channel_members` の枠組みのまま、API層の認可分岐（公開=誰でも自己参加可、非公開=既存参加者が招待）と RLS（`is_public=false` は参加者以外に存在自体が露出しない）で実現する。非公開チャンネルへのメンバー追加は chadmin 限定にせず参加者全員に開放（運用上のボトルネック回避を優先）。
- **メッセージの送信予約（F-35）・定期投稿（F-36）は専用ジョブキュー（Celery等）を導入せず**、FastAPI内蔵の `asyncio` タスク（`scheduled_dispatcher.py` 相当、30秒間隔ポーリング）で実現する。**単一インスタンス運用が前提**であり、将来Fly.ioで複数インスタンスに水平スケールする場合は `UPDATE ... WHERE status='pending' RETURNING` によるアトミックな取得に変更するか専用ワーカーへ切り出す必要がある（基本設計書10章に明記）。
- **BOT投稿（`messages.sender_type='bot'`）は本文中の@メンションでAIエージェントを起動しない**。AIエージェント処理（8章）は `sender_type='human'` の投稿のみをトリガー対象とする一貫原則とし、自動応答トリガー（F-38）もこれを踏襲して連鎖起動を防ぐ。
- **定期投稿の宛先はチャンネルのみ**（個人宛て複数可への拡張は一度実装したが方針転換で対象外に戻した。設計自体は妥当だったため次フェーズ再検討用に検討資料・要件定義書3.2節へ記録を残してある）。
- **自動応答トリガー（F-38）は時刻ベースの定期投稿ディスパッチャとは別方式**: `recurring_posts` のスケジュール列と噛み合わないため新規テーブル `trigger_rules`（T-21）に分離し、メッセージ投稿API（A-11）内で同期的に判定する（レイテンシを気にする必要がなく、投稿直後に反応させたほうが体験に合うため）。
- **アイコン画像（個人・チャンネルAI・BOT/トリガー）は汎用アップロードAPI（A-61）1本に集約**し、権限チェックはアップロードAPI自体ではなく反映先（プロフィール更新・チャンネルAI編集・定期投稿編集）の既存の権限チェックに委ねる。アイコンは会話添付ファイルと異なり全認証済み利用者に公開する（発言者表示のため組織内のあらゆる画面に登場しうる性質のものであり、機密情報ではないと判断）。
- **AI発言の根拠カード等（citation / external_system / quote_reference / pending_action）は `message_blocks` テーブル1本にJSONBペイロードでまとめる**（種類ごとにテーブルを分けない）。フロントは `block_type` に応じたコンポーネント（`components/blocks/` 想定）で描画する設計。
- **pending_actions（実行前確認、F-25）は座席予約の登録操作自体が対象外の現時点から先行してスキーマ・APIを用意**しておき、次フェーズで書き込み系連携が増えたときに使い回す。
- 画面「S-06」は当初「チャンネルAI設定」だったが、チャンネル管理者・定期投稿・自動応答トリガーなどAI設定以外の項目が増えたため**「チャンネル設定」に改称**（画面ID・DB/APIの識別子である `channel_ai_settings` 等は変更していない。ファイル名も `S-06_チャンネル設定.html` に追従済み）。
- 画面「S-02（ワークスペース）」は要件定義書の画面定義としては残るが、単独の空状態画面としては起こさず、S-03以降の各画面に共通のサイドバーとして実装済み扱い（個別モックアップファイルはない）。

## 実装状況（縦切り・画面単位で進行中）

Keirekiと同じ流儀（1画面につきDB・API・画面をひととおり作って動かす→次の画面へ。単体・結合テストは省略し、`start.bat`でローカル起動して手動確認する）で進める。デプロイ（Fly.io/Supabase）はこの縦切りフェーズの外。

- **実装済み**:
  - 認証（A-01〜A-04）、S-01 ログイン（開発用ログインを含む）、T-01 users。ディレクトリ構成・`database.py`・`auth_helpers.py`・`google_auth.py`・`lib/api.ts`・`hooks/useMe.ts`・`ToastProvider`/`ConfirmProvider`はKeirekiの該当ファイルをほぼそのまま移植（2026-08-26）。
  - チャンネル一覧・作成・参加・会話（A-05〜A-08, A-10〜A-11）、S-02 サイドバー（チャンネル一覧のみ。検索欄は未実装。予約バッジは後日F-35実装時に追加）、S-03 チャンネル会話（投稿・3秒ポーリング表示のみ。添付・要約・スレッドは未実装）、T-02 channels・T-03 channel_members・T-05 messages（AI/BOT関連カラムは先に用意のみ、未使用）。補足01（チャンネルに参加/作成）は簡易版（非公開チャンネルの招待フローは未実装、公開チャンネルの参加・作成のみ）（2026-08-26）。
  - DM（A-16〜A-19）、A-68 利用者検索（DM相手選択専用）、S-02 サイドバーのDM一覧、DM会話画面（`DmView.tsx`）、補足02 DM相手を選ぶ（`DmPickerModal.tsx`、既存DMタグ・無効化アカウントの選択不可を含む）、T-04 direct_messages・T-17 direct_message_members・T-05 messages（dm_id側）。DM開始（A-17）は呼び出し元を含む参加者集合の完全一致で既存DMを再利用し、グループDM（3名以上）にも対応済み。`useMessages`フックはチャンネル/DM共通のbasePath方式に一般化した（2026-08-26）。
  - S-04 スレッド表示（A-13/A-14。`routers/messages.py`、`require_thread_access`）とA-10/A-18への`thread_reply_count`付与。チャンネル・DMどちらの発言にもぶら下がれる汎用設計（元発言のchannel_id/dm_idに応じて権限判定を分岐）だが、モックアップ・基本設計書の例はチャンネルのみのため、DM側は設計の自然な拡張として実装した位置づけ。スレッド返信は本体タイムライン（A-10/A-18）から`thread_parent_id IS NULL`で除外する（基本設計書8.2節「スレッド内のやり取りは本体タイムラインに流れない」）。フロントは`MessageList`/`Composer`をS-03・S-04共有コンポーネントとして`components/`へ切り出し、`ThreadPanel.tsx`が`?thread=`クエリパラメータ（`useSearchParams`）でChannelView/DmViewの右側に重畳表示する（2026-08-26）。
  - S-05 横断検索（A-20。`routers/search.py`）はメッセージ検索のみを実装。pg_trgm（GINインデックス）でbody部分一致、in/from/with/before/after/on/duringのモディファイアはクエリパラメータとしてバックエンド側は完全対応済みだが、フロントはフリーテキスト入力のみで、in:/from:/with:のオートコンプリートUI（F-42のトークン分解・候補ポップオーバー）は未実装。ファイル検索（A-21/A-22未実装のため`counts.file`は常に0）・ドキュメント根拠検索（第2層AI未実装のため`counts.document`は常に0）も未実装。検索結果はチャンネル・DM両方から横断的に拾い、クリックで該当の会話へ遷移する（メッセージ単体へのアンカー・ジャンプは未対応）。サイドバー上部に検索アイコン（`/search`への導線）を追加。Windowsには`zoneinfo`のtzdataが同梱されないため`tzdata`パッケージを依存に追加した（2026-08-26）。
  - S-06 チャンネル設定（`ChannelSettings.tsx`、`/channels/:channelId/settings`）は10タブ構成のうち「チャンネル管理者」タブ（chadmin追加解除＋公開範囲切替）のみ実装。「定期投稿」「自動応答トリガー」「AI設定」7タブはスケジューラ・AIエンジンが未実装のため対象外（実装したらこの節に追記する）。A-46（参加者一覧）・A-47（chadmin追加）・A-48（chadmin解除。最後の1人は400で拒否）・A-49（公開範囲切替）を`channels.py`に追加し、新規`require_channel_admin`（当該chadminまたはシステムadmin。システムadminは非参加チャンネルでもバイパス、chadminは非公開チャンネルの非参加者に404、参加しているが非chadminには403）で保護。ChannelViewヘッダーにchadmin/adminのみ表示の設定リンクを追加。既知の制約: システムadminが一度も参加していない非公開チャンネルはA-06（`require_channel_member`、admin特例なし）で弾かれるためS-06自体を開けない（今回は着手せず、必要になったタイミングでA-06側の扱いを再検討する）（2026-08-26）。**2026-09-02にA-06・A-46のガードを`require_channel_member_or_admin`へ切り替え、この制約を解消した。後述の該当項を参照**。
  - F-43 チャンネル参加の通知（要件定義書v1.18で新規追加。検討経緯は`検討資料/20260826_チャンネル入室通知の追加.html`）。A-08（`join_channel`）の処理をトランザクション化し、参加者追加と同時にT-05へ`sender_type='bot'`・`bot_display_name='システム通知'`の発言「（氏名）さんが参加しました。」を作成する。新規テーブル・列は追加せずF-36/F-38のBOT発言の枠組みをそのまま流用。`_message_out`（`channels.py`・`dms.py`・`messages.py`の3箇所）はBOT発言時に`sender_name`を`bot_display_name`から補うよう修正（従来は`sender_user_id`が無いBOT発言のsender_nameが常にNULLになる欠陥があったが、今回初めて実際にBOT発言を生成したため顕在化し、あわせて修正した）。フロントの`MessageList`にBOTバッジ表示を追加。チャンネル作成時の自動参加（A-07）は対象外、DMは参加者追加の経路が無いため対象外（2026-08-26）。
  - S-08 管理コンソール（`AdminConsole.tsx`、`/admin`）は4タブ構成のうち「利用者管理」タブのみ実装。「ドキュメント参照範囲」「AI利用状況・コスト」「監査ログ」の3タブはDrive連携・AI呼び出し・監査ログ基盤が未実装のため対象外（実装したらこの節に追記する）。A-36（一覧）・A-37（ロール変更・有効/無効化。自分自身の無効化は400で拒否）を`routers/admin.py`に追加し、`require_roles("admin")`で保護。あわせてT-01に`last_login_at`列を追加し、Google OAuthコールバック・dev-loginの両方でログイン成功時に更新するようにした（05-3画面設計に記載済みだがDB/API側が未反映だった抜けをbackfill。F-42の際のA-68backfillと同種の対応で、検討資料は起こしていない）。chadmin_channelsは参考表示のみ（変更はS-06から行う）。フロントはLayoutサイドバーを`/admin`表示中は利用者管理ナビに差し替え、フッターにadmin限定の「管理コンソール」導線を追加。非adminのアクセスはA-36の403を検知してワークスペースへトースト通知付きでリダイレクト（基本設計書4.2節「設計判断」）（2026-08-26）。
  - S-08利用者管理の管理者ロックアウト防止を自己ロール降格にも拡張（詳細設計書API設計v1.3）。実運用中に唯一の管理者（石井直樹）が自分自身をmemberへ降格し、以後S-08を開けなくなる事象が発生したため、A-37に「自分自身のroleをmemberにする操作は400で拒否」を追加（既存の自己無効化拒否と同じ考え方）。フロントも自分自身の行のロール選択を無効化し、そもそも操作できないようにした（2026-08-26）。
  - A-12 メッセージ削除（論理削除。基本設計書8章にAPI一覧のみ記載で詳細未反映だった抜けをbackfill、詳細設計書API設計v1.4）。`routers/messages.py`に追加し、投稿者本人またはadminのみ許可（参加していない会話の発言は404、参加しているが権限が無い場合は403）。チャンネル・DM・スレッド返信・元発言のいずれも同じエンドポイントで扱う。フロントは`MessageList`にホバー時の「削除」ボタンを追加し（投稿者本人またはadminにのみ表示）、`useConfirm`で確認後に削除する。`useMessages`のsinceポーリングは既存キャッシュ行の消失を検知できないため、`bumpThreadReplyCount`と同じ考え方で`removeMessage`（一覧からその場で除外）・`decrementThreadReplyCount`（スレッド内で返信を削除した際に元発言の件数をその場で-1）を追加した（2026-08-27）。
  - サイドバーの未読バッジ（基本設計書4.2節「サイドバー」に記載済みだったがDB/API設計が未反映だった抜けをbackfill、基本設計書v1.16・詳細設計書DB設計v1.2・API設計v1.5）。新規T-22 read_states（利用者ごとの最終既読時刻のみを保持。メッセージ単位では管理しない設計判断は基本設計書6.2節）、新規API A-69（チャンネル既読化）・A-70（DM既読化）を追加し、A-05のjoined・A-16の各itemに`unread_count`（自分以外が送った・スレッド返信ではない・既読時刻または参加時刻より後の発言数）を追加した。フロントは`Layout.tsx`のサイドバーで未読のチャンネル/DM名を太字にし件数バッジを表示、`ChannelView`/`DmView`がマウント時・新着メッセージ受信時（`messages.length`変化）に既読化APIを呼ぶ。バッジをリアルタイムに追従させるため`useChannels`/`useDms`を3秒間隔ポーリング化した（従来は`useSWR`の既定挙動のみで自動更新されなかった）（2026-08-27）。
  - 実装済み画面（S-01〜S-06, S-08）の見た目を画面モックアップに合わせて統一（機能追加は無し、CSS/Tailwindクラスのみの変更）。`frontend/src/index.css`に`@theme`でモックアップの配色トークン（accent/surface/line/ink/member/chadmin/admin/bot/ok/off/danger の各色）を定義し、以後は`bg-slate-*`等のTailwind既定色ではなくこれらのトークンを使う。`lib/avatarColor.ts`で発言者・DM相手・利用者ごとに色を固定するハッシュ関数を共通化（Layout・MessageList・ThreadPanel・ChannelSettings・AdminConsole・DmPickerModalで共有）。あわせて`h-[calc(100vh-3.5rem)]`という誤った高さ指定（Layoutのmainに実際には無い3.5rem分のトップバー分を引いていた）を`h-full`に修正した（ChannelView/DmView/SearchView/ChannelSettings/AdminConsoleの5箇所）。ブラウザでの見た目は未確認（`tsc -b`・`vite build`のみで検証）（2026-08-27）。
  - 補足03 メンバー一覧（`MembersModal.tsx`）を追加し、S-03ヘッダーの参加人数（`channel.member_count`）をクリックして開けるようにした。chadmin/adminバッジ・無効化アカウント表示のため、A-46（参加者一覧）のレスポンスに`role`・`is_active`を追加。非公開チャンネルのみ「＋メンバーを追加」を表示し、これに伴いA-08（参加）を拡張して**非公開チャンネルの招待フロー（既存参加者がuser_id指定で追加）を実装**した（従来は公開チャンネルの自己参加のみだった欠けを解消）。非参加者からの招待呼び出しは404（存在を伏せる）、招待時の入室通知（F-43）は追加された本人の氏名を使う。管理者（chadmin）の変更はここでは行わずS-06に一本化（詳細設計書API設計v1.6）（2026-08-27）。
  - `MessageList`に画面モックアップと同じ日付区切り（「2026年8月19日」＋左右の罫線）を追加。前のメッセージと日付（現地時間）が変わったタイミングで自動的に挿入する。S-04スレッド返信欄には表示しない（`showDaySeparators={false}`、モックアップに合わせた挙動）（2026-08-27）。
  - F-41 @メンションの入力補助を2点追加（`Composer.tsx`）。(1) 全角「＠」もIME入力時に紛れやすいため半角「@」と同様にメンション候補ポップオーバーの起動文字として扱う。(2) 入力欄下のツールバーに画面モックアップS-03の`mention-btn`に相当するメンションボタンを追加し、押下でカーソル位置に「@」を挿入してポップオーバーを開く（直前が空白でない場合は半角空白を補ってから挿入し、`detectMentionQuery`の起動条件を満たす）。いずれもフロントの入力検知のみの変更で、A-11/A-14のリクエスト形・T-07のデータ形は変わらないためAPI設計書側の改訂は無い（2026-08-27）。
  - F-41 @メンション。新規T-07 message_blocks（block_type='mention'のみ実際に作成。citation/external_system/quote_reference/pending_actionはAIサポート未実装のため対象外）と共通処理`backend/mentions.py`を追加し、A-11（`channels.py`）・A-14（`messages.py`）の両方から呼び出す。投稿と同一トランザクションで、リクエストの`mentions`のうち当該チャンネルの参加者（A-46で判定）であるものだけをT-07へ保存し、参加者でないtarget_user_idは黙って除外する（対象外の指定を理由に投稿自体を拒否しない）。A-10・A-13は`fetch_blocks_grouped`でN+1を避けつつ`blocks`を一括付与。DM発言（元発言がDMのスレッド返信を含む）はメンション候補元（A-46）が存在しないため常に`blocks`空、`mentions`を送っても無視する。フロントは`Composer.tsx`を全面書き換えし、「@」入力でチャンネル参加者（有効アカウントのみ）を絞り込むオートコンプリート（↑↓/Enter/Tab/Escapeのキーボード操作、選択後は本文中の`@氏名`テキストが手動削除されたらメンションとして送らない整合性チェック付き）を実装。`MessageList`の`renderMessageBody`が本文中の`@display_name_snapshot`をハイライト表示し、`target_user_id`をチャンネル参加者一覧で解決した現在の表示名で描画する（A-62プロフィール編集が未実装のため実際には常にsnapshotと現在名は一致する）。AIへのメンション検知・自動応答トリガー判定との連携はAIサポート未実装のため対象外。詳細設計書API設計v1.7（2026-08-27）。
  - プロフィール編集（F-37 画像アップロード・F-39 表示名変更・F-40 プロフィール確認、補足06）。新規A-61（`routers/icons.py`、`POST/GET /api/icons`）・A-62（`PUT /api/users/me`）・A-67（`GET /api/users/{id}`、いずれも`routers/users.py`）を追加。A-61は本番はSupabase Storageを想定する設計（基本設計書2.2節）だが、このスライスはDATABASE_URLと同じ考え方でローカル開発を優先し`backend/uploads/icons`へのディスク保存で代替した（`.gitignore`に追加。Koyeb実配備時は単一コンテナに永続ディスクの保証が無いためSupabase Storageへの置き換えが必要、という制約を明記）。アップロード（multipart/form-data）のパースに`python-multipart`が必要だったため`requirements.txt`に追加（インストール漏れで一時的にuvicorn --reloadが例外を吐いて無応答になったため導入時に判明）。A-62は`role`/`is_active`同様name/picture_urlいずれも省略可の部分更新（A-37と同じ実装パターン）。A-67は所属チャンネル等を含めない（非公開チャンネルの存在が間接的に漏れるのを避けるため。基本設計書5.21節「設計判断」）。フロントは`ProfileEditModal.tsx`（補足06。ヘッダーのユーザーメニューという設計だが、このアプリの実装はサイドバー下部のユーザー情報表示をその代替として開く）とA-61→A-62の順で呼ぶ保存フロー、`ProfileCard.tsx`（F-40。S-03の発言者アイコン・表示名クリックで開くポップオーバー、`useUserProfile`フックでA-67を取得）を新規追加し、`MessageList`・`ThreadPanel`（元発言ヘッダー）に組み込んだ。ProfileCardは設計書上はS-03専用だが、MessageListはS-03・S-04・DM会話の共通コンポーネントでありA-67に閲覧側の絞り込みが無いため、スレッド返信・DM会話でも自然に動作する（スレッドをDM発言にも対応させた際と同じ、設計の自然な拡張という位置づけ）。これによりF-41の「target_user_idを現在の表示名で解決する」ロジックが実際に意味を持つようになった（従来はA-62が無く常にdisplay_name_snapshotと現在名が一致していた）。詳細設計書05-2 API設計・05-3画面設計は既にA-61/A-62/A-67・ProfileCard/ProfileEditModalの仕様を記載済みだったため、ドキュメント側の追記は無い（2026-08-27）。
  - バグ修正: 上記プロフィール画像アップロードを実装した直後、チャンネル会話・スレッド・DMのアイコンがサイドバーとは異なり画像に反映されないことが判明した。原因はA-10/A-13/A-18のレスポンス（`_message_out`、`channels.py`/`messages.py`/`dms.py`）が発言者の`picture_url`を一切含んでおらず、`MessageList`の`Avatar`が常に色付き頭文字の丸アイコンしか描画していなかったこと（画面設計11.6節のAvatarコンポーネント定義は「メッセージ一覧」を含む全画面での画像フォールバック共有を明記しており、この点で実装が設計から漏れていた）。3ルーターの`_MESSAGES_SELECT`にJOIN先の`u.picture_url AS sender_picture_url`を追加し、投稿直後のレスポンス（`post_message`/`post_reply`）にも`user.picture_url`を含めるよう修正。BOT/AI発言は`sender_user_id`が無いためJOIN結果が自然にNULLになり、追加の分岐は不要だった。フロントは`Message`型に`sender_picture_url`を追加し、`MessageList`の`Avatar`が値があれば`<img>`、無ければ従来通り色付き頭文字にフォールバックするよう変更（`ThreadPanel`は同じ`Avatar`を再利用しているため追加対応不要）。curlで新規投稿・A-10一覧取得の両方に`sender_picture_url`が含まれることを確認した（2026-08-27）。
  - バグ修正: サイドバーDM一覧のアイコンも同様に画像が反映されていなかった。こちらはA-16（`GET /api/dms`）のレスポンス自体は元から`members[].picture_url`を含んでいたため、バックエンド側の抜けは無く、`Layout.tsx`のDM一覧描画がその値を使わず常に色付き頭文字を表示していたのが原因（会話ログのAvatarとは別の実装のため、直前の修正では直っていなかった）。画像があれば`<img>`、無ければ従来通り色付き頭文字にフォールバックするよう修正した（2026-08-27）。
  - バグ修正: A-04（`GET /api/auth/me`）が`id`を数値のまま返しており、他の全APIの「idは文字列」規約（総論2.4節）に反していた。フロントの`Me.id`型は`string`と宣言されているためコンパイルは通っていたが、実行時は数値だったため`===`比較が常にfalseになり、`me.id`と文字列idを比較する3箇所が無効化されていた: (1) `MessageList`の削除ボタン表示条件（一般ユーザーは自分の発言でも削除ボタンが出ず、admin判定のみ生きていたため「adminは他人の発言を削除できるが一般ユーザーは自分の発言も削除できない」ように見えていた）、(2) `AdminConsole`の管理者ロックアウト防止（自分自身の行のロール選択を無効化する処理が効いていなかった。バックエンド側のA-37の拒否は`user.id`同士の比較のため無事だったが、UI上は選択できてしまっていた）、(3) `DmPickerModal`のDM相手検索結果からの自分自身の除外。`routers/auth.py`の`me()`で`"id": str(user.id)`に修正し、curlで一般ユーザーの自己削除（200）・他人の発言削除の拒否（403）を再確認した（2026-08-27）。
  - F-35 送信予約（S-03・S-04・DM会話の投稿欄、補足04）。新規T-18 scheduled_messages（05-1 DB設計3.13節）を追加し、A-50（作成）・A-51（自分のpending一覧）・A-52（キャンセル）を新規`routers/scheduled_messages.py`に実装した。channel_id/dm_idはT-05と同じくどちらか一方必須、thread_parent_idは任意（スレッド返信の予約にも対応）。参加者チェックはA-11/A-19と同じ基準（非公開チャンネルの非参加者404・公開チャンネルの非参加者403）だが、channel_id/dm_idがURLパスではなくボディの値のため`require_channel_member`等の依存関数はそのまま使えず、A-12削除APIと同様に手動で再現した。実際の発言化は新規`services/scheduled_dispatcher.py`（FastAPI起動時からのasyncioバックグラウンドタスク、30秒間隔ポーリング）が`status='pending' AND scheduled_at<=now()`を検出し、`UPDATE...WHERE status='pending' RETURNING`で二重発言化を防ぎつつ`messages`へ`sender_type='human'`として直接INSERTする（基本設計書10章の設計判断どおり単一インスタンス運用が前提。`main.py`のlifespanで起動/停止）。**@メンションの構造化（T-07 message_blocks）はこのスライスの対象外**とした（T-18にmentions列を追加していないため、本文に「@氏名」と入力すること自体はできてもプレーンテキストのまま発言化され、ハイライト表示・参照解決はされない。要件定義書3.2節のファイル添付対象外と同種の制約として詳細設計書API設計4.9節に明記。**2026-08-28にmentions列を追加し対応済み、後述のF-35メンション対応の項を参照**）。フロントは`Composer.tsx`に送信日時ポップオーバー（🕐ボタン）を追加し、`scheduleTarget`（channel_id/dm_id/thread_parent_id）を渡した3箇所（ChannelView・DmView・ThreadPanel）すべてで動作する。設計書の「ComposerはchannelId/threadParentIdを直接受け取る」という想定とは異なり、このアプリの既存実装（onSendコールバック方式）に合わせて送信予約だけ`scheduleTarget`経由でComposerが直接A-50を呼ぶ形にした。新規`ScheduledMessagesModal.tsx`（補足04）・`useScheduledMessages`フック（A-51、3秒間隔ポーリング）を追加し、`Layout.tsx`のサイドバー上部に予約中バッジ（🕐、件数）を新設してモーダルを共有した。宛先名（チャンネル名／DM相手名）はA-51のレスポンスに含めず、既存の`useChannels`/`useDms`のキャッシュからフロント側で解決する（他のバッジ・一覧系APIと同じ「軽量API＋フロント側で名前解決」の考え方）。curlで作成・一覧・他人からのキャンセル拒否（404）・本人によるキャンセル（204→再キャンセルは404）・過去日時拒否（400）・channel_id/dm_id同時指定拒否（422）、および実際にディスパッチャが発言化する様子（約40秒後に`sender_type='human'`の通常発言としてA-10に現れ、pending一覧から消える）まで一通り確認した（2026-08-27）。
  - AIサポート基盤の初回スライス（基本設計書8章、詳細設計書AIサポート10章）。**チャンネルAIがメンション時に応答するところまでを実装し、ドキュメントQ&A（層2）・スキル・自動対応範囲分類・実行前確認・座席予約連携・コスト上限通知は次スライス以降に持ち越した**（下記スコープ参照。**スキル・引き継ぎ先は2026-09-01にF-12スキル登録として、自動対応範囲分類は2026-09-02にF-16として実装した。後述の該当項を参照**）。新規T-08 channel_ai_settings・T-13 ai_usage_logsを追加。新規`services/ai_client.py`（OpenAI APIクライアント・コスト計算。姉妹プロジェクトKeirekiの`ai_client.py`と責務は同じだが、モデル切替UIを設けず`AI_MODEL`環境変数のみで決定し、単価表も円/1000トークン単位にする意図的な相違点あり。「Keirekiからの意図的な相違点」参照）・新規`services/ai_agent.py`（応答生成本体）を追加し、新規`routers/ai_settings.py`にA-23（GET一括取得。T-08未作成の既存チャンネルはget-or-createで既定値補完）・A-24（有効/無効切替）・A-25（キャラクタ更新）・A-26（振る舞い定義更新）を実装（`require_channel_admin`で保護）。A-07（チャンネル作成）でT-08既定行を同時作成するようにした。トリガーはA-11（`channels.py`のpost_message）内で本文に「@{persona_name}」の文字列一致を検出したときのみ（AIメンションはF-41と異なりID参照化しない。基本設計書5.22節「設計判断」と同じ考え方）、`asyncio.create_task`で非同期起動しA-11自体は応答を待たずに返す（REQ-N-05・8.7節）。生成中はT-05へ`generation_status='generating'`の仮レコードを作成する方式（詳細設計書10.3節で確定済みの方式をそのまま採用）とし、`bot_display_name`/`bot_icon_url`列をBOT発言と共用してペルソナ名・アイコンをスナップショットする。トークン使用量・概算コストはT-13へ記録するが、T-14上限管理・80%到達通知（F-29後半）は対象外。フロントは`MessageList`のAvatarを「アイコンURL優先→種別ごとの既定表示」の順に並べ替え（AI発言のペルソナアイコン表示に対応、従来はsender_typeで固定表示していた）、生成中は画面モックアップと同じ点滅ドット＋「生成中…」を表示（`index.css`に`ai-typing-blink`キーフレーム追加）。S-06に3タブ（基本設定・キャラクタ・振る舞い定義）を追加し、`Layout.tsx`の設定サイドバー・`ChannelSettings.tsx`のタブ切替を`?tab=`クエリパラメータ方式に変更した（従来はチャンネル管理者タブのみでタブ切替UI自体が無かった）。
    - **このスライスのスコープ外**（`services/ai_agent.py`冒頭コメントに集約）: 反応モードは常にメンション時のみ（`reaction_mode`の編集UI自体が無く、常定'mention_only'のまま。「投稿に自ら反応」は未実装。**2026-09-02にF-15反応モード設定として実装した。後述の該当項を参照**）／Function Calling無し（`search_documents`・`get_seat_availability`とも文書索引・座席予約連携が未実装のため、プレーンな会話応答のみ）／スキル（T-11）・自動対応範囲分類（T-12）・引き継ぎ（F-17）・実行前確認（F-25）は対象外（`out_of_scope_policy`・`fallback_handoff_user_id`は列は存在するがロジックから未参照。**スキル・引き継ぎ先は2026-09-01にF-12スキル登録として配線した。自動対応範囲分類は2026-09-02にF-16として配線した。実行前確認は引き続き対象外**）／スレッド返信（A-14）内の@メンションは対象外（チャンネル本体の投稿のみ）。**2026-09-04にユーザーからの明示的な要望でA-14にも配線した。後述の該当項を参照**／F-41のメンションピッカーの候補にチャンネルAIを含めていないため、利用者は手入力で「@{persona_name}」と書く必要がある。**2026-09-02にメンション候補一覧へチャンネルAIを追加し解消した。後述の該当項を参照**。
    - **重要: 実際のOpenAI API呼び出しは未検証**（`OPENAI_API_KEY`が開発環境に設定されていないため）。`services/ai_agent.maybe_trigger`が`is_configured()`で早期returnし何もしないこと、A-23〜A-26のCRUD・権限ガード（403/get-or-create）、メンション検知後にAPIキー未設定時は何も起きないこと、まではcurlで確認済み。実際に応答が生成されるところ（OpenAI API呼び出し・`generation_status`の遷移・T-13記録）はローカルの`.env`に`OPENAI_API_KEY`を設定してから確認する必要がある。**2026-09-02に千田氏から交付されたAPIキーを設定し実機検証済み。後述の「AI API実機検証」の項を参照**。
  - S-06「チャンネル管理者」タブにチャンネル名・説明文の編集、チャンネル削除を追加。チャンネル削除（A-09）は基本設計書の権限マトリクスに以前からAPI一覧のみ記載で未実装だった機能をbackfillし、チャンネル名・説明文の編集は要件定義書・基本設計書・API設計・画面設計・画面モックアップのいずれにも存在しなかった新規機能として追加した（新規API A-71 `PUT /api/channels/{id}`、`name`/`topic`いずれも省略可の部分更新、`name`はA-07作成時と同じ一意制約）。ユーザーからの明示的な要望による追加で、既存の「チャンネル管理者」タブに直接ぶら下げるだけの単純なCRUD拡張（設計上の選択肢が実質1つしかない）と判断し、検討資料は起こさず要件定義書（v1.19。権限マトリクスに新規行）・基本設計書（v1.17）・詳細設計書API設計（v1.10）・詳細設計書画面設計（v1.1）・画面モックアップ（S-06、v3.4）を同じコミットで改訂した。A-09（DELETE）は`channels`テーブルへのハード削除のみで、参加者・発言・メッセージブロック・送信予約・AI設定・AI利用ログ・既読状態はすべてON DELETE CASCADEで連動削除される（`database.py`の6つのFKすべてに設定済みだったため新規のCASCADE追加は不要だった）。スレッド返信のように`messages.thread_parent_id`が自己参照するケースも、親・子が同じ`channel_id`を持ち同一のカスケード操作で同時に削除されるため、FK違反は起きないことをcurlで実際に確認した（スレッド返信ありのチャンネルを削除して検証）。フロントは`ChannelSettings.tsx`に`ChannelInfoForm`（名前・説明編集）と危険操作セクション（削除対象のチャンネル名を再入力しない限り「削除する」ボタンが無効のまま。GitHubのリポジトリ削除と同種の確認パターン）を追加した（2026-08-27）。
  - 補足03「メンバー一覧」を「チャンネル情報」「メンバー」の2タブ構成にし、前者に説明・公開範囲・作成者・作成日時の確認（A-06, A-67）とチャンネルからの退出（新規A-72 `DELETE /api/channels/{id}/members/me`）を追加した。これも要件定義書・基本設計書・API設計・画面設計・画面モックアップのいずれにも記載がなかった新規機能で、検討資料は起こさず（前回のチャンネル名編集と同様、既存の補足03モーダルにタブを足すだけの単純な拡張と判断）各ドキュメントを同じコミットで改訂した（要件定義書v1.20・基本設計書v1.18・API設計v1.11・画面設計v1.2・画面モックアップv3.5）。退出はA-48（chadmin解除）と同じ「最後の管理者は操作できない」防止策を流用し、自分が当該チャンネルの最後のchadminの場合は400で拒否する（最後の1人は必ずchadminであるため、この判定だけで参加者ゼロ化も同時に防げることをcurlで確認済み: 非chadminの退出→成功、単独chadminの退出→拒否、もう1人chadminがいる状態での退出→成功、の3パターンを検証）。作成者名の解決には既存のA-67（`useUserProfile`）を再利用し、作成者が退出済みでも氏名を表示できることを確認した。フロントは`MembersModal`に`initialTab`プロパティを追加し、`ChannelView.tsx`のヘッダーでチャンネル名クリックは「チャンネル情報」タブ、参加人数ボタンクリックは「メンバー」タブでモーダルを開くよう使い分けた（`membersOpen`のbooleanステートを`membersModalTab: 'info' | 'members' | null`に置き換え）（2026-08-28）。
  - チャンネル管理者・システム管理者がメンバーをチャンネルから強制退出させる機能を追加（新規API A-73 `DELETE /api/channels/{id}/members/{userId}`）。前回のチャンネル情報タブ・自己退出（A-72）に続く新規機能で、こちらも検討資料は起こさず要件定義書（v1.21）・基本設計書（v1.19）・API設計（v1.12）・画面設計（v1.3）・画面モックアップ（補足03、v3.6）を同じコミットで改訂した。A-72（`/{channel_id}/members/me`）とA-73（`/{channel_id}/members/{userId}`）はパスが被りうる（FastAPIは`{userId}`をintで宣言しても、ルーティング自体は文字列一致でありint変換の失敗はルート未一致ではなく422になるため）。A-72を先に定義することで「me」という文字列がA-73側に誤って一致することを避けている（`channels.py`にコメントで明記）。実際にcurlで「me」宛のリクエストが正しくA-72に届き続けることを、A-73追加後に再確認した。権限guardは`require_channel_admin`（chadmin/admin。A-48・A-71・A-09と同じ）。対象が最後のチャンネル管理者の場合はA-48/A-72と同じ理由で400拒否し、chadmin同士でも実行できる（chadminという理由だけでは除外しない）。curlで4パターン確認: 非chadminによる強制退出の拒否（403）、chadminによる一般メンバーの退出（成功）、最後のchadminを対象にした拒否（400、システム管理者でも拒否）、もう1人chadminがいる状態での退出（成功）。フロントは`MembersModal`の「メンバー」タブの各行に「退出させる」ボタンを追加し、`(channel.is_channel_admin || me?.role === 'admin')`かつ対象が自分自身でない場合のみ表示する（自己退出は「チャンネル情報」タブの専用ボタンに一本化）（2026-08-28）。
  - F-43「チャンネル参加の通知」を「チャンネル参加・退出の通知」に改称し、A-72（自己退出）・A-73（強制退出）のいずれでも参加通知（F-43）と対になる退出通知「（氏名）さんが退出しました。」をT-05へ作成するようにした。参加通知と同じ`sender_type='bot'`・`bot_display_name='システム通知'`の枠組みをそのまま流用（新規テーブル・列は追加なし）。強制退出であっても本文に退出させた側の氏名は含めない（参加通知が招待者を書かないのと同じ一貫性、「誰が誰を退出させたか」という機微な情報を公開しないための設計判断として基本設計書6.2節に追記）。`leave_channel`・`remove_channel_member`とも、削除と通知作成を`conn.transaction()`で1つのトランザクションにまとめた（従来`leave_channel`はトランザクションを使っていなかったため、この対応であわせてトランザクション化した）。curlで自己退出・強制退出の両方でシステム通知（BOTバッジ付き）が会話ログに現れることを確認済み。要件定義書・基本設計書・API設計・画面モックアップ（S-03に退出通知の発言例を追加）を同じコミットで改訂した（2026-08-28）。
  - A-08（`POST /api/channels/{id}/members`）を拡張し、公開チャンネルでも既存の参加者が他の利用者をuser_id指定で招待できるようにした（従来は招待は非公開チャンネルのみで、公開チャンネルは本人の自己参加のみだった）。ユーザーからの明示的な要望による追加で、非公開チャンネルの招待ロジック（A-08の既存の`else`分岐）と全く同じ考え方を公開チャンネル側にも広げるだけの単純な拡張と判断し、検討資料は起こさず要件定義書（v1.23。権限マトリクスの行を「非公開チャンネルへのメンバー追加」から「チャンネルへの他の利用者の追加（招待）」に改称）・基本設計書（5.14節、v1.21）・詳細設計書API設計（v1.14）・詳細設計書画面設計（補足03項目定義、v1.4）・画面モックアップ（補足03、v3.8）を同じコミットで改訂した。`channels.py`の`join_channel`で、公開チャンネル向けの分岐を「target_user_id省略または自分自身→常に自己参加可」「他者のuser_id指定→呼び出し元が既存参加者であることを要求し、そうでなければ403（メッセージ「他の利用者を追加するには、先に自分がこのチャンネルに参加している必要があります」）」の2パターンに分けた。フロントは`MembersModal.tsx`の「＋メンバーを追加」ボタンの表示条件`!channel.is_public`を削除して全チャンネルで表示するようにし、ヒント文言を「公開チャンネルは招待と自己参加の両方が使える」旨に書き換えた（新規UIコンポーネントは追加せず、既存の非公開チャンネル向け検索・追加フローをそのまま再利用）。curlで3パターン確認: 公開チャンネルの既存参加者による他者の招待（201・入室通知は招待された本人の氏名）、非参加者による他者の招待の拒否（403）、自己参加は従来どおり成功（201）（2026-08-28）。
  - S-03・DM会話に「ここから未読メッセージ」区切り線を追加した。ユーザーからの明示的な要望による追加で、既存の未読バッジ（A-05/A-16のunread_count、T-22 read_states）をフロントエンドのみで区切り線の位置に変換して表示する方式とし、新規API・DB列は追加していない（未読バッジ自体が要件定義書上のF-xxに対応付けられていないため、要件定義書の改訂も無い。基本設計書6.2節に設計判断を追記、v1.22）。新規`hooks/useUnreadDivider.ts`を追加し、`conversationId`ごとに会話を開いた瞬間の`unread_count`を1回だけ記録する（React公式の「レンダー中にstateを調整する」パターンを使い、`useEffect`を介さないため`react(set-state-in-effect)`警告を回避）。表示中に既読化ポーリング（A-69/A-70）で`unread_count`が0に戻っても、この記録値は画面を開いている間ずっと固定され、区切り線の位置が動かない（Slack等の「新着メッセージ」区切り線と同じ考え方）。区切り線を出す対象メッセージは、記録した件数だけ`messages`配列の末尾から遡り、`sender_user_id !== me.id`の発言（本体タイムラインのみで元からスレッド返信は含まれない）を数えて特定する。A-10/A-18が直近50件のみを初期読み込みする制約上、未読件数が読み込み済みの範囲を超える場合は読み込み済みの先頭に区切り線を出すフォールバックを入れた。`ChannelView.tsx`は`useChannels()`の`joined`一覧、`DmView.tsx`は`useDms()`の`dms`一覧から該当チャンネル/DMの`unread_count`を引いて渡す（いずれも既存のポーリングキャッシュを流用し、追加のAPI呼び出しは無い）。`MessageList.tsx`に`unreadDividerMessageId`propを追加し、既存の日付区切り線（`DaySeparator`）と同じ描画パターンで`UnreadDivider`（accent色）を表示する。ブラウザでの見た目は未確認（`tsc -b`・`oxlint`・`vite build`のみで検証、フロントエンドのみの変更でバックエンドAPIは無変更のためcurl検証も対象外）（2026-08-28）。
  - F-43のシステム通知（「（氏名）さんが参加/退出しました。」）に対する返信（A-14）・削除（A-12）を対象外にした。ユーザーから「システム通知への返信・削除機能はない方がよいのでは」との指摘を受けての追加で、参加・退出の記録として信頼できる状態を保つ（返信スレッドが付いたり管理者の裁量で消えたりしない）ことを優先する判断に同意し実装した。フロント（`MessageList.tsx`）は`m.sender_type === 'bot' && m.sender_name === 'システム通知'`の行では返信・削除ボタン自体を出さない。REQ-N-02（権限判定はサーバー側で行う）に従い、バックエンド側（`routers/messages.py`）にも同じ判定をbot_display_name基準で追加し、`delete_message`（A-12）・`post_reply`（A-14）とも対象がシステム通知の場合は400（「システム通知は削除できません」「システム通知には返信できません」）で拒否する。F-36定期投稿・F-38自動応答トリガーは内容のあるBOT発言であり返信・削除の需要があるため対象外にしない（`sender_type='bot'`一律ではなく`bot_display_name`で狭く判定する）。curlで4パターン確認: システム通知への返信拒否（400）、adminによるシステム通知削除拒否（400、admin特例も効かない）、通常の人間発言への返信は従来どおり成功（201）、投稿者本人による通常発言の削除は従来どおり成功（200）。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書の改訂は無い。基本設計書（6.2節に設計判断を追記、v1.23）・詳細設計書API設計（A-12・A-14の説明を更新、v1.15）を同じコミットで更新した（2026-08-28）。
  - F-35 送信予約に@メンション（F-41）を対応させた。ユーザーからの明示的な要望（「メンションしながら予約投稿できるようにしたい」）による追加で、T-18 scheduled_messagesに新規`mentions`列（JSONB、`MentionInput`相当の配列）を追加した。参加者検証はA-11/A-14と異なり予約作成時点（`routers/scheduled_messages.py`の`create_scheduled_message`）では行わず、`mentions`をそのままJSONBへ保持するだけにとどめ、実際に発言化するタイミング（`services/scheduled_dispatcher.py`）まで遅らせてから`insert_mention_blocks`（`mentions.py`、既存のF-41共通処理をそのまま再利用）を呼び出す設計にした。理由は、予約から送信までの間に対象者がチャンネルを抜ける可能性があり、A-11/A-14が「投稿（発言化）時点の参加者であること」を基準にするのと同じ考え方を送信時点に揃えるため（基本設計書6.2節「設計判断」に追記）。DM宛ての予約はA-11/A-14と同じくメンション候補元（A-46）が存在しないため`mentions`を送っても無視する（channel_idがNULLの場合はmentions_to_storeを空にしてから保存）。フロントは`Composer.tsx`の`@`オートコンプリート自体は元々スケジュール中の入力でも動作していた（`mentionCandidates`propがテキストエリア共通のため）ため新規UIは追加せず、`confirmSchedule`（A-50呼び出し）にも`activeMentionsIn(text)`（`send()`と共通化した、本文から手動削除されたメンションを除外するフィルタ）で絞り込んだ`mentions`を含めるようにしただけ。curlで3パターン確認: チャンネル宛て予約（mentions付き）が約40秒後に`sender_type='human'`として発言化され、`blocks`に`block_type='mention'`が正しく付与されること（A-10と同じ形）、DM宛て予約はmentionsを指定しても201で受理されつつ発言化後も`blocks`が常に空であること、既存の即時送信（mentions無し・通常のsend()経路）が引き続き正常に動作すること。要件定義書上のF-xxに対応付けられた変更ではない（F-35自体は要件定義書に記載があるが、この拡張は詳細設計書側のみの改訂）。基本設計書（5.15節・6.2節、v1.24）・詳細設計書DB設計（T-18.mentions列、v1.3）・詳細設計書API設計（A-50、v1.16）を同じコミットで更新した（2026-08-28）。
- バグ修正: `start.bat`をエクスプローラーからダブルクリックで起動すると、`.env`で指定した`BACKEND_PORT`/`FRONTEND_PORT`が反映されず、常にハードコードされた既定値（8011/5174）にフォールバックする不具合が発生していた（ユーザーからの報告を受けて調査）。原因は`chcp`未指定のコンソールが日本語Windowsの既定コードページ（932、Shift-JIS）で起動する一方、`start.bat`・`.env`はいずれもUTF-8（日本語コメントを含む）であり、cmd.exeがUTF-8のバイト列をコードページ932として誤読することで、`.env`の`for /f`読み込み（`BACKEND_PORT`/`FRONTEND_PORT`の行）が正しく行われなくなっていたこと。PowerShellから`chcp 932`で同条件を再現し、`start.bat`冒頭（`@echo off`の直後、他のいかなる非ASCII文字より前）に`chcp 65001 >nul`を追加することで解消することを確認した（`.bat`ファイル自体の日本語コメント行も含め、コードページ切替より前に読まれる行はすべて影響を受けるため、切替は非ASCII文字が一切ないと保証できる最初の行に置く必要がある。切替後にコンソールが元々UTF-8だった場合の非回帰も確認済み）。DB_PORT・DEV_AUTH等、`.env`ファイル内で該当行に物理的に近い日本語コメントが少ない変数は本不具合の影響を受けにくく、症状が`BACKEND_PORT`/`FRONTEND_PORT`のみに見えたため原因特定に時間を要した（2026-08-28）。
- バグ修正: ローカルでのGoogle認証（総論2章、A-01/A-02）が`redirect_uri_mismatch`で失敗する不具合を、実際にGoogleへログインを試す前に発見・修正した（ユーザーから千田氏の手順書に基づく設定確認を依頼された際に、実際のcurl検証で発覚）。`google_auth.py`の`redirect_uri_for()`はリクエストの`Host`ヘッダー（`X-Forwarded-Host`優先）からコールバックURLを動的に組み立てる設計だが、ローカルの2段構成（ブラウザ→Vite:FRONTEND_PORT→FastAPI:BACKEND_PORT）では、`vite.config.ts`の`/api`プロキシが文字列省略形（`'/api': 'http://localhost:${backendPort}'`）のままだと、Viteの既定`changeOrigin: true`によりバックエンドへ転送する際に`Host`ヘッダーが転送先（`localhost:{BACKEND_PORT}`）へ書き換えられてしまい、`redirect_uri_for()`が誤って`http://localhost:8010/...`（バックエンドのポート）を組み立てていた。Google Cloud Consoleにはブラウザが実際にアクセスするフロントエンドのポート（`http://localhost:5173/api/auth/callback`）で登録されているため、このままでは一致せず認証が失敗する状態だった。`vite.config.ts`のプロキシ設定をオブジェクト形式に変更し`changeOrigin: false`を明示することで、転送時も元の`Host`ヘッダー（フロントエンドのポート）を維持するよう修正した。`curl`で`/api/auth/login`のLocationヘッダーに含まれる`redirect_uri`パラメータを確認し、修正前は`backend_port`、修正後はアクセスしたフロントエンドのポートが正しく入ることを確認済み（2026-08-28）。**実際のGoogleアカウントでの認証完了（同意画面の操作等）はブラウザでの手動操作が必要なため未検証**。
- バグ修正: ローカルでのGoogle認証で、リダイレクトURI不一致（上記）を解消した後も`Googleでログイン`が「ログインに失敗しました。もう一度お試しください。」で失敗する不具合を発見・修正した。原因はGoogleが発行したIDトークンの`iat`（発行時刻）検証で、PyJWTの既定`leeway=0`がローカルマシンとGoogle側のわずかな時刻のずれ（NTP同期済みでも数秒程度は生じうる）を一切許容せず、`jwt.exceptions.ImmatureSignatureError: The token is not yet valid (iat)`として正当なトークンまで拒否していたこと。原因調査のため`routers/auth.py`のA-02コールバック処理（state照合失敗・トークン交換/検証失敗の各分岐）に恒久的なログ出力を追加し（`oauth_failed`はフロントに詳細を返さない設計のため、原因追跡にはサーバー側ログが必須と判断）、実際にこのログから`ImmatureSignatureError`を特定した。`google_auth.py`の`verify_id_token`（`jwt.decode`呼び出し）に`leeway=60`（60秒の許容）を追加して解消し、実際にGoogleアカウントでのログインが完了することをユーザー自身のブラウザ操作で確認した（2026-08-28）。
- F-36 定期投稿・F-38 自動応答トリガー（S-06「定期投稿」「自動応答トリガー」タブ）。設計自体は基本設計書5.16節・5.19節、詳細設計書DB設計3.14/3.16節・API設計4.10節に既に完成していたため、その仕様どおりに実装した（検討資料は起こしていない）。新規T-19 recurring_posts・T-21 trigger_rulesを追加し、新規`routers/recurring_posts.py`（A-53〜A-56）・`routers/trigger_rules.py`（A-63〜A-66）を実装（いずれも`require_channel_admin`で保護）。定期投稿の発言化は既存の`services/scheduled_dispatcher.py`に合流させた（F-35と同じ30秒間隔ポーリング、`UPDATE...WHERE...RETURNING`によるアトミックな「取得と同時に次の状態へ進める」パターンを流用。recurring_postsには使い捨てのstatus列が無く「削除するまで繰り返し対象であり続ける」性質のため、'once'は`is_active=false`、それ以外は`next_run_at`を次回時刻へ進める形でこのパターンを再現した）。頻度計算（`_next_run_after`）は daily=+1日、weekly=+7日、monthly=起点(anchor_at)の日にちを基準に翌月へ、該当日が無ければ月末に繰り下げ（例: 1/31起点→2/28→3/31、月をまたいでも起点の日にちに戻ることをPythonで直接検証済み）。自動応答トリガーの判定は新規`services/trigger_matcher.py`が担い、A-11（`channels.py`のpost_message）から同期的に呼び出す（DB1件挿入のみで完結しレイテンシを気にする必要がないため、AI応答のような非同期化はしない）。F-41のAIメンショントリガーと同じ理由でA-11のみを判定対象とし、スレッド返信（A-14）は対象外（画面モックアップの「スレッド内であれば同じスレッドへ」という記述は、A-11がthread_parent_idを持たない以上このスライスでは発生しない分岐であることを基本設計書6.2節に設計判断として明記した）。`messages.recurring_post_id`/`trigger_rule_id`（既存列）に`ON DELETE SET NULL`のFKと、両者が排他であることを示すCHECK制約を新たに付与した（ルール削除後も送信済み発言は本文・アイコンとも残り、参照だけが外れる。基本設計書6.2節に設計判断を追記）。あわせて、BOT発言の送り主アイコン（`bot_icon_url`優先、次点で`bot_icon`絵文字）が`_message_out`（`channels.py`・`messages.py`・`dms.py`の3箇所）のレスポンスに含まれておらず常に🔔表示に固定されていた既知の抜け（5.16/5.19節に「アイコン未実装」と明記されていた制約）をbackfillし、`MessageList.tsx`の`Avatar`・`Message`型にも反映した（F-43システム通知は`bot_icon`が常にnullのため表示は変わらない）。フロントは新規`hooks/useRecurringPosts.ts`・`useTriggerRules.ts`と、`ChannelSettings.tsx`に`RecurringPostsTab`・`TriggerRulesTab`（新規作成・編集を同じフォームで扱い、一覧の「編集」ボタンでフォームへ値を読み込む方式）・共有`IconInput`（絵文字直接入力とA-61画像アップロードの併用、画像優先）を追加し、`Layout.tsx`の設定サイドバーに「その他の設定」グループとして2タブを追加した。curlで一通り確認: トリガー作成→一致する発言でBOT自動返信（201・`sender_type='bot'`・`bot_icon`反映）→`is_active=false`で発火しなくなること→削除後は404、定期投稿作成（`frequency='once'`、35秒後）→ディスパッチャが実際に発言化し`is_active`が自動的にfalseになること→更新（頻度変更・再有効化）→削除後もメッセージ本体と`bot_display_name`/`bot_icon`は残り`recurring_post_id`のみNULLになること、チャンネル削除時にCASCADEでrecurring_posts/trigger_rulesとも連動削除されること、過去日時のanchor_atが400で拒否されること。要件定義書上のF-36/F-38は既に記載済みのため改訂は無い。基本設計書（6.2節に設計判断3件を追記、v1.25）・詳細設計書DB設計（T-19・T-21・T-05の列注記、v1.4）・詳細設計書API設計（A-53〜A-56・A-63〜A-66の削除挙動・既定値を明記、v1.17）を同じコミットで更新した（2026-08-28）。
- バグ修正: S-08利用者管理（`AdminConsole.tsx`）の氏名の横のアイコンが、会話ログ・サイドバー等と異なり常に色付き頭文字表示に固定されていた。以前修正した会話ログ・サイドバーDM一覧のアイコン反映漏れ（picture_url未使用）と同種の問題で、この画面は個別に見落としていた。原因はA-36（`GET /api/admin/users`）のSELECTに`u.picture_url`が含まれておらず、レスポンス自体に画像URLが存在しなかったこと（フロント側の描画ロジックの問題ではなかった点が過去2件とは異なる）。`routers/admin.py`のSELECT・レスポンス整形に`picture_url`を追加し、`AdminUser`型にも反映。フロントは画像があれば`<img>`、無ければ従来どおり色付き頭文字にフォールバックするよう`AdminConsole.tsx`を修正した（他画面のAvatarと同じ「画像優先→フォールバック」の考え方）。curlで確認: A-61でアイコンをアップロード済みの利用者は`picture_url`が返り、Google認証でログインした利用者はGoogle側のプロフィール画像URLがそのまま入り、未設定の利用者は`null`のままであることを確認した（2026-08-28）。
- バグ修正: `ProfileCard`（F-40、発言者アイコン・表示名クリックで開くプロフィールカード）が、画面下寄りの発言（特に一番下の発言）でクリックすると投稿欄（Composer）の裏に隠れてしまい見えづらい不具合を修正した。原因は`ProfileCard`が常にクリックした発言行を基準に`absolute left-5 top-9`で下向きに配置される作りだったため、メッセージ一覧の`overflow-y-auto`スクロール領域の下端を越えた分がクリップされ、実質的にComposerの裏に隠れて見えなくなっていたこと。`ProfileCard`に任意の`anchor`（クリックした要素の`getBoundingClientRect()`）propを追加し、指定時は`createPortal`で`document.body`へ描画した上で`position: fixed`により画面座標で配置するよう変更した。`anchor.bottom`から見積もりカード高さ（200px）を足した位置が画面下端を超える場合は自動的に上開き（`bottom`指定）に切り替える。`MessageList.tsx`はアイコン・表示名クリック時のイベントから`anchor`を取得して渡すよう変更し、`ThreadPanel.tsx`の元発言ヘッダー（常に上部にあり同じ問題が起きない）は`anchor`省略のまま従来の相対配置を維持している（`anchor`未指定時は既存の描画パスに変更なし）。フロントエンドのみの変更でバックエンドAPIは無変更。ブラウザでの実際の見た目は未確認（`tsc`・`oxlint`・`vite build`のみで検証）（2026-08-28）。
- F-07 ファイル共有（S-03・S-04・DM会話の投稿欄、A-20横断検索のファイル検索）。要件定義書・基本設計書5.4節・T-06 message_attachments・A-21/A-22は既に表レベルで定義済みだったため、その仕様に沿って実装した（検討資料は起こしていない）。新規`backend/attachments.py`（`AttachmentInput`・`insert_attachments`・`fetch_attachments_grouped`、`mentions.py`と同じ構成）と新規`routers/attachments.py`（A-21アップロード・A-22ダウンロード）を追加。T-06.message_idはNOT NULLのため、A-21はアップロード時点ではDB行を作らずファイル実体を`backend/uploads/attachments`へ保存する（A-61アイコンと同じくSupabase Storageの本番代替。ローカル開発優先の判断は既存踏襲）のみで、`file_name`/`byte_size`/`storage_path`を返す。クライアントはこれを保持し、実際の投稿（A-11 `channels.py`・A-14 `messages.py`・A-19 `dms.py`）で`attachments`として渡すことで、発言作成と同一トランザクション内でT-06へ書き込む（F-41のメンションがT-07へ書き込むのと同じ「確定後に紐づける」パターン）。ダウンロード（A-22）は`require_thread_access`を`Depends()`を介さず直接呼び出して添付先発言の参加者チェックを再利用し（非公開チャンネル非参加者404・公開チャンネル非参加者403・DM非参加者404）、`FileResponse`の`media_type`を`application/octet-stream`に固定してブラウザでのインライン実行を防いだ。A-20（`search.py`）はこれまで`counts.file`を常に0としていたが、`message_attachments`を`messages`にJOINし`file_name`へILIKE判定する形で実装し、in/with/from/日付系の条件組み立て（`_build_conditions`）をメッセージ検索と共有した。フロントは新規`uploadAttachment`（`lib/api.ts`）、`Composer.tsx`に📎ボタン・アップロード中表示・添付チップ（削除可）を追加し、送信予約（`confirmSchedule`）では添付があれば明示的にエラーを出して拒否する（要件定義書3.2節の対象外制約どおり）。`onSend`コールバックの型に`attachments`を追加し、`ChannelView`・`DmView`・`ThreadPanel`の3箇所で送信時に渡すよう更新した。`MessageList.tsx`に`AttachmentList`（📎アイコン＋ファイル名＋サイズのダウンロードリンク、`/api/attachments/{id}`を直接リンクしCookie経由でA-22の認可を通す）を追加。`SearchView.tsx`もメッセージ・ファイルの2セクション表示に対応した。curlとPython httpx（日本語ファイル名はGit-Bashの引数エンコード崩れを避けるため）で一通り確認: アップロード→チャンネル投稿→A-10一覧に反映→ダウンロード成功（RFC5987の`filename*=utf-8''...`）→非参加者はダウンロード403、スレッド返信（A-14）・DM投稿（A-19）でも同様に添付が機能すること、A-20検索でメッセージ・ファイルの両方（日本語ファイル名含む）がヒットしcounts.fileが実件数を返すこと、チャンネル削除時にCASCADEで添付も連動削除されること。基本設計書（5.4節・6.2節に設計判断を追記、v1.26）・詳細設計書DB設計（T-06、v1.5）・詳細設計書API設計（A-21・A-22・A-20ファイル検索、v1.18）・画面モックアップ（S-03に添付ファイルの表示例、v3.10）を同じコミットで更新した（2026-08-28）。
- バグ修正: `MessageList.tsx`の`Avatar`が、BOT発言（F-36定期投稿・F-38トリガー・F-43システム通知）でも画像アップロード済みなら人間・AIと同じ円形（`rounded-full`）で表示していたため、BOTかどうかがアイコンの形からは判別できていなかった（絵文字のみのBOTは`rounded-[9px]`の角丸四角で表示されており、画像有無で見た目の一貫性が崩れていた）。原因は`sender_picture_url`の有無を種別を問わず最初にチェックする分岐順だったこと（BOT発言も`sender_type in ("ai", "bot")`のとき`bot_icon_url`が`sender_picture_url`に入る、`channels.py`等の`_message_out`の既存仕様）。`sender_type === 'bot'`の判定を先頭に移し、その中で画像（`sender_picture_url`）→絵文字（`bot_icon`）→🔔の優先順位はそのまま維持しつつ、どちらも角丸四角の同じ枠内に収めるよう修正した（ユーザーからの明示的な要望）。S-06の定期投稿/トリガータブのプレビュー（`rounded-[7px]`/`rounded-[5px]`）は元々画像・絵文字とも角丸四角で統一されており対象外。バックエンドAPIは無変更のためcurl検証は対象外（`tsc`・`oxlint`・`vite build`のみで検証）（2026-08-28）。
- UX改善: S-06「定期投稿」「自動応答トリガー」タブで、既存項目の編集画面と新規追加画面が同一フォームの使い回し（見出し・ボタン文言のみ「編集」/「追加」で切り替え）だったため、既存作成済みのものを編集しているのか2個目以降を新規作成しているのか見分けにくいというユーザー指摘を受けて改善した（`ChannelSettings.tsx`）。編集は`ProfileEditModal.tsx`等で確立済みのモーダルパターン（`fixed inset-0`のオーバーレイ＋カード、ヘッダーに✕、フッターにキャンセル/更新するのボタン対）を流用した新規`RecurringPostEditModal`/`TriggerRuleEditModal`のポップアップに切り出し、一覧末尾の常設パネルは新規作成専用（`editing`状態を持たず常にPOST、見出し・ボタン文言も固定）にした。フォーム項目のJSX自体は入力欄が多く（送り主表示名・アイコン・本文・頻度/日時、またはトリガー種別/値・本文・送り主表示名・アイコン）作成パネルと編集モーダルの二重管理を避けたかったため、新規`RecurringPostFormFields`/`TriggerRuleFormFields`（状態を持たない、値とonChangeをpropsで受け取るだけの表示専用コンポーネント）に切り出し、既存の共有`IconInput`と同じ考え方で作成パネル・編集モーダルの両方から呼び出す。編集モーダルの初期値は`item`（対象行）から`useState`の初期値として直接設定し（`useEffect`を介さない、`startEdit`関数が担っていた責務をモーダルのマウント自体に置き換えた形）、対象行の削除時（`remove`）は編集モーダルを開いたままにしないよう`editingItem`をクリアするようにした。新規API・DB変更は無く、画面のみの変更のため要件定義書・基本設計書・詳細設計書・画面モックアップの改訂は無い。バックエンドAPIは無変更のためcurl検証は対象外（`tsc -b`・`vite build`のみで検証。oxlintはこのマシンのApplication Control policyがネイティブバインディングの読み込みをブロックし実行不能だったため今回は省略）（2026-08-31）。
- 層2ドキュメントQ&A（F-19〜F-22）の最初のスライス。実装量が大きく、かつGoogle OAuthスコープ拡張（drive.readonly相当。既存ユーザーへの再同意が発生する）とOpenAI Embeddings APIの実通信検証が前提になることから、ユーザーに着手範囲を確認したうえで「Drive同期・索引・AI検索が不要な部分（フォルダ候補の登録・チャンネルへの割当）」に絞って実装した（索引・埋め込み生成・search_documentsツール・引用表示は次スライス）。新規T-09 doc_folders・T-10 channel_doc_foldersを追加（既存の05-1 DB設計3.9節どおりの設計、スキーマ変更は無し）。S-08に新規タブ「ドキュメント参照範囲」（A-38 GET一覧・A-39 POST追加・A-40 DELETE削除）を追加し、`AdminConsole.tsx`を`?tab=`クエリパラメータ方式のタブ構成（従来はタブ切替UI自体が無く利用者管理のみを常時表示していた）に変更した。フォルダ候補の追加はGoogle Picker（画面設計8.2節が想定するDriveのフォルダ選択画面）ではなく、フォルダのURL・IDを手動貼り付けする簡易フォームとした（Drive OAuthスコープ拡張はA-38〜A-41実装時点でもまだ不要にできたが、Picker連携自体は次スライス）。URL形式の場合は`routers/admin.py`の`_extract_folder_id`が正規表現でIDを抽出する。「今すぐ同期」（A-41）はDrive同期・索引化が未実装のため、S-08側でボタンを無効表示にするに留め、バックエンドにもエンドポイントを実装していない。S-06に新規タブ「参照ドキュメント範囲」（既存A-27 PUT doc-scope）を追加し、`DocScopeTab`（`folder_ids`のチェックボックス選択＋`out_of_scope_policy`のラジオ）を実装した。A-27は送信された`folder_ids`の集合でT-10を洗い替え、存在しないfolder_idは黙って無視する（F-41のメンション対象外指定と同じ考え方）。A-23（GET ai-settings）のレスポンスにも`out_of_scope_policy`・`folder_ids`を追加した（従来はドキュメントQ&A未実装のため意図的に省略していた）。**A-38（GET）はS-06でチャンネル管理者（chadminは多くの場合グローバルにはmember）が候補一覧を選択に使う必要があるため、設計時点のadmin限定からrequire_auth（認証済み全員）へ閲覧範囲を広げた**（フォルダ名・登録者名・使用中チャンネル数のみでチャンネル名等の非公開情報を含まないため情報漏えいにはならないと判断。登録・削除（A-39/A-40）は引き続きadmin限定。基本設計書6.2節に設計判断を追記）。curlで一通り確認: 一般member（admin/chadminいずれでもない）によるA-38取得成功（200、閲覧範囲を広げた効果）・同ユーザーによるA-39作成拒否（403、登録は引き続きadmin限定）、admin作成→URL形式からのID抽出・重複409、A-40削除→使用中チャンネルのfolder_idsからON DELETE CASCADEで自動的に外れること、chadmin（member権限のyamadaをA-47で昇格）によるA-27保存→無効なfolder_idの黙殺・out_of_scope_policyの不正値422まで確認した。基本設計書（6.2節に設計判断2件を追記、v1.27）・詳細設計書API設計（A-27・A-38〜A-40、v1.19）・docs/index.html（Ver 4.29）を同じコミットで更新した。要件定義書・画面モックアップは既存のF-19〜F-22・S-06・S-08の記載どおりで変更なし（2026-08-31）。
- F-42 検索条件モディファイアのオートコンプリートUIを実装した（`SearchView.tsx`）。S-05はこれまでフリーテキスト入力のみで、`in:`/`from:`/`with:`/`before:`/`after:`/`on:`/`during:`はバックエンド（`routers/search.py`）側は既に完全対応済みだったがフロント未実装だった抜けを埋めた（バックエンド・API設計6.5節はいずれも無変更）。`detectModifierQuery`はComposer.tsxのF-41 `detectMentionQuery`と全く同じ考え方（トリガー文字列の直前が空白/先頭であること、クエリに空白が入るまでが1トークン）で、`in:`/`from:`/`with:`の入力中に候補ポップオーバーを開く。候補は`in:`が`useChannels()`の参加チャンネル一覧（クライアント側で前方一致フィルタ）、`from:`/`with:`が既存のA-68 `/api/users?q=`（DmPickerModalと同じ200msデバウンス）。候補選択で本文中のトークンが`type:ラベル`の確定形に置き換わり、`resolved`状態（type→{id, label}）に記録される。検索実行時は`buildQuery`が本文中に確定時と同じ`type:ラベル`部分文字列が残っている場合のみそのモディファイアを有効とし（Composer.tsxの`activeMentionsIn`と同じ、選択後に手で消した場合の整合性チェック）、`before:`/`after:`/`on:`/`during:`は候補解決が不要なため正規表現でその場抽出、残りのフリーテキスト（`"..."`のフレーズ含む）を`q`とする。確定済みの条件はチップとして表示し✕で個別に外せる（外すと本文中のトークン文字列も連動して削除）。検索条件はURLのクエリパラメータ（`in`/`in_label`等、ラベルも保持）に反映し、ページ再読み込み・ブラウザの戻る/進むでも検索欄・チップの見た目を復元できるようにした（`composeInputText`/`composeResolved`が`buildQuery`の逆変換）。`useSearch`フックを`q`単一引数から構造化された`SearchQuery`（in/from/with/before/after/on/during）を受け取る形に拡張した。ドキュメント根拠タブ自体が層2未実装のため、F-42の「モディファイアがあるとドキュメント根拠タブを無効化する」挙動（画面設計6.4節）は対象外のまま。純粋な既存設計（詳細設計書API設計6.5節・画面モックアップS-05）どおりのフロントエンド実装であり新規API・DB変更は無いため、要件定義書・基本設計書・詳細設計書・画面モックアップの改訂は無い。バックエンドAPIは無変更のためcurl検証は対象外。パースロジック（`detectModifierQuery`/`buildQuery`/`composeInputText`/`composeResolved`）はNode上に複製した単体スクリプトで9パターン（トークン検出・複数モディファイアの組み立て・手動削除時の整合性・URLとの往復一貫性・フレーズ透過・二重指定時の片方のみ解決）を確認した上で削除し、`tsc -b`・`vite build`で検証した（oxlintはこのマシンのApplication Control policyにより今回も実行不能）（2026-08-31）。
- F-14（やりとりの要約）を実装した。要件定義書・画面モックアップには「要約ボタンを押した時点までの内容を要約する」とのみ記載があり、結果の見せ方（依頼者本人だけに表示するか、チャンネルに投稿するか）が未確定だったため、着手前にユーザーに確認した（チャンネルへのAI発言として投稿を選択）。`services/ai_agent.py`にAIメンション応答（`_generate_and_post`）と全く同じ枠組みを再利用する`start_summary`/`_generate_summary_and_post`を追加し、共通処理（発言者名解決・T-05行→Chat Completions messages変換）を`_resolve_sender_names`/`_rows_to_chat_messages`として両者から呼べるよう切り出した（既存のメンション応答フローもこの共通化に合わせてリファクタ）。生成中プレースホルダ（`generation_status='generating'`）・T-13コスト記録もメンション応答と共通。要約対象は`thread_id`指定時がそのスレッド全体（元発言＋返信、上限なし）、省略時がチャンネル本体の直近100件（`MAX_SUMMARY_CHANNEL_MESSAGES`）で、いずれも`generation_status IS NULL`で絞り込み生成中の行自身を要約対象から除外する。要約用のシステムプロンプトは通常の応答用（`_build_system_prompt`）とは別に`_build_summary_prompt`を新設し、behavior_prompt等ではなく箇条書き・断定回避・創作禁止の要約方針を指示する。新規A-15（`POST /api/channels/{id}/summarize`、`routers/channels.py`）は`require_channel_member`で保護し、`thread_id`が当該チャンネルの発言でなければ404、AI未設定・チャンネルAI無効時は`SummaryUnavailable`例外経由で400を返す（メンション応答の`maybe_trigger`が条件不成立時に黙って何もしないのと異なり、明示的なボタン操作のため理由を返す）。フロントは`ChannelView.tsx`ヘッダー（チャンネル本体、`thread_id`省略）と`ThreadPanel.tsx`ヘッダー（スレッド全体、`thread_id`指定。`parentMessage.channel_id`がある＝チャンネルのスレッドのときのみボタン表示、DMのスレッドには出さない）の2箇所に「📝 要約」ボタンを追加した。押下後は投稿完了を待たずボタンをすぐ元に戻し（`mutateMessages`/`mutateReplies`で生成中プレースホルダを早めに表示させるのみ）、実際の生成結果は既存の3秒ポーリング・`MessageList`の「生成中」表示にそのまま乗る。curlで一通り確認: AI未設定時の400、`thread_id`が数値でない場合の422、他チャンネルの発言idを指定した場合の404、非参加者の403、`_fetch_summary_source_rows`のSQL（チャンネル本体・スレッド全体の両方）が期待どおりの行を返すこと、既存のAIメンション応答経路がリファクタ後も従来どおり動作すること（`@AI`宛て投稿がAPIキー未設定時に静かに無視されること）を確認した。**実際のOpenAI API呼び出し（要約文の生成）はチャットAI機能と同様に未検証**（`OPENAI_API_KEY`が開発環境に設定されていないため）。**2026-09-02に実機検証済み。後述の「AI API実機検証」の項を参照**。基本設計書（6.2節に設計判断を追記、v1.28）・詳細設計書API設計（A-15、v1.20）・docs/index.html（Ver 4.30）を同じコミットで更新した。要件定義書・画面モックアップ（S-03/S-04の要約ボタン）は既存の記載どおりで変更なし（2026-08-31）。
- F-29 AI利用コストの管理のうち、S-08「AI利用状況・コスト」タブ（A-42/A-43）を実装した。新規T-14 ai_usage_limitsを05-1 DB設計3.11節に定義済みのカラム構成のまま実テーブル化し、`scope='global'`は`scope`列の部分ユニークインデックスで最大1行、`scope='channel'`は`channel_id`列の部分ユニークインデックスでchannel_idごとに最大1行へ制約した（`routers/admin.py`のA-43はこれらをON CONFLICTの対象にして洗い替える。同じscope+channel_idへの複数回PUTで行が増えないことをSQLで確認済み）。A-42（`GET /api/admin/usage?month=YYYY-MM`、month省略時はJSTの当月）はT-13 ai_usage_logsを`total`・`by_channel`・`by_user`の3通りに集計する。旧いレスポンス例はby_channelのみだったが、基本設計書8.6節「月次・チャンネル別・利用者別に集計して表示する」・05-3画面設計「利用者別テーブル」の記載に合わせてby_userを追加した（backfill）。`limits.global`・`limits.channels[]`にそれぞれの`used_pct`（該当月のcost_yen÷monthly_limit_yen×100）を含める。**しきい値（notify_threshold_pct）到達時の通知メール送信・上限到達時の応答停止は実装していない**（要件定義書8.2節のとおり上限到達時の挙動は千田氏との別途協議事項のため、このスライスは設定の保存とused_pct表示のみ行う。notify_emailは保存するだけで実際に送信はしない）。フロントは新規`useUsageStats(month)`フックと`AdminConsole.tsx`の3つ目のタブ`UsageTab`（月選択・概算コスト合計・チャンネル別/利用者別の内訳テーブル・全体上限の編集フォーム`GlobalLimitForm`）を追加した。`GlobalLimitForm`は`ChannelSettings.tsx`のtabコンポーネント群と同じ「非同期データが読み込まれてから条件付きレンダーし、propsからuseStateの初期値を直接設定する」パターン（`{!usage ? <p>読み込み中...</p> : <UsageTabBody usage={usage} .../>}`）でuseEffectを使わずに済ませた。**チャンネル別上限（scope='channel'）の作成・編集UIはこのスライスでは対象外**とした（バックエンドは対応済みで、既存の値があれば一覧に参考表示するのみ。追加するにはAPIを直接呼ぶ必要がある）。curlで一通り確認: 集計値の正確性（3件のテスト用ai_usage_logsを直接INSERTしてcall_count/tokens/cost_yenの合計・チャンネル別・利用者別内訳が一致すること）、A-43の上書き（同じscopeへの2回目のPUTで行が増えないこと）、used_pctの計算、バリデーション（不正なscope・channel_id未指定・monthly_limit_yen≤0がいずれも422）、無関係な月を指定した場合の0件応答、非adminの403を確認した。基本設計書（v1.29）・詳細設計書DB設計（T-14、v1.6）・API設計（A-42・A-43、v1.21）・docs/index.html（Ver 4.31）を同じコミットで更新した。要件定義書・画面モックアップは既存のF-29・S-08の記載どおりで変更なし（2026-08-31）。
- S-08「監査ログ」タブ（A-44、T-16）を実装し、S-08の4タブすべてが揃った。新規`backend/audit_log.py`（`record(conn, event_type, actor_user_id, summary, target_channel_id=None, target_field=None)`のみの薄い共通処理、`mentions.py`等と同じ構成）を追加し、`routers/auth.py`のA-02（OAuthコールバック）・dev-loginの成功時に`event_type='login'`を、`routers/ai_settings.py`のA-24〜A-27の成功時に`event_type='channel_ai_setting_change'`を記録する（A-45 handoffは当時未実装のため対象外だった。**2026-09-01にA-28〜A-30・A-45を実装し、いずれも同じevent_typeで記録するようになった。後述のF-12スキル登録の項を参照**）。`summary`は「どの項目が変更されたか」の説明のみで、変更後の実際の値・差分は含めない（例: A-26は「振る舞い定義を更新しました」であって本文は含めない、05-1 DB設計3.12節「本文差分は保持しない」）。**T-16.target_channel_idはON DELETE CASCADEではなくSET NULLにした**（他の多くのFKはCASCADEだが、監査ログはチャンネル削除後も「いつ・誰が・どの項目を変更したか」の記録自体を残す目的のテーブルであり、CASCADEにすると最も確認したい記録が消えてしまうため。`messages.recurring_post_id`等と同じ「履歴は消さない」設計判断。基本設計書6.2節に追記）。新規A-44（`GET /api/admin/audit-logs`、`routers/admin.py`）は`event_type`/`channel_id`/`actor_user_id`/`after`/`before`で絞り込み、新しい順に1ページ50件返す。フロントは新規`useAuditLogs(filters)`フックと`AdminConsole.tsx`の4つ目のタブ`AuditLogTab`（種別セレクト・実行者セレクト（既存`useAdminUsers()`を再利用）・期間の2つの日付入力）を追加した。対象チャンネルでの絞り込みUIは対象外とし（全チャンネル一覧を返す管理者向けAPIが無いため）、一覧の「対象」列に表示するのみに留めた。**実装中にバグを1件発見・修正**: `before`フィルタの初期実装が`+timedelta(days=1)`していたため「指定日を含まない」という自分で書いたdocstringの意図と逆に「指定日を含む」動作になっていた（`routers/search.py`の`before`と同じ「含まない」規約に合わせて修正）。curlで一通り確認: ログイン2件・チャンネルAI設定変更4件（A-24〜A-27）の記録、`event_type`/`channel_id`/`actor_user_id`/`before`/`after`の各絞り込み、修正後の`before`/`after`の境界値が正しいこと、チャンネル削除後も監査ログ行がtarget_channel_id=NULLで残ること（CASCADEではなくSET NULLが効いていることの直接確認）、不正なevent_type・日付形式が422、非adminが403を確認した。基本設計書（6.2節に設計判断を追記、v1.30）・詳細設計書DB設計（T-16、v1.7）・API設計（A-44、v1.22）・docs/index.html（Ver 4.32）を同じコミットで更新した。要件定義書・画面モックアップは既存のF-2x・S-08の記載どおりで変更なし（2026-09-01）。
- S-05検索結果のタブ切替UIを実装した（F-06、詳細設計書API設計6.4節）。従来「メッセージ」「ファイル」を縦に並べているだけだった表示を、実際にクリックで切り替わるタブ（すべて/メッセージ/ファイル、件数バッジ付き）にした。設計どおり「初回検索でtype=allとして3種すべてを取得し、タブ切替のたびに再検索はしない」方式とし、`SearchResultsTabs`が`result.items`をクライアント側で`type`によって絞り込むだけに留めた（1タブ最大20件の「もっと見る」追加取得は今回は対象外）。ドキュメント根拠タブは層2検索が未実装のため常に無効表示（グレー・カーソル`not-allowed`・ツールチップ）とした。新しい検索を実行するたびに`activeTab`を「すべて」へ戻すため、`SearchResultsTabs`を`key={searchParams.toString()}`で新しい検索ごとに再マウントする方式にし、useEffectでの同期を避けた（Reactの「keyで状態をリセットする」公式パターン）。**このタスクでは初めてPlaywrightで実際にブラウザを操作して検証した**（`run`スキル経由。chromium-cliが使えない環境だったため生Playwrightスクリプトで代替、`playwright`パッケージと対応Chromiumバイナリをこのマシンに導入）。curlでチャンネル・メッセージ2件・添付ファイル1件（同じキーワードを含む）を作成し、そのセッションCookieをPlaywrightのブラウザコンテキストへ注入して`/search`にアクセス、実際に「メッセージ」「ファイル」タブをクリックして表示内容が正しく絞り込まれること（メッセージタブにファイル名が出ない、ファイルタブに本文が出ない等）、「すべて」に戻すと両方表示されること、ドキュメント根拠タブが無効化されていること、タブ切替時に`/api/search`への新規リクエストが発生しないこと（Playwrightのnetworkイベントで0件を確認）、consoleエラーが0件であることをスクリーンショット付きで確認した。純粋な既存設計（詳細設計書API設計6.4節・05-3画面設計）どおりのフロントエンド実装であり新規API・DB変更は無いため、設計文書側の改訂は無い。検証用に作成したテストデータ・スクリプトはすべて削除済み（2026-09-01）。
- S-08のチャンネル別上限編集UI（A-43 scope='channel'）と監査ログの対象チャンネル絞り込みUI（A-44 channel_id）を実装した。いずれもバックエンドは既に実装済みで、フロントにUIを足すだけの追加。**チャンネル別上限**: `ChannelLimitsSection`（`AdminConsole.tsx`）が既存の上限一覧（編集ボタン付き）と新規追加パネルを表示する。編集はモーダル（`ChannelLimitEditModal`）、追加は常設パネル（`ChannelLimitAddPanel`）で画面を分け、S-06定期投稿/トリガーで一度指摘を受けた「新規作成と編集の見分けにくさ」と同じ問題を最初から避けた。追加候補は当月`usage.by_channel`のうち未設定のチャンネルに限定（全チャンネル一覧を返す管理者向けAPIが無いため。月を変えれば候補も変わる）。月次上限額・通知しきい値・通知先の3項目入力欄は`UsageLimitFormFields`として切り出し、`GlobalLimitForm`・`ChannelLimitAddPanel`・`ChannelLimitEditModal`の3箇所で共有した（`GlobalLimitForm`もこの切り出しにあわせてリファクタ）。**監査ログの対象チャンネル絞り込み**: `useAuditLogs({})`（フィルタ無し）を追加でもう1回呼び、そこに含まれる`target_channel_id`から絞り込み用`<select>`の選択肢を作る（チャンネル一覧を返す管理者向けAPIが無いための代替。channel_idで絞り込み中でも選択肢自体は消えないよう、表示用の一覧とは別に取得する設計）。**このタスクもPlaywrightで実際にブラウザ操作して検証した**: curlでテスト用チャンネル・当月AI利用ログ・監査ログ1件（A-24呼び出し）を作成し、そのセッションをブラウザに注入して(1)チャンネル別上限の追加→一覧に反映→編集モーダルで金額変更→一覧に反映、(2)監査ログの対象チャンネルセレクトにそのチャンネルが選択肢として現れる→選択すると該当行のみに絞り込まれる（8件→1件）、をスクリーンショット付きで確認し、途中経過も含めてDBに正しく永続化されていることを直接SQLで確認した。consoleエラー0件。テストデータ・検証スクリプトはすべて削除済み。新規API・DB変更は無いため設計文書側の改訂は無い（2026-09-01）。
- S-05の「もっと見る」（1タブ20件超の追加取得）を実装した（F-06、詳細設計書API設計6.4節「超過分は「もっと見る」でtype指定の追加リクエスト（type=message&page=2等）を行う」）。`useSearch.ts`から`searchParamsFor(query, extra?)`を切り出し（`SearchQuery`→`URLSearchParams`変換）、初回検索（`useSearch`本体）と「もっと見る」の追加リクエストの両方で共有した。`SearchResultsTabs`に`extraItems`（もっと見るで積み増した行）・`loadedPages`（type別の既読ページ数）を追加し、`allItems = [...result.items, ...extraItems]`をtypeで絞り込んで描画する。ボタンは`もっと見る（残りN件）`で残り件数を明示し、`type=message`/`type=file`をそれぞれ独立に追跡するため、片方だけ全部読み込んでももう片方はまだボタンが出る。`key={searchParams.toString()}`での再マウント（既存のタブ切替リセットと同じ仕組み）により、新しい検索のたびに積み増し状態も自動的に初期化される。Playwrightで実際に検証: 25件のテストメッセージ（SQLで直接INSERT）を用意し、初回20件表示→「もっと見る（残り5件）」クリック→`type=message&page=2`が1回だけ発火→25件全件表示・ボタン消失→タブ切替後も25件維持、をスクリーンショット・ネットワークログで確認した。consoleエラー0件。純粋な既存設計どおりのフロントエンド実装であり新規API・DB変更は無いため、設計文書側の改訂は無い。テストデータ・検証スクリプトはすべて削除済み（2026-09-01）。
- F-12 スキル登録・F-17 引き継ぎ先設定を実装した（同じS-06「スキル」タブに同居させる。基本設計書5.9節「スキルにない業務依頼の引き継ぎ先も設定する」の記載どおり）。新規T-11 channel_skills（channel_id・title・instructions）を追加し、新規A-28（POST）・A-29（PUT）・A-30（DELETE、いずれも`routers/ai_settings.py`、`require_channel_admin`で保護）を実装、A-23の一括取得レスポンスに`skills`配列を追加した。新規A-45（`PUT /api/channels/{id}/ai-settings/handoff`）は`fallback_handoff_user_id`を更新し、指定時は当該チャンネルの参加者であることをAPI側で検証する（基本設計書8.3節に既に明記されていた要件）。**指定した人物が退出・無効化された場合はNULL（既定のチャンネル管理者へ引き継ぎ）に自動的に戻す**設計判断（基本設計書8.3節）を、`channels.py`の`leave_channel`（A-72）・`remove_channel_member`（A-73）と`routers/admin.py`の`update_user`（A-37、`is_active=false`時）の3箇所に同じ`UPDATE channel_ai_settings SET fallback_handoff_user_id = NULL WHERE ...`パターンで実装し、curlで3経路とも実際にNULLへ戻ることを確認した（自己退出はA-73と同じコードパスのため個別確認は省略）。`services/ai_agent.py`の`_build_system_prompt`を拡張し、新規`_build_skills_section`がT-11の内容を「# あなたのスキル」節として列挙し、どのスキルにも当てはまらない依頼を受けた場合は引き継ぎ先（未指定時は「このチャンネルの管理者」という汎用ラベル）へ相談するよう案内する指示を追記する（スキルが0件のチャンネルではこの節自体を省略）。**out_of_scope_policyは層2ドキュメントQ&A関連のため今回もプロンプトへは配線していない**（AIサポート基盤の「スコープ外」に残る唯一の項目）。AI応答本文に含まれる「@氏名」は参考情報としてのテキストに留まり、F-41のようなクリック可能なメンション（message_blocks）には未対応（AI応答経路には`insert_mention_blocks`を配線していないため）。フロントは`ChannelSettings.tsx`に定期投稿・トリガーと同じ「新規作成パネル＋編集モーダル」構成の`SkillsTab`（`SkillFormFields`共有）を追加し、引き継ぎ先は`useChannelMembers`から取れる有効な参加者一覧のセレクトで即保存する。`Layout.tsx`のS-06サイドバーに8番目のタブとして追加した。curlで一通り確認（作成・更新・存在しないskill_idへのPUT/DELETEが404・非chadmin/非adminからの403・非参加者user_idを指定した引き継ぎ先設定の400）に加え、`_build_skills_section`を実際のDB行に対して直接呼び出し、スキル節・引き継ぎ先名の解決・引き継ぎ先未指定時の既定ラベルの3パターンが期待どおりの文字列になることを確認した。**実際のOpenAI API呼び出し（AIがこのスキル指示に実際に従うか）はチャットAI機能と同様に未検証**（`OPENAI_API_KEY`が開発環境に設定されていないため）。**2026-09-02に実機検証済み。後述の「AI API実機検証」の項を参照**。Playwrightで実際のブラウザ操作も確認: スキルの追加→一覧反映→編集→削除の一連の操作、引き継ぎ先セレクトの選択肢（参加者一覧）と保存、consoleエラー0件。テストデータ・検証スクリプトはすべて削除済み。基本設計書（v1.31）・詳細設計書API設計（A-28〜A-30・A-45、v1.23）・docs/index.html（Ver 4.33）を同じコミットで更新した。要件定義書・画面モックアップは既存のF-12・F-17・S-06の記載どおりで変更なし（2026-09-01）。
- F-15 反応モード設定を実装した（S-06「反応モード」タブ）。既存A-24（`PUT /api/channels/{id}/ai-settings/general`）のリクエストに`reaction_mode`（`'mention_only'`/`'proactive'`、必須、それ以外は422）を追加し、新規エンドポイントは作らなかった（基本設計書・詳細設計書API設計とも元々A-24を対応APIとして明記済み）。基本設定タブ（`GeneralTab`、`is_ai_enabled`を変更）と反応モードタブ（新規`ReactionTab`、`reaction_mode`を変更）はいずれもこのA-24を共用し、自分が変更しない側のフィールドも現在値のまま一緒に送る（A-27参照ドキュメント範囲がfolder_ids・out_of_scope_policyを束ねて送るのと同じ考え方）。**「投稿に自ら反応」モードでAIがどの投稿に反応するかの判定ロジックは設計書が規定していなかった（グレー）ため、着手前にユーザーに確認した**（案1: 投稿ごとに軽量な事前判定をLLMに1回追加で呼んでから反応要否を決める2段階方式、案2: 人間の投稿には関連性判定を挟まず常に応答するシンプルな方式、案3: 「？」や疑問系キーワードでの文字列ヒューリスティック、の3案を提示し、コストを増やさない案2を選択）。`services/ai_agent.py`の`maybe_trigger`に`settings["reaction_mode"] != "proactive" and not detect_mention(...)`という条件分岐を1行追加するだけで実現でき（proactiveのときはメンション判定自体をスキップし、`sender_type='human'`の投稿であれば常に起動する）、判定用の追加LLM呼び出しは行わない。この設計判断は基本設計書8.1節に`callout diff-new`として追記した（要件定義書F-15自体の記載は変更なし）。フロントは画面モックアップのカード型ラジオ（クリックで即選択・即保存、選択中はaccent色の枠とfillされたドットで表示）をそのまま再現した`ReactionTab`を追加し、`Layout.tsx`のS-06サイドバーに「スキル」の次・「定期投稿」の前として追加した（画面モックアップのタブ順どおり）。curlで一通り確認（不正なreaction_modeの422、reaction_mode省略時の422、更新後の監査ログ記録）に加え、DB直結の単体テストで`maybe_trigger`のゲート条件3パターン（mention_only×メンション無し→ブロック、mention_only×メンション有り→通過、proactive×メンション無し→通過）が期待どおりであることを確認した。Playwrightで実際のブラウザ操作も確認: カード選択→トースト表示→リロード後も選択状態が保持されること、consoleエラー0件。**実際のOpenAI API呼び出し（proactiveモードで実際に毎回応答が生成されるか）はチャットAI機能と同様に未検証**（`OPENAI_API_KEY`が開発環境に設定されていないため）。**2026-09-02に実機検証済み。後述の「AI API実機検証」の項を参照**。テストデータ・検証スクリプトはすべて削除済み。基本設計書（8.1節に設計判断を追記、v1.32）・詳細設計書API設計（A-24、v1.24）を同じコミットで更新した。要件定義書・画面モックアップは既存のF-15・S-06の記載どおりで変更なし（2026-09-02）。
- F-16 自動対応範囲の設定を実装した（S-06「自動対応範囲」タブ）。新規T-12 channel_auto_response_rules（channel_id・request_category・response_level。UNIQUE(channel_id, request_category)、channel_idはON DELETE CASCADE）を追加し、新規A-31（`PUT /api/channels/{id}/ai-settings/auto-response`）を`routers/ai_settings.py`に実装した。A-27参照ドキュメント範囲・A-31とも同じ「洗い替え」方式（送信されたルール集合でT-12をDELETE→INSERT、差分計算はしない）を採用し、新規エンドポインは1本のみ追加した。同じrequest_categoryが複数送られた場合はPython側のdictで最後の指定を採用し（DBのUNIQUE制約違反を避ける）、response_levelは'auto'/'confirm'/'human'以外422。**request_category（依頼内容カテゴリ）をチャンネル管理者が自由に追加・削除できる仕様にするか、画面モックアップが示す6例の固定候補にするかは着手前にユーザーに確認した**（要件定義書REQ-F-15が「担当部署が自ら決められる」ことを求めており、モックアップの6例は記入例として扱う自由編集案を選択）。フロントは`ChannelSettings.tsx`に`AutoResponseTab`を追加し、DocScopeTabと同じ「ローカルで編集→まとめて1回のPUTで保存」方式（行の追加・削除・区分（自動対応可／確認のうえ対応／人が対応）変更のたびに個別リクエストは発生させない）とした。区分の見た目（ピル）は画面モックアップのok/warn/human配色と一致する既存トークン（ok/bot/member）をそのまま再利用し、新規CSS変数は追加していない。**「人が対応」に区分される依頼をAIがどう検知するかも設計書が規定していなかったため、着手前にユーザーに確認した**（案1: 区分一覧をシステムプロンプトに含めてAI自身に1回の応答内で判断・引き継ぎ案内させる、案2: 投稿ごとに軽量な事前分類LLM呼び出しを追加してから本応答を生成する2段階方式、案3: request_category名の部分一致による機械的フィルタ、の3案を提示し、F-15反応モードと同じくコストを増やさない案1を選択）。`services/ai_agent.py`に新規`_build_auto_response_section`（T-12を「# あなたが対応してよい依頼の目安」節として列挙し、「人が対応」該当時は引き継ぎ案内を追記）を追加し、`_build_system_prompt`へ配線した（設計書どおりの節順で、スキル節より前に配置）。引き継ぎ先の名前解決ロジックは`_build_skills_section`と重複していたため、新規`_resolve_handoff_label`共通ヘルパーへ切り出した（両関数から共有）。'confirm'（確認のうえ対応）は実行前確認（F-25・pending_actions）が未実装で書き込み系ツール自体が存在しないため、現状は'auto'と同じ「通常どおり応答してよい」扱いとし、システムプロンプト内の説明文でその旨を明記するに留めた。ルールが1件も登録されていないチャンネルでは節自体を省略する（スキル節と同じ挙動）。curlで一通り確認（response_level不正値の422、洗い替えの動作確認、同一カテゴリ重複送信時の最後勝ち、非chadminからの403、監査ログ記録）に加え、`_build_auto_response_section`を実際のDB行に対して直接呼び出し、区分ラベルの列挙・引き継ぎ先名の解決・ルール0件時の節省略の3パターンが期待どおりの文字列になることを確認した。**実際のOpenAI API呼び出し（AIが実際に「人が対応」区分を判別できるか）はチャットAI機能と同様に未検証**（`OPENAI_API_KEY`が開発環境に設定されていないため）。**2026-09-02に実機検証済み。後述の「AI API実機検証」の項を参照**。Playwrightで実際のブラウザ操作も確認: 3カテゴリの追加→区分変更（ピルクリック）→保存→リロード後の永続化確認→1件削除→再保存→件数確認、ピルクリックの選択クラス切り替えとAPIレスポンスの一致をDOM属性＋直接API呼び出しの両面で確認、consoleエラー0件。テストデータ・検証スクリプトはすべて削除済み。基本設計書（8.1節に設計判断を追記、v1.33）・詳細設計書DB設計（T-12、v1.9）・API設計（A-31、v1.25）・AIサポート（10.2節、v1.2）・docs/index.html（Ver 4.35）を同じコミットで更新した。要件定義書・画面モックアップは既存のF-16・S-06の記載どおりで変更なし。**ユーザーから2点フィードバックを受け同じコミットで修正した**: (1) 「人が対応」ピルの選択色が`member`トークン（薄いグレー、#f1f5f9/#475569）で他の2区分（ok=緑、bot=amber）と比べて視認しにくかったため、既存の`danger`トークン（赤系、bg-danger-bg/text-danger-text/border-danger-border）に差し替えた。(2) 依頼内容（request_category）を一度追加した後に文言を修正する手段が削除→再追加しかなかったため、行ごとに「編集」ボタン（インライン`<input>`に切り替え、確定/キャンセルボタンで確定する。Enter/Escapeキーにも対応）を追加した。いずれもフロントエンドのみの変更でバックエンドAPI・DBは無変更のため設計文書側の改訂は無い（2026-09-02）。
- S-06「参照ドキュメント範囲」タブの「参照範囲外の質問への対応」を画面モックアップと同じカード型ラジオ（タイトル＋説明文＋選択時に塗りつぶされるドット）に作り直した。従来は素の`<input type="radio">`＋ラベルの簡易実装で、F-15反応モードタブで導入したカード型ラジオ（`ReactionTab`）と見た目が揃っていなかった。両タブで見た目が完全に同じだったため、共通の`RadioCard`コンポーネント（タイトル・説明文・選択状態・onClick・disabledのみを受け取る表示専用）に切り出し、`DocScopeTab`・`ReactionTab`の両方から使うようにした（クリック時の挙動はコンポーネント外：`DocScopeTab`はローカル状態を更新するだけで下部の「保存」ボタンでまとめて送信、`ReactionTab`は選択時に即A-24を呼んで保存する、という既存の違いはそのまま維持）。選択肢の文言も画面モックアップの実際のコピー（「厳格に制限」「一般回答を許可」、各説明文）に合わせて更新した。あわせて、直近のF-16自動対応範囲タブ追加時にEditツールの文字列一致の都合で発生していた副作用（`SkillFormFields`の説明コメントが誤って`ReactionTab`の直前に迷い込んでいた）に気づき、本来の位置（`SkillFormFields`の直前）に戻した。新規API・DB変更は無いフロントエンドのみの変更のため、設計文書側の改訂は無い。Playwrightで実際のブラウザ操作を確認: カードクリックでの選択→保存→リロード後の値の一致（`DocScopeTab`）、リファクタ後も`ReactionTab`の選択即保存が壊れていないことの回帰確認、consoleエラー0件。テストデータ・検証スクリプトはすべて削除済み（2026-09-02）。
- F-40 プロフィールカードに「DMを送る」ボタンを追加した。ユーザーからの明示的な要望による追加で、要件定義書・画面モックアップのいずれにも記載がなかった新規機能。既存のS-06系タブ拡張（チャンネル名編集・自己退出等）と同様、既存の仕組み（A-16参加中DM一覧・A-17 DM開始、いずれもF-05で実装済み）にボタンを1つ足すだけの単純な拡張で設計上の選択肢が実質1つしかないと判断し、検討資料は起こさず要件定義書（F-40の記載を拡張、v1.24）・基本設計書（5.21節、v1.34）・詳細設計書画面設計（S-03項目表・ProfileCardコンポーネント定義、v1.5）・画面モックアップ（S-03、プロフィールポップオーバーに「💬 DMを送る」ボタンを追加）を同じコミットで改訂した。新規API・DB変更は無い。`ProfileCard.tsx`に`useDms()`（サイドバーと同じSWRキャッシュを共有）・`useMe()`を追加し、`userId !== me.id`のときのみボタンを表示する。押下時はまず`useDms()`のキャッシュから一対一DM（`members.length === 1 && members[0].id === profile.id`）を探し、見つかればAPI呼び出し無しで`/dms/{id}`へ即遷移、無ければ`POST /api/dms`（A-17）で新規開始してから遷移する（`DmPickerModal.tsx`の`existingDmWith`・`start`と同じロジック）。カードの高さ見積もり（`CARD_HEIGHT_ESTIMATE`、下開き/上開きの判定にのみ使う）をボタン追加分だけ200→240に調整した。Playwrightで実際のブラウザ操作を確認: 他者の発言者名クリック→カードに「DMを送る」表示→押下→`/dms/{id}`へ遷移、自分自身の発言者名クリック→「DMを送る」ボタンが表示されないこと（0件）、consoleエラー0件。テストデータ・検証スクリプトはすべて削除済み（2026-09-02）。
- F-05に自分専用DM（メモ・下書き・To-do・リンクやファイルの保管用途、Slackの「Myself」相当）を追加した。ユーザーからの明示的な要望による追加で、要件定義書・基本設計書・詳細設計書・画面モックアップのいずれにも記載がなかった新規機能。着手前に実装可否・影響範囲を調査したところ、T-17 direct_message_membersのPRIMARY KEY (dm_id, user_id)には最小人数の制約が無く自分1行のみのDMを作ること自体はDB上可能である一方、A-17（DM開始）の`other_ids.discard(user.id); if not other_ids: raise 422`という既存バリデーションが「自分以外を指定しなかった場合」を常にエラー扱いしており、また`_dm_out`の`members`解決クエリ（`u.id <> $2`で常に自分を除外）が自分専用DMでは`members`を空にしてしまう、という2箇所が実際のブロッカーだと判明した。新規テーブル・新規APIは追加せず、この2箇所を修正する方式で対応した：(1) `routers/dms.py`の`create_dm`から早期404チェックを削除し、`member_user_ids`に自分のidのみを指定した場合（Pydantic`Field(min_length=1)`により少なくとも1件は送られている前提のため、除外後に空集合になるのは「自分だけを明示的に指定した」場合に限られる）を自分専用DMの開始として扱うようにした。(2) `_dm_out`が全参加者idを取得したうえで、除外後に空になる（＝自分専用DM）場合だけ自分自身を`members`に含め、判別用の新規フィールド`is_self`をレスポンスに追加した（`members`が「自分以外の相手」を返すという既存の意味づけ自体は変えていない）。フロントは`types/index.ts`の`Dm`に`is_self`を追加し、`DmPickerModal.tsx`に検索結果とは独立した「自分」の固定枠（常時一覧の先頭に表示、他の相手との組み合わせ選択は不可な排他的選択）を追加、`DmView.tsx`・`Layout.tsx`は`is_self`のときだけタイトル・サイドバー表示名を「自分（メモ）」固定表記にし📝アイコンを添える（それ以外の投稿・スレッド・既読化・検索等の挙動は通常のDMと完全に共通のため変更なし）。curlで一通り確認（自分専用DMの新規作成→`is_self:true`・`members`に自分自身、再取得で`created:false`となる再利用、通常の1:1/グループDM作成が引き続き正常動作すること、A-16一覧に混在して正しく表示されること、メッセージ投稿・取得）に加え、Playwrightで実際のブラウザ操作（DM相手選択モーダルの「自分」行選択→開始→サイドバーに📝「自分（メモ）」表示→メッセージ送信→再度モーダルを開くと「DM済み」タグが表示されることの一連の流れ）を確認した。consoleエラー0件。テストデータ・検証スクリプトはすべて削除済み。基本設計書（5.3節に設計判断を追記、v1.35）・詳細設計書DB設計（T-17、v1.10）・API設計（A-16・A-17、v1.26。あわせて実装と食い違っていた旧いリクエスト例のbackfillも実施）を同じコミットで更新した。要件定義書（F-05、v1.25）・画面モックアップ（補足02、「自分」固定枠の追加）も同じコミットで更新した（2026-09-02）。
- システム管理者が一度も参加していない非公開チャンネルでもS-06チャンネル設定を開けるようにした（2026-08-26付の既知の制約の解消）。ユーザーから「システム管理者は他人の非公開チャンネルに未参加でも入れたり中身を閲覧できる方がよいか、既に何か定義されているか」と質問を受け、調べた結果を回答した：要件定義書の権限マトリクスは「チャンネルの削除」「チャンネル名・説明文の編集」「チャンネルの公開／非公開の切り替え」「担当外チャンネルのAI設定編集」をいずれもadmin「○（全チャンネル）」とする一方、「非公開チャンネルの一覧表示・検索・閲覧」（＝会話内容）はadminも含め参加者限定のままと明確に区別しており（「参加していない非公開チャンネルの一覧・内容が既定で見えるわけではない点はmember・chadminと同様」の注記あり）、この区別は既に設計判断として下されていた。前者（管理系操作）はA-09・A-23〜A-31・A-45・A-47〜A-56・A-63〜A-66が既に`require_channel_admin`でシステムadminバイパスを持っており矛盾は無かったが、実際にはChannelSettings.tsx（S-06）がヘッダー表示・チャンネル管理者タブのためにA-06（チャンネル詳細）・A-46（参加者一覧）も呼んでおり、この2つは`require_channel_member`のままバイパスが無かったため、非参加の非公開チャンネルではS-06自体を開けなかった（実際に該当の状態を再現し、S-06が完全に開けることをスクリーンショットで確認した上で着手の可否をユーザーに確認）。`auth_helpers.py`に新規`require_channel_member_or_admin`（`require_channel_member`と同じ判定に`require_channel_admin`と同じ`if user.role == "admin": return user`バイパスを加えたもの）を追加し、A-06・A-46のガードをこれに切り替えた。この2つは発言本文を一切含まないメタデータ専用エンドポイントであるため、要件定義書が明記する「会話内容は参加者限定」という区別は崩さない。発言本文を扱うA-10・A-11・A-14・A-15・A-18・A-19・A-69・A-72等は変更せず`require_channel_member`のまま（adminバイパス無し）とした。ただし、A-06が非参加adminにも200を返すようになった副作用として、S-03（会話画面）の既存ガード（`ChannelView.tsx`がA-06のエラーだけを見て404/403ならワークスペースへ戻す仕組み）が非参加adminを締め出せなくなる問題に気づき、A-06のレスポンスに新規`is_member`（呼び出し元が実際の参加者か）を追加し、`ChannelView.tsx`に`channel && !channel.is_member`のときも同じくワークスペースへ戻す分岐を追加した（S-06側は元々`is_channel_admin || me.role==='admin'`でゲートしており`is_member`を見る必要が無いため変更不要）。curlで一通り確認（非参加adminのA-06/A-46が200で`is_member:false`、非参加adminのA-10/A-11が引き続き404、非管理者の一般利用者は従来どおり404、実際の参加者は`is_member:true`）に加え、Playwrightで実際のブラウザ操作を確認：非参加adminが対象チャンネルのS-06を開けチャンネル管理者タブ（名称編集・chadmin一覧）が正しく表示されること、同じチャンネルのS-03を直接開くとメインペインが空白のままリダイレクト相当の挙動になり会話内容が一切見えないこと、consoleエラー0件。テストデータ・検証スクリプトはすべて削除済み。詳細設計書（総論5.1・5.2・5.9節に設計判断を追記、v1.5）・API設計（A-46、v1.27）を同じコミットで更新した。要件定義書・基本設計書は元々「チャンネル管理系操作はadmin全チャンネル可」としていた記載どおりで変更なし。**この直後、ユーザーから「実際にこの機能を使うには石井直樹119はアプリ上でどう操作すればその設定画面にたどり着けるのか」と聞かれ、調査した結果、S-06自体は開けるようになったが「そのチャンネルを見つけて開く」導線が存在しないこと（サイドバーの＋・横断検索はいずれも自分の参加チャンネルの範囲に限定、S-08の各タブもチャンネル名をプレーンテキストで表示するのみでクリックできない）が判明した。ユーザーの了承のもと、最小の追加としてS-08「監査ログ」タブの「対象」列（`AdminConsole.tsx`のAuditLogTab）を、そのチャンネルのS-06へのリンク（`target_channel_id`があるときだけ`<Link to={`/channels/${id}/settings`}>`、無いときは従来どおり`—`表示）に変更した。この列はchadminがそのチャンネルのAI設定を変更した記録があるときのみ埋まるため万能の発見手段ではないが、既存の画面に手を加えるだけで実現できる最小の一手として選んだ（S-08に全チャンネル一覧を返す新規APIを追加する等のより網羅的な対応は今回は見送り）。Playwrightで実際に確認：chadminが非公開チャンネルのAI設定を変更→監査ログに記録→システムadmin（非参加）が監査ログタブでそのチャンネル名のリンクをクリック→実際にそのチャンネルのS-06（チャンネル管理者タブ）へ遷移できること、consoleエラー0件。新規API・DB変更・設計文書側の改訂は無い（既存のリンク先S-06の権限自体は直前の変更で対応済みのため）。**さらにユーザーから「利用者管理タブのチャンネル管理者列と、AI利用状況・コストタブのチャンネル別上限からもS-06へ飛べるようにしたい」との追加依頼を受け、同様にリンク化した。「利用者管理」タブの`chadmin_channels`（そのユーザーがchadminを務める全チャンネル、非公開含む）は従来チャンネル名の文字列配列のみでリンクを組み立てるためのidが無かったため、A-36（`routers/admin.py`）のSQLを`array_agg(c.name ...)`1本から`array_agg(c.id ...)`・`array_agg(c.name ...)`の2本（いずれも同じ`ORDER BY c.name`で順序を揃える）に変更し、`{id, name}`オブジェクトの配列としてzipして返すようにレスポンス形状を変更した（フロントの`AdminUser.chadmin_channels`型・`types/index.ts`も追従）。「AI利用状況・コスト」タブのチャンネル別上限（`ChannelLimitsSection`）は元々`UsageChannelLimit`が`channel_id`・`channel_name`の両方を持っていたため、バックエンド変更は不要でフロントの表示（`# {channel_name}`のテキスト）を`<Link to={`/channels/${channel_id}/settings`}>`に置き換えるだけで済んだ。curlでA-36の新しいレスポンス形状を確認し、Playwrightで両タブから実際にリンクをクリックしてS-06へ遷移できることを確認した（利用者管理タブは非公開チャンネル「非公開」を含む複数チャンネルのリンクが正しいidで生成されること、AI利用状況タブは新規に設定したチャンネル別上限のリンクが遷移すること）。consoleエラー0件。テストデータ・検証スクリプトはすべて削除済み。詳細設計書API設計（A-36、v1.28）・docs/index.html（Ver 4.39）を同じコミットで更新した。**続けてユーザーから「管理者がS-08のリンクから非公開チャンネルのS-06を開いたあとS-03（会話画面）へ移行しようとすると、サイドバー以外真っ白な画面になり何が起きたか分かりづらい。参加していない旨のメッセージを出してほしい」との指摘を受けた。`ChannelView.tsx`の`channel && !channel.is_member`分岐（1.5版で追加した非参加admin向けの早期return）を、無言の`return null`から「🔒 {チャンネル名} には参加していません」という見出し・説明文・「⚙ チャンネル設定を開く」という戻り導線（`/channels/{id}/settings`へのLink）を表示するUIに差し替えた。この変更は`channel.is_member=false`の分岐だけが対象で、その1つ上にある`channelError`（404/403、F-34「存在を伏せる」対象の未参加者向け無言リダイレクト）には手を加えていない——この分岐に来る時点でA-06自体は既に成功しておりチャンネルの存在・名称はシステムadminへ開示済み（S-08のリンクを踏んだ以上、伏せる理由が無い）なので、両者を区別する判断基準は前回（1.5版）の設計判断のとおり。Playwrightで実際に、S-06を開いてから「← {チャンネル名} に戻る」リンクでS-03へ遷移するという指摘どおりの操作手順を再現し、メッセージが表示されること、「チャンネル設定を開く」リンクでS-06へ戻れること、consoleエラー0件（発言一覧等の背景取得が404になる既存の挙動は変更前と同じ）を確認した。テストデータ・検証スクリプトはすべて削除済み。詳細設計書（総論5.9節、v1.6）・docs/index.html（Ver 4.40）を同じコミットで更新した（2026-09-02）。**
- **AI API実機検証**: 千田氏から交付されたOpenAI APIキーをローカル`.env`に設定し（`OPENAI_API_KEY`は本人が直接編集、チャットへの貼り付けはさせていない。`.gitignore`済み・`git log --all --full-history -- .env`でコミット履歴に一度も含まれていないことを確認済み）、これまで「実際のOpenAI API呼び出しは未検証」としていたAIサポート機能（チャットAIメンション応答、F-14要約、F-12スキル、F-15反応モード、F-16自動対応範囲）を初めて実機で検証した。**`AI_MODEL`は`gpt-5-nano`を採用**（千田氏から指定モデルは無いとの回答だったため、2026-09-02時点でOpenAI公式単価ページ（developers.openai.com/api/docs/pricing）で確認できる最安のテキスト生成モデルをローカル動作確認用に選定。$0.05/$0.40（1Mトークン、入力/出力）。本番投入前に品質面で必要なら`gpt-4.1-nano`や`gpt-4o-mini`へ変更する前提でユーザー了承済み）。`services/ai_client.py`の`MODEL_COSTS`に`gpt-5-nano`の単価行を追加した（既存の`gpt-4o-mini`行と同じ円/USD換算レートで円/1000トークンへ変換。未知モデルは既定モデルの単価で概算する既存仕様のため、単価行の追加を忘れると誤ったコストが表示され続ける）。**実機検証で初めて顕在化する実装バグを2件発見・修正した**（いずれもAPIキー未設定時のcurl検証では検出できない性質のバグ）: (1) `ai_agent.py`が全チャット補完呼び出しにハードコードした`TEMPERATURE = 0.5`を渡していたが、`gpt-5-nano`のようなreasoning系モデルは既定値(1)以外の`temperature`を一切サポートせず`openai.BadRequestError: Unsupported value`になることが判明したため、`temperature`パラメータ自体を削除しAPIの既定値に委ねるよう修正した（`AI_MODEL`を環境変数だけで自由に差し替えられる設計上、モデルごとに対応が割れるパラメータは指定しないほうが頑健という判断）。(2) reasoning系モデルは`reasoning_effort`を指定しないと内部の思考に`max_completion_tokens`を使い切ってしまい、応答本文が空のまま`finish_reason='length'`で返る（`reasoning_tokens`として消費済みのため課金は発生する）ことを、生のAPIレスポンスを直接調査して特定した。`reasoning_effort='minimal'`の指定で解決する一方、非reasoningモデル（`gpt-4o-mini`で確認）に同じパラメータを渡すと逆に「Unrecognized request argument」で400エラーになるため、新規`ai_client.REASONING_MODELS`／`is_reasoning_model()`と、`ai_agent._completion_extra_kwargs()`（reasoning系モデルのときだけ`reasoning_effort='minimal'`を付与する小さなヘルパー、`_generate_and_post`・`_generate_summary_and_post`の2箇所で共有）を追加し、モデル種別に応じて条件付きで付与するようにした。修正後、使い捨てのテストチャンネル（`ai-verify-test`）上で以下5点を実際のOpenAI API呼び出しで確認した: チャットAIメンション応答（応答本文の生成・`generation_status`の`generating`→解除・T-13への使用量/コスト記録）、F-14要約（チャンネル本体の直近発言を要点箇条書きで要約）、F-12スキル遵守（登録した指示文の必須文言を応答に一字一句含める）、F-16自動対応範囲（`response_level='human'`のカテゴリに一致する依頼には直接回答せず、設定した引き継ぎ先の氏名を案内する）、F-15反応モード`proactive`（「@AI」の明示的な言及が無い発言にも自発的に応答する）。検証後、テストチャンネルごと削除した（CASCADEでスキル・発言・T-13記録も連動削除）。層2ドキュメントQ&A（OpenAI Embeddings API）はこのスライスの対象外のまま（Drive OAuthスコープ拡張が別途必要なため）。今回の修正はモデル対応パラメータの実装バグ修正のみで設計判断・API仕様に変更は無いため、設計文書の改訂は無い（2026-09-02）。
- チャットAIメンション応答（`ai_agent.py`の`_generate_and_post`）が参照する会話履歴の件数を`MAX_HISTORY_MESSAGES`で200→20件に縮小した。ユーザーから、共有された「AI API編」手順書の「直近だけ送る：『最新の10往復まで』と決めて、古い分は送らない。いちばん簡単で、まずはこれで十分」という指針との乖離を指摘され、この基準（human+aiあわせて概ね20件。bot発言混在のため厳密な往復数ではない）に合わせて縮小した。直前のAI API実機検証でreasoning系モデル（`gpt-5-nano`）が`reasoning_tokens`を消費しやすいことが判明したばかりで、送信するトークン量自体を抑える意義も大きいと判断し即対応した。F-14要約（`MAX_SUMMARY_CHANNEL_MESSAGES = 100`）はチャンネル全体を俯瞰する別用途のため対象外（`start_summary`は明示的なボタン操作であり毎回発生する会話履歴とは性質が異なる）。設計判断・API仕様に変更は無いため設計文書の改訂は無い（2026-09-02）。
- AIメンション（本文中の「@ペルソナ名」）をF-41の人間宛メンションと同じ見た目（青背景・太字、画面モックアップS-03の`.mention`）でハイライト表示するようにした。ユーザーからの明示的な要望による追加。AIメンションはF-41と異なりID参照化されずmessage_blocks（T-07）に残らない（基本設計書5.22節の設計判断、`services/ai_agent.detect_mention`と同じ本文中の文字列一致のみ）ため、`MessageList.tsx`の`renderMessageBody`にAIメンション検出を追加した: 構造化された`blocks`由来のマッチに加えて、新規引数`aiPersonaName`が渡されていれば本文中の「@{aiPersonaName}」を直接検索し、同じ`bg-accent-100 text-accent-700`のスタイルで両者をマージして描画する。`aiPersonaName`はチャンネルAIのペルソナ名（既定"AI"）で、`ChannelView.tsx`→`MessageList`・`ThreadPanel`（チャンネルのスレッドのみ、DMのスレッドでは渡さない）の2箇所に配線した。**取得元はA-23ではなく新規にA-06（`GET /api/channels/{id}`）のレスポンスへ`ai_persona_name`を追加する形にした**（A-23は`require_channel_admin`でchadmin/admin限定のため、会話画面を開く全参加者からは呼べない。A-06は元々`require_channel_member_or_admin`で参加者全員が呼べる上、`channel_ai_settings`は必ずA-07作成時に既定行を持つため`persona_name`を単純にSELECTするだけで済んだ）。DM会話（DmView.tsx）は元々AIが存在しない（AI応答はチャンネルのみ対応）ため`aiPersonaName`を渡していない。**実装中に別の既存バグを発見・修正した**: `_message_out`（`channels.py`・`messages.py`・`dms.py`の3箇所）の`sender_name`解決が`sender_type == "bot"`のときだけ`bot_display_name`を使う分岐になっており、AI発言（`sender_type == "ai"`）が抜けていたため、AI発言のヘッダー表示名が常にnull（フロントは「(不明)」にフォールバック）になっていた。AI発言も同じ`bot_display_name`列にペルソナ名をスナップショットしているため（`services/ai_agent.py`）、3箇所とも`row["sender_type"] in ("bot", "ai")`に修正した。curlで、A-06が`ai_persona_name`を一般memberからも取得できること、AI発言の`sender_name`が正しくペルソナ名（既定"AI"）になることを確認したうえで、Playwrightで実際にブラウザ操作を確認: 「@AI」を含む発言を投稿→本文中の「@AI」が青背景でハイライトされること、AIの返信ヘッダーが「(不明)」ではなく「AI」と表示されること、をスクリーンショットで確認した（consoleエラーは無関係な既存のfavicon.ico 404のみ、`vite`開発サーバーの既知の無害な挙動で今回の変更とは無関係）。テストチャンネル・検証スクリプトはすべて削除済み。新規API・DB変更は無い（既存のA-06レスポンスへのフィールド追加のみ）ため設計文書側の改訂は無い（2026-09-02）。
- チャンネルAIの既定のペルソナ名（T-08 persona_name）を「AI」から「Kogack AI」に変更した。ユーザーからの明示的な要望による追加。画面モックアップ（S-06「キャラクタ」タブの名前欄、S-03の発言例）は元々`value="Kogack AI"`・`@Kogack AI`を使っており、DB既定値・バックエンドのフォールバック文字列側がこれに追従していなかった抜けだった（画面モックアップ自体の変更は無い）。`database.py`の`CREATE TABLE`の`DEFAULT`を変更し、既存DBにも反映されるよう`ALTER TABLE channel_ai_settings ALTER COLUMN persona_name SET DEFAULT 'Kogack AI'`を追記（起動時に毎回実行される安全なスキーマ変更。既存行のデータ自体は書き換えない）。既存行（当時10チャンネル、いずれも旧既定値のまま未カスタマイズ）は一度きりの手動SQL（`UPDATE channel_ai_settings SET persona_name = 'Kogack AI' WHERE persona_name = 'AI'`）でバックフィルし、database.py側には埋め込まなかった（スキーマのADD COLUMN IF NOT EXISTSと異なり、値の上書きを起動のたびに繰り返すとチャンネル管理者が将来"AI"という文字列に意図的に戻した場合を巻き戻してしまうため）。`services/ai_agent.py`の5箇所・`routers/channels.py`のA-06フォールバック・`ChannelSettings.tsx`の2箇所のフォールバック文字列も同様に変更した。アイコン未設定時の2文字アバター（Avatar/`char-avatar`の"AI"表示）は persona_name とは独立した固定の略称表示のため対象外（画面モックアップ・実装とも変更なし）。curlで、既存チャンネルのA-06が`ai_persona_name: "Kogack AI"`を返すこと・新規チャンネル作成でも同じ既定値が入ることを確認し、Playwrightで実際に「@Kogack AI」を含む発言を投稿→本文中の該当箇所が青背景でハイライトされること・AIの返信ヘッダーが「Kogack AI」と表示されること・実際に応答が生成されることをスクリーンショットで確認した。テストチャンネル・検証スクリプトはすべて削除済み。詳細設計書DB設計（T-08、v1.11）・API設計（A-23レスポンス例、v1.29）・docs/index.html（Ver 4.41）を同じコミットで更新した。要件定義書・基本設計書・画面モックアップは変更なし（2026-09-02）。
- F-41のメンション候補一覧（「@」ボタン押下・本文への直接入力の両方）にチャンネルAIを含めるようにした。ユーザーからの明示的な要望による追加で、AIサポート基盤スライスから既知のスコープ外だった「F-41のメンションピッカーの候補にチャンネルAIを含めていない」を解消した。画面モックアップS-03のメンションポップオーバーは元々候補の先頭に専用アイコン付きで「Kogack AI」を表示しており（`mp-hint`「当該チャンネルの参加者＋チャンネルAI」）、これに合わせた。A-06（`GET /api/channels/{id}`）のレスポンスへ新規`ai_is_enabled`を追加した（前回追加した`ai_persona_name`と同様、A-23と異なり参加者全員が呼べるA-06から軽量に取得する）。`ChannelView.tsx`で`channel.ai_is_enabled`のときだけ`mentionCandidatesWithAi`の先頭にAIの合成候補（`{id: 'ai', name: persona_name, isAi: true}`）を追加してComposerへ渡す（チャンネルAIが無効なチャンネルでは「@ペルソナ名」と書いてもAIは応答しないため候補にも出さない）。`Composer.tsx`の`MentionCandidate`に`isAi`フラグを追加し、(1) 候補一覧の描画でAI行だけ`MessageList`のAvatarと同じ角丸四角のグラデーションアイコンにする、(2) `selectCandidate`でAI選択時は本文への「@ペルソナ名 」挿入のみ行いmentions配列（T-07構造化メンション）には追加しない（基本設計書5.22節の設計判断どおり、AIメンションはID参照化しない）、の2点を分岐した。スレッド返信（ThreadPanel経由のComposer）には配線していない（A-14内の@AI検知は既存スコープ外のまま、基本設計書8章）。curlでA-06の`ai_is_enabled`がAI有効/無効の両チャンネルで正しく返ることを確認し、Playwrightで実際のブラウザ操作を確認: 「@」ボタン押下・本文への直接入力のいずれでも候補先頭にAI専用アイコン付きで表示されること、選択して送信した発言の`blocks`が空（構造化メンションを作らない）でありながら実際にAIが応答すること、AI無効チャンネルでは候補に出ないこと（0件）。テストチャンネル・検証スクリプトはすべて削除済み。新規API・DB変更は無い（既存A-06への1フィールド追加のみ）ため設計文書側の改訂は無い（2026-09-02）。
- 投稿欄（Composer）で入力中の本文も、選択済みメンション（人間・チャンネルAIいずれも）を青くハイライト表示するようにした。ユーザーからの明示的な要望による追加。`<textarea>`自体は部分的な文字装飾ができないため、`MessageList`/`renderMessageBody`同様の「投稿後の表示」ではなく、業界で広く使われる透明textarea＋背後オーバーレイ方式（react-mentions等と同じ技法）を採用した: `Composer.tsx`のtextareaを`bg-transparent text-transparent caret-ink`にして文字自体を不可視化し、同じ位置に`position:absolute inset-0`で重ねた`aria-hidden`なdiv（`pointer-events-none`）に同じ本文をハイライト付きで描画する。**両者のフォントサイズ・行間・パディング・折り返し（`whitespace-pre-wrap break-words`）を完全に一致させないと文字がずれるため、ハイライト部分にも背景色・文字色以外（padding・font-weightなど、グリフの幅や位置に影響する装飾）は一切加えていない**（投稿後の表示side chip（`rounded px-1 font-semibold`）とはあえて意匠を分けた）。新規`findMentionHighlights(text, mentions, aiPersonaName)`が、選択済みmentions state（`display_name_snapshot`）とAIメンション（`@{aiPersonaName}`の文字列一致、複数出現にも対応）の両方から本文中の該当範囲を検出する（本文から手で消された分は`indexOf`が見つからず自然にハイライトが外れる、既存の`activeMentionsIn`と同じ考え方）。オートリサイズで動的に高さが変わる既存のuseEffect・スクロール（10行超で内部スクロールする既存挙動）にも対応するため、textareaの`onScroll`でオーバーレイ側の`scrollTop`を同期させている。新規`aiPersonaName`propを`Composer`に追加し、`ChannelView.tsx`・`ThreadPanel.tsx`（スレッド返信、AIは実際には応答しないが表示の一貫性のため同じくハイライトする）から`channel.ai_persona_name`を渡した（DM会話は元々AIが無いため対象外）。Playwrightで実際のブラウザ操作を確認: 「@」ボタン・本文への直接入力いずれからも人間・AIそれぞれの選択後に即座に青くハイライトされること、本文を選択し直して置き換える（＝メンション文字列を削除する）とハイライトが消えること、10行相当の長文・複数回出現するAIメンション・実際にtextareaが内部スクロールする状態（画面外の行を含む）のいずれでもハイライト位置が実際の文字位置とずれないこと、をスクリーンショットで確認した。検証中に誤って実チャンネル（雑談）へテスト用の発言を複数回送信してしまった（Playwrightの`.type()`が本文中の改行文字をEnterキー入力として送出し、このアプリの「Shift無しEnter＝即送信」という既存仕様どおり毎回送信されてしまったため）が、直後にAPIで全件削除して原状回復した（AI応答分はsender_user_idを持たないため投稿者本人判定に一致せず、削除にはadmin権限が必要だったことも確認した）。新規API・DB変更は無いフロントエンドのみの変更のため、設計文書側の改訂は無い（2026-09-02）。
- Composer.tsxの`detectMentionQuery`（F-41）から「直前の文字が空白または本文の先頭でなければメンション開始とみなさない」という制限を外した。ユーザーからの明示的な要望で、「こんにちは@」のように単語の途中・直前に空白が無い位置で「@」を入力した場合でも、その場でメンション候補一覧を表示するようにした（従来は直前に半角/全角スペースを挟むか本文冒頭でなければ候補が出なかった）。「@」直後からカーソルまでに空白・改行を含む場合は候補を閉じる既存の判定はそのまま残しており、クエリの区切り方自体は変更していない。挿入・ハイライト（`selectCandidate`・`findMentionHighlights`）はいずれも「@」の直前文字に依存しない実装だったため変更不要だった。横断検索（S-05）のモディファイア入力補助（`SearchView.tsx`の`detectModifierQuery`）はF-41と同じ考え方で作られた別実装だが、今回のユーザーからの要望はメッセージ投稿欄限定のため対象外とした（検索欄の`in:`/`from:`等はそもそも文中に埋め込む用途を想定しておらず、様子は異なる）。Playwrightで実際のブラウザ操作を確認: 「こんにちは@」の直後に候補が表示されること、選択すると「こんにちは@Kogack AI 」に正しく挿入されハイライトも即座に効くこと、consoleエラーは無関係な既存のfavicon.ico 404のみ。新規API・DB変更は無いフロントエンドのみの変更のため、設計文書側の改訂は無い（2026-09-02）。
- バグ修正: 横断検索（S-05、A-20）でAI・BOT発言がヒットした際、発言者名が常に「(不明)」と表示されていた。会話ログ・DM一覧のアイコン反映漏れ（2026-08-27）・S-08利用者管理のアイコン反映漏れ（2026-08-28）と同種の「AI/BOT発言はsender_user_idが無いため素朴なJOINだけでは発言者名が解決できない」問題で、`_message_out`（channels.py等）は既に`sender_type in ("bot","ai")`のとき`bot_display_name`を使う分岐を持っていたが、`routers/search.py`のメッセージ検索・ファイル検索の2つのSELECTには元々この分岐が無く、常に`u.name`（sender_user_idへのJOIN、AI/BOT発言では必ずNULL）をそのまま返していたのが原因。両SELECTに`m.sender_type, m.bot_display_name`を追加し、レスポンス整形時に`_message_out`と同じ`bot_display_name if sender_type in ("bot","ai") else sender_name`の分岐を適用した（ファイル検索側は実際には現状BOT/AI発言に添付ファイルが付くことは無いが、他と分岐を揃えるため同様に修正）。テスト用チャンネルで実際にAIへメンション→応答を待ってから検索し、`sender_display_name`が「Kogack AI」になることをcurl・Playwrightの両方で確認した。テストデータ・検証スクリプトはすべて削除済み。ユーザーからの報告を受けての修正で、要件定義書上のF-xxに対応付けられた変更ではないため要件定義書の改訂は無い。新規API・DB変更も無い（2026-09-02）。
- 横断検索（S-05、A-20）の結果一覧で、ヒットした検索語をオレンジ色でハイライト表示するようにした。ユーザーからの明示的な要望による追加。フロント側は`routers/search.py`の`_parse_terms`と同じ規則（`"…"`はフレーズのまま1語、それ以外は空白区切り）をミラーした新規`parseHighlightTerms`で`query.q`（検索欄のフリーテキスト部分）を語に分解し、新規`highlightTerms`がexcerpt・file_nameの両方から大文字小文字を区別しない一致で該当範囲を`<mark>`要素（既存の`bot-bg`/`bot-text`トークンを流用、新規CSS変数は追加せず。@メンションハイライトが`accent`トークンを流用しているのと同じ考え方）で囲む。複数の検索語が重なってヒットする場合は範囲をマージし二重に囲まないようにした。**実装中に、バックエンドのexcerpt生成（`_excerpt`、80文字の抜粋）に既存のバグを発見・修正した**: 検索語の一致開始位置が80文字以内でも、語自体が80文字目をまたいで終わる場合、従来の単純な先頭80文字切り出しでは語の途中で切れてしまい（例:「zzhighlightprobe」が「zz」で切れる）、この状態のままだとハイライトも中途半端にしか効かなかった。`_excerpt`に`terms`引数を追加し、素朴な先頭切り出しで検索語の終端まで収まるかを判定するよう修正（`match_start`だけでなく`match_end`もlimit以内かを見る）、収まらない場合は最初にヒットした検索語を中心に前後を「…」で省略したスニペットを切り出すようにした（Google検索結果のスニペットと同じ発想）。curl・Playwrightで実際に確認: 短いメッセージ・80文字を超える長いメッセージ（検索語がちょうど末尾付近にまたがるケースを含む）・添付ファイル名のいずれでも検索語が省略されずオレンジでハイライトされること、複数件ヒットした場合もそれぞれ正しくハイライトされること。テストデータ・検証スクリプトはすべて削除済み。新規API・DB変更は無い（既存の`excerpt`文字列の生成ロジック改善のみ）ため設計文書側の改訂は無い（2026-09-02）。
- バグ修正: F-41メンション候補一覧（Composer.tsx）のアイコンが、常に色付き頭文字（人間）またはAIグラデーション表示（チャンネルAI）に固定されており、実際に発言したときのアイコン（プロフィール画像・AIペルソナアイコン）と一致していなかった。ユーザーからの報告を受けて修正。会話ログ・DM一覧・S-08利用者管理で過去に繰り返し発生したのと同種の「画像優先→フォールバック」の分岐漏れで、`MentionCandidate`型に`picture_url`が無く候補一覧側の描画がこれを一切見ていなかったのが原因。`MentionCandidate`に`picture_url`を追加し、`MessageList.tsx`の`Avatar`と同じ優先順位（画像があれば`<img>`、無ければ人間は色付き頭文字・AIはグラデーション「AI」表示、AIの画像はAvatarと同じ角丸四角・人間の画像は円形）で描画するよう修正した。チャンネル参加者（`ChannelMember`）は元々`picture_url`を持っていたため`ChannelView.tsx`側の変更は不要だったが、チャンネルAIの合成候補（`{id:'ai', ...}`）にはアイコン情報が無かったため、新規A-06レスポンスフィールド`ai_persona_icon_url`（`channel_ai_settings.persona_icon_url`、A-23と異なり参加者全員が取得できる）を追加し、`mentionCandidatesWithAi`から渡すようにした。curlでA-06が`ai_persona_icon_url`を返すことを確認し、Playwrightで実際のブラウザ操作を確認: 「@」入力で開く候補一覧の各行の`<img>`が、会話ログのAvatarと同じURL（ローカルアップロード画像・Googleプロフィール画像の両方）を指し、`naturalWidth`から実際に画像として読み込めていることをDOM直接評価で確認した。テストデータ・検証スクリプトはすべて削除済み。新規API・DB変更は無い（既存A-06レスポンスへのフィールド追加のみ）ため設計文書側の改訂は無い（2026-09-02）。
- 開発用ログイン（dev-login/dev-users）のユーザーを1名追加した。ユーザーからの明示的な要望による追加。`backend/seed.py`は「無ければ作る」形の追記式（既存3ユーザーと同じパターン）のため、既存コードと同じ形で`takahashi@kogasoftware.com`（高橋 健太、role='member'）を追記した。dev-users一覧（`GET /api/auth/dev-users`）はDBの`users`テーブルを`is_active=true`で単純にSELECTするだけの実装（ハードコードされた候補リストではない）ため、`seed.py`にユーザーを追記するのがこの機能を拡張する唯一の方法（新規APIやハードコードリストは不要）。ローカルの稼働中DBに対して実際に`seed.py`を実行し、dev-login・`/api/auth/me`の両方で実際にログインできることをcurlで確認し、Playwrightで実際にS-01ログイン画面の開発用ログイン一覧に「高橋 健太」が表示されることを確認した。新規API・DB変更は無い（既存の`users`テーブルへの1行追加のみ）ため設計文書側の改訂は無い（`start.bat`経由で`seed.py`が自動実行されるため、次回以降の新規DB作成時にも自動的に投入される）（2026-09-02）。
- バグ修正: アイコン画像未設定ユーザーの色付き頭文字フォールバックが、画面によって色がバラバラだった。ユーザーからの報告を受けて調査したところ、発言者（`MessageList.tsx`）・メンション候補（`Composer.tsx`）・管理コンソール（`AdminConsole.tsx`）等9箇所は既に共通の`avatarColorFor(id)`（`lib/avatarColor.ts`、idのハッシュ値から固定色を決める）で統一されていたが、サイドバー左下の自分のプロフィール表示（`Layout.tsx`、クリックで「プロフィールを編集」モーダルを開くボタン）とそのモーダル自身の大アイコン（`ProfileEditModal.tsx`）の2箇所だけが、`avatarColorFor`を使わず全ユーザー共通の固定色（`bg-[#cbd5f5]`＋`text-accent-700`）にハードコードされていたのが原因。この2箇所を他と同じ`avatarColorFor(me.id)`＋`text-white`に統一した。アイコン画像未設定のテストユーザー（`takahashi@kogasoftware.com`）で実際に確認: サイドバー左下・プロフィール編集モーダル・発言時のアイコン・別ユーザーから見たメンション候補一覧・管理コンソールの利用者管理タブの5箇所すべてで同じ色（黄色、`avatarColorFor('5')`の結果）になることをスクリーンショット・`getComputedStyle`の直接評価で確認した。テストデータ（投稿した確認用発言・チャンネル参加）はすべて削除済み。フロントエンドのみの変更でバックエンドAPI・DBは無変更のため設計文書側の改訂は無い（2026-09-03）。
- `MessageList.tsx`の`Avatar`が、AIペルソナアイコン画像を設定済みのチャンネルAI発言でも人間と同じ円形（`rounded-full`）で表示しており、実在の人物と見分けがつかないというユーザーからの指摘を受けて修正した。BOT発言（F-36定期投稿・F-38トリガー・F-43システム通知）は画像有無に関わらず常に角丸四角（`rounded-[9px]`）で人間の円形と区別しているのに対し、AI発言は「画像があれば`sender_picture_url`優先で円形描画」という種別を問わない共通分岐に先に引っかかっていたため、未設定時のみ角丸四角の「AI」グラデーション表示にフォールバックする構造になっていた（当初の設計判断は基本設計書5.22節「AIメンションはID参照化しない」とは無関係の、このAvatarコンポーネント固有の実装順序の問題）。BOT分岐の直後に`sender_type === 'ai'`の専用分岐を追加し、BOTと同じ「角丸四角の枠内で画像→フォールバック表示の優先順位」構造（画像があれば`<img>`、無ければ「AI」のグラデーション表示）に変更した。これにより人間の円形とBOT・AIの角丸四角という2形状に整理され、画像設定の有無に関わらずAI・BOT発言だと一目で判別できるようになった。`ThreadPanel.tsx`は同じ`Avatar`を再利用しているため追加対応不要。`Composer.tsx`のメンション候補一覧は2026-09-02の時点で既にAI候補を角丸四角（`rounded-[8px]`）で描画済みだったため対象外。A-61でアップロードしたテスト画像をチャンネルAIのペルソナアイコンに設定し、実際にメンション応答を生成させて`getComputedStyle`で角丸四角（`border-radius: 9px`）になっていることを直接確認した。テストチャンネル・アイコンはすべて削除済み。フロントエンドのみの変更でバックエンドAPI・DBは無変更のため設計文書側の改訂は無い（2026-09-03）。
- F-14要約（要約ボタン）で生成された発言が、会話上で要約機能により作成されたものだと分かるようにした。ユーザーからの明示的な要望による追加。通常のAIメンション応答と要約結果はいずれも`sender_type='ai'`・`thread_parent_id`もチャンネル本体では同じくNULLになるため、既存の列だけでは構造上区別できない（チャンネル本体の投稿として要約するケース）ことが分かったため、新規`messages.is_summary`列（BOOLEAN、既定false）を追加した。`services/ai_agent.start_summary`の生成中プレースホルダINSERT時のみ`true`を設定する（実際の要約文が確定する`_generate_summary_and_post`のUPDATEは`body`/`generation_status`のみ更新するため、この列の値はそのまま引き継がれる）。`channels.py`・`messages.py`・`dms.py`の3つの`_message_out`すべてにレスポンスフィールドとして追加した（DMはF-14がチャンネル専用APIのため実際には常にfalseだが、他2ルーターと同じ分岐に揃えた）。フロントは`Message`型に`is_summary?: boolean`を追加し、`MessageList.tsx`の「AI」バッジの隣に「📝 要約」バッジ（ChannelView/ThreadPanelの要約ボタンと同じ絵文字・配色）を条件表示するようにした。curlで実際に要約を生成させ`is_summary: true`が返ることを確認し、Playwrightで実際に「📝 要約」バッジが会話ログに表示されることを確認した。テストデータはすべて削除済み。詳細設計書DB設計（T-05、v1.12）・docs/index.html（Ver 4.42）を同じコミットで更新した。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-03）。
- バグ修正: Googleの同意画面で「キャンセル」を選択すると、理由不明のまま何度試してもログインできなくなる不具合を修正した。層2ドキュメントQ&Aに向けたDrive OAuthスコープ拡張の実機検証（一時的にスコープへ`drive.readonly`を追加して実際にログインフローを試した）を進める中でユーザーから提起された懸念で、実際にコードを確認したところ現実に存在する不具合と判明した。従来`routers/auth.py`のA-02コールバックは、Googleからの`code`が無い場合を一律`state`不一致と同じ扱いにして`oauth_failed`（「ログインに失敗しました。もう一度お試しください。」）へ誘導していたが、Googleは同意画面拒否時に`code`の代わりに`error=access_denied`を返すため、この場合も理由を伝えないまま同じ汎用エラーになっていた。スコープにDrive等の追加権限を含めると実際に同意画面で「キャンセル」を押す選択肢が生まれるため、拒否した利用者はチャット機能自体にもログインできないまま原因不明で詰まる状態になる（ログイン権限とDrive権限が同一リクエストにまとめられているため、部分的な許可という選択肢が無い）。コールバックに`error`クエリパラメータを新たに受け取り、`error=access_denied`かつ`state`がCookie値と一致した場合（改ざんされた偽のエラー遷移を弾くため、一致しない場合は従来どおり`oauth_failed`のまま）のみ、新規エラー種別`consent_denied`（「Googleへのアクセス許可が完了しなかったため、ログインできませんでした。もう一度『Googleでログイン』を押し、表示された内容を『許可』してください。」）へ誘導するようにした。curlでGoogleからの3種類の応答（`error=access_denied`＋state一致→`consent_denied`、`error=access_denied`＋state不一致のなりすまし試行→従来どおり`oauth_failed`、`error=invalid_scope`等の他のエラー値→`oauth_failed`）を再現して確認し、Playwrightで実際に表示されるメッセージも確認した。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書の改訂は無い。詳細設計書（総論7.2節、v1.7）・docs/index.html（Ver 4.43）を同じコミットで更新した（2026-09-03）。
- バグ修正: あるユーザーがチャンネル会話を開いたまま、ログイン・ログアウトのボタンを経由しないセッション切り替え（複数タブ・別ブラウザでの再ログイン・dev-loginのAPI直接呼び出し等）が起きると、そのタブは直前のユーザーが見ていたURLをそのまま新しいユーザーの権限で描画してしまい、非公開チャンネルであれば「🔒 {チャンネル名} には参加していません」というメッセージ経由で存在まで意図せず開示していた（ユーザーからの報告。層2向けDrive OAuthスコープ拡張の実機検証中に発見）。ログイン・ログアウトの各ボタン自体は既に`navigate()`で安全な起点へ遷移する実装だったが、それらを経由しないセッション切り替えは保護されていなかった。`App.tsx`に、`useMe()`が返すユーザーIDがアプリ実行中に変化したことを検知するガードを追加した。**当初は`useEffect`内で`navigate('/', {replace:true})`を呼ぶ実装にしたが、Playwrightでの実機検証で再現性のない失敗が発生した**（`ChannelView`が新しいユーザーの権限で一度マウントされてしまってから遅れて是正する形になり、他のフック（`useChannel`・`useMessages`等）の再検証と競合すると是正が効かないケースがあった。本番ビルド・StrictMode無効でも同じ不具合が再現したため、開発時のStrictMode二重実行が原因ではないと確認済み）。レンダー中にstateを調整するReactの公式パターン（`useUnreadDivider.ts`と同じ手法）へ変更し、ユーザーIDの変化を検知したら`ChannelView`等のルート要素を一切描画せず`<Navigate to="/" replace />`のみを返すようにした（新しいユーザーの権限で古いURLの内容が一瞬でも描画されない）。あわせて、この検知自体が確実に一定時間内（数秒）に働くよう、`useMe()`にも`usePolling`と同じ3秒間隔の`refreshInterval`を追加した（フォーカス／オンライン復帰イベントの発火だけに頼ると、タブがフォーカスを失わない限り検知されないケースがあった。実機検証でもこれが無いと5回中1〜2回程度失敗することを確認し、追加後は安定して再現しなくなった）。Playwrightで実際に、非公開チャンネルを開いた佐藤花子のタブのCookieを裏で管理者アカウントへ直接すり替え（アプリのボタンを経由しない想定の再現）、タブ自体は何も操作せずに数秒待つだけで、リーク（🔒メッセージの表示）が起きず管理者自身の所属チャンネルへ自動的に遷移することを複数回連続で確認した。**続けてユーザーから、同じ切り替え操作でサイドバーのDM相手一覧にも直前のユーザーのものが数秒間残る（若干のタイムラグ）との報告を受けた**。原因は同じで、SWRのキャッシュキーがURL（`/api/dms`等）のみでユーザーを問わないため、`Layout`が再マウントされても`useDms()`等は各フック自身の次の自然な再検証（ポーリング間隔）までは古いデータを表示し続けてしまうこと。`pendingSwitch`を消費する同じ`useEffect`内で、SWRのグローバル`mutate(() => true, undefined, {revalidate: true})`を呼び、切り替わった直後に全キャッシュ（チャンネル一覧・DM一覧を含む）を即座に再取得するようにした。Playwrightで実際に、山田太郎（DM相手に佐藤花子を含む）から長井比呂（DM相手は石井直樹119のみ）へCookieをすり替え、旧ポーリング間隔（3秒）の半分の1.5秒後にはサイドバーのチャンネル一覧・DM一覧・URLのすべてが長井比呂自身のものに揃っていることをスクリーンショットで確認した。テストチャンネル・検証スクリプトはすべて削除済み。詳細設計書（総論5.9節、v1.8）・docs/index.html（Ver 4.44）を同じコミットで更新した。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書の改訂は無い（2026-09-03）。
- S-06「参照ドキュメント範囲」タブで、フォルダ内の特定ファイルだけを参照範囲に含められるようにした（ユーザーからの明示的な要望「フォルダの中のどのファイルだけを参照範囲に含める、みたいなことができるとよい」）。着手前に実装方式を確認したところ、実際のDrive APIでフォルダの中身（サブフォルダ名・件数）を自動列挙する画面モックアップS-06どおりの方式は、GCP側のDrive API有効化（本書時点で千田氏へ依頼中、未解決）・Drive OAuthスコープの全社展開の可否（要件定義書8.2節の別途協議事項と関連、未解決）・各利用者のDriveアクセストークンをサーバー側に保存・更新する基盤（未実装）のいずれも前提として必要と判明したため、これらの解決を待たず、S-08の既存フォルダ登録（A-39）と同じ「URL/IDの手動貼り付け」方式のまま個別ファイルも登録できる方式を選んだ（ユーザーへ選択肢を提示し確認）。新規テーブルは追加せず、T-09 doc_foldersに`item_type`（'folder'/'file'）・`parent_folder_id`（自己参照FK、ON DELETE CASCADE。フォルダはNULL、ファイルは必ず登録先フォルダのidを持つ）を追加した。T-10 channel_doc_foldersは無変更（`folder_id`がフォルダ・ファイルどちらのidも指せるようになっただけで、A-27の「idの集合を洗い替える」既存ロジックはそのまま使い回せる）。`routers/admin.py`のA-39（`create_doc_folder`）に`parent_folder_id`（省略可）を追加し、指定時はそのフォルダ配下のファイルとして登録する（登録先が存在しなければ404、`item_type='file'`の行を指定した場合＝ファイルの下にファイルは登録できないため422）。ファイルURLからのID抽出用に`_DRIVE_FILE_URL_RE`（`/file/d/<id>`・`?id=<id>`形式）を追加し、既存の`_extract_folder_id`を`_extract_drive_id(raw, *, is_file)`に一般化した。A-38（一覧）・A-40（削除）は`item_type`/`parent_folder_id`をレスポンスに含める・CASCADE削除の説明を追記するのみで、SQL自体はほぼ無変更（A-40は元々`DELETE FROM doc_folders WHERE id=$1`のままで新しいFKのCASCADEに任せられた）。フロントは`AdminConsole.tsx`の`DocFoldersTab`をツリー表示（フォルダの下に登録済みファイルを字下げ表示）にし、追加フォームに「フォルダ全体」/「フォルダ内の特定ファイル」のラジオ切替（ファイル選択時は登録先フォルダの`<select>`が出現）を追加した。`ChannelSettings.tsx`の`DocScopeTab`もチェックボックスをツリー化し、フォルダ・その配下のファイルを独立して選択できるようにした（フォルダにチェック＝フォルダ全体を含める、個別ファイルにチェック＝そのファイルだけを絞り込んで含める、という2つの独立したチェックボックスである旨をヒント文言で明記）。curlで一通り確認（ファイル候補の作成・一覧のitem_type/parent_folder_id・ファイルの下にファイルを登録しようとした場合の422・存在しない登録先の404・重複drive_folder_idの409・親フォルダ削除時に子ファイルもCASCADEで連動削除されること・A-27保存でファイルのidを含めて洗い替えできること）に加え、Playwrightで実際のブラウザ操作を確認：S-08でファイル候補を追加→ツリーに反映→S-06の参照ドキュメント範囲タブで該当フォルダの下に表示→ファイルだけにチェック（フォルダ自体は未チェックのまま）→保存→リロード後も選択状態が正しく持続することを確認した。テストデータ（作成した候補・チャンネルへの割当）はすべて削除済み、consoleエラーは既存の未認証時`/api/auth/me`401（ログイン前の`/login`遷移時の既知の挙動）のみ。実際のDrive API連携版（画面モックアップが想定する自動列挙）は、上記3つの前提が解決してから改めて着手する。基本設計書（6.2節、v1.36）・詳細設計書DB設計（T-09、v1.13）・API設計（A-38〜A-40、v1.30）・docs/index.html（Ver 4.45）を同じコミットで更新した。要件定義書・画面モックアップは既存のF-22・S-06・S-08の記載どおりで変更なし（2026-09-03）。
- S-08「ドキュメント参照範囲」タブの一覧を画面モックアップと同じ見た目に作り直した（ユーザーからの明示的な要望。直前のS-06参照ドキュメント範囲タブのツリー表示に続く対応）。モックアップは字下げによるツリー表示ではなく、フォルダ→その配下ファイルの順に並べたフラットな一覧＋「親フォルダ名 ＞ 項目名」というパンくず形式の名前表記（例:「開発部 ＞ 運用ドキュメント」）であるため、`AdminConsole.tsx`の`DocFoldersTab`をS-06のツリー実装とは別に、この見た目に合わせて書き直した。新規`flatRows`（トップフォルダを`flatMap`でその子ファイルと一緒に並べる）・`nameOf`（file行はパンくず名を組み立てる）・`dateOf`（`created_at`を`toLocaleDateString('ja-JP')`で整形。モックアップのメタ行に日付表示があったが従来は表示していなかった抜けもここで解消）を追加。行の見た目もモックアップの`.folder-row`（1本の枠の中で罫線区切り、削除ボタンはボーダー無しのテキストリンク、ホバーで赤くなるのみ）に合わせ、従来の「行ごとに個別の角丸ボックス＋ボーダー付き削除ボタン」から変更した。モックアップのメタ行「12件」は実際のDrive同期による真のファイル数だが、このアプリはまだ同期を実装していないため、フォルダ行には代わりに「登録ファイルN件」（管理者が実際に登録した子ファイル候補の数、誤解を避けるため文言を分けた）を表示し、既存の実データである「使用中のチャンネルN件」もあわせて残した。モックアップの「今すぐ同期」ボタン＋「最終同期: ...」の`sync-bar`は、従来一覧の追加フォーム内にボタンとして埋め込まれていたのを独立したバーとして一覧の直下に切り出した（「対象N件」は登録済み候補の総数で代用、実際の同期は引き続き未実装のため無効表示のまま）。フォルダ選択画面（Google Picker）が使えない制約は変わらないため、「＋ 新しい候補を追加」の手動URL入力フォーム自体はモックアップの単発ボタンと異なる形のまま維持した（この差異はCLAUDE.md直前の項目で説明済みの設計判断のとおり）。Playwrightで実際のブラウザ操作を確認: フォルダ登録→そのフォルダ内へのファイル登録→一覧がフラットな並びでパンくず名・日付・登録ファイル件数・使用中チャンネル件数を正しく表示すること、削除ボタンでの削除、をスクリーンショットで確認した。テストデータはすべて削除済み。バックエンドAPI・DBは無変更（フロントエンドのみの見た目の変更）のため、設計文書側の改訂は無い（2026-09-03）。
- バグ修正: S-06「振る舞い定義」タブのヒント文言が、監査ログ機能（T-16・A-44、2026-09-01実装）の実装後も更新されておらず、変更者・日時が監査ログに記録されることを画面上のどこにも示していなかった。ユーザーから「要件定義書REQ-F-08（振る舞い定義の変更履歴を追跡できること）が現在の監査ログで実現できているか」との調査依頼を受けて調査した結果判明した。バックエンド側（A-26→`audit_log.record()`→T-16、A-44での取得、S-08「監査ログ」タブでの一覧表示）はREQ-F-08・7章「監査」の要件（変更者・日時の記録と事後確認）を正しく満たしており実装上の不備は無かったが、`ChannelSettings.tsx`の`PromptTab`だけ、監査ログ実装前に書かれた「S-08監査ログタブと同様に未実装のため記録しない旨をヒントに明記する」というコメント・ヒント文言のまま取り残されており、chadmin本人が自分の変更が監査ログに記録されることを画面から知る手段が無かった（データの欠落ではなく、画面表示の記載漏れ）。ヒント文言を画面モックアップどおり「編集のたびに上書き保存されます。変更者・日時は監査ログ（S-08管理コンソール）に記録されます（過去バージョンの一覧・差分表示は対象外）。」に修正し、古いコメントも実態に合わせて更新した。Playwrightで実際に修正後の文言が表示されることを確認した。バックエンドAPI・DBは無変更のフロントエンドのみの変更のため、設計文書側の改訂は無い（2026-09-03）。
- バグ修正: S-05横断検索の検索欄で、検索条件モディファイア（F-42）の説明が書かれたplaceholder文言「メッセージを検索（in:チャンネル from:投稿者 のように条件を指定できます）」が、検索欄自体の幅（`max-w-md`、実測クライアント幅約400px）を超えて末尾が見切れ、読めなくなっていた不具合をユーザーからの報告で修正した。ネイティブ`<input>`のplaceholderは折り返し・省略記号（…）表示がなく、幅を超えた分は単純に切れて非表示になるため、Playwrightでcanvas経由の実測描画幅（元の文言は約457px、欄の幅を上回っていた）を確認したうえで、欄に収まる短い文言「メッセージを検索（in:チャンネル from:投稿者）」（実測約285px）に差し替えた（`SearchView.tsx`）。なお完全なモディファイア一覧（in:/from:/with:/before:/after:/on:/during:/"…"）自体は、画面モックアップの設計どおりplaceholderではなく検索欄クリック時の候補ポップオーバー下部に既に表示されており、そちらは対象外（見切れていない）。フロントエンドのみの変更でバックエンドAPI・DBは無変更のため設計文書側の改訂は無い（2026-09-03）。
- S-05横断検索の結果一覧に、発言者のアイコンを表示するようにした（ユーザーからの明示的な要望「検索でヒットした発言の発言者の横にその人のアイコンを表示することはできますか」）。画面モックアップS-05の`result-row`は元々発言者アイコンを想定していない（名前・日時のテキストのみ）ため、モックアップに無い追加として扱った。`routers/search.py`のmessage/file両方のSELECTに`m.sender_type, m.sender_user_id, m.bot_icon, m.bot_icon_url, u.picture_url AS sender_picture_url`を追加し、レスポンスの`sender_type`・`sender_user_id`・`sender_picture_url`・`bot_icon`を、A-10等の`_message_out`と全く同じ優先順位（AI/BOT発言は`bot_icon_url`→`bot_icon`絵文字→既定表示、人間は`picture_url`→色付き頭文字）で返すようにした。フロントは新規に描画ロジックを増やさず、`MessageList.tsx`の`Avatar`コンポーネントを`SearchView.tsx`から直接再利用した。再利用にあたり2点変更した: (1) `Avatar`のprops型を`message: Message`（全フィールド必須）から、実際に使う6フィールドだけを`Pick`した新規`AvatarSource`型に緩め、`SearchResultItem`のような別の形のデータからも呼べるようにした（`SearchView.tsx`に`avatarSourceFor()`を追加し、`sender_display_name`→`sender_name`、`message_id`/`attachment_id`→`id`のようにフィールド名の差分を吸収する変換を行う）。(2) 会話ログの34px固定サイズでは検索結果一覧（密なリスト表示）には大きすぎたため、新規`size`prop（既定34、省略時は完全に従来どおり）を追加した。サイズ・角丸半径・フォントサイズはTailwindの任意値クラス（`rounded-[9px]`等）ではなく`style`で指定するよう変更した（動的に生成するクラス名はTailwindのビルド時静的スキャンで拾われずCSSが生成されないため）。検索結果はメッセージ行に18px、ファイル行に16pxのアイコンを表示する。curlで実際にA-20のレスポンスに新フィールドが含まれることを確認し（人間の発言はpicture_url、BOT発言のシステム通知はsender_picture_url null・bot_icon null→既定🔔表示になることを確認）、Playwrightで実際のブラウザ操作を確認: 「テスト」で検索した結果一覧に人間（画像アイコン）・AI（「AI」グラデーション角丸四角）の両方が正しい形・優先順位で表示されること、画像は全て`naturalWidth>0`で正常に読み込めていること、会話ログ本体（既定34pxサイズ）の見た目に回帰が無いことをスクリーンショットで確認した。テストデータの新規作成は行わず、既存の検索結果を読むだけで確認できたため、クリーンアップ対象も無い。詳細設計書API設計（A-20、v1.31）・docs/index.html（Ver 4.46）を同じコミットで更新した。要件定義書・基本設計書・画面モックアップは変更なし（2026-09-03）。
- S-05横断検索の結果クリックでその発言までスクロールし薄いオレンジでハイライトする機能を追加した（ユーザーからの明示的な要望「検索してヒットした発言をクリックすると会話画面に飛ぶのは良いが、その発言全体を薄いオレンジでハイライトして目立たせたい」）。着手前に調査したところ、A-10/A-18は元々「直近50件のみ」しか読み込めず「もっと古いメッセージを読み込む」機能自体が無いため、検索結果（過去の発言を探すためのもの）の多くはこの範囲の外にあり、単純なDOM要素へのスクロールだけでは大半のケースで機能しないと判明。ユーザーに実装範囲（直近50件のみ対応の簡易版か、任意の古さの発言にも対応する版か）を確認し、後者（推奨案）を選択した。**バックエンド**: A-10（`routers/channels.py`）・A-18（`routers/dms.py`）に新規パラメータ`around`（発言id）を追加し、指定した発言を中心に前後25件ずつ（`AROUND_WINDOW`）を取得する`_around_rows`ヘルパーを両ルーターにそれぞれ実装した（`since`と同時指定時は`since`優先、対象が存在しない・他チャンネルの発言・スレッド返信の場合は404）。汎用的な「無限スクロールでの過去メッセージ読み込み」機能自体は今回も見送り、既存の「1回のGETで完結する」設計を踏襲した点は基本設計書6.2節に設計判断として明記。A-20（`routers/search.py`）のmessage/file両方の結果に`thread_parent_id`を追加し、file結果には添付先の発言id（`message_id`）も追加した。**フロントエンド**: `useMessages`に`anchorMessageId`引数を追加し、初回取得を`?around=`に切り替える。SWRキー自体に`::around=<id>`という（実際のURLには使わない）識別子を含めることでanchor変化時に即座に再取得させつつ、fetcher内部では常に`basePath`から実URLを組み立てる（SWRの`key`をそのままfetch URLに使うと`?around=`が二重に付く問題に気づき、keyとfetch URLを分離した）。anchorMessageIdがundefinedに変わっただけでは内部state（`state.current`）をリセットしない設計とし、`?highlight=`をURLから消してもそれまでの読み込み内容を保持したまま通常の`since`継続ポーリングへ自然に移行できるようにした。`MessageList.tsx`に`highlightMessageId`propを追加し、該当行に`id="message-{id}"`属性と`bg-bot-bg`（既存の薄いオレンジ系トークン、S-05検索語ハイライトの`<mark>`と同じ流用）背景＋`transition-colors duration-700`を付与、`scrollIntoView({block:'center'})`で1回だけスクロールする（`scrolledFor` refで同じ対象への再スクロールを防ぐ）。`ChannelView.tsx`/`DmView.tsx`は`?highlight=`を読み取り`anchorMessageId`として`useMessages`・`highlightMessageId`として`MessageList`/`ThreadPanel`へ渡し、既存の「新着で末尾へ自動スクロール」effectをハイライト中は止める（`MessageList`内部の`scrollIntoView`と競合し常に末尾へ引き戻されてしまうため）。3秒後に`?highlight=`を自動的に消すtimer effectを追加し、ハイライトのフェードアウトと通常表示への復帰を両立させた。スレッド返信（`thread_parent_id`あり）は本体タイムラインに流れないため、`SearchView.tsx`の`openResult`が`?thread=<元発言id>`も付けてスレッドパネル側を開くようにした（A-13は元々スレッドの全返信を無条件取得するため`around`相当の対応は不要）。`ThreadPanel.tsx`にも`highlightMessageId`propを追加し、内部の`MessageList`へ転送するとともに、ハイライト対象が実際にそのスレッドの返信一覧に含まれる間は自身の末尾自動スクロールを止める。`ChannelView.tsx`側は`threadId`が同時に付いている場合、`anchorMessageId`を返信自体のidではなくスレッドの元発言id（`threadId`）にする分岐を入れた（`ThreadPanel`の`parentMessage`は本体`messages`から探す実装のため、元発言が直近読み込み分の外にあると見つからずnullになる問題への対処）。Windows特有の事情でDocker Desktopが未起動＋DB_PORT（55433）がWindowsの動的ポート除外範囲と偶然重なり`kogack-db`を起動できない問題に遭遇したが、同じデータボリュームを再利用する一時コンテナ（別ポート）を作って検証した（本来のコンテナ・ポート設定は変更していない）。curlで`around`の境界値（存在しない発言・他チャンネルの発言・スレッド返信はいずれも404）を確認し、Playwrightで実際に日付が古い発言（直近50件の外）・スレッド返信の両方でハイライト→scrollIntoView→3秒後のフェードアウトと`?highlight=`除去、および通常表示（ハイライト無し）での末尾自動スクロールに回帰が無いことを確認した。テストデータの新規作成は行わず既存メッセージを使い回したため、クリーンアップ対象も無い。基本設計書（6.2節、v1.37）・詳細設計書API設計（A-10・A-18・A-20、v1.32）・docs/index.html（Ver 4.47）を同じコミットで更新した。要件定義書・画面モックアップは既存のF-06・S-05の記載どおりで変更なし（2026-09-04）。
- スレッド返信（A-14）内でのチャンネルAIへのメンションに対応した（ユーザーからの明示的な要望「スレッド内でAIに呼びかけても発言できない仕様か、できるならメンション先候補にもAIを入れてほしい」）。AIサポート基盤の初回スライス（2026-08-28）時点から「次スライスでA-14にも同じ配線を追加する」と明記されていた対象外事項で、`services/ai_agent.py`冒頭コメントの「このスライスのスコープ外」一覧を更新した。`ai_agent.maybe_trigger`に`thread_id`引数（省略可）を追加し、`routers/messages.py`のA-14（`post_reply`）から元発言がチャンネルの場合のみ呼び出すようにした（DMのスレッドは対象外、A-11と同じ判断基準）。**スレッド内は`reaction_mode`（F-15、proactive）に関わらず常に明示的な「@ペルソナ名」メンションを要求する**設計判断を追加した（proactiveをスレッド内の人間同士のやり取りにまで広げると毎回AIが割り込む形になり要望の範囲を超えるため。基本設計書8.1節に追記）。AIの応答は`start_summary`と同じ方式で`thread_parent_id`付きの発言として同じスレッドへ投稿される（本体タイムラインには流れない）。会話履歴も新規`_fetch_history_rows`ヘルパーでチャンネル全体ではなくそのスレッド自身（元発言＋返信、直近`MAX_HISTORY_MESSAGES`件）に絞り込むようにした（Slack等の類似ツールと同じく、スレッド内でメンションされたAIはそのスレッド自身の文脈を見るべきと判断）。フロントは`ThreadPanel.tsx`に新規`aiIsEnabled`・`aiPersonaIconUrl`propを追加し、`ChannelView.tsx`の`mentionCandidatesWithAi`と全く同じロジックで`mentionCandidatesWithAi`をスレッド側でも構築し、既存の`Composer`の`mentionCandidates`に渡すよう変更した（DMのスレッドではこれらpropを渡さないため、DMは従来どおりAI候補なし）。curlで一通り確認（スレッド内で「@Kogack AI ...」と返信→同じスレッドへのAI発言として応答が生成されること、本体タイムラインには一切現れないこと、メンション無しの返信では発火しないこと）に加え、Playwrightでスレッドパネル自身のメンション候補ポップオーバーに「Kogack AI」が正しいアイコンで表示されることを確認した。テストチャンネル・検証スクリプトはすべて削除済み。詳細設計書は既存のAIサポート10章の記載どおり（当初から想定されていた次スライスの実装のため、基本設計書8.1節の設計判断追記以外は改訂なし）。要件定義書・画面モックアップは変更なし（2026-09-04）。
- ホスティング先をKoyebからFly.ioへ変更し、リポジトリ直下に`Dockerfile`・`.dockerignore`・`fly.toml`を新規作成した（ユーザーの意向。「乗り換えます」との明示的な指示を受けて設計文書側も同じコミットで更新）。`Dockerfile`はマルチステージビルド（ステージ1: `node:24-slim`でfrontendを`npm ci`→`npm run build`、ステージ2: `python:3.14-slim`でbackendの`requirements.txt`を`pip install`し、ステージ1の`frontend/dist`だけをコピーする）で、最終イメージにNode.js本体・node_modulesを一切含めない構成にした（`backend/main.py`が元々「backendの1つ上の階層にある`frontend/dist`」を静的配信する設計だったため、コンテナ内でも同じ相対位置関係を再現するだけで済んだ。コード変更は不要）。非rootユーザー（`appuser`）で実行し、起動コマンドは`CMD ["sh", "-c", "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"]`でFly.ioが渡す環境変数`PORT`を動的に読む（決め打ちしない）。ベースイメージのバージョン（Node 24・Python 3.14）はKeirekiの`Dockerfile`（Node 22・Python 3.12、Koyeb向け）とあえて揃えず、このリポジトリのローカル開発環境の実際のバージョンに合わせた（「Keirekiからの意図的な相違点」に追記）。`.dockerignore`は`.env`・`node_modules`・`__pycache__`・`.git`に加え、`backend/uploads`（ローカル開発用のアイコン・添付ファイル保存先。本番はSupabase Storageへ置き換える想定のため不要）や`docs/`等の設計ドキュメント一式も除外した。**ユーザーから「AIはよく間違える」として実際にチェックすべき項目のリスト（PORT決め打ち・.dockerignoreの.env漏れ・マルチステージの有無・COPY順序・存在しないファイルのCOPY・架空のライブラリ名/バージョン・ネイティブライブラリ不足）を提示され、それぞれについて実際に`docker build`・`docker run`まで実行して検証した**（推測や「動くはず」で済ませず、実機で確認）。具体的には: `docker run -e PORT=18080`で実際に18080番で待受することを`docker logs`で確認（固定値でないことの実証）、`docker build`が実際に最後まで成功すること自体が「存在しないファイルのCOPY」「架空のライブラリ名・バージョン」が無いことの実証になる点を明記、`cryptography`（Google IDトークンのRS256検証に使用。KeirekiがWeasyPrint用にPango/Cairo等のapt-get installを必要とするのと対比される、唯一ネイティブ部分を持つ依存）について`pip install`の成功だけでなく`from cryptography.hazmat.primitives.asymmetric import rsa`が実際にimportできることをコンテナ内で直接実行して確認し、Kogackの依存関係一式（PDF出力・画像処理ライブラリなし）ではapt-get installが不要と判断できることを示した。`fly.toml`は`internal_port=8000`（Dockerfileの`ENV PORT`既定値と一致）・`/healthz`へのヘルスチェック・本番/ステージングをアプリ単位（`kogack`/`kogack-staging`）で分離する構成にしたが、**Fly.io CLIはこの環境にインストール済みだった一方、未ログイン状態のため`fly config validate`等による実機検証はできておらず、Dockerfileと異なりこのファイルの正しさは実際に`fly deploy`するまで確証できていない**（ユーザーへその旨を明記して伝達済み）。基本設計書（2.1・9.2・9.6節に設計判断・表更新、v1.39）・詳細設計書総論（7.5節、v1.9）・docs/index.html（Ver 4.49）を一貫して更新し、`backend/main.py`・`routers/attachments.py`・`routers/icons.py`・`.env.example`のコード内コメントのKoyeb表記もFly.ioへ更新した（`google_auth.py`は現在進行中のDrive PoC用の未コミット変更と同じ行に近接していたため、コンフリクトを避けるためあえて据え置いた）。要件定義書・画面モックアップは変更なし（ホスティング先を個別のF-xx/S-xxに紐づけて記載していないため）。
- バグ修正＋新機能: AIの生成を強制的に中断できる機能（A-74）を追加し、あわせてバックエンドプロセス再起動時の自動復旧を実装した。ユーザーからの報告「雑談チャンネルのAIが、生成中のままの状態を数十分も続けている」を受けて調査したところ、実際に`雑談`チャンネルの発言（id=34、`generation_status='generating'`のまま46分経過）が見つかり、原因は`services/ai_agent.py`の非同期タスク方式（`asyncio.create_task`でAI応答生成をfire-and-forget起動する設計、8.1節）が「タスクを開始したプロセス自体が生成中に再起動・クラッシュした場合」の復旧手段を一切持っていなかったこと（`ai_client.py`は`timeout=60.0, max_retries=1`を明示設定済みで、単なるAPI応答の遅さであれば60〜120秒程度で必ず`except Exception`に捕捉されて解消するはずであり、46分もの長時間固まっていたことから、プロセス再起動でタスク自体が失われた「オーファン化」以外の説明がつかないと判断した。ローカル開発の`uvicorn --reload`は`.py`編集のたびに再起動するため、この日の一連の作業中に実際に再起動と生成中のタイミングが重なったとみられる）。**対策は2段構え**: (1) 新規`_active_generations: dict[int, asyncio.Task]`（`ai_agent.py`モジュールレベル）に、`_generate_and_post`・`_generate_summary_and_post`（F-14要約も同じ仮レコード方式のため同じリスクを持つ）が開始時に`asyncio.current_task()`で自分自身を登録し`finally`で必ず取り除く。新規`cancel_generation(message_id)`が、対応表に実行中のタスクが見つかればその場で`task.cancel()`しつつ、見つからなくても（オーファンでも）`UPDATE messages SET body=..., generation_status=NULL WHERE id=$1 AND generation_status='generating'`で必ず復旧できるようにした（`WHERE generation_status='generating'`の条件により、キャンセルとほぼ同時に生成が正常完了した場合の競合で後から上書きされる事故を防ぐ。両関数の完了時UPDATE・エラー時UPDATEも同じ条件に変更した）。`_generate_and_post`/`_generate_summary_and_post`に`except asyncio.CancelledError: raise`を追加し、キャンセル時は本文の書き込みをcancel_generation側に一任してそのまま再送出する（asyncioの作法として、キャンセルされたタスクは最終的にCancelledErrorを伝播させる必要がある）。(2) 新規`ai_agent.recover_orphaned_generations()`を`main.py`のlifespan起動時に呼び出し、起動時点で`generation_status='generating'`のまま残っている行を無条件に一括復旧する（プロセスが新しく立ち上がった直後は`_active_generations`が必ず空＝前プロセスのタスクを絶対に持ち得ないため、見つかった行はすべてオーファンと判断してよい）。**新規API A-74**（`POST /api/messages/{id}/cancel-generation`、`routers/messages.py`）を追加し、対象は`sender_type='ai'`かつ`generation_status='generating'`の発言に限定（404/400で弾く）、権限はA-12削除と異なり投稿者本人・admin限定にせず**そのチャンネルの参加者なら誰でも中断可能**にした（AI発言に「所有者」という概念が無く、生成が詰まっている状態は参加者全員の閲覧を妨げるため）。フロントは`MessageList.tsx`の「生成中…」インジケーターの右に常時表示（ホバー時のみ表示する削除ボタンとは異なり、参加者全員が気づけるようにするため）の「■ 中断」ボタンを追加した。中断後の表示反映は次の3秒ポーリングに任せる（AI応答完了時の表示更新も同じくポーリング任せで一貫している）。**動作確認は実際に実運用中に発生していた本物の障害データを使って行った**: バックエンドを再起動（`.py`編集による`--reload`）した瞬間に`recover_orphaned_generations()`が実行され、46分間固まっていた`雑談`チャンネルの発言id=34が実際に自動復旧されることを確認（curlで`generation_status: null`・本文が「サーバーの再起動により、この発言の生成は中断されました。もう一度お試しください」に変わったことを確認）。あわせて使い捨てのテストチャンネルで、実際にAI生成をトリガーしてから即座にA-74を呼び出し、本物の実行中タスクを`cancel()`できること（HTTP 200、中断メッセージへの更新を確認）、中断後8秒待っても（cancel済みのタスクの応答が後から上書きしてこないこと）本文が変わらないこと、既に完了済み・AIでない発言・存在しない発言への呼び出しがそれぞれ400/400/404になること、非参加者からの呼び出しが404（存在を伏せる）になることを確認した。Playwrightで実際のブラウザ操作も確認：「生成中…」表示の隣に「■ 中断」ボタンが表示されること、クリックすると成功トーストが出て（3秒ポーリング後に）中断済みの表示に変わること、consoleエラーは無関係な既存の401のみ。テストチャンネル・検証スクリプトはすべて削除済み。基本設計書（8.7節に設計判断を追記、v1.40）・詳細設計書API設計（A-74、v1.33）・docs/index.html（Ver 4.50）を同じコミットで更新した。要件定義書上のF-xxに対応付けられた変更ではないため（F-31生成中表示に対する運用上の補完）、要件定義書・画面モックアップの改訂は無い（2026-09-04）。
- **未実装**: 層2ドキュメントQ&Aの索引・AI検索（Drive同期・埋め込みベクトル生成・search_documentsツール・回答根拠の引用表示。Drive OAuthスコープ拡張とOpenAI Embeddings APIの実通信検証が前提）、実行前確認・座席予約連携（上記AIサポート基盤の「スコープ外」参照。スキル・反応モード・自動対応範囲分類はF-12・F-15・F-16実装により対象外リストから外れた）、AI利用コストの80%到達時の通知メール送信・上限到達時の応答停止（上限到達時の挙動は千田氏との別途協議事項）。Fly.ioへの実デプロイ・GitHub Actionsによる自動デプロイは2026-09-04に完了した（実装状況末尾を参照）。主要画面（S-01〜S-06, S-08）はひととおり縦切りを終え、S-05・S-08はいずれも設計どおりの機能をひととおり実装済み。AIサポート・F-36/F-38・F-07・層2の第一歩・F-42・F-14・F-29・F-12・F-15・F-16にも着手したため、次は層2の索引・AI検索本体（Drive OAuthスコープ拡張・OpenAI Embeddings APIの実通信検証が前提）に取り組むか、実行前確認（F-25）・座席予約参照連携に着手するかをユーザーと相談する。
- Fly.io本番（`kogack`）への初回実デプロイと、GitHub Actionsによるpush時自動デプロイ（Fly.io編手順書どおり、検討資料は起こしていない）を完了した。ユーザーの実機操作とこちらのcurl/`fly` CLIでの検証を交互に行いながら進め、2件の実害のある落とし穴を踏んだため記録する。(1) **`fly launch`はオーガニゼーションを明示しないとPersonal orgがデフォルトになる**仕様で、Personal orgにはカードを登録していないため`failed to get placements: requested machine count exceeds organization limit`のエラー後「カードを登録するか」と聞かれる状態になった（個人のカードを登録しない方針のため、ここで中断）。`fly orgs list`で千田氏に招待してもらった共有オーガニゼーション（カード登録済み）のスラッグを確認し、`fly launch`に`--org <slug>`を明示して解消した。(2) **`fly deploy`はGitの状態ではなく作業ディレクトリの実ファイルをそのままビルドする**ため、層2Drive PoC検証用の未コミット変更（`google_auth.py`のOAuthスコープへの`drive.readonly`追加、`routers/auth.py`のaccess_tokenを平文ファイルへ書き出すTEMPコード）が残ったまま初回`fly deploy`を実行してしまい、本番の`/api/auth/login`が実際に`drive.readonly`を含むリダイレクトを返すことを事後のcurl検証で発見した（幸い、この状態で実際にログインを完了した利用者はいなかったことを確認済み）。`git stash`で該当2ファイルのみ直前のコミット状態へ戻し再デプロイして解消した。**教訓として、本番への`fly deploy`前に`git status`がクリーンであることを確認する運用が必要**（今回は未確認のまま進めてしまった）。ほかに、手順書が明示する最低限のsecrets（`DATABASE_URL`・`JWT_SECRET`）だけでは不足しており、**Kogackでは`GOOGLE_CLIENT_ID`・`GOOGLE_CLIENT_SECRET`も設定しないとGoogle認証が501で機能しない**ことも判明した（`google_auth.py`の`is_configured()`が両方を要求する。`GOOGLE_REDIRECT_URI`はリクエストのHostヘッダーから動的導出される設計のため未設定のままで問題ない）。Google側のリダイレクトURI登録（千田氏対応）が有効かは、実際のログイン完了を待たず、`/api/auth/login`のLocationヘッダーから取り出した`redirect_uri`を含む実際の認可URLをGoogle自身の認可エンドポイントへ直接投げ、`redirect_uri_mismatch`にならず通常のサインイン画面が表示されることで検証した。`.github/workflows/fly-deploy.yml`は手で書く前に`fly launch`が既に自動生成していたためそのまま採用した（`main`・`master`両方がトリガー対象になっている）。`FLY_API_TOKEN`は`fly tokens create deploy -x 999999h`で作成し、値を会話にもコードにも出さずユーザー自身のブラウザからGitHubのリポジトリSecretsへ直接登録した。初回の自動デプロイはGitHub Actionsのランナー起動が加わり手動`fly deploy`単体（約3.5分）より時間がかかったが、`fly releases`への新バージョン追加と`fly status`のヘルスチェック通過で成功を確認した。事前に手で調整していた`fly.toml`（`[[vm]]`ブロック追加、ヘルスチェック間隔の`30s`への修正）は`fly launch`実行時にコメントごと自動生成し直されたが値は保持された。デプロイ先は本番アプリ`kogack`（ステージングの`kogack-staging`ではない）。基本設計書・詳細設計書側はFly.io採用自体の設計判断を2026-09-04付「ホスティング先をKoyebからFly.ioへ変更」の項で既に記録済みのため、今回は運用上の実施記録のみで設計文書の追加改訂は無い（2026-09-04）。
- バグ修正: 上記の初回デプロイ直後、Googleログインはできるがチャンネル・DMのメッセージ一覧取得が「サーバーエラーが発生しました」で失敗する障害が発生した（ユーザーからの報告）。`fly logs --no-tail`で実際のトレースバックを確認したところ`_message_out`の`row["is_summary"]`で`KeyError`。原因は`database.py`の`AUTO_MIGRATE`が`APP_ENV=production`のとき既定で無効になる設計（直前の項の初回デプロイ時点から変更していない既存仕様）で、本番Fly.ioは起動時にSCHEMAを一切適用しないため、`messages.is_summary`列（2026-09-03追加）が本番のSupabase DBへ反映されないまま残っていたこと。ローカルの`.venv`のasyncpgから本番`DATABASE_URL`へ直接接続し`information_schema.columns`を照会して`is_summary`列が実際に存在しないことを実測で確認した。復旧は2段階で行った: (1) `database.py`の`SCHEMA`定数を直接importして本番へ適用するPythonスクリプトを用意したが、DBへの書き込みを伴う操作のため権限の自動判定でブロックされた。回避を試みず、アプリ自身が既に持つ仕組み（`init_pool()`の`AUTO_MIGRATE`分岐）を使う方式に切り替え、`fly secrets set AUTO_MIGRATE=1`でユーザー自身に実行してもらい、再起動時にSCHEMAが適用されることで復旧した（SCHEMA全体が`CREATE TABLE IF NOT EXISTS`・`ADD COLUMN IF NOT EXISTS`・制約追加は`DO $$ ... EXCEPTION WHEN duplicate_object THEN NULL; END $$`でいずれも冪等に保たれているため、既存データ・制約に影響なく不足分のみ追加されることを事前にコードで確認済みだった）。(2) 復旧後、今後同種の事故（新しい列を追加するたびに本番だけ反映されない）を防ぐか・起動速度を優先するかをユーザーに相談し、**常時有効のままにする**方針で合意した。`database.py`側の既定値も「本番のみ無効」から「常時有効（明示的に`AUTO_MIGRATE=0`を指定した場合のみ無効）」へ反転し、Fly secretsの`AUTO_MIGRATE=1`と齟齬が出ないようにした（起動のたびに冪等なSQLが1回余分に走りコールドスタートがわずかに遅くなるトレードオフより、スキーマ取りこぼしの再発防止を優先する判断）。復旧後`fly logs`でエラーが解消し`/api/channels/*/messages`・`/api/dms/*/messages`等が200 OKで応答することを確認した。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-04）。
- バグ修正: 本番でチャンネルAIへメンションしても応答が生成されない、かつペルソナ名が「Kogack AI」になっていない障害が発生した（ユーザーからの報告）。原因は2件。(1) `fly secrets list`で確認したところ`OPENAI_API_KEY`・`AI_MODEL`が本番に未設定のままだった（手順書4章の最低限secretsにこの2つは含まれておらず、Google OAuth関連secretsのときと同様に後から気づいた抜け）。`services/ai_agent.py`はAPIキー未設定時`is_configured()`で早期returnし黙って何もしない仕様のため、症状としては「メンションしても無反応」にしか見えなかった。(2) 本番DBを直接照会したところ`channel_ai_settings.persona_name`列の既定値自体は`AUTO_MIGRATE`適用により正しく'Kogack AI'になっていたが、既存の1チャンネルは値が'AI'のまま残っていた。2026-09-02の「AI」→「Kogack AI」既定値変更時、既存行の一括更新は起動のたびに繰り返されるのを避けるため意図的に`database.py`へ埋め込まず一度きりの手動SQLで対応した経緯があり、その手動SQLは当時のローカルDBに対してのみ実行され本番には一度も当たっていなかった。修正はユーザー自身に`fly secrets set OPENAI_API_KEY=...`・`fly secrets set AI_MODEL=...`を実行してもらい、(2)はDB直接操作ではなくS-06「キャラクタ」タブから通常のアプリ操作で該当チャンネルの名前を再保存する形で行った（直前の項でDB書き込みスクリプトが権限の自動判定でブロックされた教訓を踏まえ、既存のAPI・画面経由で解決できる場合はそちらを優先する方針とした）。両方の対処後、実際に「@Kogack AI」のメンションで応答が生成されることを確認した（2026-09-04）。
- AI応答の生成が体感で遅いとの指摘を受け、`AI_MODEL`を`gpt-5-nano`から`gpt-4.1-nano`へ変更した。`gpt-5-nano`は推論（reasoning）系モデルで、`reasoning_effort='minimal'`を指定していても応答本文の生成前に内部の思考トークンを消費する分だけ待ち時間が伸びる（2026-09-02のAI API実機検証で判明した特性そのもの）。これは「安いモデルだから遅い」のではなく「たまたま最安だったモデルが推論系だった」ことが原因と判断し、非推論系かつ`gpt-5-nano`より新しい世代の`gpt-4.1-nano`（2026-09-04時点のOpenAI公式単価$0.10/$0.40、1Mトークン）へ切り替えた。`gpt-4o-mini`（2026-09-02時点で比較検討した候補）より安価かつコンテキスト長も大きいためこちらを選んだ。`services/ai_client.py`の`MODEL_COSTS`に単価行を追加した（既存の円/USD換算レートに揃える）。`REASONING_MODELS`には追加していない（非推論系のため`reasoning_effort`を渡さない挙動が正しい）。ローカル`.env`・本番Fly secretsの両方を更新した。応答品質の実機検証はまだ行っていないため、品質に問題があれば`gpt-4o-mini`等への再変更を検討する（2026-09-04）。
- バグ修正: メッセージ一覧の`since`差分ポーリング（A-10/A-18）が、AI応答の「生成中」→本文確定という更新を拾えない不具合を修正した。ユーザーから「AIへ投げかけた後、そのチャンネル画面で待ち続けるより、他のチャンネル/DM画面へ切り替えた方が速く表示される。待ち続けると2分以上表示されないこともある」との報告を受けて調査した。原因は`routers/channels.py`（`routers/dms.py`も同様）の`list_messages`が`since`の絞り込みに`m.created_at > $2`を使っていたこと。AI応答は`generation_status='generating'`のプレースホルダとして先に作成され、本文確定時は`UPDATE messages SET body=...`のみで`created_at`は変わらない。クライアントが一度でも「生成中」状態のこの行をポーリング取得すると、`since`カーソルがその行自身の`created_at`まで進んでしまい、以後`created_at > since`を満たせなくなるため、**本文確定後の内容が同じ画面に留まる限り永久に届かない**（画面を切り替えると`since`無しの全件取得になるため、そこで初めて完成後の内容が見えていた）。この欠陥自体は元から存在していたが、直前の2件の変更（ポーリング間隔短縮・応答速度改善）により「生成中の瞬間をポーリングで捕まえる」確率が上がったことで顕在化した。修正は3点: (1) `services/ai_agent.py`の生成完了・エラー・中断・オーファン復旧の全7箇所のUPDATE文に`updated_at = now()`を追加（従来`created_at`同様`updated_at`もDEFAULT値のまま更新されていなかった）。(2) `routers/channels.py`・`routers/dms.py`の`list_messages`の`since`絞り込みを`m.created_at > $2`から`m.updated_at > $2`へ変更（ORDER BYも同様）し、`_message_out`（`channels.py`・`dms.py`・`messages.py`の3箇所）のレスポンスへ`updated_at`を追加。(3) フロント`useMessages.ts`のカーソル追跡を`updated_at`基準に変更し、差分取得のマージ処理を「常に末尾へ追記」から「id一致で上書き（新規行のみ追記）」に変更した（同じ行が2回目以降のsinceポーリングで再度返ってきても、表示上その場で内容が更新されるようにするため）。`Message`型に`updated_at`を追加（`useThread`のスレッド返信は全件再取得のみで元々この不具合を持たないが、型を揃えるため`messages.py`の`_message_out`にも同様に追加した）。ローカルDocker Postgresへ直接検証スクリプトを実行し、修正後のクエリ（updated_at基準）ではプレースホルダ更新後の行を再取得できること、旧方式（created_at基準）では再現どおり取得できないことを実測で確認した（Windowsの動的ポート除外範囲の関係でDB_PORTが一時的に使えなかったため、同じdockerボリュームを別ポートの使い捨てコンテナで検証）。あわせて、直前の「ポーリング間隔短縮」は試験的措置としていたが、本不具合の修正により正常に機能することを確認できたため本採用とする。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（詳細設計書API設計のsince差分取得の説明自体は「created_at基準」等の実装詳細までは踏み込んでいないため改訂不要と判断）（2026-09-04）。
- バグ修正: 利用者がプロフィールで表示名を変更した後も、チャンネルAIの返信が変更前の名前で呼びかけ続ける事象が発生した（ユーザーからの報告）。本番DBを直接確認したところ、名前を「伊藤博文」に変更した37秒後に生成されたAI応答でも、まだ古い名前「長井ひろさん」を使っていることを実測で確認した。原因は`_resolve_sender_names`（会話履歴の人間発言に発言者名をラベル付けする処理）自体は`SELECT name FROM users WHERE id=...`で毎回最新の名前を引いており正しいが、**AI自身が過去に生成した返信の本文**（例:「こんにちは、長井ひろさん！」）はそのままDBに保存され、以後の会話履歴として毎回モデルへ渡され続けること。モデルは自分の過去の発言パターン（呼び方）をそのまま踏襲する傾向があり、直近の人間発言のラベルが新しい名前に更新されていても上書きしない。会話履歴のテキスト自体（AI自身の過去発言）を書き換えるのはリスクが高い（自由記述のため意図しない誤爆の恐れがある）と判断し、`services/ai_agent.py`の`_build_system_prompt`に`requester_name`引数を追加し、今回の依頼者（メンション/proactiveの起動者）の現在の名前を都度システムプロンプトへ明示注入して「これより前の会話履歴で別の名前が使われていてもそれは古い情報なので使わず、必ずこの名前を呼んでください」という指示を追加する形で対応した。`_generate_and_post`で依頼者の現在名を`names`（既存の解決結果）から取得し、無ければ直接DBへフォールバック。ローカルで`_build_system_prompt`を直接呼び出し、指示文が期待どおり組み立てられることを確認した（実際にモデルがこの指示に従うかまでは、OpenAI API呼び出しを伴う実機検証が別途必要）。F-14要約（`_generate_summary_and_post`/`_build_summary_prompt`）は依頼者個人への呼びかけではなく会話全体の要約という性質上、今回は対象外とした（同種の事象が起きうる可能性はあるが、要約内で複数人の名前に触れうるため対応方針が異なり、必要になれば別途検討する）。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-04）。
- バグ修正: 表示名を変更した直後、同じチャンネル画面に留まっている限り、F-41メンション候補（自分自身を含む）が変更前の名前のまま表示され続ける不具合を修正した（ユーザーからの報告。チャンネルを切り替えると反映されることから原因を特定）。`hooks/useChannelMembers.ts`（A-46、メンション候補の元データ）が素の`useSWR`を使っており自動再取得の仕組みを持たず、チャンネルを開いた最初の1回しか取得していなかったことが原因。他の一覧（チャンネル・DM・メッセージ）と同じ`usePolling`（3秒間隔）に揃えて解消した。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-04）。
- バグ修正: プロフィール編集・定期投稿編集など各種モーダルで、本文中のテキストをドラッグして選択し、マウスをモーダルの外（オーバーレイ側）で離すと、選択のつもりが誤ってモーダルごと閉じてしまう不具合を修正した（ユーザーからの報告）。原因は、各モーダルがオーバーレイ（背景）に`onClick={onClose}`、モーダル本体に`onClick={e => e.stopPropagation()}`を付けるだけの実装だったこと。ドラッグの開始位置（mousedown）と終了位置（mouseup）が異なる要素（モーダル本体とオーバーレイ）にまたがる場合、ブラウザの`click`イベントは両者の共通の祖先要素＝オーバーレイ自身で発火するため、モーダル本体側の`stopPropagation`では防げない（`e.target`がオーバーレイになってしまう）。新規共通hook `hooks/useOverlayClose.ts`を追加し、mousedownの開始位置も明示的にオーバーレイ自身であることを追加で要求する（`onMouseDown`でオーバーレイ自身への押下かを記録し、`onClick`ではその記録と`e.target === e.currentTarget`の両方を満たした場合のみ`onClose`を呼ぶ）ことで解消した。この不具合を持っていた全モーダル（`ProfileEditModal`・`JoinChannelModal`・`ScheduledMessagesModal`・`MembersModal`・`DmPickerModal`・`ChannelSettings.tsx`内の3モーダル＝スキル/定期投稿/トリガー編集・`AdminConsole.tsx`のチャンネル別上限編集モーダル、計8箇所）に同じhookを適用した。既存の`onClick={e => e.stopPropagation()}`（モーダル本体側）はそのまま残している（通常のモーダル内クリックがオーバーレイへバブリングしても`e.target`がオーバーレイと一致しないため実害はなく、変更を最小限に留めるため）。確認ダイアログ（`components/ui/ConfirmDialog.tsx`）はオーバーレイクリックでの閉じる機能自体を持たない設計のため対象外。`tsc -b`・`oxlint`（今回は実行できた）で確認、新規の警告は無し。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-04）。
- バグ修正: `auto_stop_machines`でアプリが長時間停止した後に復帰すると、定期投稿（F-36）・送信予約（F-35）の欠落回がまとめて追いつき送信される際、いずれも`created_at`が実際にディスパッチャが動いた瞬間（＝チャンネルを開いてアプリが起動した瞬間）になってしまい、本来別々の日時に投稿されるはずだった複数件が同じ時刻に見える不具合が発生した（ユーザーからの報告。2日間アプリを開かなかった間の毎日16:15の定期投稿2件が、いずれもチャンネルを開いた瞬間の時刻になっていた）。「欠落回をスキップせずまとめて追いつかせて送信する」設計自体は`services/scheduled_dispatcher.py`冒頭コメントに明記された意図的な仕様（基本設計書5.16節）だが、`created_at`を本来の予定時刻へさかのぼらせる仕組みが元から無かったことが原因。定期投稿の設計時点（2026-08-28）ではまだ`auto_stop_machines`（Fly.io、2026-09-04追加）が無く、アプリが起動していない期間が実運用で何日も続くケースを想定していなかったための見落としと考えられる。ユーザーから「メッセージは投稿時刻を持っているのだから、表示側で昇順に並べればよいのでは」という指摘を受け、(1) `_dispatch_recurring_posts`・`_dispatch_due_messages`のINSERT文に`created_at`を明示的に指定し、それぞれ本来の予定時刻（`next_run_at`・`scheduled_at`）にする（`updated_at`は意図的にDEFAULT `now()`のまま維持。sinceポーリングの差分検知はupdated_at基準のため、ここを過去の時刻にすると逆にこの発言がポーリングで検知されなくなる）、(2) `useMessages.ts`のフェッチャーで、id一致上書き・新規行末尾追記のマージ後に`created_at`昇順で並べ替える処理を追加した（従来はマージ後の並べ替えをしておらず、過去の日時を持つ発言が画面の一番下に挿入される想定だった）、の2点で対応した。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-04）。
- バグ修正: 直前のモーダルのドラッグ選択修正をデプロイした後も「治っていない」とユーザーから報告があった。まず`curl`で本番の`index.html`が参照するJSバンドルのファイル名を取得し、それをローカルの同一コミットから`npm run build`したものと突き合わせたところ、ハッシュ・バイト数とも完全一致し、**サーバー側は最新の修正済みコードを返していることを確認した**。次に修正ロジック自体を疑い、Playwrightを一時導入して`useOverlayClose`と全く同じロジックを持つ最小の再現ページ（素のJS版・実際のReact合成イベントを使う版の2種類）を作成し、textarea内でmousedown→モーダル外でmouseup、という操作を実際にChromiumで自動操作した結果、いずれも正しく閉じないことを実測で確認した（Firefox/WebKitはこの環境固有の起動タイムアウトで検証できず、Chromiumのみの確認に留まる）。修正ロジック自体・サーバー配信ともに正しいことが分かったため、原因を「ブラウザ側が古いキャッシュを見ている」方向へ絞り込み、`curl -I`で本番のレスポンスヘッダーを確認したところ、**`index.html`に`Cache-Control`が一切設定されておらず、ブラウザの既定のヒューリスティックキャッシュに委ねられていた**ことが判明した（`/assets/index-*.js`はファイル名自体がビルドごとのコンテンツハッシュのため長期キャッシュされても実害は無いが、`index.html`はURLが常に同じであるため、これが古いまま残ると新しいJSバンドルへのURLも古いバンドルへのURLのまま固定されてしまう）。`backend/main.py`の`spa_fallback`で返す`index.html`・dist直下の実ファイル（favicon.ico等、ビルドハッシュを持たないもの）の両方に`Cache-Control: no-cache`を明示するよう修正した（`FileResponse`が自動付与する`Last-Modified`/`ETag`による条件付きGETは活きるため、キャッシュを一切禁止するのではなく「使う前に毎回サーバーへ検証させる」形にした）。ユーザーには、今回発生していた分についてはブラウザのハード再読み込み（Ctrl+Shift+R）または別ウィンドウ（プライベートブラウジング）での確認を依頼し、今回のヘッダー修正により今後のデプロイでは同様の「直したのに反映されて見えない」事象自体が起きなくなる見込みであることを伝えた。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-04）。
- F-41 @メンションをDMへも拡張した（ユーザーからの明示的な要望「DMでもメンションできるようにしたい」）。要件定義書F-41は元々「当該チャンネルの参加者」とチャンネル限定で書かれており、`mentions.py`・`routers/dms.py`ともDM向けの実装が一切無かった（`insert_mention_blocks`が`channel_members`専用、`dms.py`の`_message_out`は`blocks`を常に`[]`固定、`PostMessageRequest`に`mentions`フィールド自体が無い）ことをコード調査で確認したうえで着手した。バックエンドは`mentions.py`の`insert_mention_blocks`を`channel_id`/`dm_id`のいずれかを受け取る形に一般化し、DMのときは`direct_message_members`を検証対象にした。呼び出し元3箇所（`channels.py` A-11、`messages.py` A-14、新規`dms.py` A-19）を新シグネチャに揃え、A-14（スレッド返信）は従来「元発言がチャンネルの場合のみ」だった分岐を撤廃してchannel_id/dm_idどちらもそのまま渡すだけの形にした。`dms.py`の`_message_out`・`list_messages`（since/around/初期取得の3経路）に`fetch_blocks_grouped`を追加し、DM発言でも実際に`blocks`が返るようにした。あわせて`_dm_out`のmembersに`is_active`を追加し（ChannelMemberと同じ、無効化アカウントを候補から除外するため）、フロントの`DmMember`型・`MentionSourceMember`型（`MessageList`/`ThreadPanel`が要求する`members`の型を、従来固定だった`ChannelMember[]`から`{id,name,picture_url,is_active}`の構造的最小形へ緩めた新規共有型）を追加した。`DmView.tsx`は`ChannelView.tsx`の`mentionCandidatesWithAi`と同じ考え方で、DMメンバー（`is_active`で絞り込み、チャンネルAI相当の合成候補は追加しない）を`Composer`・`ThreadPanel`へ渡すようにした。**ローカル環境で実機検証した**: Docker Desktop起動→既存ボリュームを使い捨てコンテナで復旧（Windowsの動的ポート除外範囲の関係で本来のDB_PORTが使えなかったため今回も別ポートで代替）→バックエンドを`dev-login`可能な設定でローカル起動→Playwrightで実際に(1)DM本体でのメンション候補表示・選択・送信・`message_blocks`への保存、(2)DMのスレッド返信でも同様、の両方が正しく動作することをAPI応答の直接確認込みで確認した（候補ボタンを`onMouseDown`で選択する実装のため、最初のテストスクリプトは`page.click("text=...")`の対象があいまいで見かけ上失敗し、`button`要素へスコープを絞って再テストして解消した経緯を含む）。要件定義書（F-41、v1.26）・基本設計書（5.22節、v1.41）・詳細設計書API設計（4.3/4.4節、v1.34）・docs/index.html（Ver 4.51）を同じコミットで更新した。F-35送信予約のDM宛てmentionsは今回も対象外のまま（`scheduled_messages.py`の作成時点でDM宛ては`mentions`を空にしてから保存する既存の仕様を変更していないため。ユーザーからの要望は通常投稿・スレッド返信の範囲だったため、あわせて広げるかは別途判断する）（2026-09-07）。
- ユーザーから「チャンネルAI設定を何も設定していないと、AIはチャンネルの名前も作成者も知らない状態になっている」との指摘を受け調査した。実際に`services/ai_agent.py`の`_build_system_prompt`を確認したところ、AIへ渡している情報はペルソナ名・振る舞い定義・スキル・自動対応範囲・固定ルールのみで、**チャンネル自体のname・topic（説明文）・作成者は一切渡していなかった**（chadminが振る舞い定義に手動で書けば伝わるが、書かなければ完全に無知。DM検証の前の実機テストでも「このチャンネルについて説明してください」→「わかりません」という応答が実際に確認できていた）。設計判断ではなく単純な抜けと判断し、新規`_fetch_channel_context`（`channels`とcreatorの`users`をJOINして取得。name/topic自体は非公開情報ではなく参加者には既に見えているため渡すこと自体に問題は無い）を追加し、`_build_system_prompt`に`channel_context`引数を加えて「あなたが常駐しているチャンネルは「{name}」です。説明文: {topic}（作成者: {creator_name}）」という自己紹介行を追加した（topic/creator_nameが無い場合はその部分を省略し、末尾に空白等が残らないようにした）。`_generate_and_post`（メンション応答・proactive・スレッド内メンションいずれも通る共通経路）にのみ配線し、F-14要約（`_build_summary_prompt`は別系統）は対象外のまま（要約は特定チャンネルへの自己紹介文脈より会話内容そのものの要約が主目的のため、今回のユーザーからの指摘の範囲外と判断）。ローカルで`_build_system_prompt`を直接呼び出し、topic/creator_nameの有無それぞれで出力を確認した（実際にモデルがこれを使って正しく回答するかはOpenAI API呼び出しを伴う実機検証が別途必要、これまでの同種の変更と同じ）。あわせて、ユーザーから「根拠文書を設定していないと雑談に応答してくる」との指摘も受けたが、調査の結果これは「設定の有無」の問題ではなく、層2ドキュメントQ&A自体が未実装のため参照ドキュメント範囲（S-06）の設定値がそもそも現状のAI応答に一切効いていないことが原因と判明した。雑談への応答自体はF-15の会話相手としての振る舞いと整合し意図的な挙動と考えられるため、こちらは**ユーザーの判断で様子見（今回は対応しない）**とした。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-07）。
- F-30「AI回答への注意喚起表示」（REQ-N-04）を実装した（ユーザーからの依頼「AIの回答には誤りが含まれうる旨の記述を実装したい」）。要件定義書・基本設計書5.12節・画面モックアップS-03（`.ai-disclaimer`、「⚠ AIの回答には誤りが含まれる場合があります。」）にはいずれも既に仕様が記載済みで、`ChannelSettings.tsx`のDocScopeTabのヒント文言も「誤りを含みうる旨の注意喚起（F-30）はどちらの設定でも常時表示されます」と**既に実装済みであるかのように説明していた**が、実際には`MessageList.tsx`側にこの表示自体が存在せず、一度も画面に出ていなかった（コード検索で確認。設計書は正しく、実装だけが漏れていたパターン）。`MessageList.tsx`のAI発言本文の直後（`generation_status='generating'`中は表示しない）に、画面モックアップと同じ文言・スタイル（`mt-[7px] flex items-center gap-[5px] text-[11px] text-ink-subtle`）で追加した。`ThreadPanel.tsx`の元発言ヘッダーは`MessageList`を経由せず個別に本文を描画しているため（`renderMessageBody`を直接呼ぶ実装）、同じ表示をこちらにも別途追加した（AI発言に人間がスレッド返信した場合、元発言側にも表示されるようにするため）。要約ボタン経由の発言（`is_summary`）も`sender_type='ai'`のため自然に対象に含まれる。既存の設計書側の記載が既に正確だったため、要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-07）。
- 層2ドキュメントQ&A（F-19〜F-22）に向けたGoogle Drive連携の最初のスライスに着手した。以前S-08ドキュメント参照範囲の実装時（2026-09-03）に洗い出した3つの前提（(1) GCP側のDrive API有効化、(2) Drive OAuthスコープの全社展開の可否、(3) 利用者ごとのDriveアクセストークンをサーバー側に保存・更新する基盤）のうち、(1)(2)について千田氏の許可が得られたと報告を受けたため、(3)に着手した。以前層2Drive PoC検証用にstashへ残していた未コミット変更（`google_auth.py`のスコープ拡張・`routers/auth.py`のaccess_tokenをファイルへ書き出すTEMPコード）は、正式実装に必要な「サーバー側での安全な保存」「refresh_tokenによる自動更新」のどちらも持たない使い捨て検証コードだったため、このまま使わず新たに実装した（stash自体は削除せず残置）。新規T-23 `google_drive_tokens`（user_id・access_token・refresh_token・expires_at・scope、他の全テーブルと同じくRLS有効）を追加し、`google_auth.py`の`build_auth_url`にdrive.readonlyスコープを恒久的に追加、`access_type=offline`・`prompt=consent`を付与してrefresh_tokenを確実に受け取れるようにした（新規`refresh_access_token`関数も追加）。`routers/auth.py`のA-02コールバックで、Googleから返ってきたトークンをT-23へUPSERTする処理を追加した（refresh_tokenが今回のレスポンスに含まれない場合は`COALESCE`で既存の値を保持し、誤って`NULL`で上書きしないようにした）。新規`services/google_drive.py`の`get_valid_access_token(user_id)`は、保存済みのaccess_tokenが期限切れ間近（5分以内）ならrefresh_tokenで自動更新してから返す（次のスライスで実際のDrive APIを呼び出す際にこの関数を使う想定）。**実際の動作確認（Drive権限の同意画面が出るか、トークンがT-23へ保存されるか、そのトークンで実際にDrive APIを呼び出せるか）はdev-loginでは代替できず実際のGoogleアカウントでのログインが必要なため、本番デプロイ後にユーザー自身の再ログインを待って確認する**。索引作成・埋め込みベクトル化・検索本体（`search_documents`ツール）は、この土台の動作確認が取れてから次のスライスとして着手する。基本設計書（6.2節該当calloutに追記、v1.42）・詳細設計書DB設計（T-23、v1.14）・docs/index.html（Ver 4.52）を同じコミットで更新した。要件定義書・画面モックアップは変更なし（2026-09-07）。**2026-09-08、実際に本番でaccess_token取得後にDrive APIを呼び出す検証を行ったところ、新たなブロッカーが判明した**: access_tokenの取得・自動更新自体は成功する一方、実際のDrive APIリクエスト（`files.list`）は`403 domainPolicy`（"The domain administrators have disabled Drive apps."）で拒否される。これはGCPコンソール（console.cloud.google.com、Drive API有効化・OAuth同意画面）とは別のGoogle Workspace管理コンソール（admin.google.com）側の設定（セキュリティ→APIの制御→アプリへのアクセス設定）で、ドメイン全体に対してサードパーティ製アプリのDrive API利用自体が禁止されているために発生する。ユーザーから「Fly.io側のストレージをGoogleドライブの代わりに使えないか」と提案があったため検討したが、**採用しないと判断した**。(1) このブロックは「保存先」ではなく「Drive APIを呼び出せるかどうか」自体の制限であり、Fly.io側へ文書をコピーする場合もコピー元の取得（Drive APIの`files.get`等）で同じ403に当たるため、迂回にならない。(2) 要件定義書REQ-F-20（F-21「閲覧権限に基づく回答フィルタリング」）は「本システムで最も注意を要する機能」と明記されており、質問者本人のDrive上の実際の閲覧権限を都度確認する設計が前提のため、Drive依存を外すと二重管理（文書の陳腐化）または独自の権限管理システムの新規構築（この一番注意を要する要件を作り直すことになりリスク増）のいずれかが必要になり、ブロッカー回避の労力に見合わない。あわせて、この検証の副産物として**本番Fly.ioに永続ストレージ（Volume）が1つも作成されていないこと**（`fly volumes list`が空、`fly.toml`に`[[mounts]]`無し）が判明した。F-07添付ファイル実装時（2026-08-28）に注記した「本番はSupabase Storageへの置き換えが必要」を放置していたことに相当し、現状プロフィール画像・添付ファイル（`backend/uploads`）はデプロイのたびに失われている可能性が高い。これはDriveの件とは独立した既存課題として別途対応を検討する（このコミットでは未対応）。**続けてユーザーから千田氏へこのWorkspace設定変更を依頼してよいか確認したところ、「千田氏はGoogle Workspaceの管理権限を持っておらず、この設定は許可できない」との回答があった**。つまりこれまで技術的な相談窓口だった千田氏は、この特定の設定（ドメイン全体のAPIアクセス制御）を変更できる権限者ではないことが判明した。次に必要なのは、kogasoftware.comのGoogle Workspace超管理者（または「サービス管理」の委任管理者権限を持つ人物）が組織内の誰なのかを特定すること（情報システム部門や、Workspaceドメインの運用を委託している外部ベンダー等が候補として考えられる）。この人物が特定できるまで、層2の索引・AI検索本体の着手は事実上ブロックされたままとし、次の作業対象は他の未実装事項（F-25実行前確認・座席予約参照連携、または上記のFly.io永続ストレージ課題）を優先することをユーザーと相談する必要がある（2026-09-08）。
- チャンネル会話画面（S-03、`/channels/{id}`）で、削除済み・存在しないチャンネルのURLへ直接アクセスした際に無言の空白画面のままだったのを、明示的なメッセージ「🗑 このチャンネルは削除されています」に差し替えた（ユーザーからの明示的な要望「既に削除されているチャンネルのURLをたたいた場合には、このチャンネルは削除されています、のような文章を表示させたい」）。着手前に、バックエンド（`auth_helpers.require_channel_member`・`require_channel_member_or_admin`）が「チャンネルが存在しない」場合と「非公開チャンネルの非参加者」の場合をいずれも同じ404で返す設計（F-34「非公開チャンネルは非参加者に存在自体を伏せる」、2026-08-26時点で確立済み）であることを確認し、この2つをフロント側で区別する情報がそもそも無いと判明した。`ChannelView.tsx`の`channelError`分岐（404/403）を、従来の`return null`（無言でこの画面だけ空白になる）から、404・403いずれも同一の「このチャンネルは存在しないか、削除されました。」というメッセージ＋「← ワークスペースに戻る」リンクを表示する形に変更した。**404（存在しない/削除済み/非公開の非参加者）と403（公開チャンネルの非参加者）をあえて同じ文言にまとめている**のは、理由ごとに表示を出し分けると「削除済み」と「非公開で単に参加していないだけ」を利用者が区別できるようになってしまい、F-34の設計が崩れるため（バックエンド自身もこの2つを区別する情報を持っていない）。既存の`channel && !channel.is_member`分岐（2026-09-02追加、システム管理者がS-08経由で非参加の非公開チャンネルのS-06へ到達した際の「🔒 参加していません」表示）とは別のコードパスで、そちらはA-06自体が200を返す（チャンネルの存在が既にadminへ開示済み）ケースのため今回の変更の対象外（従来どおり）。フロントエンドのみの変更でバックエンドAPI・DBは無変更のため設計文書側の改訂は無い。`tsc -b`・`vite build`で検証（2026-09-08）。**直後にユーザーから「参加していない非公開チャンネルのURLではなんと表示されるか」との質問を受け、非adminは同じ画面（区別する情報が無いため）、adminはA-06のバイパスにより別画面（🔒 参加していません）になる旨を回答したところ、「このチャンネルは削除されたか、参加権限がありません、と表示させるのはどう？」との指摘を受けた**。当初の文言「このチャンネルは削除されています」は、実際には存在するが参加権限が無いだけの非公開チャンネルに対しても一律に「削除されている」と断定してしまい不正確だったため、ユーザー提案どおり両論併記（「このチャンネルは削除されたか、参加権限がありません」／「このチャンネルはすでに削除されているか、参加する権限がありません。心当たりがある場合はチャンネル管理者にご確認ください。」）に修正した。F-34のプライバシー設計（2つのケースを区別できないようにする）はどちらの文言でも変わらず維持される。`tsc -b`・`vite build`で再検証（2026-09-08）。
- 前項のFly.io永続ストレージ課題を受けて、プロフィール画像（A-61）・添付ファイル（A-21/A-22）を当初の設計（基本設計書2.2節）どおりSupabase Storageへ本移行した。着手前にユーザーと保存先を相談した結果、(1) 現在Kogackが使っているSupabaseプロジェクトは各自が作成した個人のFreeプランアカウント上にあり会社名義の契約ではないこと、(2) Fly.ioは会社カードで一括契約されていること、という組織的な事情を踏まえ、**「小さく安定していて当面無料枠に収まりそうなプロフィール画像・添付ファイルはSupabase Storage（個人アカウントのFree枠）、伸びしろが大きく個人アカウントへの請求リスクが高い参照ドキュメント（次項）はFly.io Volumes」という費用リスクに応じた使い分け**で合意した（Fly VolumesはGB単価がSupabase Storage超過分の約7倍・無料枠自体が無く・停止中のマシンでも課金され続けるため、コスト最適だけで見ればSupabase Storageに統一したいところだが、参照ドキュメントは容量が伸びやすく個人のFreeアカウントを最初に圧迫する可能性が高いカテゴリと判断した）。あわせて、Supabase自体が個人アカウントである点・本番データベース自体もそこに乗っている点は、今回のストレージ判断とは別の、より大きなリスクとして千田氏へ別途相談する必要がある旨をユーザーと確認済み（会社名義への移行が実現すれば、この使い分け自体を見直す余地がある）。新規`services/storage.py`にSupabase Storage REST APIの薄いラッパー（`upload`/`download`/`public_url`、`google_auth.py`と同じ生httpx呼び出しのスタイル。supabase-py等の新規ライブラリは追加しない）を追加した。`SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`環境変数が未設定の場合は`is_configured()`がFalseを返し、`routers/icons.py`・`routers/attachments.py`とも既存のローカルディスク保存（`backend/uploads/`）へフォールバックする（DATABASE_URL/OPENAI_API_KEYと同じ、環境変数の有無で本番/ローカル開発を切り替える既存パターンをそのまま踏襲。新規のDB列・スキーマ変更は無い）。アイコン用バケット（既定名`icons`）はPublicとして作成する想定（アイコンは全認証済み利用者に公開する既存の設計判断のため、アップロード後は公開URLをそのまま`picture_url`等へ保存でき、`GET /api/icons/{filename}`の配信プロキシ自体が不要になる。ローカル開発フォールバック用にエンドポイント自体は残置）。添付ファイル用バケット（既定名`attachments`）は必ずPrivateとして作成する想定（チャンネル/DM参加者限定という既存の公開範囲を維持するため、公開URLは発行せずA-22がサービスロールキーでバイト列を取得しバックエンド経由でのみ配信する。日本語ファイル名のダウンロード時ヘッダー（RFC5987 `filename*=utf-8''...`）は、ローカルディスク経路（`FileResponse`が自動生成）と同じ形式をSupabase経路でも手動で組み立てて揃えた）。**バケット自体の作成（ダッシュボード操作）とSUPABASE_URL/SUPABASE_SERVICE_ROLE_KEYの取得・設定（ローカル`.env`・本番`fly secrets set`の両方）はユーザー側の作業が必要で、それが完了するまでは未設定時のフォールバック（ローカルディスク保存）のまま動作する**。`python -c "import services.storage; import routers.icons; import routers.attachments"`でモジュールの読み込み自体に問題が無いことを確認した。**ユーザーがバケット作成（icons=Public/MIMEタイプ制限あり、attachments=Private）とSUPABASE_URL/SUPABASE_SERVICE_ROLE_KEYの設定（ローカル`.env`・本番Fly secrets両方）を完了した後、実際の本番Supabaseプロジェクトに対して直接検証した**: (1) iconsバケットへPNGバイト列をアップロード→返ってきた公開URLに認証無しでGETし、中身が一致することを確認（Publicバケット設定が正しく機能）、(2) iconsバケットへ意図的に`application/octet-stream`でアップロードを試みると`415 invalid_mime_type`で拒否されることを確認（ユーザーが設定したMIMEタイプ制限が実際に効いている）、(3) attachmentsバケットへ任意バイナリをアップロード→`storage.download()`（サービスロールキー経由）で内容が一致して取得できることを確認、(4) 同じattachmentsバケットの中身を公開URL経由（サービスロールキー無し）で取得しようとすると400で失敗することを確認（Privateバケット設定が正しく機能し、参加者限定という公開範囲の要件を満たしている）。検証に使ったテストオブジェクトは4項目とも確認後に削除済み。要件定義書・画面モックアップは変更なし（保存先の変更のみで機能・画面に変化は無いため）。基本設計書2.2節は元々Supabase Storageを前提としており記載と実装が一致した形になるため改訂は無い（2026-09-09）。
- 層2参照ドキュメント（F-19〜F-22）のSlice 2a「実ファイルアップロード＋Fly Volume土台」に着手した。前項（Drive連携のdomainPolicyブロック・千田氏の権限不足）を受けて、ユーザーと相談のうえ「プロフィール画像・添付ファイルはSupabase Storage（個人アカウントのFree枠）、参照ドキュメントはFly.io Volumes（会社カード契約）」という費用リスクに応じた使い分けで合意し（Postgresへ直接保存する案は、DBの無料枠が500MBとStorageの1GBより狭く、超過時はFreeプランで読み取り専用モードに入るため、より深刻な障害を招くとして却下した）、「土台（Fly Volume・アップロードAPI・DBテーブル）→権限モデル（限定公開・閲覧者ACL等）」の順で進めることをユーザーと確認した。このコミットは前者（土台）のみ。
  - **Dockerfile/entrypoint.shの変更**: Fly Volumeのマウント先（/data）はFly側でroot所有のままコンテナへ渡ってくるため、既存の`USER appuser`固定では書き込めない。新規`backend/entrypoint.sh`を追加し、コンテナはrootのまま起動→`/data`が存在すれば所有権をappuserへchown→`su appuser -c`でuvicorn本体を非rootへ権限降格して実行、という手順に変更した（Dockerfileの`USER appuser`は削除し、`ENTRYPOINT`をentrypoint.sh経由に変更）。**ローカルでdocker build・docker runにより実機検証した**: (1) volumeマウントを模したDocker named volumeを`/data`にアタッチし、entrypoint実行後に所有権がappuserへ変わること、appuserとして実際にファイルを書き込めることを確認、(2) 実際にPostgresコンテナへ接続させてアプリを起動し、`/proc/<pid>/status`のUidを直接確認して、リクエストを処理する実プロセス（uvicorn/Python本体）がroot(uid 0)ではなくappuser(uid 10001)で動作していることを確認（`su`自身のPID 1は仕様上rootのまま残るが、これは外部入力を一切処理しない待機プロセスのため実害はないと判断）。
  - **fly.tomlに`[[mounts]]`（source='kogack_docs', destination='/data'）を追加**。事前に`fly volumes create kogack_docs --region nrt --size 1 --app kogack`でVolume自体をユーザー自身に作成してもらう必要がある（このfly.tomlの変更だけでは自動作成されない。過去のsecretsと同様、課金を伴うインフラ操作のためユーザー自身の実行とした）。
  - **DB**: T-09 doc_foldersを拡張し、URL/ID貼り付け専用だったこのテーブルを実ファイルアップロードにも対応させた（新規テーブルは作らず、既存の「folder/fileを同じテーブルで扱う」パターンをそのまま踏襲）。新規列`source`('drive'/'upload')・`storage_path`・`byte_size`・`mime_type`を追加し、`drive_folder_id`のNOT NULL制約を外した（UNIQUE制約はNULL同士を区別しないPostgresの挙動により複数のupload行と共存可能）。CHECK制約`doc_folders_source_matches_columns_check`でsource別に列の整合性を強制する。**既存のdoc_folders_parent_matches_type_check（「item_type='file'なら必ずparent_folder_idを持つ」）がupload行（親フォルダを持たないトップレベル項目）と矛盾するため、DROPしてsource別条件を含む_v2として再作成した。ここで実際にハマった点を記録する**: SCHEMAは起動のたびに全文を実行する設計のため、旧`ADD CONSTRAINT doc_folders_parent_matches_type_check`の文をそのまま残しておくと、初回起動でv2への差し替えが完了した「次」の起動時に、この旧ADD文がまだ存在しない制約名として再実行され、その時点で既に存在するupload行に対して検証が行われてCheckViolationErrorとなり**起動そのものが失敗する**という、ローカルでの2回連続起動テストで初めて発覚した不具合を作り込みかけた（1回目の起動・手動INSERT検証だけでは気づけず、2回目の起動を明示的に試して発見）。旧ADD文自体を削除し、経緯をコメントとして残すことで解消した。この一件を踏まえ、既存制約を後から差し替えるすべての変更は、ローカルで「初回起動→実データ投入→再起動」を最低2周期通しで確認することを今後の教訓とする。
  - **バックエンド**: 新規`services/doc_storage.py`（`/data`が存在すればそれを、無ければ`backend/uploads/docs`をローカル開発フォールバックとして使う、DATABASE_URL/Supabase Storageと同じ環境依存切替パターン）を追加。新規`POST /api/admin/doc-folders/upload`（admin限定、20MBまで、形式制限なし。F-07添付ファイルと同じ方針）を`routers/admin.py`に追加し、既存のA-38一覧・A-40削除にsource/byte_size/mime_typeを反映（A-40はsource='upload'の場合、Fly Volume上の実ファイルも道連れで削除し孤立ファイルの蓄積を防ぐ）。
  - **フロントエンド**: S-08「ドキュメント参照範囲」タブに3つ目の登録方式「ファイルをアップロード」を追加（既存の「フォルダ全体」「フォルダ内の特定ファイル」と並ぶラジオ選択）。一覧表示にサイズ（B/KB/MB自動切替）・📎アイコン（Driveの📄/📁と区別）を追加。`lib/api.ts`に`uploadDocFile`（既存のuploadIcon/uploadAttachmentと同じFormDataパターン）を追加。フォルダでの整理（parent_folder_id）は次スライスの対象のため、アップロードしたファイルは常にトップレベル項目としてDriveフォルダと同列にフラット一覧へ表示する。
  - **検証**: ローカルで使い捨てPostgresコンテナ・DEV_AUTH有効なバックエンド・Viteフロントエンドを立ち上げ、Playwrightで実際にadminとしてログイン→S-08でファイルをアップロード→一覧に📎アイコン・ファイル名・サイズ・登録者・使用中チャンネル数が表示されること→削除→一覧から消えること→ローカルディスク上の実ファイルも同時に削除されていることを直接確認した。テストファイル・検証用コンテナはすべて削除済み。**このスライスでは限定公開・閲覧者ACL・チャンネル参加時の権限チェック等（次スライス）は未実装のため、アップロードした文書は既存のDrive候補と同じく「参照範囲に追加したチャンネルの参加者全員が対象」という扱いになる**。索引化・埋め込みベクトル生成・AI検索（search_documentsツール）も引き続き未実装。要件定義書・画面モックアップは変更なし（F-22の実装方式の追加という位置づけ）。基本設計書2.2節・6.2節に設計判断を追記予定（次のコミットでまとめて改訂、docs側の改訂はコード変更と分離せず同じ単位で行う従来方針からの意図的な逸脱——理由: 今回は土台のみで機能として未完成のため、権限モデルまで実装が揃った段階で一括して設計書を更新する方が、読み手にとって「どこまでが仕様として確定したか」が分かりやすいと判断。CLAUDE.md実装状況節にはこの判断自体も記録しておく）（2026-09-09）。
- 層2参照ドキュメントの閲覧権限モデル（Slice 2b）を実装した。ユーザーと詳細に協議のうえ確定した設計（8項目、下記(1)〜(8)）どおり実装した。
  1. システム管理者だけがS-08でドキュメントフォルダ・ファイルの登録（URL/ID貼り付け・実ファイルアップロードいずれも）ができる（既存のadmin限定を継続）
  2. 登録時に、フォルダ・ファイル単位で「限定公開にする（誰が閲覧可能か指定）」を選べる（全社員閲覧可＝既定のまま、が誰でも参照範囲に含められる）
  3. チャンネル管理者がS-06でそのチャンネルのAIが参照できるフォルダを指定する（既存A-27）
  4. 公開チャンネルの参照範囲には限定公開フォルダを一切含められない（ハードブロック、確認では回避不可）
  5. 非公開チャンネルは限定公開フォルダも含められるが、参加者全員が閲覧権限を持っている場合のみ。権限の無い参加者がいる場合は、その参加者を名指しした確認（「○○はこのファイルの閲覧権限がありません。○○をこのチャンネルから退出させますか？」）を経て、「はい」の場合のみ強制退出させて追加する
  6. 5の強制退出対象が「そのチャンネルの最後の管理者」の場合は、既存の安全策（A-48/A-72/A-73）と同じく操作自体を拒否する（確認で「はい」を押しても実行されない）
  7. 非公開チャンネルへ新しく参加者を追加する際、既にそのチャンネルが参照している全ての限定公開フォルダの閲覧権限を確認し、権限の無い利用者の参加は拒否する（強制はできない、権限を先に付与する以外の回避策は無い）
  8. 管理者が後から特定利用者の閲覧権限を剥奪した場合、既にそのフォルダを参照しているチャンネルの参加者だったら5と同じ確認→強制退出の扱いにする

  **データモデル**: 新規テーブルは作らず、doc_foldersに`is_restricted`（BOOLEAN）を追加し、新規T-24 `doc_folder_viewers`（folder_id×user_id、限定公開フォルダの閲覧者一覧）を追加した。doc_foldersがフォルダ・ファイル・アップロードのいずれの行も同じテーブルで扱う既存パターンにより、フォルダ単位・ファイル単位どちらの粒度でも自然に閲覧権限を持たせられる（新しい別テーブルへ分岐する必要が無かった）。

  **バックエンド**: 新規`services/doc_permissions.py`に、3箇所（S-08閲覧者編集・S-06参照範囲への追加・A-08招待）で共通して使う判定・強制退出ロジックを集約した（`is_restricted`判定・`members_missing_access`・`channels_missing_access_for_folder`・`folders_user_cannot_view`・`check_last_admin_conflicts`・`force_remove_member`）。`force_remove_member`はA-73（`remove_channel_member`）と同じ強制退出処理（削除・F-17引き継ぎ先NULL化・F-43退出通知）を再現しているが、A-73自体は既存の動作確認済みコードを変更するリスクを避けるためあえて手を加えず、意図的に重複を許容した（コード内コメントに明記）。
    - **A-39（`create_doc_folder`）・新規アップロードAPI**: `is_restricted`・`viewer_user_ids`を受け取り、同一トランザクションで`doc_folder_viewers`へ書き込む（新規フォルダのため確認は不要、影響を受ける既存チャンネルが無いため）。アップロードAPI（multipart/form-data）は`viewer_user_ids`をJSON文字列（`viewer_user_ids_json`）として受け取る。
    - **新規 `PUT /api/admin/doc-folders/{id}/viewers`**（(2)(8)）: 既存フォルダの限定公開フラグ・閲覧者リストを編集する。変更により閲覧権限を失う参加者がいる既存チャンネルがあれば、`force=false`（既定）は409（`{message, affected: [{channel_id, channel_name, members}]}`）を返し何も変更しない。`force=true`は対象者を強制退出させてから更新するが、対象に最後の管理者が含まれる場合はforceでも400で操作全体を拒否する。
    - **A-27（`update_doc_scope`）**: `channels.is_public`を確認し、限定公開フォルダが requested_ids に含まれていれば422で即拒否（確認では回避不可）。非公開チャンネルでは「新たに追加しようとしている」限定公開フォルダ（既存の設定に既に含まれるものは対象外——招待時チェックにより既に参加者全員の権限が保たれているはずという不変条件が前提）についてのみ、参加者の権限を確認し、409/force=trueの同じパターンで扱う。
    - **A-08（`join_channel`）**: 招待対象のuser_idについて、そのチャンネルが参照する全限定公開フォルダの閲覧権限を確認し、1件でも欠けていれば403で拒否する（公開チャンネルは限定公開フォルダを持てないため実質的に非公開チャンネルの招待でのみ発動する）。
    - `_doc_folders_query`（A-38一覧等）で、`channel_doc_folders`と`doc_folder_viewers`の両方を素朴にLEFT JOINしてGROUP BYすると一方が複数件・もう一方も複数件の場合に行が掛け合わされる典型的なfan-outバグに気づき、それぞれをサブクエリで先に集約してから外側でJOINする形に直した（実装中に自分で気づいて修正、実機での不具合確認はしていないが、既存のCOUNT集計ロジックへの影響を避けるため）。
  **フロントエンド**: 新規`ViewerPicker`コンポーネント（DmPickerModal/補足02と同じ200msデバウンスの利用者検索を、チェックボックス複数選択向けに簡略化。全社員をチェックボックスで並べるのではなく検索して選ぶ方式）を追加し、S-08の新規登録フォーム・新規`EditViewersModal`（既存フォルダの閲覧権限編集）の両方から使う。S-08の一覧に🔒「限定公開」バッジと閲覧可能人数を追加。S-06の`DocScopeTab`は、(a) 公開チャンネルでは限定公開フォルダのチェックボックスをdisabled＋ツールチップで選択自体を防ぐ（バックエンドのハードブロックと二重の防御）、(b) 保存時に409を受け取ったら対象者を名指しした確認ダイアログ（`useConfirm`）を出し、「はい」なら`force=true`で再送信する、という2点を追加した。**このタブには実装中に別のバグを発見した**: 従来のフォルダ一覧が`item_type==='folder'`の行だけを起点にし、その子として`parent_folder_id`一致の行を辿る構造だったため、Slice 2aで追加したアップロードファイル（`parent_folder_id`が常にNULLの単独ファイル）が一覧に一切表示されず、S-06からアップロード文書を参照範囲に含めることが実質不可能になっていた（Slice 2aのテストではS-08側の表示のみ確認しS-06側を見落としていた）。起点を「`item_type==='folder'`」から「`parent_folder_id===null`」（Driveフォルダ・アップロード単独ファイルの両方を含む）に変更し解消した。あわせて`ApiError`に`detailObj`（構造化された409/400の`detail`をオブジェクトのまま保持するフィールド）を追加した（既存の`apiFetch`は`detail`を常に文字列として扱っており、オブジェクトが返ると`[object Object]`のような無意味な文字列になり確認ダイアログの材料が失われるため）。
  **検証**: ローカルで使い捨てPostgres・DEV_AUTH有効なバックエンド・Viteフロントエンドを使い、(1) curl相当の直接HTTPリクエストで(3)〜(8)の全パターン（409での対象者列挙、force=trueでの強制退出＋F-43退出通知の投稿確認、招待拒否、権限付与後の招待成功、閲覧権限剥奪のカスケード確認、公開チャンネルでの422、全社公開フォルダは公開チャンネルでも成功、最後の管理者保護によりforce=trueでも400かつ状態が一切変化しないこと）を確認し、(2) Playwrightで実際にS-08での🔒バッジ表示、S-06での確認ダイアログ（対象者名を含む）→「はい」→保存成功トースト、という一連の操作を確認した。テスト用に作成したチャンネル・フォルダ・検証用コンテナ・プロセスはすべて削除済み。要件定義書・画面モックアップは変更なし（F-22の実装方式の追加という位置づけ、Slice 2aと同様）。基本設計書・詳細設計書側の改訂は、層2全体（索引・AI検索本体）の実装が揃った段階でまとめて行う方針を維持する（Slice 2aのコミットで明記した判断を踏襲）（2026-09-09）。
- 層2ドキュメントQ&A（F-19〜F-22）のSlice 3「索引化・AI検索本体」を実装した。これでSlice 2a（アップロード＋Fly Volume土台）・Slice 2b（閲覧権限モデル）に続き、F-19（ドキュメント質問応答）・F-20（回答根拠の提示）が実際に機能する状態になった。着手前にDrive連携のdomainPolicyブロックが未解決のままである（前項参照）ことを踏まえ、**このスライスで実際に検索対象になるのはアップロードした実ファイル（source='upload'）のみ**とユーザーと確認した（source='drive'の候補は引き続き中身を取得できないため索引化できない）。
  - **前提の実機検証**: 着手前にOpenAI Embeddings API（`text-embedding-3-small`、1536次元）とSupabase上でのpgvector拡張の利用可否を確認した。pgvectorは`pg_available_extensions`には存在したが未インストールだったため、**確認の一環として本番Supabaseに対し`CREATE EXTENSION IF NOT EXISTS vector`を実行し有効化した**（安全な追加的操作、既存データへの影響なし）。実際にvector型の一時テーブルでコサイン距離演算子`<=>`による類似度検索も動作することを確認した。
  - **データモデル**: 新規テーブルは最小限にし、doc_foldersへ`index_status`（'not_applicable'/'pending'/'indexing'/'ready'/'failed'）・`index_error`を追加、新規T-25 `doc_chunks`（folder_id・chunk_index・content・embedding vector(1536)、UNIQUE(folder_id, chunk_index)）を追加した。既存のmessage_blocks（T-07）は元々block_type='citation'を許容するCHECK制約を持っていた（実装時から予約されていたが未使用）ため、スキーマ変更なしでそのまま使える。
  - **ローカル開発環境への影響（重要）**: ローカルDB用Dockerイメージを`postgres:16`から`pgvector/pgvector:pg16`（同じPostgres 16にpgvectorを同梱しただけの公式イメージ、完全な互換）へ`start.bat`で切り替えた。**既存の`kogack-db`コンテナは`docker start`で使い回す実装のため、start.bat側の変更だけでは既存環境が自動的に新イメージへ切り替わらない**（既に存在するコンテナ名にヒットして`docker run`（新イメージでの再作成）が実行されないため）。実際にこのマシンの`kogack-db`が`postgres:16`のまま残っていることを確認したため、コンテナのみを削除した（データは名前付きボリューム`kogack-db-data`に独立して保存されており、ボリュームは削除していないため次回`start.bat`実行時に同じデータのまま新イメージ（pgvector付き）で再作成される。他の開発者・別環境でも同様の一度きりの対応が必要な場合がある）。
  - **バックエンド**: 新規`services/doc_indexer.py`（テキスト抽出: PDF=pypdf、Word=python-docx、txt/md=そのままデコード。対応外形式はindex_status='failed'。文字数ベースの単純なチャンク分割、CHUNK_SIZE=1500・OVERLAP=150・MAX_CHUNKS=300で極端なコスト増を防止）・新規`services/doc_search.py`（`search(channel_id, query)`: そのチャンネルの参照範囲かつindex_status='ready'のフォルダに限定して埋め込みベクトルのコサイン距離検索）を追加した。`ai_client.py`に埋め込み生成用の`embed_texts`（バッチ対応、複数チャンクを1リクエストで埋め込む）を追加。アップロード直後にAI応答生成と同じfire-and-forget（`asyncio.create_task`）でバックグラウンド索引化を起動し、S-08は`usePolling`化（従来は素のuseSWRで自動更新されず、useChannelMembers等と同種の抜けだった）して状態変化を追従表示する。
  - **AIサポート（`services/ai_agent.py`）**: 新規`search_documents`のOpenAI Function Calling定義を追加し、`_generate_and_post`にツール呼び出しループ（`_run_chat_with_tools`、最大`SEARCH_DOCUMENTS_MAX_ROUNDS`=3回、最後の1ラウンドはtools無しで強制的にテキスト回答させる）を実装した。このチャンネルの参照範囲に索引済み文書が無い場合はツール自体を提示しない（`doc_search.channel_has_indexed_documents`）。実際に参照した文書は重複排除のうえmessage_blocksへblock_type='citation'として記録し（F-20）、複数ラウンド分のトークン使用量を合算して1件のT-13 ai_usage_logsに記録する。**out_of_scope_policy（S-06、これまで保存のみで未使用だった設定）を初めてAIの挙動へ反映した**: 'strict'は検索結果が見つからない場合に一般知識で補わず定型文のみで回答を打ち切らせ、'general'は明示のうえ一般知識での補足を許可する。
  - **実機検証で発見・対処した2つの問題**: (1) 類似度検索に足切り（DISTANCE_THRESHOLD）が無いと、無関係な質問でも常に何らかの結果が上位k件として返ってしまい、検索結果が実際には無関係であるにもかかわらずAIが一般知識で補足して回答したり、使っていない文書が引用として表示されたりする不具合を実機で確認した。実測（関連する質問はdistance 0.47〜0.56、無関係な質問は0.83〜0.90）をもとにDISTANCE_THRESHOLD=0.7を追加し解消した。(2) out_of_scope_policy='strict'の指示文言を、最初は「一般知識で補わないこと」という抽象的な指示にしていたが、実機検証で「見つかりませんでした。ただし、一般的には～」のように指示に反して一般知識を続けて答えてしまう事象を複数回確認した（`gpt-4.1-nano`という小型・安価なモデル特有の「役に立とうとして情報を足してしまう」傾向と考えられる）。プロンプトを「search_documentsの結果が『関連する内容が見つかりませんでした。』という文字列だった場合に限り、次の1文だけで回答を終える」という、ツールの実際の返り値と直接結び付けた条件形の指示に書き換えることで、複数回の実機検証（関連する質問4パターン・無関係な質問2パターン）で安定して期待どおりの挙動になることを確認した。**この種のプロンプトベースの指示は、他の`reaction_mode`・自動対応範囲分類等と同様、モデルが100%従う保証はない**（実際に本番相当のモデルで確認できたのはこのセッションで試した質問パターンの範囲内であり、無条件の保証ではない）。
  - **検証**: pgvector対応のPostgres・DEV_AUTH有効なバックエンド（実際の`OPENAI_API_KEY`を使用）・Viteフロントエンドをローカルで用意し、(1) 架空の社内規定（「有給休暇の年間最大繰越日数は17日」「在宅勤務手当は月額4200円」等、モデルが知りようのない具体的な数値）を記載したテスト文書をアップロード→pending→indexing→ready（約2秒）の状態遷移を実際に確認、(2) チャンネルの参照範囲に割当→そのAIへメンションで質問→実際にsearch_documentsが呼ばれ、テスト文書の数値と完全に一致する回答＋正しい引用が返ることを複数チャンネル・複数質問パターンで確認、(3) T-13 ai_usage_logsに複数ラウンド分の合計トークン数が1件として正しく記録されることを確認、(4) Playwrightで実際のブラウザ操作を確認（S-08の索引状態バッジ、会話ログでの「📄 参照: test_kitei.txt」表示、consoleエラーは無関係な既存の401のみ）。テスト用チャンネル・フォルダ・検証用コンテナ・プロセス・ローカルファイルはすべて削除済み。要件定義書・画面モックアップは既存のF-19・F-20・F-22の記載どおりで変更なし。基本設計書・詳細設計書側は、層2の当初計画していた範囲（座席予約参照連携を除く）がこれでひととおり実装できたため、次のコミットでまとめて改訂する（Slice 2aから続く「機能として完成した段階でまとめて更新する」という方針どおり）（2026-09-09）。
- S-08「ドキュメント参照範囲」タブのファイルアップロード（Slice 3）で、素の`<input type="file">`をドラッグ＆ドロップ対応のドロップゾーンに差し替えた（ユーザーからの明示的な要望「エクスプローラーからD&D、またはファイルを選択、という形にしたい」）。新規`FileDropzone`コンポーネント（`AdminConsole.tsx`）は、破線枠の領域全体をクリックすると非表示の`<input type="file">`を発火させ、`onDrop`でも同じ`onChange(file)`ハンドラへ合流させる（アップロード処理自体は既存の`uploadFile` state・`uploadDocFile`呼び出しをそのまま使い、ファイルの受け取り口だけを差し替えた）。ドラッグ中（`dragOver`）は枠線・背景色を変えて視覚的にフィードバックし、キーボード操作（Enter/Space）でもファイル選択ダイアログを開けるようにした（`role="button"`・`tabIndex={0}`）。ファイル未選択時は「ここにファイルをドラッグ＆ドロップ／またはクリックしてファイルを選択」、選択後はファイル名・サイズ・「クリックまたはドラッグ＆ドロップで変更」を表示する。Playwrightで実際のブラウザ操作を確認: 空の状態の表示、`input[type=file]`へのファイルセット（クリック選択のシミュレーション）後の選択済み表示、実際に「📎 アップロード」ボタンを押して一覧に反映されるところまでの一連の流れ、をスクリーンショット付きで確認した（実際のD&D操作自体はブラウザのネイティブなドラッグイベントに依存するため自動テストでは行っていないが、`onDrop`ハンドラの実装自体は`onChange`と同じ受け口を通るため、クリック選択の確認で実質的にカバーされている）。新規API・DB変更は無いフロントエンドのみの変更のため、設計文書側の改訂は無い（2026-09-09）。
- S-08「ドキュメント参照範囲」タブから、Google DriveのURL/IDを貼り付けて候補登録する「フォルダ全体」「フォルダ内の特定ファイル」の2方式をUIから撤去し、「ファイルをアップロード」の単一フローに簡略化した（ユーザーからの指摘「そもそもGoogleドライブのフォルダURLを入力する欄いらなくないですか？GoogleドライブのフォルダはAIで読み込めないんですよね？」）。着手前に本番DBを直接確認し、Drive候補（source='drive'）が実際には1件も登録されていないことを確認したうえで撤去した（削除しても失われる登録データが無いことを確認済み）。**この2方式は、Google Workspace管理コンソールのdomainPolicyブロックが解消されても、実際にDriveから文書を取得・索引化する仕組み自体をSlice 3で作っていない（source='upload'のみを検索対象にする設計）ため、登録してもAI検索には一切使われず、S-06参照範囲でチェックしても何も起きないという分かりにくい罠になっていた**（この罠自体はユーザーとの直前のやり取りで洗い出した上で撤去に至った）。あわせて、Drive同期専用だった「今すぐ同期」ボタン・説明文（実装されておらず常時disabled表示だった）も、参照先のDrive登録方式ごと不要になったため削除した。バックエンド（A-39 `create_doc_folder`のsource='drive'受け口、`_extract_drive_id`等）自体は削除していない——Drive連携が実際に機能するようになった時点で、このUIだけ復活させれば済む想定（DBスキーマ・API・S-06/S-08の一覧表示（`item_type='folder'`のツリー表示等）はDrive候補の存在を引き続き問題なく扱える設計のまま）。Playwrightで実際のブラウザ操作を確認: 簡略化後の画面にフォルダ関連の文言（「フォルダ全体」「フォルダ内の特定ファイル」「今すぐ同期」）が一切残っていないこと、簡略化後もアップロード→一覧反映の流れが引き続き正常に動作すること、consoleエラーは無関係な既存の401のみ。新規API・DB変更は無いフロントエンドのみの変更のため、設計文書側の改訂は無い（画面モックアップは元々Google Picker前提の理想形を示したままにしておき、このUI簡略化はDrive連携再開までの一時的な実装判断としてこのCLAUDE.mdにのみ記録する）（2026-09-09）。
- S-08のファイルアップロードを複数ファイル・フォルダ単位の選択に対応させた（ユーザーからの質問「ファイルを追加の所にフォルダごと入れたらそのフォルダの中のファイルは全部AIが読める？」に対し、当時の実装（1ファイルずつ、フォルダのドロップは非対応）では「いいえ」と回答したところ、追加を依頼された）。`FileDropzone`（`AdminConsole.tsx`）を単一`File`から`File[]`を扱う形に拡張し、(1) クリック選択の`<input>`に`multiple`を付けて複数ファイルの一括選択に対応、(2) 別枠の非表示`<input>`に`webkitdirectory`（非標準だが主要ブラウザは実装済み）を付けた「📁 フォルダごと選択」リンクを追加、(3) ドラッグ＆ドロップは`DataTransferItem.webkitGetAsEntry()`でエントリを取得し、ディレクトリの場合は`FileSystemDirectoryReader.readEntries()`を（1回の呼び出しでは全件返るとは限らない仕様のため、空配列が返るまで）繰り返し呼び出して再帰的にサブフォルダも含めて全ファイルを集める`walkEntry`/`readAllEntries`ヘルパーを実装した。バックエンドのアップロードAPIは1ファイルずつしか受け付けない設計のまま変更せず（`services/doc_storage.py`）、`DocFoldersTab.add()`側で選択された全ファイルを順番に（並列にはせず、フォルダ選択時に大量の同時リクエストが飛ぶのを避けるため）アップロードし、1件失敗しても残りは続行したうえで成功・失敗件数をまとめてトースト表示する（限定公開設定は選択した全ファイルに共通で適用される）。**実際のOS由来のドラッグ＆ドロップ操作はPlaywrightでの自動テストが困難なため、`DataTransferItem.webkitGetAsEntry()`が返すFileSystemEntry（ファイル・ディレクトリの`isFile`/`isDirectory`/`file()`/`createReader().readEntries()`）を模したダミーオブジェクトを`page.evaluate`で構築し、実際に`drop`イベントを発火させて検証した**（フォルダ1件・その中の直下ファイル1件・サブフォルダ1件（さらにその中にファイル1件）という2階層の構成で、再帰的に2ファイルとも正しく検出されることを確認）。あわせて、通常のクリックでの複数ファイル選択（2ファイル同時選択→2件アップロード→一覧に両方反映）も確認した。テストファイル・検証用コンテナはすべて削除済み。新規API・DB変更は無いフロントエンドのみの変更のため、設計文書側の改訂は無い（2026-09-09）。
- S-06「参照ドキュメント範囲」タブの一覧を、画面モックアップの`.doc-tree`/`.doc-row`/`.doc-row.child`と同じ見た目（1本の枠内で行を罫線区切り、子ファイル行は字下げ＋背景色を変える）に作り直した（ユーザーからの明示的な要望「画面モックアップと同じように、ファイルがどのフォルダに入っているのかがわかりやすいように表示して」）。従来の実装は機能的には既にツリー構造（フォルダの下に子ファイルを字下げ表示）を持っていたが、見た目が`<ul>`のネスト＋左ボーダーの簡易的な表現に留まっており、モックアップの「行ごとに罫線区切り＋子行は背景色も変える」という視覚表現に合わせていなかった。`ChannelSettings.tsx`の`DocScopeTab`を、トップレベル項目とその子ファイルを1本のフラットな行配列（`flatRows`）に組み立ててから描画する形に書き直した（ネストした`<ul>`のまま各階層に`last:border-b-0`を付けると、フォルダブロックの区切りごとに境界線が欠けてしまう問題に気づき、境界線の要否をflatRows全体に対する「配列の最後の行かどうか」で判定するようにした）。子行にはモックアップ同様`pl-[38px]`の字下げと`bg-surface-subtle`の背景色を付け、フォルダ行には登録済み子ファイル数（「（N件登録済み）」）を追加表示し、フォルダに何件のファイルが紐づいているか一覧からすぐ分かるようにした。限定公開バッジ・disabled状態（公開チャンネルでの限定公開フォルダ）は既存の挙動のまま維持した。ローカルで使い捨てPostgres・DEV_AUTH有効なバックエンド・Viteフロントエンドを立ち上げ、Playwrightで実際に(1) 子ファイル2件を持つフォルダ・子ファイル無しのフォルダ・限定公開フォルダを登録し、公開チャンネルでは限定公開フォルダがグレーアウト表示、非公開チャンネルでは選択可能になることをスクリーンショットで確認した。テストデータ・検証用コンテナはすべて削除済み。新規API・DB変更は無いフロントエンドのみの変更のため、設計文書側の改訂は無い（2026-09-09）。
- S-06「参照ドキュメント範囲」タブ・S-08「ドキュメント参照範囲」タブの両方で、フォルダを常時全展開表示していたのを「フォルダ名をクリックすると中の登録ファイルが表示される」開閉式（アコーディオン）に変更した（ユーザーからの明示的な要望「モックアップのチャンネル設定と管理コンソールと同じようにしたい。フォルダ名をクリックするとその中のファイルが表示されて、一つずつチェックボックスで選択できるみたいな感じ」）。画面モックアップ自体には開閉インタラクションの定義が無い（`.doc-row`は静的な常時表示）ため、この挙動はモックアップに無い新規のUI改善として実装した。両タブとも新規`expandedFolders`（`Set<string>`、展開中のフォルダid）とトグル関数を追加し、フォルダ行の矢印（▶/▼）付きボタンをクリックすると`flatRows`の組み立て時にそのフォルダの子ファイル行を含めるかどうかを切り替える。**S-06 DocScopeTabのチェックボックスは選択専用のまま維持し、フォルダ名・矢印・アイコン部分だけを別のボタン要素にして展開専用にする**（チェックボックスクリックで展開状態が変わらないよう、クリック領域を明確に分離）。**S-08 DocFoldersTabでは実装中に1件バグを作り込みかけて実機検証で発見・修正した**: 最初の実装は矢印記号（▶）だけをボタンにしており、フォルダ名のテキスト部分はボタンの外（ただのdiv）に置いていたため、ユーザーが要望した「フォルダ名をクリックすると展開される」が実際には成立していなかった（矢印の1文字だけがクリック対象という、要望とは異なる状態のまま気づかず先に進みかけた）。Playwrightで`button:has-text('開発部')`のロケータが0件だったことから発覚し、矢印・アイコン・名前・件数を1つのbutton要素にまとめる（S-06と同じ構造）よう修正した。**S-06側ではさらに2件目のバグを発見・修正した**: 「選択済みのファイルを含むフォルダは初期状態から展開しておく」という初期化ロジック（`useState`の初期値関数内で`folders`を参照）が、`useDocFolders()`（`settings`とは別の非同期フック）のデータ到着より先にコンポーネントがマウントされるケースで、`folders`がまだ空配列のまま初期化されてしまい、既存の選択が保存されたページでもリロード後にフォルダが畳まれたまま（選択済みのファイルが見えない）になる不具合があった。これも実際にリロード→スクリーンショットで確認して発覚し、`DocScopeTab`を「`useDocFolders`の`isLoading`が解消するまでプレースホルダ表示→解消後に実装本体（`DocScopeTabBody`、`folders`をpropsで受け取る）をマウントする」という、`GlobalLimitForm`等で確立済みの「非同期データが揃ってから条件付きレンダーし、propsからuseStateの初期値を直接設定する」パターンに書き直して解消した。ローカルで使い捨てPostgres・DEV_AUTH有効なバックエンド・Viteフロントエンドを立ち上げ、Playwrightで実際に(1) S-08・S-06とも畳んだ状態→フォルダ名クリックで展開→子ファイルが字下げ表示されることを確認、(2) S-06で子ファイルにチェック→保存→保存成功トースト確認、(3) ページを実際にリロードし、チェック状態が正しく永続化されていること・該当フォルダが自動的に展開された状態で表示されること（2件目のバグの直接的な再現・修正確認）をスクリーンショットで確認した。テストデータ・検証用コンテナはすべて削除済み。新規API・DB変更は無いフロントエンドのみの変更のため、設計文書側の改訂は無い（2026-09-09）。
- 投稿欄（Composer、S-03・S-04・DM会話・スレッド返信で共有）のEnterキーの挙動を「Enter＝改行、Ctrl+Enter（Macは⌘+Enter）＝送信」に変更した（ユーザーからの明示的な要望「スラックと同じように、エンターキーで改行、ctrl+enterで送信という仕様にしてほしい」）。従来は「Enter単体＝即送信、Shift+Enter＝改行」だった（要件定義書・基本設計書・画面モックアップのいずれにも明記の無い、実装時の既定挙動）。`Composer.tsx`の`handleKeyDown`で、送信条件を`e.key === 'Enter' && !e.shiftKey`から`e.key === 'Enter' && (e.ctrlKey || e.metaKey)`に変更し、Enter単体のケースでは何も処理せず（`preventDefault`しない）textarea標準の改行動作にそのまま委ねる形にした。メンション候補ポップオーバー表示中のEnter（候補確定）は既存のまま変更していない（ポップオーバーを閉じる分岐が先に来るため、通常のEnter処理には到達しない）。送信ボタンに`title="Ctrl+Enter（Macは⌘+Enter）でも送信できます"`を追加し、挙動が変わったことに気づけるようにした。ローカルで使い捨てPostgres・DEV_AUTH有効なバックエンド・Viteフロントエンドを立ち上げ、Playwrightで実際にEnterキー押下では送信されず2行のテキストが本文に残ること、続けてCtrl+Enterを押すと実際に2行のメッセージが送信され本文が空にリセットされることを、メッセージ件数の増加・textareaの値の両方で確認した（テスト投稿は確認後に削除済み）。Composer 1箇所の修正で全画面（S-03・S-04・DM・スレッド返信・送信予約）に反映される（既存の共有コンポーネント構造どおり）。定期投稿（F-36）・自動応答トリガー（F-38）の本文入力欄はComposerを使わない独自の`<textarea>`のため対象外（元々Shift+Enter/Enterの区別自体が無い単純な複数行入力欄）。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-09）。
- バグ修正: チャンネル会話の横幅を超える半角の長い連続文字列（スペースを含まない、URLやランダムな英数字の羅列など）を入力・投稿すると、投稿欄・会話ログのいずれも自動改行されず画面全体が横スクロールになる不具合を修正した（ユーザーからの報告「チャンネル会話の横幅を超えるような半角の長文を打つと、画面が横スクロールになる。自動改行ができるようにしたい」）。原因は`whitespace-pre-wrap`（改行・スペースを保持しつつ折り返す）だけでは、スペース等の折り返し可能な区切りが無い長い1トークンを折り返せないこと。**`MessageList.tsx`・`ThreadPanel.tsx`の本文表示div**に`overflow-wrap: break-word`相当の`break-words`を追加し、折り返せる場所が無い場合は単語の途中でも改行するようにした（日本語テキストは元々文字単位で折り返せるため影響なし）。**`Composer.tsx`の投稿欄**は透明textarea＋背後オーバーレイ方式（2026-09-02実装の`@メンションハイライト`と同じ構造）で、オーバーレイ側（見た目の表示）には元々`break-words`が付いていたが、**実際の入力を受け付けるtextarea要素自体には付いていなかった**ため、入力中はtextarea本体が横に伸びてしまっていた（オーバーレイとの折り返し設定を完全に一致させる必要があるという、メンションハイライト実装時の設計制約どおりの箇所が実際に食い違っていた）。textareaのclassNameに`break-words`を追加して解消した。ローカルで使い捨てPostgres・DEV_AUTH有効なバックエンド・Viteフロントエンドを立ち上げ、Playwrightで実際にスペース無し200文字の半角文字列を入力→投稿欄・会話ログとも折り返されて表示されること、`document.documentElement.scrollWidth`と`clientWidth`が一致し横スクロールが発生していないことを、入力時・送信後の両方で確認した（テスト投稿は確認後に削除済み）。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-09）。
- 各種の自由記述文字列フィールドに上限を設定・見直した（ユーザーからの明示的な要望「メッセージは10000字以内、ユーザー名は最大21文字、チャンネル名は最大80文字などのようにいろいろなところで文字列制限を設けたい」）。既存実装は一部のフィールドにのみバックエンド（Pydantic `Field(max_length=...)`）・フロント（`maxLength`属性）の制限があり、値もバラバラ（メッセージ本文4000字・ユーザー名100字・チャンネル名256字など）で、チャンネルの説明文（topic）には上限が全く無い状態だった。着手前にユーザーへ「指定3項目のみか、他のフィールド（BOT表示名・AIキャラクタ名・スキルのタイトル/指示文等）も見直すか」「DBスキーマ側にもCHECK制約を追加するか」を確認し、**全フィールドを見直す・アプリ層（バックエンドPydantic＋フロントmaxLength）のみで担保しCHECK制約は追加しない**（現状のKogackの全テーブルがTEXT型でアプリ側バリデーションに任せる設計と一貫させるため）方針で合意した。
  - **メッセージ本文（4000字のまま維持）**: 当初ユーザーの要望どおり4000→10000字に変更し、`channels.py`（A-11）・`dms.py`（A-19）・`messages.py`（A-14）・`recurring_posts.py`（A-53/A-55、本文欄）・`scheduled_messages.py`（A-50）・`trigger_rules.py`（A-63/A-65、action_body）の計8箇所、およびフロント`Composer.tsx`・`ChannelSettings.tsx`（定期投稿・トリガー本文欄）の`maxLength`を10000に変更したが、pushする前にユーザーから「10000字は多すぎるかもしれない、Slackも4000字なので4000に戻してよさそう」との再考を受け、同意のうえ全箇所4000字へ戻した（この経緯を含めて1コミットにまとめている）。
  - **ユーザー名（100→21字）**: `users.py`のA-62（`UpdateMeRequest`）、フロント`ProfileEditModal.tsx`。**Google認証による初回登録時（`routers/auth.py`のA-02コールバック）は、Google側のプロフィール名が本人の入力ではなく21字を超えうるため、着手前にユーザーへ「切り詰めて保存するか、制限せずそのまま保存するか」を確認し、切り詰める方針で実装した**（`len(name) > 21`なら`name[:21]`）。
  - **チャンネル名（256→80字）**: `channels.py`のA-07（`CreateChannelRequest`）・A-71（`UpdateChannelRequest`）、フロント`ChannelSettings.tsx`（`ChannelInfoForm`）・`JoinChannelModal.tsx`。
  - **チャンネル説明文（topic、新規に500字）**: 従来上限が無かった抜けを解消。A-07・A-71のバックエンド、フロント`ChannelSettings.tsx`・`JoinChannelModal.tsx`。
  - **その他の見直し**: AIペルソナ名（`ai_settings.py`のA-25、100→50字。ユーザー名ほど厳しくせず「Kogack AI」のような名前を許容）、定期投稿・トリガーのBOT表示名（`recurring_posts.py`・`trigger_rules.py`、100→50字）。スキルのタイトル（100字）・指示文（4000字）、自動対応範囲のrequest_category（100字）、振る舞い定義（8000字）、AIの口調（500字）、通知先メールアドレス（200字、フロント`AdminConsole.tsx`に`maxLength`が無かった抜けも合わせて追加）は、既存の値が既に妥当な範囲と判断し変更していない。
  - **検証**: ローカルで使い捨てPostgres・DEV_AUTH有効なバックエンドを立ち上げ、curl相当の直接HTTPリクエストで4つの主要な境界値（メッセージ本文10000字OK/10001字422、ユーザー名21字OK/22字422、チャンネル名80字OK/81字422、topic500字OK/501字422。この時点ではメッセージ本文はまだ10000字だった）をすべて確認した。Google認証時の切り詰めロジックは実際のOAuthフローを伴わず単体で`len(name) > 21: name = name[:21]`の挙動のみ確認した（30字の日本語文字列→21字、既に21字以下の英語名はそのまま、の2パターン）。フロントは実際にPlaywrightでプロフィール編集モーダルに30文字入力しても`maxLength`により21文字でキャップされること、投稿欄`textarea`の`maxlength`属性が意図した値になっていることを確認した。メッセージ本文を4000字へ戻した後の再検証は、既存の8箇所・4000という値自体が変更前から動いていた実績のある値であるため、`tsc -b`・`grep`での置換漏れ確認のみに留めた。テスト用に作成したメッセージ・チャンネルはすべて削除済み。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-09）。
- 前項の文字数制限見直しにともない、既に登録済みで新しい上限を超えているユーザー名・チャンネル名・チャンネル説明文を先頭から切り詰める処理を追加した（ユーザーからの明示的な要望「既に存在しているユーザー名やチャンネル名が文字数制限を超えている場合は、先頭の許容されている文字列分だけを抜き取るようにしてください」）。着手前に本番Supabaseへ読み取り専用の確認クエリを実行し、実際に超過データが存在することを確認した（`users.name`101字超過1件、`channels.name`256字1件、いずれも過去の実機検証で作成したテストデータらしき値。`channels.topic`の超過は0件）。**本番DBへの直接UPDATE（切り詰め処理そのもの）は権限の自動判定でブロックされた**（2026-09-04の「AI」→「Kogack AI」バックフィル時と同じ制限）ため、同じ教訓に従いアプリ自身が既に持つ仕組み（`database.py`の`AUTO_MIGRATE`、起動時に毎回`SCHEMA`を冪等に適用する既存の仕組み）経由で実行する方式に切り替えた。`SCHEMA`の末尾に`UPDATE users SET name = LEFT(name, 21) WHERE char_length(name) > 21`等3文（users.name・channels.name・channels.topic、それぞれ21字・80字・500字）を追加した——`LEFT()`は文字数ベースの切り詰めでマルチバイト文字も正しく扱う。**通常のCREATE TABLE IF NOT EXISTS等と異なりDML（データ変更）である点が既存パターンとは違うが、`WHERE char_length(...) > N`の対象が一度切り詰められると以後は0件になり実質何もしなくなるため、AUTO_MIGRATE=1（常時有効）で毎起動実行しても冪等で安全**（2026-09-02のペルソナ名バックフィルはこの冪等性が無かったため意図的にSCHEMAへ埋め込まなかったが、今回は条件自体に冪等性があるため踏み込んで良いと判断した）。ローカルで使い捨てPostgres・DEV_AUTH有効なバックエンドを使い、(1) 上限を超えるテスト用ユーザー名（40字）・チャンネル名（100字）・topic（600字）を直接INSERTで用意し、(2) バックエンド起動（AUTO_MIGRATE適用）で実際に21字・80字・500字へ切り詰められることを確認、(3) バックエンドを2回連続で起動し直しても2回目でエラーが起きず（`doc_folders`制約変更時の教訓どおり2周期テスト）、既に21字以下になった行が再度更新されても実害が無いことを確認した。テストデータは確認後に削除済み。本番への適用は、この変更を含むコミットが次にデプロイされた時点（Fly.ioは`AUTO_MIGRATE=1`が secrets に設定済み）で自動的に行われる想定。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-09）。
- メッセージ本文中のURL自動リンク化を実装した（ユーザーからの明示的な要望「WEBサイトなどのURLをメッセージに送ったら、そのURLをクリックできるようになり、クリックすると実際にそのサイトに飛べるようになる、という機能」）。要件定義書にはURL自動リンク化そのものを定義したF-xxは存在しない（着手前に確認済み。F-38「トリガーによる自動応答」の本文でURLに触れているのみで無関係）ため、要件定義書上のF-xxに対応付けられない新規機能として扱う。本文中の`http(s)://`から始まる範囲を検出し`<a target="_blank" rel="noopener noreferrer">`へ変換する処理を、既存のF-41メンションハイライト（`MessageList.tsx`の`renderMessageBody`、`start`/`end`のmatches配列を組み立てて本文をスライスしながら描画する既存の仕組み）に統合した——`renderMessageBody`は`ThreadPanel.tsx`からも共有されているため、1箇所の修正で会話ログ・スレッド元発言の両方に反映される。新規`findUrlMatches`は、URLとして妥当な文字（RFC3986のunreserved/reserved文字相当、`[A-Za-z0-9\-._~:/?#[\]@!$&'()*+,;=%]`）が続く範囲を1URLとして検出する（この文字クラスに日本語文字は含まれないため、「詳細はhttps://example.comを見てください」のような文中URLでも「を見てください」まで巻き込まない）。ただし文字クラス自体に`)`や`.`等の区切り文字も含むため、「(https://example.com)。」のように文の区切りとして使われた記号を誤って含めてしまうことがあり、これを避けるため末尾の句読点的な記号（`[.,;:!?)\]}'"]`）は検出後に切り落とす（開き括弧との対応までは見ない簡易版、Slack等の実装と同程度の精度と割り切った）。matchesの型を`{kind:'mention', label}`/`{kind:'url', url}`のdiscriminated unionに拡張し、レンダリング時に種別で分岐する。投稿欄（Composer.tsx）側の入力中プレビューは対象外とした（要望は「送信したメッセージ」がクリックできることであり、入力中のリアルタイムリンク化は要求の範囲外と判断）。ローカルで実際にPlaywrightを使い、(1) 文中URL・括弧付きURL（クエリパラメータの`&`を含む）・半角スペース区切り＋全角句点付きURLの3パターンを1通の発言で送信し、いずれも意図した範囲だけが`<a>`要素になり`rel="noopener noreferrer"`が付与されていることを確認、(2) 実際にリンクをクリックして新しいタブが開き正しいURLへ遷移することを確認、(3) 「@Kogack AI」メンションと「https://example.com/test」URLを同じ発言に混在させ、両方が同時に正しくハイライト・リンク化されることを確認した（メンションとURLの検出ロジックが干渉しないことの回帰確認）。テストメッセージ・AI生成タスクはすべて削除・中断済み。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（フロントエンドのみの変更でバックエンドAPI・DBは無変更）（2026-09-09）。
- S-05検索条件モディファイア（F-42、`before:`/`after:`/`on:`/`during:`）が「使い方が分からない」とのユーザーからの質問を受けて調査したところ、実装自体（バックエンド`routers/search.py`の`_as_date`/`_as_month`・フロントの`buildQuery`によるクエリパラメータ分離）は正しく動作しており、原因は**正しい日付フォーマット（ISO形式 `YYYY-MM-DD`、`during:`のみ`YYYY-MM`）を示すヒントが不十分だったこと**と判明した（誤ったフォーマットで入力してもエラーにならず日付条件が黙って無視されるだけなので、なぜ効かないのか利用者には分からない）。この調査結果を報告したうえで、ユーザーから4点の改善依頼を受けて実装した: (1) カレンダー入力欄の追加、(2) ヒント文言への具体的なフォーマット例の追記、(3) 検索欄入力中のモディファイアトークンの色付けによる識別、(4) ヒント文言の常時表示。
  - **カレンダー入力欄**: `detectModifierQuery`（従来`in:`/`from:`/`with:`のみ検出）を`before:`/`after:`/`on:`/`during:`も検出するよう拡張し、これらのトークン入力中はポップオーバーに候補一覧の代わりに`<input type="date">`（`during:`のみ`type="month"`）を表示する新規`selectDate`ハンドラを追加した。`<input type="date"/"month">`のvalueはブラウザのロケール表示に関わらず常にISO形式（`YYYY-MM-DD`/`YYYY-MM`）を返すため、バックエンドが期待する形式とそのまま一致する。既存の`handleKeyDown`（矢印キー・Enter/Tabでの候補選択）は`candidates`が空の日付系ポップオーバーでは働かせないよう分岐を追加した（`candidates.length`が0のまま通すと0除算や存在しない候補の選択でクラッシュする）。
  - **モディファイアトークンの色付け**: 検索欄をComposer.tsxの@メンションハイライトと同じ「透明input＋背後オーバーレイ」方式に変更した。新規`findModifierHighlights`が、確定済み（`resolved`にある）`in:`/`from:`/`with:`ラベルの完全一致と、`before:`/`after:`/`on:`/`during:`の`\S+`パターンの両方を検出し、該当範囲を青背景でハイライトする。in:/from:/with:は候補選択で確定するまでハイライトしない（直接テキスト入力しても実際には検索条件として機能しない既存の仕様と整合させた）一方、日付系は直接テキスト入力でも機能する既存仕様どおり、値が入力された時点でハイライトされる。
  - **常時表示ヒント**: 検索欄の下の青い案内ボックス（「検索対象は、あなたが参加している...」）に、モディファイア一覧と日付フォーマットの具体例（`before:2026-09-09`、`during:2026-09`）を常時表示するセクションを追加した。従来はポップオーバーを開いたとき（`in:`等を打ち始めたとき）にしか見えず、そもそも存在に気づけないことが「使い方が分からない」の一因だったため。
  - **検証**: ローカルで使い捨てPostgres・DEV_AUTH有効なバックエンド・Viteフロントエンドを立ち上げ、Playwrightで実際に(1) 検索画面を開いた直後からヒントが表示されていること、(2) `before:`と入力するとカレンダー入力欄のポップオーバーが開き、日付を選ぶと`before:2026-09-05 `が検索欄に正しく挿入されること（`during:`は`type="month"`で`during:2026-09`になることも確認）、(3) `before:2026-09-05`部分が実際に青くハイライトされること、(4) 検索実行後にURLへ`before=2026-09-05`として正しく分離され、チップにも反映されることを確認した。**検証中に見つけた既存の軽微な挙動**（今回の変更範囲外、対応はしていない）: 日付を直接テキスト入力し末尾にスペースを入れずに検索ボタンを押すと、ポップオーバーが開いたまま残る（`detectModifierQuery`がまだ「入力中」と判定し続けるため）。日付ピッカーから選ぶ場合は自動的に末尾スペースが付き問題にならないが、手入力派には気づきにくい点として記録しておく。テストデータはすべて削除済み。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-09）。
- バグ修正: チャンネル名が半角の長い連続文字列（スペースを含まない）だと、「チャンネルに参加する」画面（補足01、`JoinChannelModal.tsx`）で自動改行されず横スクロールになる不具合を修正した（ユーザーからの報告）。2026-09-09の会話メッセージ本文での同種の不具合修正（`break-words`追加）と同じ原因・同じ対処で、`whitespace-pre-wrap`や通常のテキスト折り返しだけでは、スペース等の区切りが無い長いトークンを折り返せない。着手前に他のチャンネル名表示箇所を横断的に確認したところ、同じリスクを持つ箇所が他に2つ見つかったため、あわせて修正した: (1) `JoinChannelModal.tsx`の参加可能チャンネル一覧（報告箇所、`break-words`を追加）、(2) `ChannelView.tsx`のS-03ヘッダー（`min-w-0 flex-shrink truncate`を追加し、はみ出た分は省略記号`...`にする。高さ固定の1行ヘッダーのため複数行折り返しではなく省略表示を選んだ）、(3) `MembersModal.tsx`の補足03タイトル（`min-w-0`＋`break-words`を追加、モーダルは高さ制約が緩いため複数行折り返しにした）。サイドバー（`Layout.tsx`）・S-05検索結果・メンション候補一覧（`Composer.tsx`）は元々`truncate`が付いており対象外、S-08管理コンソールのチャンネル名リンク（テーブルセル内）はテーブル自体が横スクロール前提の表示のため今回は対象外とした。ローカルで実際に63文字の半角連続文字列のチャンネル名を持つテストチャンネルを作成し、Playwrightで(1) 参加画面で2行に折り返され横スクロールが発生しないこと、(2) 会話画面のヘッダーで省略記号付きの1行に収まり所属メンバー等のボタンのレイアウトが崩れないこと、(3) メンバー一覧モーダルのタイトルが複数行に折り返されモーダル幅を超えないことを、`document.documentElement.scrollWidth`とスクリーンショットの両方で確認した。テストチャンネルは削除済み。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-09）。
- S-08「ドキュメント参照範囲」タブのアップロードを、フォルダ単位でグルーピングして管理できるようにした（ユーザーからの報告「フォルダごとD&Dしたときに、フォルダでまとめて読み込むというようにならず、2件のファイルを選択中と表示される。エクスプローラーでフォルダごと選択することもできない。アップロード後もフォルダごとにまとまらずファイルがバラになる」）。着手前に、(1) フォルダをD&D/選択したときGoogleドライブのフォルダ登録（`item_type='folder'`→子`file`）と完全に同じ構造でグルーピングするか、(2) 見た目の改善に留めるか、また(1)を選んだ場合サブフォルダの入れ子（既存のdoc_foldersは2階層のみ）をどう扱うかをユーザーに確認し、**フォルダ単位でグループ化し、サブフォルダは全てフラット化して親フォルダの直下の子にする**方針で合意した。
  - **調査で判明した3つの原因**: (a) D&D時の`walkEntry`が再帰的にFileだけを集めてフラット化しており、元の「どのフォルダに属していたか」という情報がそもそも失われていた、(b) クリックでのファイル選択ダイアログでフォルダ自体を選べないのはブラウザ標準のOSファイル選択ダイアログの仕様上の制約（Kogack側では回避不可）で、対処として既に「📁 フォルダごと選択」という専用ボタンを用意していたが、選択前（`entries.length===0`）のときしか表示されず見落とされやすかった、(c) アップロードAPI（`upload_doc_file`）が常に`item_type='file', parent_folder_id=NULL`で固定登録しており、フォルダという単位でグルーピングする経路自体が存在しなかった。
  - **DBスキーマ**: 既存のCHECK制約（`doc_folders_parent_matches_type_check_v2`・`doc_folders_source_matches_columns_check`、2026-09-09のSlice 2a時点で「source='upload'のfileは常にparent_folder_id NULL」「source='upload'は常にitem_type='file'」を強制していた）を、`_v3`/`_v2`として緩める形に差し替えた（アップロード経由の「フォルダ」＝ファイル実体を持たない仮想フォルダ行、および「アップロードフォルダの子ファイル」という新しいケースを許容するため）。**旧バージョンのADD文はここでも一切残さず**、2026-09-09に確立した「既存制約を後から差し替える変更は`ALTER TABLE ... DROP CONSTRAINT IF EXISTS`で旧名を先に消してから新名で作り直す」パターンを踏襲した。
  - **バックエンド**: 新規`POST /api/admin/doc-folders/upload-folder`（`routers/admin.py`）で、ファイル実体を持たない仮想フォルダ（`item_type='folder', source='upload', drive_folder_id/storage_pathともNULL`）を1件作成できるようにした。既存`upload_doc_file`に`parent_folder_id`（Form、省略可）を追加し、指定時はその値をDBの`parent_folder_id`に設定する（親フォルダの存在確認と`item_type='folder' AND source='upload'`であることのチェック付き。Driveフォルダの子にアップロードファイルを混在させないための制約）。**`delete_doc_folder`（A-40）にも見落としを発見・修正した**: フォルダを削除するとDB上の子ファイル行はCASCADEで自動的に消えるが、Fly Volume上の実ファイルは個別削除が必要なため、従来の実装（削除対象1件だけのsource/storage_pathを見る）のままだと、フォルダ削除時に子ファイルの実体が孤立して残ってしまう新しいギャップが生じていた。削除前に「自分自身＋`parent_folder_id`が一致する子ファイル」のうち`source='upload'`かつ`storage_path`を持つものをまとめて集めておき、DELETE後にそれぞれ`doc_storage.delete()`する形に修正した。
  - **フロントエンド**: `FileDropzone`（`AdminConsole.tsx`）の内部表現を`File[]`から`UploadEntry[]`（`{file, folderName, displayName}`。`folderName`はフォルダに属さない単独ファイルなら`null`、`displayName`はサブフォルダ由来の相対パスを含む表示名）へ変更した。D&D時の`walkEntry`はフォルダ自身の名前を含まない相対パスを返すよう`prefix`引数を追加し、トップレベルのエントリがディレクトリの場合はその名前を`folderName`として中の全ファイル（サブフォルダも再帰的にフラット化）に付与する。`webkitdirectory`のinput選択時は各`File.webkitRelativePath`（例:「資料/sub/a.pdf」）の先頭セグメントを`folderName`として抽出する。選択中の表示を「📁 フォルダ名（N件のファイルを含むフォルダ）」とグルーピングして見せるようにし、**「📁 フォルダごと選択」ボタンを常時表示に変更した**（従来は未選択時のみで見落とされやすかったというユーザーの報告そのものへの対処）。`DocFoldersTab.add()`は`folderName`でグループ化し、フォルダに属するグループは`createUploadDocFolder`でフォルダを1件作成→そのidを`parent_folder_id`として各ファイルを`uploadDocFile`でアップロードする2段階フローに変更した。サブフォルダ由来のファイルは、実際にアップロードするファイル名を`new File([entry.file], entry.displayName, ...)`で相対パス込みの名前に作り直し、同名ファイルの混同を避ける。
  - **検証**: ローカルで使い捨てPostgres・DEV_AUTH有効なバックエンド・Viteフロントエンドを立ち上げ、(1) curl相当の直接HTTPリクエストでフォルダ作成→子ファイル2件のアップロード→一覧のフォルダ/ファイル構造・存在しない親フォルダへのアップロードの404を確認、(2) DBスキーマ変更をローカルで「初回起動→実データでのAPI呼び出し→2回目の起動」の2周期テストで冪等性を確認（2026-09-09の教訓どおり）、(3) Playwrightで`DataTransferItem.webkitGetAsEntry()`をモックしたダミーのFileSystemEntry（フォルダ「資料」直下にa.txt・サブフォルダ「sub」内にb.txtという2階層構成）を使い、実際に`drop`イベントを発火させて「📁 資料（2件のファイルを含むフォルダ）」という表示・「a.txt（9B）、sub/b.txt（9B）」という相対パス表示を確認、(4) 実際に「アップロード」ボタンを押して2件成功のトースト→S-08一覧でフォルダ→子ファイル構造が正しく表示されること→S-06「参照ドキュメント範囲」タブでも同じ構造がツリー表示されることを確認、(5) フォルダを削除し、DB上の子ファイル行の連動削除に加えFly Volume（ローカルでは`backend/uploads/docs`）上の実ファイルも孤立せず削除されることを、削除前後のディレクトリ内容の差分比較で確認した。テストデータ・検証用コンテナはすべて削除済み。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-09）。
- ログイン時のGoogle同意画面から、実際には使っていないGoogle Drive関連の権限要求を撤去した（ユーザーからの指摘「もうGoogleドライブ使ってないのでこの画面いらなくないですか？」、Drive同意を求める同意画面のスクリーンショットの共有を受けて着手）。2026-09-07に層2ドキュメントQ&A向けとして`google_auth.py`の`build_auth_url`へ追加した`drive.readonly`スコープ（`access_type=offline`・`prompt=consent`込み）は、2026-09-08にGoogle Workspace管理コンソールのdomainPolicyブロックが判明し、2026-09-09に層2の実装自体を実ファイルアップロード方式（Drive非依存）へ切り替えたことで、実際には一切使われないまま残っていた（ユーザーからの報告どおり、ログインのたびに使っていない権限の同意画面が表示され続けるだけの状態になっていた）。`build_auth_url`のscopeを`openid email profile`のみに戻し、`prompt`を`select_account consent`から`select_account`へ、`access_type=offline`を削除した（いずれもrefresh_token取得・Drive同意画面表示のためだけに追加していたもの）。あわせて、ユーザーに「T-23 google_drive_tokensへの保存処理も削除するか」を確認したところ「削除する（推奨）」を選択されたため、`routers/auth.py`のA-02コールバックからaccess_token/refresh_tokenをT-23へUPSERTする処理を削除した（スコープを要求しなくなった以上、ここで受け取るaccess_tokenはDrive権限を含まない通常のログイン用トークンであり、保存しても無意味なデータにしかならないため）。これに伴い`routers/auth.py`で`datetime`/`timedelta`/`timezone`の全てのimportが不要になったため削除した。**T-23テーブル自体・`services/google_drive.py`・`google_auth.py`の`refresh_access_token`関数は削除せず残置した**（将来Workspace管理者が特定でき、または別のドメインポリシー変更でDrive連携を再開できるようになった際の土台として。`services/google_drive.py`は現時点でもどこからも呼ばれていないデッドコードであることを確認済みだが、意図的な設計判断として残す）。`python -c "import ast; ast.parse(...)"`で両ファイルの構文を確認した（実際のGoogle OAuthフローはdev-loginでは代替できないため、構文確認とコードレビューに留める）。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-10）。
- バグ修正: チャンネル説明文（topic）が半角の長い連続文字列（スペースを含まない）だと、補足03「メンバー一覧」の「チャンネル情報」タブで自動改行されず横スクロールになる不具合を修正した（ユーザーからの報告）。2026-09-09のチャンネル名・メッセージ本文での同種の不具合修正（`break-words`追加）と同じ原因・同じ対処で、`whitespace-pre-wrap`だけではスペース等の区切りが無い長いトークンを折り返せない。着手前に`channel.topic`の他の表示箇所も横断的に確認したところ、`ChannelView.tsx`のS-03ヘッダー・`JoinChannelModal.tsx`の参加可能チャンネル一覧はいずれも既に`truncate`（1行省略表示）が付いており横スクロールのリスクが無いことを確認し、対象外とした（`MembersModal.tsx`「チャンネル情報」タブの説明欄だけが全文を複数行表示する唯一の箇所で、かつ`break-words`が抜けていた）。`MembersModal.tsx`の該当divに`break-words`を追加して解消した。フロントエンドのみの変更でバックエンドAPI・DBは無変更のため設計文書側の改訂は無い。`tsc -b`・`vite build`で検証（2026-09-10）。
- バグ修正: チャンネル説明文（topic）が長く、かつチャンネル名が短いと、S-03会話画面のヘッダーでチャンネル名自体が見切れる（例:「雑談」が「雑」だけになる）不具合を修正した（ユーザーからの報告。前項のMembersModal修正の直後に発覚）。原因はFlexboxの縮小配分アルゴリズムにあった: `ChannelView.tsx`のヘッダーでチャンネル名・説明文の両spanがいずれも`min-w-0 flex-shrink`（＝`flex-shrink:1`、上限なし）で、スペース不足時の縮小量がCSS仕様上「各要素のflex-basis（＝コンテンツの自然幅）× flex-shrink値」に比例配分されるため、説明文の自然幅が非常に大きい（長い）場合、その分だけ説明文が縮小配分の重みを独占し、結果として短いチャンネル名の方が（自然幅に対する比率としては同程度でも）絶対px数としてほぼ0まで潰されてしまっていた（例: 幅30pxの短い名前が実測16pxまで圧縮されtruncateでほぼ消える一方、説明文は圧縮後も大きな幅を維持し続ける、という直感に反する挙動）。修正は`MessageList.tsx`の添付ファイル名表示（`max-w-[220px] truncate`）と同じ既存パターンを踏襲し、説明文span（`channel?.topic`）に`max-w-[220px]`を追加した。CSS Flexbox仕様の「制約違反の反復解決」（ある要素がmax-widthの制約に達すると、そこで縮小を打ち切り、残りの縮小分は他の縮小可能要素へ再配分される）により、説明文が220pxで頭打ちになった分の縮小要求がチャンネル名側の縮小要求から差し引かれる形になり、短い名前は自然幅のまま表示されるようになる。着手前に他の`channel.topic`表示箇所（`JoinChannelModal.tsx`の一覧・前項で修正したばかりの`MembersModal.tsx`）も横断確認したが、いずれも既に`truncate`のみの単独表示（説明文が他要素と縮小配分を奪い合う構造ではない）のため対象外と判断した。**検証はフルスタック起動ではなく、`vite build`で生成した実際のCSS（`dist/assets/index-*.css`）を読み込む最小限のスタンドアロンHTML＋Playwrightで行った**（Docker Desktopが起動しておらず、この確認のためだけにフルスタックを立てるより軽量で確実と判断）。(1) 修正前の構成（説明文に`max-w`無し）を再現し、実際に「雑談」の`clientWidth`が自然幅30pxから16pxへ圧縮されtruncateされる（`scrollWidth(30) > clientWidth(16)`）ことを実測でバグ再現を確認、(2) 修正後は`clientWidth`が自然幅30pxのまま（truncateされない）ことを確認、(3) スクリーンショットで「雑」の1文字だけに切れていた表示が「雑談」の全文表示に変わることを視覚的に確認、(4) 逆のケース（長いチャンネル名＋短い説明文、2026-09-09の既存修正が対象としていたシナリオ）で回帰が無い（ヘッダー全体が横スクロールを起こさずチャンネル名が正しく省略表示される）ことも確認した。テスト用HTML・スクリプト・スクリーンショットはいずれもスクラッチパッドに作成したものでリポジトリには含めていない。フロントエンドのみの変更でバックエンドAPI・DBは無変更のため設計文書側の改訂は無い（2026-09-10）。
- 2026-09-08に追加した削除済み/非参加チャンネルの案内文言を、ユーザーからの指摘でさらに言い換えた（`ChannelView.tsx`）。見出しを「このチャンネルは削除されたか、参加権限がありません」から「このチャンネルは既に存在しないか、参加権限がありません」に、説明文も同じ言い回しに揃えて変更した。「削除された」は必ず過去にそのチャンネルが存在したことを含意してしまう一方、この画面は「そもそも存在しないチャンネルID」「非公開チャンネルの非参加者」「実際に削除されたチャンネル」の3ケースをバックエンドが区別せず同じ404/403で返す設計（総論5.9節、F-34「存在を伏せる」）を前提にしており、より中立で正確な表現に置き換えた。両論併記でプライバシー設計（削除済みか単に権限が無いだけかを利用者が区別できないようにする）を崩さない点は従来どおり。フロントエンドのみの変更でバックエンドAPI・DBは無変更のため設計文書側の改訂は無い。`tsc -b`・`vite build`で検証（2026-09-10）。
- メッセージ本文の簡易書式（太字・コードボックス・箇条書き・取り消し線）を実装した（ユーザーからの明示的な要望「Slackのメッセージ入力欄と同じように、コードのボックス、下線、ボールド、箇条書きのような機能を付けたい」）。要件定義書・基本設計書・詳細設計書・画面モックアップのいずれにも記載がなかった新規機能。着手前にユーザーへ2点確認した: (1) 入力方式は「記法入力のみ」「記法入力＋ツールバーボタン」「さらに入力中もリアルタイムで装飾されるライブプレビュー」の3案から「記法入力＋ツールバーボタン」を選択（Slackの実際の挙動に近く、GitHubのコメント欄と同じ「選択範囲をボタンで挟む」補助UIパターンで実装コストも抑えられるため）、(2) 「下線」はSlack自体の書式メニューにも標準Markdownにも存在しない機能のため「下線は諦めて取り消し線にする」を選択した。**記法はSlack自体のmrkdwn（単一`*`太字・単一`~`取り消し線）ではなくGFM（GitHub Flavored Markdown）風の`**太字**`・`` `インラインコード` ``・``` ```コードブロック``` ```・行頭「- 」の箇条書き・`~~取り消し線~~`を採用した**（設計判断。Slack自体の単一文字記法は、日本語の波ダッシュ「〜」（例:「また明日〜」）や文中で単発の`*`を使う日本語文章との誤検出が非常に多く、この点はSlackとの機能名の一致よりも実用上の安全性を優先した）。
  - **表示側（`MessageList.tsx`のrenderMessageBody、`ThreadPanel.tsx`と共有）**: 既存のメンション・URL自動リンク（2026-09-09実装）と同じ「本文中の位置に対するmatches方式」だけでは、コードブロック・箇条書きのような行を跨ぐ構造を表現できないため、本文を(1)コードブロック単位→(2)箇条書き行の連続単位、の2段階で先に分割してから、残った通常の文章部分にだけ既存のインライン装飾ロジック（`renderInlineSegment`、太字・取り消し線・インラインコード・メンション・URLを扱う）を適用する構成にリファクタした。インライン装飾は複数の候補が同じ範囲に重なりうる（例: コードスパンの中に偶然`**`が含まれる）ため、優先度（コード＞太字/取り消し線＞メンション＞URL）付きの重なり解決アルゴリズムを新設し、優先度が高く開始位置が早い候補を採用、それと重なる低優先度の候補は描画しない方式にした（Markdownの一般的な「コードスパンの中身はそれ以上解釈しない」という挙動を再現）。ネストした書式（太字の中にメンション等）には対応しない簡易実装と割り切った。コード表示の背景色は`bg-surface-subtle`ではなく`bg-surface-muted`を使った（発言行のホバー背景が`hover:bg-surface-subtle`のため、同じ色にするとホバー時にコードの箱が視認できなくなる実害に気づき、Playwrightでのホバー再現検証で確認して修正した）。インラインコードはさらに`border border-line`を追加し、淡い背景色だけでは境界が分かりにくかった点を補強した。
  - **入力側（`Composer.tsx`）**: 投稿欄下のツールバーに4つのボタン（太字B・取り消し線S・コード`</>`・箇条書き≡）を追加した（📎添付ボタンの右、@メンションボタンの左）。いずれも`wrapSelection(prefix, suffix)`共通ヘルパーで実装し、選択範囲があればマーカー文字列で前後を囲んで囲んだ範囲ごと選択し直す（続けて別の書式を重ねがけしやすいように）、選択が無ければマーカーの間にカーソルを置いてそのまま入力できるようにする、というGitHubのコメント欄と同じ挙動にした。コードボタンは選択範囲に改行を含むかで自動的にインラインコード（単一`` ` ``）とコードブロック（``` ``` ```、改行付き）を切り替える「賢いトグル」にし、ボタン数を1つに抑えた。箇条書きボタンは選択範囲を含む行全体を対象に「- 」を付け外しするトグル動作（既に全行付いていれば外す）で、空行は対象外のまま維持する。**ユーザーが選択した方式どおり、入力中のリアルタイム装飾（太字等が入力中から実際に太字に見える）は実装していない**（既存のメンションハイライトと同じ透明textarea＋オーバーレイ方式をさらに拡張すれば技術的には可能だが、スコープ外として選ばれた）。
  - **検証**: Docker Desktopが起動しておらず、この確認のためだけにフルスタックを立てるより軽量と判断し、2026-08-31のF-42検索モディファイア実装時に確立した「パースロジックをNode上に複製した単体スクリプトで確認し、確認後に削除する」手法を踏襲した。(1) `renderMessageBody`相当のロジック（コードブロック分割・箇条書き分割・重なり解決込みの`renderInlineSegment`）を複製し、太字/取り消し線/インラインコード/コードブロック/箇条書きの単純ケース、コードスパン内の`**`が太字化されないこと、コードブロック内の「- 」が箇条書き化されないこと、メンション・太字・URLが混在するケース、箇条書き項目内でもインライン書式が効くこと、**波ダッシュ「〜」が取り消し線と誤検出されないこと・単発の`*`が太字と誤検出されないこと**（Slack自体の単一記法を避けた設計判断の直接的な検証）の12パターンをすべて確認した。(2) `Composer.tsx`の`wrapSelection`/`wrapCode`/`insertBulletList`のテキスト操作ロジック（選択範囲をstart/endの数値で模擬）を複製し、選択ありでの太字化、選択無しでのカーソル位置挿入、単一行/複数行選択でのコード/コードブロック自動切替、単一行・複数行・トグル解除・空行維持・前後の行への非干渉を含む箇条書きの9パターンを確認した。(3) `vite build`で生成した実際のCSS（`dist/assets/index-*.css`）を読み込む最小限のスタンドアロンHTML＋Playwrightで、太字・取り消し線・インラインコード・コードブロック・箇条書き・ツールバーボタンの実際の見た目をスクリーンショットで確認し、この過程で上記のホバー時視認性の不具合を発見・修正した。テスト用スクリプト・HTML・スクリーンショットはいずれもスクラッチパッドに作成したものでリポジトリには含めていない。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（フロントエンドのみの変更でバックエンドAPI・DBは無変更）（2026-09-10）。
- 直前の書式ツールバー実装の直後、ユーザーから「書式に関するマークを入力欄の上側に、ファイル添付やメンションのマークを入力欄の下側に配置し、役割が違うことをわかりやすくしたい」との要望を受け、`Composer.tsx`のボタン配置を変更した。当初は太字・取り消し線・コード・箇条書きの4ボタンを📎添付ボタンの右・@メンションボタンの左という下段の並びに追加していたが、これだと「本文の見た目を変える書式」と「本文に付随させるもの（添付ファイル・宛先指定）」という役割の異なる機能が同じ行に混在し見分けにくかった。書式4ボタンをtextareaの直前（入力欄の上）の新しい行へ移し、下段には📎添付・@メンション・🕐送信予約（ml-auto）・送信ボタンのみを残した。ポップオーバー（メンション候補・送信予約）は元々`bottom-full`でComposer全体（最外周のrelativeコンテナ）を基準に配置しており、内部の行を1つ増やしても位置がずれないことを確認済み。`vite build`で生成した実際のCSSを使ったスタンドアロンHTML＋Playwrightのスクリーンショットで、上段に書式4ボタン・下段に📎/@/🕐/送信が意図どおり分かれて表示されることを確認した。フロントエンドのみの変更でバックエンドAPI・DBは無変更のため設計文書側の改訂は無い（2026-09-10）。
- バグ修正: 箇条書きボタンが、何も入力されていない空の行にカーソルがあるだけの状態で押しても何も起きなかった不具合を修正した（ユーザーからの報告「何も入力されていない行で箇条書きボタンを押しても箇条書きのマークが出てくるようにできる？」）。`Composer.tsx`の`insertBulletList`は元々、複数行選択にまたがる空行（段落の区切りとして意図的に維持したい行）を書式付け対象から除外する実装だったが、この「空行は対象外」というルールが「対象行がその空行1行だけ」のケース（=入力し始める前の空の投稿欄・空の1行にカーソルだけがある状態）にも一律に適用されてしまい、常に何も起きない状態になっていた。また、当時の`allBulleted`（既に箇条書き済みかどうかの判定）が空行も「条件を満たす」とみなす実装だったため、仮に空行除外を外しただけだと空の1行を「既に箇条書き済み」と誤判定し「解除」動作（実際には何も変わらない）になってしまう副作用があった。修正は2点: (1) `allBulleted`の判定を、空行を除いた行（`nonBlankLines`）だけで行うようにし、かつ「対象が空行しかない場合はfalse」（＝箇条書き未適用として扱う）にした。(2) 空行の書式付けは「対象が複数行にまたがる場合は維持（段落区切りとして）、対象がその1行だけの場合は「- 」を付ける」という条件分岐に変更した。これにより、空の投稿欄（またはカーソルのある空行）でボタンを押すと「- 」が挿入されカーソルがその直後に置かれ、そのまま箇条書きの1件目を入力できるようになった。もう一度ボタンを押すと（`- `のみの行は`allBulleted`の判定で「既に箇条書き済み」と正しく認識されるため）元の空行に戻る対称的なトグルになる。2026-08-31のF-42検索モディファイア実装以来の手法（パースロジックをNode上に複製した単体スクリプトで確認）を踏襲し、既存9パターンに加え「空の投稿欄でも箇条書きマークが付く」「カーソルがマークの直後に来る」「もう一度押すと解除される」の3パターンを追加した計12パターンをすべて確認した。テストスクリプトはスクラッチパッドに作成したものでリポジトリには含めていない。フロントエンドのみの変更でバックエンドAPI・DBは無変更のため設計文書側の改訂は無い（2026-09-10）。
- 箇条書きの行でEnterを押すと、次の行にも自動的に「- 」が付いて箇条書きが続くようにした（ユーザーからの明示的な要望「箇条書きの行で改行すると自動的に次の行の先頭にも箇条書きのマークが出るようにしたい」）。`Composer.tsx`の`handleKeyDown`に、既存の「Ctrl+Enterで送信」判定の直後、「Enter単体（Shiftなし）でカーソル位置に選択範囲が無い」場合の分岐を追加した: カーソルのある行（`\n`区切りで特定した現在行）が`- `で始まっていれば、ブラウザ標準の改行を`preventDefault`で止め、代わりに`\n- `を手動で挿入してカーソルをその直後に置く。**多くのエディタ（Notion・GitHub等）と同じく、何も入力していない箇条書き行（`- `だけの行）でEnterを押した場合は、そのままだと空の「- 」が際限なく増え続けてしまうため、マーカーを外して箇条書きから抜ける**分岐にした（前項で追加した`insertBulletList`の「空の箇条書き行はもう一度押すと解除される」トグルと対になる自然な挙動）。テキスト選択がある状態（選択範囲を置き換える通常の入力）は対象外とし、素朴にカーソル位置のみのケースに絞った（対象行の特定が曖昧になるため）。2026-08-31のF-42検索モディファイア実装以来の手法（ロジックをNode上に複製した単体スクリプトで確認）を踏襲し、(1) 箇条書き行末でのEnter→次行にも「- 」、(2) 複数件続けて箇条書きが継続すること、(3) 空の箇条書き行でEnter→マーカーを外して抜ける（本文・カーソル位置の両方を確認）、(4) 行の途中（末尾以外）にカーソルがあってもEnterで継続する、(5) 箇条書きでない普通の行では一切介入しない、(6) 前に別の文章がある状態でも対象行だけ正しく継続される、の7パターンをすべて確認した。テストスクリプトはスクラッチパッドに作成したものでリポジトリには含めていない。フロントエンドのみの変更でバックエンドAPI・DBは無変更のため設計文書側の改訂は無い（2026-09-10）。
- 簡易書式機能（2026-09-10）を拡張し、コードの文字色・斜体・下線を追加した（ユーザーからの明示的な要望「コードボックスの中の文字は黒とは別の色にしてほしい。斜体や下線もSlackにあったので実装したい」）。
  - **コードの文字色**: `MessageList.tsx`のインラインコード・コードブロックの文字色を`text-ink`（黒に近い既定色）から新規`text-code-text`（`index.css`に`--color-code-text: #6d28d9`を追加、紫系）へ変更した。画面モックアップには存在しない新規機能のため、既存の役割トークン（admin-text等）を流用せず専用トークンとして追加した（他画面の配色変更に波及しないようにする狙い）。
  - **斜体（`_斜体_`）**: GFM標準の単一アンダースコア記法を採用した。太字（`**`）・取り消し線（`~~`）を二重文字にしてSlack自体の単一記法（日本語の波ダッシュ・文中の単発`*`との誤検出）を避けたのと矛盾するように見えるが、**単一アンダースコアは日本語の日常的な文章にはほぼ出現せず、実際に衝突しうるのはPythonのダンダーメソッド名（`__init__`等）程度に限られるという判断で、GFM標準に合わせて単一文字を採用した**（一般利用者の日常的な文章での強調表現と頻繁に衝突する単一`*`とは性質が異なる、というのが太字と斜体で採用方針を分けた理由）。この既知の限界（`__init__`のようなダンダー名が丸ごと斜体として誤検出される）はバッククォートで囲めば回避できる（コードスパンの中身は装飾を解釈しないため）ことをNode単体テストで確認した。
  - **下線（`++下線++`）**: Slack自体の書式メニューにも標準Markdownにも下線記法は存在しないため独自に定めた（着手前のユーザーとの合意どおり、当初は下線を諦めて取り消し線で代替していたが、今回改めて下線自体の追加を要望されたため新設）。`++`を選んだ理由は、既存の増分演算子`i++`のような単発の出現ではマッチせず（`++`が対になって初めて反応するため）、実用上のリスクが小さいと判断したため（`__`（ダブルアンダースコア）は太字の別記法として広く認知されており紛らわしく、かつPythonのダンダー名との衝突も`_`単体版と変わらず悪化するだけなので採用しなかった）。
  - 優先度付き重なり解決アルゴリズム（コード＞太字/斜体/下線/取り消し線＞メンション＞URL）に斜体・下線を同じ優先度階層（太字・取り消し線と同格）で追加した。コードスパン・コードブロックの中身は従来どおり斜体・下線もさらに解釈しない。
  - `Composer.tsx`の書式ツールバー（入力欄上の行）に斜体（I）・下線（U）ボタンを追加し、太字（B）の直後・取り消し線（S）の直前に配置した（B・I・U・Sを装飾系としてひとまとまりに、続けてコード・箇条書きという並び）。いずれも既存の`wrapSelection`共通ヘルパーで実装（`_`/`_`、`++`/`++`）。
  - **検証**: 既存のNode単体スクリプト（`renderMessageBody`相当ロジックの複製）に斜体・下線の単純ケース、コードスパン内では斜体・下線も解釈されないこと、**上記の既知の限界（`__init__`が斜体と誤検出されること）とバッククォートでの回避、単発の`++`（インクリメント演算子）が誤検出されないこと**を追加した計18パターンをすべて確認した。あわせて`vite build`で生成した実際のCSSを使ったスタンドアロンHTML＋Playwrightのスクリーンショットで、コードの紫色表示・斜体・下線・更新後のツールバー（B I U S コード 箇条書き）の見た目を確認した。テストスクリプト・HTMLはスクラッチパッドに作成したものでリポジトリには含めていない。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（フロントエンドのみの変更でバックエンドAPI・DBは無変更）（2026-09-10）。
- S-04スレッドパネル（`ThreadPanel.tsx`）の横幅をマウスドラッグで変更できるようにした（ユーザーからの明示的な要望「スレッドの枠の横幅をマウスで変更できるようにしたい」）。従来は`w-[380px]`固定だった`<aside>`をReact stateで管理する`style={{width}}`に変更し、左端（`border-l`の位置）に幅8pxの透明なドラッグハンドルを重ねて配置した（ホバー時・ドラッグ中はアクセントカラーの半透明背景で存在を示す）。ドラッグ中は`window`全体で`mousemove`/`mouseup`を監視する（マウスがハンドルの外へ出ても追従させるため、多くのリサイズUIで採用される標準的な実装パターン）。幅は320px（最小）〜720px（最大、ただしウィンドウ幅からも制限し`window.innerWidth - 300`を上回らないようにする。狭いウィンドウで本体側の会話画面が潰れきってしまうのを防ぐため）にクランプする。**幅は`localStorage`（キー`kogack_thread_panel_width`）に保存し、次回スレッドを開いたとき・別チャンネルのスレッドを開いたときも直前の幅を引き継ぐ**（サーバー同期が必要な設定ではなく個人のブラウザ内の画面調整に過ぎないと判断。プライベートブラウジング等でlocalStorageが使えない場合は既定値380pxにフォールバックする）。ドラッグ中は`document.body`の`cursor`を`col-resize`・`userSelect`を`none`に一時的に変更し、素早いマウス移動でも余計なテキスト選択やカーソルのちらつきが起きないようにした（`useEffect`のクリーンアップで確実に元へ戻し、ドラッグ中にコンポーネントが予期せずアンマウントされた場合もリスナー・スタイルが残留しない設計）。**検証はフルスタックではなく、実際のドラッグ操作の数式・DOM構造を複製した最小限のスタンドアロンHTML＋Playwrightの`page.mouse`（実際のmousedown→mousemove→mouseupイベント列）で行った**（Docker Desktopが起動しておらず、素のDOM/マウスイベントのロジックなのでこの方が確実で軽量と判断）。(1) 左へ100pxドラッグ→幅が380→480pxに広がること、(2) 右へ大きくドラッグ→最小幅320pxでクランプされること、(3) 左へ大きくドラッグ→ウィンドウ幅900px時点の動的な最大幅（`min(720, 900-300)=600px`）で正しくクランプされること、(4) ドラッグ終了後にlocalStorageへ正しい幅が保存されることを、実測（`getBoundingClientRect().width`）で確認した。テストHTML・スクリプトはスクラッチパッドに作成したものでリポジトリには含めていない。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（フロントエンドのみの変更でバックエンドAPI・DBは無変更）（2026-09-10）。
- DM会話画面（`DmView.tsx`）にも、S-03チャンネル版（2026-09-08・2026-09-10）と同じ考え方で、自分が参加していないDM・存在しないDM IDのURLへ直接アクセスした際の明示的な案内メッセージを追加した（ユーザーからの明示的な要望「DMの画面もチャンネルの画面と同じように、参加していないDM画面のURLを直接打ち込んだとき用にメッセージを表示させたい」）。従来は`if (!dmsLoading && !dm) { return null }`で無言のまま画面が空白になっていた。**DMにはチャンネルのA-06に相当する単体の詳細取得APIが無く**、A-16（`GET /api/dms`、自分が参加しているDM一覧のみを返す）から該当DMが見つからない場合、それが「そもそも存在しないID」なのか「存在するが自分が参加者ではない」のかをフロント側で区別する情報がそもそも無い（バックエンドが一覧に含めない、という形でしか表現していない。DMは開始時に参加者が固定される設計のため他人のDMのURLを知ること自体が非公開チャンネル以上に稀ではあるが、直接入力すれば同じ状況は起こりうる）。ChannelView.tsxと同じ「両論併記」の考え方を踏襲し、見出し「このダイレクトメッセージは表示できません」（ユーザー提案の文言をそのまま採用）・説明文「既に存在しないか、参加する権限がありません。心当たりがある場合は送信元にご確認ください。」・「← ワークスペースに戻る」リンクを表示するようにした（`react-router`の`Link`を新規import）。`vite build`で生成した実際のCSSを使ったスタンドアロンHTML＋Playwrightのスクリーンショットで、チャンネル版と同じ体裁で表示されることを確認した。フロントエンドのみの変更でバックエンドAPI・DBは無変更のため設計文書側の改訂は無い（2026-09-10）。
- 投稿欄（Composer）に絵文字入力ボタンを追加した（ユーザーからの明示的な要望「Slackのように絵文字入力ボタンをメンションの横につけたい」）。フルの絵文字ピッカーライブラリは導入せず、業務チャットでよく使う絵文字60種を新規`EMOJI_LIST`定数として直接コードに持つ簡易版にした（検索・カテゴリ分け・スキントーン切替は対象外。新規npm依存を増やさない方針）。要望どおり「メンションの横」（📎添付ボタンの直後・@メンションボタンの直前）に配置し、クリックすると📎/@/🕐と同じ丸ボタンから8列グリッドのポップオーバーが開く。選択（`onMouseDown`＋`preventDefault`でtextareaのフォーカスを奪わない、既存のメンション候補と同じ実装パターン）するとカーソル位置にそのまま絵文字を挿入してポップオーバーを閉じる。メンション・送信予約の各ポップオーバーとは排他的に開閉する（`toggleEmojiPopover`/`insertMentionTrigger`/`toggleSchedulePopover`が互いに他方の`open`状態をfalseにする）。**実機検証で1件バグを発見・修正した**: 絵文字ボタン自体をクリックするとブラウザの既定挙動でフォーカスがそのボタンへ移ってしまい、textareaの`onKeyDown`に実装したEscapeでの「閉じる」処理が（フォーカスがtextarea上に無いため）効かなくなっていた。`insertMentionTrigger`が挿入後に明示的に`el?.focus()`でtextareaへフォーカスを戻しているのと同じパターンを`toggleEmojiPopover`にも追加し（ポップオーバーを開いた直後に`textareaRef.current?.focus()`）解消した。**検証は静的HTMLの複製ではなく、実際のComposer.tsxコンポーネントを一時的なVite開発サーバー上のテストページ（`emoji-test.html`＋`dev-emoji-test.tsx`、いずれも検証後に削除）にマウントし、Playwrightで実際にクリック・キー入力を行う形で行った**（透明textarea＋オーバーレイ方式やポップオーバーの絶対配置など、DOM/フォーカスの実挙動に依存する箇所が多く、静的HTMLの複製では前述のEscapeバグを検出できなかったと考えられるため、今回は実コンポーネントでの検証を選んだ）。(1) 絵文字ボタンが実際にメンションボタンと隣接して配置されていること、(2) クリックでポップオーバーが開くこと、(3) 絵文字選択でカーソル末尾位置に正しく挿入されポップオーバーが閉じること、(4) 本文の途中にカーソルを置いた状態でもその位置に正しく挿入されること、(5) Escapeキーでポップオーバーが閉じること（前述バグの回帰確認）を確認した。consoleエラーはテストページ自体のfavicon.ico 404のみで無関係。検証用ファイルはすべて削除し`git status`がComposer.tsxの変更のみになることを確認した。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（フロントエンドのみの変更でバックエンドAPI・DBは無変更）（2026-09-10）。
- 絵文字だけのメッセージを大きく表示する「ジャンボ絵文字」表示を実装した（ユーザーからの明示的な要望「文章無しで絵文字だけで送ったら少し大きく表示されてほしい」、Slack・Discord等の同種機能と同じ考え方）。`MessageList.tsx`に新規`isEmojiOnlyBody(text)`を追加し、本文（前後の空白を除く）が絵文字・結合用の記号（異体字セレクタU+FE0F・ZWJ（ゼロ幅結合子、`🙇‍♂️`等の結合絵文字に使用）・肌の色modifier）・空白のみで構成されている場合にtrueを返す。該当する発言の本文divは通常時の`text-[13.5px] leading-[1.75]`ではなく`text-[32px] leading-snug`（約2.4倍）で描画する。ZWJ・異体字セレクタは目に見えない制御文字のため、正規表現リテラルへ直接埋め込むと編集時に見落とし・文字化けのリスクがあるので`String.fromCharCode(0x200d)`/`String.fromCharCode(0xfe0f)`で明示的に生成してから`RegExp`コンストラクタで組み立てた（`‍`/`️`のエスケープ表記でも良かったが、生成過程を変数名で説明できる後者の書き方を選んだ）。**あまりに多くの絵文字を並べて全部大きく表示すると逆に読みにくくなるため、EMOJI_ONLY_MAX_COUNT（10件）を超える場合は対象外（通常サイズのまま）にする設計判断を加えた**（Discord等の実装が絵文字の並び数に上限を設けているのと同じ考え方）。既知の限界として、数字＋U+FE0F＋U+20E3（囲み文字）で構成されるキーキャップ絵文字（`1️⃣`等）は数字が`\p{Extended_Pictographic}`に含まれないため判定対象外になる（この用途では稀なケースとして許容し、コード内コメントに明記した）。`ThreadPanel.tsx`の元発言ヘッダー（`MessageList`を経由せず個別にrenderMessageBodyを呼ぶ実装）にも同じ判定を適用した（スレッド返信一覧は既存の共有`MessageList`をそのまま使うため自動的に対象に入る）。Composer（投稿欄）自体の入力中表示は対象外（要望は「送ったら」＝表示側の話であり入力中のリアルタイム拡大は範囲外と判断）。Node単体スクリプトで16パターン（単一/複数の絵文字のみ、前後の空白、改行を挟む場合、異体字セレクタ付き（❤️）、ZWJ結合絵文字（🙇‍♂️）、肌の色modifier付き（👍🏽）、文章のみ、絵文字と文章の混在、空文字、空白のみ、上限ちょうど/超過、絵文字でない記号・数字のみ）を確認し、あわせて`vite build`で生成した実際のCSSを使ったスタンドアロンHTML＋Playwrightのスクリーンショットで通常メッセージとの見た目の違いを確認した。テストスクリプト・HTMLはスクラッチパッドに作成したものでリポジトリには含めていない。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（フロントエンドのみの変更でバックエンドAPI・DBは無変更）（2026-09-10）。
- 発言への絵文字リアクション機能を実装した（ユーザーからの明示的な要望「Slackのように発言一つ一つに対して絵文字でリアクションできるようにしたい。返信・削除ボタンの左隣によく使いそうな絵文字何種類かを配置し、その横にメッセージの絵文字ボタンと同じように絵文字一覧を開くボタンも付ける感じで」）。要件定義書上のF-xxに対応付けられた新規機能で、チャンネル・DM・スレッド返信のいずれの発言にも対応する。
  - **データモデル**: 新規T-26 `message_reactions`（message_id・user_id・emoji、UNIQUE(message_id, user_id, emoji)で同じ人が同じ絵文字を二重に付けられないようにする。message_id削除時はON DELETE CASCADE）を追加した。emojiの種類を固定のCHECK制約等で縛らず任意の文字列として保持する（Composer.tsxの絵文字ピッカーの選択肢に依存させないため、将来ピッカー側の選択肢を増やしてもスキーマ変更が不要になる）。
  - **バックエンド**: 新規`backend/reactions.py`（`attachments.py`・`mentions.py`と同じ構成）に`fetch_reactions_grouped(pool, message_ids, current_user_id)`（複数メッセージ分をまとめて取得しN+1を避ける、既存のfetch_blocks_grouped/fetch_attachments_groupedと同じパターン。絵文字ごとに件数・自分が付けているか（reacted_by_me）・付けた人の現在の表示名一覧（user_names、ツールチップ用）に集約する）と`toggle_reaction(pool, message_id, user_id, emoji)`（既に自分がその絵文字で付けていれば削除、無ければ追加するトグル）を実装した。**`toggle_reaction`は同じトランザクションで`messages.updated_at`も更新する**（2026-09-04にAI応答の表示反映で踏んだのと同じ教訓——本文以外の更新でもupdated_atを進めないと、sinceベースの差分ポーリングで他の参加者の画面に反映されないため）。新規A-75（`POST /api/messages/{id}/reactions/toggle`、`routers/messages.py`）を追加し、権限はA-12削除・A-74中断と同じ「その会話の参加者であること」（adminは参加していないチャンネル/DMでもバイパス）だが、**投稿者本人限定にはしない**（誰の発言にも誰でもリアクションできるのがSlack等の一般的な挙動のため）。システム通知（F-43）へのリアクションも特別扱いしない（返信・削除は「参加・退出の記録の信頼性」を保つため対象外にしているが、リアクションはその記録自体を書き換えるものではないため対象外にする理由が無いと判断した）。`_message_out`（`channels.py`・`dms.py`・`messages.py`の3箇所、A-10/A-11/A-13/A-14/A-18/A-19/A-74の計7エンドポイント）に`reactions`フィールドを追加し、GET系のすべての呼び出し箇所で`fetch_reactions_grouped`を呼ぶよう更新した（新規投稿直後のレスポンスは常に空配列でよいため、POST系の呼び出しはクエリを追加していない）。S-05横断検索（A-20）は今回のユーザーの要望が会話画面（返信・削除ボタンの並び）を対象にしたものだったため対象外とした。
  - **フロントエンド**: `Composer.tsx`の絵文字入力ボタン用`EMOJI_LIST`を`export`し、リアクション機能でも同じ一覧を再利用する（2箇所で別々の絵文字セットを持つと選べる絵文字が画面によって違うという分かりにくさが生まれるため）。`MessageList.tsx`に新規`QUICK_REACTION_EMOJIS`（👍❤️😂🎉👀の5種、ユーザーの要望どおり「よく使いそうな絵文字何種類か」）と、それを描画する`ReactionQuickButtons`（クイックボタン＋絵文字ピッカーを開くボタン）・`EmojiGridPopover`（Composer.tsxの絵文字ピッカーと同じ見た目の8列グリッド、EMOJI_LIST全件）・`ReactionPills`（発言本文の下に表示する絵文字＋件数のピル、自分が付けている絵文字はアクセントカラーで強調しクリックで削除、ホバーで誰が付けたかのツールチップ）の3コンポーネントを追加してexportした。要望どおり「返信・削除ボタンの左隣にクイックリアクション、その横にピッカーを開くボタン」という配置にした（既存のホバー時アクションバー`absolute right-4 top-1 hidden group-hover:flex`を、投稿者本人・chadmin等に限定されない全参加者向けに拡張する形）。`ThreadPanel.tsx`の元発言ヘッダー（`MessageList`を経由せず個別に描画している箇所）にも同じ3コンポーネントをimportして同様に配置した。
  - **その場での反映（楽観的更新）**: `hooks/useMessages.ts`に新規`updateMessageReactions(messageId, reactions)`（`bumpThreadReplyCount`等と同じパターン）を追加し、`MessageList.tsx`の新規`onReactionToggled`プロパティ経由でChannelView/DmViewの`messages`一覧をトグルAPIのレスポンス（サーバーが実際に確定した最新のreactions配列）でその場更新する（3秒〜1秒のポーリングを待たない）。`hooks/useThread.ts`も同様の`updateReplyReactions`を追加し、返信一覧（ThreadPanel内のMessageList）に同じ仕組みを適用した。ThreadPanelの元発言ヘッダー自体（MessageListを経由しない独立した描画）は、ChannelView/DmView側の`messages`一覧を直接更新する手段がないため楽観的更新の対象外とし、次のポーリング（チャンネル/DMのメッセージ一覧は1秒間隔）で自然に反映されるのを待つ設計にした（数秒以内には反映される、割り切った設計判断）。
  - **実機検証で発見・修正したバグ（`hooks/useMessages.ts`・`hooks/useThread.ts`）**: Playwrightでの実機検証中、同じ発言に短い間隔で連続してリアクションを追加すると、2件目の反映が表示に現れない現象を観察した。原因を調査した結果、`useMessages.ts`のsinceベースの差分ポーリング（1秒間隔）が非同期（`await apiFetch`中に他の処理が進む）であるため、**楽観的更新（`updateMessageReactions`）がポーリングのフェッチ処理の実行中（awaitで待っている間）に割り込み、ポーリングが後から解決した際にそのポーリング自身が発行された時点での（楽観的更新より古い）スナップショットで上書きしてしまう**競合を理論上のリスクとして特定した。`updated_at`を比較し、ローカルの方が新しい場合は古いポーリングレスポンスでの上書きをスキップするようマージロジックを修正した。`useThread.ts`（スレッド返信一覧、こちらは差分取得ではなく毎回全件を取り直す設計）にも同じ種類の競合リスクがあるとみて、同じ`updated_at`比較によるマージへ作り替えた（従来は素の`apiFetch`をそのままSWRのfetcherに渡すだけで、状態追跡やマージを一切行っていなかった。返信一覧の楽観的更新`updateReplyReactions`もこの機会にuseThread.ts自身が持つ形にリファクタし、ThreadPanel.tsx側で直接SWRの`mutate`を操作していた従来のアドホックな実装を、useMessages.tsと同じ「フックが自身の状態整合性を持つ」設計に揃えた）。**なお、実際にPlaywrightで観察された現象の直接の再現条件は最終的にテスト側の検証待ち時間が短すぎたことだったと判明し（待ち時間を延ばすと問題なく正しく表示された）、この競合が実際に本番相当の環境で発生したことを直接確認できたわけではない**が、理論的に起こりうる不具合として特定できた以上、修正自体は残した（1秒間隔ポーリングという環境では発生確率が高まりうる、予防的な修正という位置づけ）。
  - **検証**: Docker Desktopを起動し、既存の`kogack-db-data`ボリュームを再利用する使い捨てPostgresコンテナ・DEV_AUTH有効なバックエンド・Viteフロントエンドをローカルで用意した。(1) curl相当の直接HTTPリクエストで、複数利用者間でのリアクション追加・同じ絵文字への複数人での加算・reacted_by_meが利用者ごとに正しく異なること・トグルでの削除・存在しない発言/空emoji/未認証の各エラーケース・非参加者からの404・システムadminのバイパス・チャンネル/DM/スレッド返信の3経路すべてでの動作・`updated_at`が実際に更新されポーリングに反映されることを確認した。(2) **DBスキーマ変更を伴うため、2026-09-09に確立した「初回起動→実データ投入→再起動」の2周期テストを実施し、2回目の起動でもエラーが起きないこと・既存のリアクションデータが保持されたまま新しいトグルも正常に動作することを確認した**（`message_reactions`は新規テーブルの`CREATE TABLE IF NOT EXISTS`のみで既存制約の差し替えは伴わないため本来リスクは低いが、念のため実施）。(3) Playwrightで実際のブラウザ操作を確認: クイックリアクションボタンでの追加→ピル表示、絵文字ピッカーを開いて任意の絵文字を追加→2件目のピルも正しく表示、ピルをクリックしてのトグル削除、スレッドパネルの元発言ヘッダー・返信一覧の両方でのリアクション、をスクリーンショット付きで確認した。テスト用に作成したメッセージ・DM・スレッド返信はすべてAPI経由で削除し、検証用コンテナ・Vite/バックエンドプロセスもすべて停止・削除した。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-10）。
- ポーリング間隔を緩和し、100人規模で使った場合のサーバー負荷を軽減した（ユーザーが第三者から受けた指摘「アプリが一定期間毎にAPI投げていそうなのが気になった。100人とかが使用するとそこそこ負荷がかかりそうな気もします」の共有を受けて対応）。現状のポーリング構成を洗い出して試算したところ、ログイン中は常にサイドバー4本（`/api/auth/me`・`/api/channels`・`/api/dms`・`/api/scheduled-messages`、いずれも3秒間隔）が動き、会話画面を開くとメッセージ一覧（1秒間隔、2026-09-04にユーザーの要望で短縮・本採用済み）・チャンネル参加者一覧（3秒間隔）が追加され、スレッドパネルも開くとさらに1本（3秒間隔）加わる構成だった。会話画面を開いている利用者1人あたり秒間約2.7回（スレッドまで開くと約3回）のリクエストが常時発生し、100人が同時にアクティブな場合バックエンドへ秒間250〜300リクエスト程度が継続的に流れる計算になることをユーザーへ報告した。**対応方針を3案（(1) 何もしない・様子見、(2) 間隔を少し伸ばす低リスクな軽微修正、(3) WebSocket/SSEへの本格的な置き換えを検討する、のいずれか）提示して確認したところ、(2)を選択された**。`hooks/usePolling.ts`（チャンネル一覧・DM一覧・チャンネル参加者一覧・送信予約一覧・スレッド返信一覧・S-08ドキュメント一覧が共通で使う既定値）の`refreshInterval`を3000ms→5000msへ、`hooks/useMessages.ts`（メッセージ一覧のみ個別に上書きしている値）を1000ms→2000msへ緩和した。**`hooks/useMe.ts`は意図的に対象外**とした——2026-09-03の実機検証で、ログイン・ログアウトのボタンを経由しないセッション切り替え（複数タブでの別アカウントログイン等）を確実に検知するには3秒間隔の定期再検証が必要（間隔を伸ばすと非公開チャンネルの内容が新しいユーザーの権限で一瞬でも開示されてしまうリークを防ぎきれなくなる可能性がある）と確認済みのセキュリティ関連の挙動のため、`usePolling`を経由せず独自に`refreshInterval:3000`を指定している既存の実装（変更対象外）をそのまま維持した。全てを完全に元の3秒へ戻すのではなく間を取った値にしたのは、2026-09-04時点でユーザーから「表示が遅い」という体感速度への要望があった経緯を踏まえ、応答性への影響を最小限に抑えるため。試算では、会話画面を開いている利用者1人あたりのリクエスト量が約2.7回/秒→約1.63回/秒（約39%減）になり、100人同時利用時で秒間約270〜300→約163リクエスト程度まで下がる見込み。実際の初期リリース規模（想定利用者200名、同時アクティブ100人は多めの見積もり）を踏まえ、まずはこの軽微修正を適用したうえで、実際の本番負荷を見てから追加対応（WebSocket化等）を検討する方針とした。フロントエンドのみの変更でバックエンドAPI・DBは無変更のため設計文書側の改訂は無い。`tsc -b`・`vite build`で検証（2026-09-10）。
- バグ修正: 絵文字リアクションの「絵文字を選んでリアクション」ボタンから開くピッカー（`EmojiGridPopover`）が、画面下寄りの発言で投稿欄（Composer）の裏に隠れて見えない不具合を修正した（ユーザーからの報告「メッセージのリアクションのスタンプを一覧で選択する画面がメッセージ入力欄の後ろに表示されるので見えない」）。原因はProfileCard（F-40、2026-08-28に同種の不具合を修正済み）と全く同じで、従来は発言行（`overflow-y-auto`スクロール領域の中）を基準に`absolute`配置していたため、会話ログのスクロール領域の下端でクリップされ、画面下寄りの発言では実質的に投稿欄の裏へ隠れて見えなくなっていた。**ProfileCard.tsxが確立済みの「`document.body`へ`createPortal`し、クリックした要素の`getBoundingClientRect()`を基準に`position: fixed`で配置、下に十分な余白が無ければ自動的に上開きに切り替える」パターンをそのまま踏襲した**。`MessageList.tsx`の`ReactionQuickButtons`（「もっと見る」ボタン）が`onTogglePicker`にボタン自身の座標を渡すよう変更し、`EmojiGridPopover`に`anchor: DOMRect`・`onClose`を新設、ProfileCardと同じ`document.addEventListener('mousedown', ...)`による外側クリックでの自動クローズも追加した（従来はクリック外での自動クローズが無く、選択するかボタンを再度押すまで開いたままだった点も副次的に改善）。`ThreadPanel.tsx`の元発言ヘッダー側の絵文字ピッカーも同じ`anchor`方式に揃えた。**実機検証で、この修正とは別にもう1件バグを発見・修正した**: `hooks/useMessages.ts`の`updateMessageReactions`（リアクショントグルの結果をその場で反映させる楽観的更新）を実装した際、フック側の関数は正しく用意していたにもかかわらず、**呼び出し元の`ChannelView.tsx`・`DmView.tsx`が`useMessages()`から`updateMessageReactions`を分割代入して`<MessageList onReactionToggled={...}>`へ渡す配線を漏らしていた**（ThreadPanel.tsx側の返信一覧は正しく配線済みだったため、コードレビュー時に見落とした）。この結果、絵文字ピッカーから絵文字を選択してもサーバー側には正しく保存される（トグルAPI自体は200で成功する）のに、画面上のピル表示にはその場で反映されず、次のポーリング（当時1秒、現在2秒）を待つまで一見「反映されていない」ように見える状態になっていた。実際にPlaywrightでクリックした絵文字がピルとして表示されるかを検証して初めて発覚した（サーバー側のレスポンスやDBの状態だけを見る検証では気づけない、フロントエンドの配線漏れ特有のバグ）。両ファイルに`updateMessageReactions`の分割代入と`onReactionToggled`プロパティを追加して解消した。あわせて、上開き/下開きを判定する`openUpward`のしきい値計算に、実際の配置で使う「+6px（発言行との間隔）」を含めていなかったことによる境界値のズレ（ギリギリ収まると判定されたケースで実際には6px分だけ画面下端をはみ出す）も実機検証で発見し、判定式に同じ余白を含めるよう修正した。Playwrightで、意図的に低めのビューポート（600px）で画面最下部の発言に対してピッカーを開き、(1) ピッカーが画面内に完全に収まること（修正前は3px分だけ下端をはみ出していたことも実測で確認済み）、(2) 投稿欄の領域と重ならず上開きに切り替わること、(3) ピッカー内の絵文字を実際にクリックでき、選択した絵文字がその場でピルとして表示されること（配線漏れバグの回帰確認）をスクリーンショット・座標の実測で確認した。テスト用メッセージ・検証用コンテナ・Vite/バックエンドプロセスはすべて削除・停止済み。フロントエンドのみの変更でバックエンドAPI・DBは無変更のため設計文書側の改訂は無い（2026-09-10）。
- ブラウザのデスクトップ通知（Web Notifications API）を追加した（ユーザーからの明示的な要望「通知機能を付けられますか？（JavaScriptの通知API？）」。事前にユーザーへ、①タブが開いている間だけ届く簡易版（Notification API、フロントのみ）と②ブラウザを閉じていても届くWeb Push（Service Worker＋VAPID＋購読情報のDB保存＋投稿時のプッシュ送信が必要な大きめの追加）の違いを説明し、①を選択された）。**Kogackにはプッシュ基盤が無く、リアルタイム配信自体は引き続きポーリング（WebSocket/Supabase Realtime/Pusher等への移行は2026-09-10にユーザーと相談のうえ「将来検討として保留、当面はポーリング継続」と決定済み）**のため、既存の`useChannels`/`useDms`のポーリングが返す`unread_count`の増分に相乗りして、Kogackのタブが非表示（他タブ・他アプリを見ている／最小化）のときだけOSの通知を出す方式にした。新規`hooks/useDesktopNotifications.ts`（`joined`・`dms`・`me.id`を受け取り、会話キーごとに直近の`unread_count`を`useRef`のMapで記憶。初回観測時はベースライン記録のみで通知しない＝ページ読み込み直後に既存の未読ぶんが一斉通知されるのを防ぐ。増分検知時、`document.visibilityState === 'visible'`なら未読バッジで十分として通知を出さない＝Slackの既定と同じ。4件以上の会話が一度に増えたらまとめて1通のダイジェストにする。アカウント切り替え（`App.tsx`の`pendingSwitch`ガード）で`me.id`が変わったらベースラインを作り直し、別ユーザーの既存未読を新着と誤検知しないようにする。通知クリックで`window.focus()`＋該当チャンネル/DMへ`navigate()`）を追加し、`Layout.tsx`から呼ぶ。ヘッダーの🔍の左に、許可が未設定（`Notification.permission === 'default'`）のときだけ🔔ボタン（クリックで`Notification.requestPermission()`）、ブロック済み（`'denied'`）のときは薄い🔕＋説明ツールチップを表示し、許可済みのときはボタン自体を隠す。**重要な副次修正**: SWRは既定でタブ非表示中はポーリングを停止する（`refreshWhenHidden: false`）ため、`useChannels`/`useDms`をそのままにすると「タブ非表示時こそ通知したいのに、そのとき新着検知のポーリングが止まっている」という矛盾が生じる（Playwright検証で実際に通知が飛ばず発覚）。`useChannels`/`useDms`に`refreshWhenHidden`を渡すようにし、その値は新規`desktopNotificationsEnabled()`（`Notification.permission === 'granted'`）で決める——通知を許可していない利用者のバックグラウンドタブには余計なポーリング負荷をかけない（2026-09-10のポーリング負荷軽減方針と両立させるため、全員一律で非表示時ポーリングを有効にはしない）。Docker上の使い捨てPostgres（`kogack-db-data`ボリューム再利用）＋DEV_AUTH有効なバックエンド＋Viteをローカルに立て、Playwrightで`window.Notification`をスタブし`document.visibilityState`を`'hidden'`に固定して検証: (1) 非表示タブ＋許可済みで別ユーザーが共有チャンネルへ投稿→約1ポーリング周期後に「新しいメッセージ／#雑談（未読N件）」の通知が生成される、(2) その通知のonclickで`/channels/1`へ遷移する、(3) 許可未設定のときヘッダーに🔔ボタンが正しいツールチップで表示される、(4) タブが可視のときは新着があっても通知を出さない、の4点を確認した。テストデータ・検証用コンテナ・プロセス・スクラッチファイルはすべて削除・停止済み。既知の限界（デュアルモニターでKogackが別画面に見えている場合も`visibilityState`は`'visible'`のため通知は出ない。タブ/ブラウザを完全に閉じている間は届かない＝①の仕様どおり）。要件定義書上のF-xxに対応付けられた変更ではない（F-31新着反映に対する運用上の補完）ため要件定義書・基本設計書・画面モックアップの改訂は無い。フロントエンドのみの変更でバックエンドAPI・DBは無変更。`tsc -b`・`vite build`で検証（2026-09-10）。
- 上記デスクトップ通知の直後、ユーザーから「メンションされたときと、所属チャンネルに新着が来ただけのときで、アプリの動きがほぼ変わらない」との指摘を受け、案1（サイドバーにメンション専用バッジ）＋案2（デスクトップ通知の「メンション・DMのみ」モード）を実装した。従来「メンションされた」ことに固有の表現は会話を開いたときの本文中`@氏名`の青ハイライトだけで、サイドバーの未読バッジ・チャンネル名太字・デスクトップ通知はいずれも「自分以外の全新着」を等しく扱っていた（＝賑やかなチャンネルにいると通知が鳴りっぱなしになり、肝心の名指しが埋もれる）。
  - **バックエンド（`routers/channels.py` A-05のみ）**: `joined`一覧の各チャンネルに`unread_mention_count`を追加。`unread_count`と全く同じ期間条件（`msg.created_at > COALESCE(rs.last_read_at, cm.joined_at)`・自分以外の発言・スレッド返信除外）に、`message_blocks`（T-07、`block_type='mention'`・`payload->>'target_user_id' = $1::text`）のJOINを重ねた件数。AIメンションは`message_blocks`に入らない（基本設計書5.22節の設計判断）ため自然に対象外になる＝正しい。既読化（A-69）で`last_read_at`が進めば`unread_count`と同時に0へ戻ることも確認済み。DM（A-16）には追加しない——DMは1件でも「自分宛て」であり赤バッジで区別する意味が薄いため（通知側もDMはmodeに関わらず常に通知対象）。
  - **サイドバー（`Layout.tsx`）**: チャンネル行のバッジを「`unread_mention_count > 0` なら赤（`bg-danger-text`、既存トークン流用、新規CSS変数なし）の`@n`バッジ、そうでなく`unread_count > 0` なら従来どおりグレーの件数バッジ」に変更。メンションがあるチャンネルは件数の代わりに`@n`を出す（名指しの存在を最優先で示す。Slackの赤メンションバッジと同じ考え方。他に何件未読があるかは開けば分かる、と割り切り）。`Channel`型に`unread_mention_count?: number`を追加。
  - **デスクトップ通知（`hooks/useDesktopNotifications.ts`）**: `mode: 'all' | 'mentions'`（既定`'all'`、`localStorage`キー`kogack_notif_mode`に永続。スレッド幅と同じ「個人のブラウザ内の調整でサーバー同期不要」の判断）を追加。会話キーごとの記憶を`{count, mentions}`に拡張し、`mentions`が増えた会話は**件数に関わらず常に個別通知**（「あなたへのメンション／#チャンネル名」、`tag`を`kogack-m-*`にして一般新着と分離）、`count`だけ増えた会話は`isDm || mode === 'all'`のときのみ通知（一般新着は従来どおり4件以上でダイジェスト集約）。`refreshWhenHidden`のゲート（`desktopNotificationsEnabled()` = `permission === 'granted'`）は変更なし——`'mentions'`モードでもメンション増分の検知にバックグラウンドポーリングは必要なため。
  - **通知設定UI（新規`components/NotificationSettingsButton.tsx`）**: ヘッダーの🔔を、従来「許可済みなら非表示」だったのを常時表示に変更し、クリックで小さなポップオーバーを開く（許可未設定→「通知を有効にする」ボタン／ブロック済み→ブラウザ設定を促す説明／許可済み→「すべての新着」「メンション・DMのみ」のラジオ2択）。ポップオーバーはヘッダー（`overflow`クリップの外・画面左端固定のサイドバー内）にあるためポータルは使わず、ボタン基準の`absolute`だと幅240pxが左にはみ出るので画面左端基準の`fixed left-2 top-14 w-[244px]`で配置（実機検証で最初`right-0`にして左が見切れ、`fixed`へ修正）。外側クリックで閉じる。
  - **検証**: Docker上の使い捨てPostgres（`kogack-db-data`再利用）＋DEV_AUTHバックエンド＋Viteで、(1) curl相当の直接リクエストで、一般発言では`unread_mention_count`が増えず`@yamada`メンション発言でのみ増えること・既読化で両方0に戻ることを確認、(2) Playwrightで`window.Notification`スタブ＋`visibilityState`固定により、サイドバーに赤い`@1`バッジ（`title="あなたへのメンション 1 件"`、他に一般未読2件があっても`@1`表示）が出ること、`mode='mentions'`では一般発言で通知が飛ばず`@yamada`メンションで「あなたへのメンション」通知が飛ぶこと、通知設定ポップオーバーが許可未設定時は「通知を有効にする」ボタン・許可済み時はラジオ2択を表示し「メンション・DMのみ」選択が`localStorage`へ保存されることを、スクリーンショット込みで確認した。テストデータ・検証用コンテナ・プロセス・スクラッチファイルはすべて削除・停止済み。`unread_mention_count`はA-05レスポンスへのフィールド追加のみ（新規API・DBスキーマ変更なし、`message_blocks`は既存）のため、詳細設計書API設計はbackfill候補だが今回は未改訂（次にAPI設計を触るコミットでまとめる）。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-10）。
- Slackの`@channel`相当（チャンネル全員への通知）を実装した（ユーザーからの明示的な要望「チャンネルにおける@channelの全体通知ができるようにしてほしい」）。直前のメンション区別（サイドバー赤バッジ＋通知の「メンション・DMのみ」モード）の仕組みへそのまま乗せる形で、`@channel`を「そのチャンネルの参加者全員が個人宛てメンションされたのと同じ扱い」にする。
  - **表現方法**: AIメンション（基本設計書5.22節「AIメンションはID参照化しない」）と同じ考え方で、`@channel`も個別の`target_user_id`を持たない。`message_blocks`（T-07）に`block_type='mention'`・`payload={"kind":"channel"}`の行を1件だけ保存する（新規テーブル・新規block_type・CHECK制約変更は不要、payloadはJSONBで自由形式のため）。`@here`は在席・オンライン状態を追跡する仕組みがKogackに無いため対象外（`@channel`のみ）。利用可否はチャンネル参加者なら誰でも（Slackの既定と同じ。乱用が問題になればchadmin限定への制限を後から足せる）。スレッド返信・DMには`@channel`候補を出さない（A-05の`unread_mention_count`が`thread_parent_id IS NULL`のみ対象＝スレッド返信の`@channel`は通知に繋がらず中途半端になるため。DMは`insert_mention_blocks`が`channel_id is None`のとき`kind='channel'`を黙って捨てる）。
  - **バックエンド**: `mentions.py`の`MentionInput`に`kind: str = 'user'`を追加（デフォルト値付きのため既存の`kind`なしペイロードもそのまま通る）。`insert_mention_blocks`を、`channel_id`があり`kind='channel'`のエントリが1件でもあれば`{"kind":"channel"}`ブロックを1件だけ先頭に入れる→残りの個人宛てメンションは従来どおり参加者検証、という形に再構成。`routers/channels.py`のA-05 `unread_mention_count`サブクエリのJOIN条件を`mb.payload->>'target_user_id' = $1::text OR mb.payload->>'kind' = 'channel'`に拡張し、1発言に個人宛て＋`@channel`が両方あっても二重計上しないよう`count(*)`→`count(DISTINCT msg.id)`に変更。既読化（A-69）で`last_read_at`が進めば`@channel`ぶんも`unread_count`と同時に0へ戻る（同じ期間条件のため）。
  - **フロントエンド**: `MentionPayload`型に`kind?: 'user' | 'channel'`、`Composer.MentionCandidate`に`isChannel?: boolean`を追加。`ChannelView.tsx`のメンション候補一覧の先頭に合成候補`{id:'channel', name:'channel', isChannel:true}`を常に追加（次にAI、その後に参加者）。`Composer.selectCandidate`で`isChannel`のときは本文へ`@channel `を挿入し`mentions`state へ`{target_user_id:'channel', display_name_snapshot:'channel', kind:'channel'}`を1件だけ（重複選択しても増やさない）追加。候補一覧では`@`の赤い角丸アイコン＋「チャンネル全員に通知」のヒントを表示。`MessageList.renderMessageBody`のメンションブロック解釈に「`payload.kind==='channel'`なら`needle/label`とも`@channel`」の分岐を追加（個人宛ては`display_name_snapshot`を使うが`@channel`はそれを持たないため）。既存の`renderInlineSegment`（`bg-accent-100`の青ハイライト）・`Composer.findMentionHighlights`（入力中のハイライト）はいずれも`mentions`/needle経由で`@channel`を自動的に拾う。
  - **検証**: Docker上の使い捨てPostgres（`kogack-db-data`再利用）＋DEV_AUTHバックエンド＋Viteで、(1) curlで`@channel`発言を投稿→`blocks`に`{block_type:'mention', payload:{kind:'channel'}}`が付くこと、投稿者以外の参加者（yamada、未読）の`unread_mention_count`が+1・投稿者本人は+0、1発言に`@channel`と`@yamada`個人宛てを両方入れても`DISTINCT`で+1（+2にならない）、既読化で両方0、を確認。(2) Playwrightで、Composerの`@`ピッカーに「@channel／チャンネル全員に通知」が候補として出ること、選択で`@channel `が挿入され送信ペイロードの`mentions`に`kind:'channel'`が入ること、投稿された発言内で`@channel`が青ハイライト（`span.bg-accent-100`）されること、受信側（yamada）で`mode='mentions'`（メンション・DMのみ通知）でもサイドバーに赤`@1`バッジが出てデスクトップ通知が飛ぶことを、スクリーンショット込みで確認した。テストデータ・検証用コンテナ・プロセス・スクラッチファイルはすべて削除・停止済み。`@channel`は要件定義書のF-xxに対応付けられた機能ではない（F-41メンションの拡張という位置づけ）ため要件定義書・基本設計書・画面モックアップの改訂は無い（詳細設計書API設計のA-05/A-11/T-07まわりはbackfill候補だが、`unread_mention_count`と同様に次にAPI設計を触るコミットでまとめる）（2026-09-10）。
- UI改善: 発言ホバー時のアクションバー（クイックリアクション絵文字＋絵文字ピッカーボタン＋返信・削除ボタン）を1枚の枠（枠線＋白背景＋影＋角丸）で囲んだ（ユーザーからの報告「スレッドの返信に絵文字リアクションしようとすると、メッセージの時刻表示と絵文字候補がかぶる。名前や時刻の上にかぶるのは構わないので、枠で囲って上にかぶっても違和感ないデザインにしてほしい」）。従来は`ReactionQuickButtons`のクイック絵文字ボタンが枠も背景も持たない裸のボタンで、返信・削除ボタンだけが個別に`border+bg+shadow`のピル形状という不揃いな見た目で、特に横幅の狭いS-04スレッドパネルの返信一覧では時刻・発言者名の直上に文字が重なって読めなくなっていた。`MessageList.tsx`のホバーバー`div`（`absolute right-4 top-1`）を`rounded-md border border-line bg-surface px-1 py-0.5 shadow-sm`のコンテナに変更（`top-1`→`top-0`、`gap-1`→`gap-0.5`）し、内側の返信・削除ボタンからは個別の`border/bg/shadow`を外して`hover:bg-surface-muted`のプレーンなボタンに統一、リアクション群と返信・削除群の間に細い区切り線（`h-4 w-px bg-line`）を入れた。`ThreadPanel.tsx`の元発言ヘッダー側のホバーバー（リアクションのみ、返信・削除は無い）も同じ枠に揃えた。白背景で下の時刻・名前を完全に覆うため、重なっても浮遊ツールバーとして自然に見える（Slackのメッセージホバーツールバーと同じ考え方）。Docker上の使い捨てPostgres＋DEV_AUTHバックエンド＋Viteで、Playwrightで実際にスレッド返信をホバーし、バーが`border:1px solid`・`background:白`・`box-shadow`あり・`border-radius:6px`の1枚の箱になっていること、5個のクイック絵文字＋ピッカー＋削除が同じ箱に収まること、メイン会話ログ側でも同じ枠が出て回帰が無いことをスクリーンショットで確認した。テストデータ・検証用コンテナ・プロセスはすべて削除・停止済み。フロントエンドのみのCSS変更でバックエンドAPI・DBは無変更のため設計文書側の改訂は無い（2026-09-10）。
- 既にスレッドがある発言でもホバー時の「返信」ボタンを表示するようにした（ユーザーからの明示的な要望）。従来は`MessageList.tsx`の`showReplyButton`が`!(m.thread_reply_count ?? 0)`を条件に含んでおり、返信が1件でも付くとホバーの「返信」ボタンが消えて発言下の「💬 N件の返信」導線のみになっていた。この条件を外し、システム通知以外・`onOpenThread`が渡されている発言では常に「返信」ボタンを出す（クリック時の`onOpenThread(m.id)`は既存のまま＝既存スレッドでもそのスレッドが開く。「💬 N件の返信」導線は件数・表示中状態も示すため併存させる）。`ThreadPanel.tsx`の返信一覧側の`MessageList`は`onOpenThread`を渡していないため、返信に対して「返信」ボタンが出る（入れ子スレッド化）ことは無い。Playwrightで、返信済みの発言をホバー→「返信」ボタンが出る・「💜 1件の返信」導線と併存する・クリックで既存返信の入ったスレッドが開くことを確認した。フロントエンドのみの変更でバックエンドAPI・DBは無変更のため設計文書側の改訂は無い（2026-09-10）。
- UI全体の表示倍率（文字サイズ）設定を追加した（ユーザーからの要望「設定画面か何かで文字の大きさを変更できる機能」）。着手前に案A（UI全体をズーム）・案B（会話メッセージの文字だけ拡大縮小）を提示し案Aを選択された。Kogackはフォントサイズを`text-[12.5px]`のようにpx直書きしている箇所が435箇所あり（`grep`で実測）remベース化して`html`のfont-sizeで拡大する王道の方法は現実的でないため、`document.documentElement`の`zoom`プロパティ（ブラウザのズームと同じ視覚拡大効果）で一括拡大する方式にした。新規`lib/uiZoom.ts`（4段階: 小0.9／標準1／大1.1／特大1.25、`localStorage`キー`kogack_ui_zoom`に保存。スレッド幅・通知モードと同じ「個人のブラウザ内設定でサーバー同期不要」の判断）・新規`hooks/useUiZoom.ts`を追加し、`Layout.tsx`のサイドバー footer（プロフィール表示の上）に「文字サイズ」の4段階セグメントボタンを常設した（新規ポップオーバーは作らず、常時見えるコンパクトな行にした）。**`zoom`は要素とその子孫の描画寸法を一律スケールするが、`vh`/`vw`はビューポート基準のまま変わらないため、`h-screen`（100vh）等のビューポート依存サイズを持つ要素は zoom 適用後に画面をはみ出し縦横スクロールが出る**ことが実装中に判明し、`Layout.tsx`のルート（`h-screen`→`h-[calc(100vh/var(--ui-zoom))] w-[calc(100vw/var(--ui-zoom))]`）・`Login.tsx`のルート（`min-h-screen`→`min-h-[calc(100vh/var(--ui-zoom))] w-[calc(100vw/var(--ui-zoom))]`）の2箇所で「zoomで割った値を指定してzoom適用後にちょうど100vh/100vwへ戻す」補正を行った（`index.css`の`:root`に`--ui-zoom: 1`の既定値を追加し、`--ui-zoom`が未設定でも`calc()`が必ず解決できるようにした）。**初回描画のちらつき防止**のため、React未マウントの`frontend/index.html`の`<head>`に、`localStorage`を読んで`documentElement.style.zoom`を即座に適用する小さなインラインスクリプトを追加した（`lib/uiZoom.ts`と同じロジックのミニ版で、ロジックが2箇所に分散する点はコード内コメントで明記）。Docker上の使い捨てPostgres・DEV_AUTHバックエンド・実際に稼働中だったローカルVite開発サーバー（ユーザー自身が`start.bat`で起動していたものを再利用、新規サーバーは検証後すぐ停止）を使い、Playwrightで(1) 小/標準/大/特大の4段階すべてで`document.documentElement.scrollWidth/scrollHeight`が`clientWidth/clientHeight`と一致し縦横スクロールが一切発生しないこと、(2) 各段階でサイドバーfooter（文字サイズ操作・ログアウトボタン）が画面内に収まり続けること、(3) `document.body`へ`createPortal`する既存の浮遊UI（ProfileCard、F-40・`position:fixed`＋`getBoundingClientRect()`アンカー方式）が特大（125%）でも正しい位置に完全に画面内へ収まって表示されること（zoom環境下でも`getBoundingClientRect`の値と`position:fixed`の配置が整合することを実機で確認）、(4) リロード後もzoom設定が保持されること、をスクリーンショット込みで確認した。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（フロントエンドのみの変更でバックエンドAPI・DBは無変更）（2026-09-11）。
- Slackの`@here`相当（送信時点でアクティブだった参加者への通知）を実装した（ユーザーからの明示的な要望「@channelは付けてくれたと思いますが、@hereの機能も付けられますか？」）。`@channel`実装時に「在席・オンライン状態を追跡する仕組みがKogackに無いため対象外」としていた制約を解消するため、まず在席判定の仕組みを新設し、その上に`@here`を載せた。着手前に実現方式（在席判定の仕組み・アクティブ判定の有効期間）を提示し、既定案（既存ポーリングへの相乗り・30秒）で合意を得てから実装した。
  - **在席判定の新設**: 新規`users.last_seen_at`（TIMESTAMPTZ）を追加。新規WebSocket等は導入せず、既存の`/api/auth/me`（A-04）への3秒間隔ポーリング（`hooks/useMe.ts`、ログイン中は常時動いておりタブが非表示になると自動的に止まる設計）に相乗りし、呼ばれるたびに`last_seen_at`を更新する（書き込み頻度を抑えるため、前回の記録から20秒以上経っているときだけUPDATEする条件付き）。「ポーリングが続いている＝実際にそのタブを開いて見ている」という既存の性質をそのまま在席判定に転用した形で、新規のハートビート機構は作っていない。
  - **`@here`の表現方法**: `@channel`（`message_blocks`に`payload={"kind":"channel"}`を1件保存、参加者全員に一律適用）とは異なり、`@here`は「誰が対象か」が送信時点のスナップショットで決まる。`mentions.py`の`insert_mention_blocks`に、`channel_members`と`users`をJOINして`last_seen_at`が`HERE_ACTIVE_WINDOW_SECONDS`（30秒、ユーザーと合意した既定値）以内・`is_active=true`・送信者本人を除く参加者を送信時点で特定し、`payload={"kind":"here","user_ids":[...]}`として1件保存する処理を追加した（新規`sender_user_id`引数を追加し、3つの呼び出し元＝A-11 channels.py・A-14 messages.py・A-19 dms.pyすべてから渡す。dms.pyは`channel_id`が常にNoneのため@here自体は従来どおり黙って無視される）。個々のuser_idを特定してしまえば、以降の扱いは通常の個人宛てメンションと同じ枠組みに乗る。
  - **A-05 `unread_mention_count`**: JOIN条件に`mb.payload->'user_ids' ? $1::text`（jsonb配列に自分のidが含まれるか）を追加。既存の個人宛て・`@channel`条件とOR結合し、`count(DISTINCT msg.id)`のままで二重計上も防げる（変更なし）。これにより`@here`もサイドバーの赤バッジ・デスクトップ通知の「メンション・DMのみ」モードにそのまま乗る（追加のフロント側配線は不要）。
  - **フロントエンド**: `Composer.MentionCandidate`に`isHere`を追加し、候補一覧で`@channel`の直後に「@here／今アクティブな人に通知」を表示（`@channel`の赤`@`アイコンと区別するため緑＝`bg-ok-text`の`@`アイコンにした）。選択時は`kind:'here'`のメンションエントリを1件（重複選択しても増えない）追加し本文に「@here」を挿入する。`ChannelView.tsx`のメンション候補（`@channel`の次）に追加、スレッド返信・DMには出さない（`@channel`と同じ理由：スレッド返信は`unread_mention_count`の対象外のため通知に繋がらない）。`MessageList.renderMessageBody`のメンションブロック解釈に`payload.kind==='here'`→`needle/label`とも`@here`の分岐を追加（`@channel`と同型）。
  - **検証**: ユーザーが`start.bat`で実際に稼働させていたローカル開発環境（`--reload`により編集内容が自動反映済み）に対し、(1) curlで、A-04を呼んでいない（＝在席判定上「非アクティブ」な）利用者は`@here`のブロックに含まれず`unread_mention_count`も増えないこと、A-04を呼んで直後に投稿された`@here`ではその利用者が正しく`user_ids`に含まれ`unread_mention_count`が+1になること、送信者自身は除外されること、既読化で0に戻ることを実際のAPI応答（`blocks`の中身）で直接確認した。(2) Playwrightで、Composerの`@`ピッカーに`@channel`の直後に「@here／今アクティブな人に通知」が出ること、選択で`@here `が挿入され送信ペイロードに`kind:'here'`が入ること、投稿された発言内で`@here`が青ハイライトされることを確認した。テストで作成した発言はすべてAPI経由で削除済み。`@here`は要件定義書のF-xxに対応付けられた機能ではない（`@channel`と同じくF-41メンションの拡張という位置づけ）ため要件定義書・基本設計書・画面モックアップの改訂は無い（詳細設計書側はbackfill候補として次にAPI設計を触るコミットでまとめる、`@channel`実装時の方針を踏襲）（2026-09-11）。
- デスクトップ通知②（Web Push、タブ・ブラウザを閉じていても届く方式）を実装した（ユーザーからの明示的な要望「これパソコンは開いているけど、チャットアプリを閉じてるときに新着メッセージが来ても通知は来ないですか？」「②を実装するのに必要なことは？」を経て、着手前に提示した実現案（新規テーブル・新規API3〜4本・新規ライブラリ・Service Worker・投稿系API3箇所への配線という規模、および「すべて／メンション・DMのみ」の切替をサーバー側設定へ移す必要があること）に同意を得てから着手）。①（Web Notifications API、タブが開いている間のみ）に続く追加で、①は削除・置換せず併存させる（①はどのブラウザでも即座に機能する簡易版、②は対応ブラウザ・ネットワーク到達性に依存するがタブ・ブラウザを閉じていても届く）。
  - **VAPID鍵**: `pywebpush`（新規依存、requirements.txt）に同梱される`py_vapid`で生成した鍵ペアを環境変数（`VAPID_PRIVATE_KEY`・`VAPID_PUBLIC_KEY`・`VAPID_SUBJECT`）として保持する。PEM形式ではなく**生の32バイト（秘密鍵）/65バイト（公開鍵、非圧縮点）をbase64url単一行文字列にした形式**を採用した——`py_vapid.Vapid.from_string()`がRAW/DERを自動判別して読めることを実機で確認済み（`.env`・Fly secretsに1行で収まり、PEMの改行を気にする必要がない）。生成手順は`.env.example`にワンライナーで明記した。ローカル`.env`にも動作確認用の鍵ペアを生成・設定済み（本番用は別途生成しFly secretsへ設定する必要があり、このコミットには含まれない）。
  - **設定の置き場所（他モジュールとの整合性の見落としを実機で発見・修正）**: 当初`push_sender.py`で`os.environ.get("VAPID_PRIVATE_KEY")`と素朴に書いたところ、`is_configured()`が常にFalseになる不具合を実機検証中に発見した。原因はKogackの`.env`読み込みが`database.py`の`ROOT_ENV`辞書へのロードのみで`os.environ`自体は書き換えない設計のため（`ai_client.py`・`google_auth.py`・`storage.py`が採用している`os.environ.get(key) or ROOT_ENV.get(key)`という共通パターンに倣う必要があった）。`push_sender.py`にも同じ`_env()`ヘルパーを追加して解消した。
  - **データモデル**: `users.notif_mode`（'all'/'mentions'、既定'all'）を新設——**従来①はこの設定を`localStorage`のみで管理していたが、②はサーバー自身が「誰に送るか」を判定する必要があるため、サーバー側（DB）を正とする設定に変更した**（着手前にユーザーへ確認し合意済みの変更）。新規T-27 `push_subscriptions`（user_id・endpoint・p256dh・auth、UNIQUE(user_id, endpoint)で同一端末の重複購読のみ防ぎ、1人が複数端末で購読できるようにした）。
  - **バックエンド**: 新規`services/push_sender.py`が実際の送信を担う。`notify_channel_message`（A-11投稿後）は`mentions.mention_summary()`（新規、blocksから「@channelが含まれるか」「個人宛て・@hereで特定されたuser_id集合」を取り出す共通ヘルパー）で判定した「この発言は自分宛てか」を使い、`notif_mode='mentions'`の利用者には自分宛てのときだけ送る（①のクライアント側ロジックと同じ判断基準をサーバー側に再現）。`notify_dm_message`（A-19投稿後）はDMなので①と同じくnotif_modeに関わらず常に送る。**スレッド返信（A-14）は対象外**（①がunread_count/unread_mention_countの増分＝`thread_parent_id IS NULL`のみ集計するA-05と同じスコープに揃えた。着手前の提案では「投稿系API3箇所」としていたが、実装時にこの整合性を優先し2箇所に絞った）。いずれも投稿処理の完了後に`asyncio.create_task()`でfire-and-forget起動し、投稿APIの応答を待たせない（AIメンション応答・自動応答トリガーと同じ非同期パターン）。送信失敗（無効化された購読の404/410、その他の例外）は`push_sender.py`内で握りつぶし購読を自動削除するのみで、投稿API自体には一切影響しない設計とし、実際にでたらめな（暗号化キーとして不正な）購読情報に対して送信を試みても投稿APIが201を返し続けサーバープロセスも生存し続けることを実機で確認した。新規`routers/push.py`（`GET /api/push/public-key`・`POST /api/push/subscribe`・`POST /api/push/unsubscribe`）、A-62（`PUT /api/users/me`）に`notif_mode`を追加、A-04（`GET /api/auth/me`）のレスポンスにも`notif_mode`を追加。
  - **フロントエンド**: 新規`public/sw.js`（Service Worker。`push`イベントでJSONペイロードを`showNotification()`、`notificationclick`で既存タブへのフォーカス＋遷移／無ければ新規タブを開く）。新規`hooks/usePushSubscription.ts`が①の許可（`Notification.permission==='granted'`）が下りたタイミングでService Worker登録→（未購読なら）VAPID公開鍵で購読→購読情報をサーバーへPOST、という一連を行う（失敗しても①は影響を受けない設計）。`hooks/useDesktopNotifications.ts`の`mode`管理を`localStorage`ベースから`me.notif_mode`（サーバー）ベースへ変更し、`setMode`は`PUT /api/users/me`で永続化する。`NotificationSettingsButton.tsx`の説明文を、許可すると②（対応ブラウザでの closed-browser 通知）も有効になる旨がわかるように更新した。
  - **実機検証で判明した重要な注意点（`pushManager.subscribe()`の所要時間）**: 実際のGoogleのプッシュ配送基盤（Chromeの場合FCM）へ登録する実ネットワーク通信のため、**数秒〜十数秒かかることがある**（実測で約15秒かかったケースを確認）。当初Playwrightで5秒しか待たずに「購読が完了しない」ように見え不具合を疑ったが、20秒待つと正しく完了することを確認し、コードの不具合ではなく単純な所要時間だったと判明した（`usePushSubscription.ts`のコメントに記録）。
  - **検証**: (1) DBスキーマ変更（`users.notif_mode`のCHECK制約追加ADD COLUMN・新規T-27テーブル）を「初回起動→実データ投入→再起動」の2周期テストで確認（2026-09-09の教訓どおり）。(2) curlでA-04/A-62のnotif_mode読み書き・422バリデーション、`/api/push/public-key`の公開鍵配布、`/api/push/subscribe`の登録・不正な購読情報での422、を確認。(3) `mention_summary`を含む送信対象判定ロジックを、実際の`push_sender.notify_channel_message`をDB接続込みで直接呼び出し（送信自体は関数差し替えでキャプチャ）検証: 通常発言ではmentionsモードの利用者は対象外・allモードの利用者は対象、個人宛てメンションではmentionsモードの利用者もそのメッセージに限り対象（タイトルが「あなたへのメンション」になる）、`@channel`ではmentions/allどちらの利用者も対象になり両方とも「あなたへのメンション」タイトルになること、の3パターンいずれも期待どおりだった。(4) **Playwrightで実際のブラウザ・実際のGoogleのプッシュ配送基盤を使った本物のエンドツーエンド購読を確認**（非シークレットプロファイルが必要——Chromeは仕組み上シークレットモードでPush APIを使わせない仕様のため、`launchPersistentContext`で通常プロファイルを使った）: Service Worker登録→`serviceWorker.ready`→`pushManager.subscribe()`→実際のFCMエンドポイント（`https://jmt17.google.com/fcm/send/...`）を取得→`POST /api/push/subscribe`が201で購読情報を保存、という一連が実際に動作することを確認した。(5) Service Workerの`push`イベントハンドラ自体も、Playwrightの`ServiceWorker.evaluate()`で本物の`PushEvent`を合成的にディスパッチして検証し、`showNotification()`が正しいtitle/body/tag/urlで呼ばれることを確認した。**実際にブラウザ・タブを閉じた状態でOS通知が表示されるところまでは、この検証環境（自動化されたサンドボックス環境）では観察できなかった**（inbound方向のプッシュ配送＝Google側からこの環境内のブラウザへ実際に届くところまでは検証の性質上確認できない範囲。それ以外の全構成要素——購読・保存・送信ロジック・受信ハンドラ——は実際のコード・実際のネットワーク往復で確認済み）。テストデータ（購読情報・投稿・notif_mode変更）はすべて削除済み。要件定義書上のF-xxに対応付けられた変更ではない（F-31新着反映に対する運用上の補完、①と同じ位置づけ）ため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-11）。
- デスクトップ通知に「オフ」オプションを追加した（ユーザーからの質問「通知を許可してから、一切の通知を許可しないという変更をすることもできる？」に対し、ブラウザの通知許可自体はJavaScriptから取り消せない仕様であること・Kogack内には「オフ」の選択肢が無かったことを説明した上で、「通知をオフにするオプションを追加しましょう。そしてデフォルトでは、今の状態と同じように、通知を許可するまでは一切の通知が届かない状態にする仕様を維持しましょう」という要望を受けて実装した）。
  - `notif_mode`（`users`テーブル、A-04/A-62で読み書き）に`'off'`を追加し、`'all' | 'mentions' | 'off'`の3値にした。**既定値は変更していない**（引き続き`'all'`のまま）——「通知を許可するまでは何のnotif_modeでも通知が一切飛ばない」という既存の前提（`Notification.permission`が`'granted'`になるまでは①②とも動かない）は変わらないため、既定を`'off'`にする必要はないという設計判断（ユーザーの要望どおり）。
  - **DBスキーマ**: `users_notif_mode_check`のCHECK制約を広げる際、2026-09-09に確立した「既存の制約名を明示的にDROPしてから同名でADDし直す」パターンを踏襲した（旧ADD文は残さない。実際に運用中のDBで制約名`users_notif_mode_check`（Postgresの既定命名規則どおり）を直接確認してから対応した）。
  - **バックエンド**: `services/push_sender.py`の`notify_channel_message`・`notify_dm_message`の両方に`notif_mode == 'off'`のスキップを追加した。**DMは元々notif_modeを見ずに常に通知していたが、'off'のときはDMも含め一切送らないよう変更した**——「オフ」は文字どおり全面的な無効化であるべきという判断（DMだけ特別扱いで漏れ出ると「オフにしたのに来る」という不整合になるため）。A-62のバリデーションも`'off'`を許容するよう更新。
  - **フロントエンド**: `NotificationSettingsButton.tsx`の許可済みパネルに3つ目のラジオ「オフ／通知を受け取らない」を追加。`permission==='granted'`でも`mode==='off'`のときはベルアイコンを🔕（ブロック時と同じ見た目）にし、ひと目で「今は届かない」状態と分かるようにした。`useDesktopNotifications.ts`の①判定ロジックに`mode==='off'`のスキップを追加——**ただし会話ごとの既読件数の記録（`seenRef`によるベースライン更新）自体は`'off'`中も継続する**設計にした。記録を止めてしまうと、後で`'all'`/`'mentions'`に戻したときに「オフだった間にあった未読」が一斉に「新着」として通知される（既存の「ページ読み込み直後の一斉通知を防ぐ」仕組みと同じ理由でここでも避けたかった副作用）。
  - **検証**: (1) DBスキーマ変更（CHECK制約の入れ替え）を3回連続の起動サイクルで確認。(2) curlでA-62が`'off'`を受理し永続化されることを確認。(3) `push_sender`の送信対象判定を実際のDB接続込みで直接検証: `notif_mode='off'`の利用者は、たとえ個人宛てに`@メンション`されていても通知対象から外れること（オフがメンションより優先されること）、DM宛てのメッセージでも`'off'`の利用者には一切送られないこと（従来の「DMは常時通知」ルールより優先されること）を確認した。(4) Playwrightで、ポップオーバーに「オフ」が表示されること、選択するとベルアイコンが🔕に変わり`title`が「デスクトップ通知はオフになっています」になること、`me.notif_mode`がサーバー側で`'off'`に永続化されること、`'off'`中に新着が来ても①のブラウザ通知が一切発火しないこと（`window.Notification`スタブで検証）を確認した。テストデータはすべて削除済み。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-11）。
- バグ修正: チャンネルAIに参照ドキュメントの内容について質問すると、「search_documentsを実行しています。少々お待ちください。」のような予告の文章だけが表示され、実際の回答が一切返らない不具合を修正した（ユーザーからの報告）。原因調査のため、実際のOpenAI API（`gpt-4.1-nano`）へ、本番と同じ`_build_system_prompt`・`SEARCH_DOCUMENTS_TOOL`定義を使った実機再現テストを行い、**モデルがsearch_documents関数を実際には呼び出さず（`tool_calls`が空のまま）、関数を呼び出す旨をテキストの本文として書いて応答を終えてしまうことがある**という、Function Calling APIの`tool_choice`既定値（`"auto"`）に起因するモデル側の挙動を実機で再現・特定した（カジュアルな言い回しの質問・複数の情報を一度に尋ねる質問で高頻度に発生。実測で8問中3〜4問がこの失敗になった）。`_run_chat_with_tools`のプロンプト（`_build_doc_scope_section`）に「予告の文章だけで終えず必ず関数を呼び出すこと」という指示を追加しただけでは改善しなかった（8問中6問が同じ失敗のまま、実機で確認済み）ため、**コード側の対処**として、1ラウンド目（`round_num == 0`）のみ`tool_choice="required"`を指定し、ツールが提示されている場合はモデルに必ずsearch_documentsを呼び出させるよう`services/ai_agent.py`の`_run_chat_with_tools`を修正した（2ラウンド目以降は従来どおり`"auto"`のままとし、既に検索済みで十分な情報がある場合にまで強制はしない）。この対処により、ドキュメント参照範囲を持つチャンネルでは雑談等ドキュメントと無関係な発言でも1回だけ余分にsearch_documentsが呼ばれる副作用があるが、実機検証では最終的な回答の質・自然さに悪影響は見られなかった。修正後、実際のOpenAI APIに対して同条件（内容が見つかる質問・見つからない質問・複数質問を同時に尋ねるケース・雑談）で実際に出荷されるコード（`ai_agent._run_chat_with_tools`関数そのもの、`doc_search.search`のみモック）を直接呼び出して検証したところ、6問中6問すべてで実際に検索してから正しく回答する（引用も正しく`citations`に反映される、見つからない場合は正直に「見つかりませんでした」と答え一般知識で埋めない）ことを確認した。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-11）。
- バグ修正: AIが長文の回答を生成すると、その1行目だけが画面に表示され、続きを読むには手動でスクロールする必要がある不具合を修正した（ユーザーからの報告）。原因は`ChannelView.tsx`・`DmView.tsx`・`ThreadPanel.tsx`の「新着で末尾へ自動スクロール」effectがいずれも`messages.length`（返信一覧は`replies.length`）のみを依存配列にしていたこと。AI応答は「生成中…」という短いプレースホルダとして先に1行追加され（`length`変化でこの時点ではスクロールが発火し正しく末尾へ移動する）、その後ポーリングで本文が実際の長い回答へ更新される（同じ行の内容が変わるだけで配列の件数は変わらない）際にはこのeffectが再発火せず、スクロール位置が「生成中…」だった頃の高さのまま据え置かれ、結果として長い回答の1行目だけが見え続きは手動スクロールが必要という症状になっていた。3ファイルとも、依存配列に「最後のメッセージ（返信）のupdated_at」を追加した（本文確定・生成完了・要約完了のいずれも`updated_at`が必ず更新される、2026-09-04の同種の修正で確立済みの列をそのまま再利用）。ユーザーの実際のローカル開発環境（`start.bat`で起動済みのもの）に対し、Playwrightで実際に「@Kogack AI」へ長文（箇条書き10項目＋説明）を要求する質問を送り、生成完了後に会話ログのスクロール位置が`scrollTop + clientHeight === scrollHeight`（完全に最下部）になっていることを実測で確認し、スクリーンショットでも末尾（10項目目・結びの文・AI回答の注意書き）まで表示されていることを確認した。検証用に作成したチャンネルはAPI経由で削除済み。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-11）。
- チャンネル会話のF-41メンション候補一覧で、`@here`（送信時点でアクティブな参加者への通知）の表示位置を一覧の一番下に変更した（ユーザーからの明示的な要望「@を入力したときに候補が出てくる順番で、@hereを一番下に表示されるようにして」）。従来`ChannelView.tsx`の`mentionCandidatesWithAi`は`@channel`・`@here`・[チャンネルAI]・参加者、の順（`@channel`と`@here`を隣接させて先頭にまとめていた）だったが、`@here`だけを配列の末尾（参加者一覧の後ろ）に移動した。`@channel`・チャンネルAIの位置・DM/スレッド返信に`@channel`・`@here`自体を出さない既存の挙動は変更していない（`ThreadPanel.tsx`・`DmView.tsx`のメンション候補にはそもそも`@here`が含まれないため対象外）。候補一覧の描画（`Composer.tsx`の`filteredCandidates`）は配列の順序をそのまま`.filter()`するだけで並べ替えは行わないため、この配列の並び替えだけで表示順が確定する。ユーザーの実際のローカル開発環境（`start.bat`で起動済みのもの）に対し、Playwrightで実際に「@」を入力し、ポップオーバーの表示順が「channel→Kogack AI→参加者（佐藤花子・山田太郎・石井直樹・長井比呂）→here」になっていることをテキスト内容・スクリーンショットの両方で確認した。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（フロントエンドのみの変更でバックエンドAPI・DBは無変更）（2026-09-11）。
- S-06「参照ドキュメント範囲」タブで、フォルダのチェックボックスにチェックを入れると、その配下の全ファイルにも自動的にチェックが入るようにした（ユーザーからの明示的な要望「フォルダの横のチェックボックスにチェックを入れたら、自動的にそのフォルダの中にあるファイルすべてにチェックが入るようにしてほしい」）。**この連動は見た目の便宜だけでなく機能上も必要な修正だったことが調査で判明した**: 索引化（`doc_chunks`）は個々のファイル自身に対して行われ、フォルダ自身の`index_status`は常に`'not_applicable'`のまま`'ready'`になることが無いため（`doc_search.search`・`channel_has_indexed_documents`はいずれも`index_status='ready'`を条件にする）、従来はフォルダのidだけを`channel_doc_folders`に入れても実際には何も検索対象に含まれない状態だった。つまり、この連動が無いままだと「フォルダにチェックを入れたつもりが実際には何もAI検索に反映されない」という、ユーザーが気づきにくい形で機能していない状態になっていた。`ChannelSettings.tsx`の`DocScopeTabBody`の`toggle`関数を、対象がトップレベルのフォルダ（`parent_folder_id === null && item_type === 'folder'`）の場合はその配下の子ファイルの選択状態もまとめて連動させる（チェックONで子も全てON、OFFで子も全てOFF）よう変更した。公開チャンネルで限定公開の子ファイルがある場合は、既存のガード（公開チャンネルは限定公開フォルダ・ファイルを選択不可）と同じ条件で連動対象から除外し、disabled表示のチェックボックスを裏側から勝手にONにしてしまわないようにした。あわせて、フォルダをチェックしたときは（畳まれていれば）自動的に展開し、連動して子ファイルにチェックが入ったことがその場で見えるようにした（未チェック化の際は畳んだままにする、チェックを外しただけで表示状態まで変えると挙動が読みにくくなるため）。子チェックボックスから親フォルダの選択状態を逆算する仕組み（indeterminate表示等）は今回のユーザーの要望の範囲外のため追加していない。ローカルで使い捨てのテストフォルダ（子ファイル2件）を作成し、Playwrightで実際に(1) フォルダのチェックボックスをON→フォルダ自身と子ファイル2件がいずれもチェック済みになること、(2) 続けてフォルダのチェックボックスをOFF→3件ともチェックが外れること、をDOM状態の直接評価とスクリーンショットで確認した。テストデータ（フォルダ・子ファイル）はAPI経由で削除済み。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（フロントエンドのみの変更でバックエンドAPI・DBは無変更）（2026-09-11）。
- チャンネル設定（S-06）で、内容を変更してから「保存」ボタンを押さずに別の画面へ移動しようとした際に確認ダイアログを出す仕組みを実装した（ユーザーからの明示的な要望「保存されていない内容がありますのように確認メッセージを出したい」）。着手前に技術的な制約を説明し2案を提示した: 案A（アプリ内のクリックによる画面遷移＋ブラウザのタブを閉じる/リロード/URL直接入力はカバーするが、ブラウザの「戻る」ボタンは対象外）／案B（react-routerをデータルーターモードへ移行し`useBlocker`で「戻る」ボタンも含め完全にブロックするが、ルーティング構造の大きな変更を伴う）。**案Aを選択**（react-routerが宣言的モード＝`<BrowserRouter>`、`main.tsx`のため、データルーター専用の`useBlocker`は使えない制約がある。実際に`node_modules/react-router`の`useBlocker`実装が`useDataRouterContext`を要求し宣言的モードでは例外を投げることをソース確認済み）。対象はS-06の「保存」ボタンを押す形式の5タブ（チャンネル管理者のチャンネル名・説明編集＝`ChannelInfoForm`、キャラクタ、振る舞い定義、参照ドキュメント範囲、自動対応範囲）で、基本設定・反応モード・スキルの引き継ぎ先など選択と同時に即保存されるタブ（そもそも「未保存」という状態が発生しない）とS-08管理コンソールは対象外（ユーザーには両方確認済み、S-08は今回含めない前提で進めた）。
  - **仕組み**: 新規`lib/unsavedChanges.tsx`に`UnsavedChangesProvider`（`dirtyRef`を保持するContext、`beforeunload`のブラウザ標準確認ダイアログもここで登録）・`useReportDirty(isDirty)`（各編集タブがレンダーのたびに呼び、共有refへ書き込む。アンマウント時＝画面遷移が実際に確定した後にのみfalseへ戻す）・`useUnsavedChangesGuard()`（未保存なら即許可、あれば`useConfirm()`で確認しその結果を返す）を追加した。`main.tsx`で`ConfirmProvider`の内側・`App`の外側に`UnsavedChangesProvider`を配置（`useConfirm()`に依存するため）。新規`components/GuardedLink.tsx`（`GuardedLink`/`GuardedNavLink`、react-routerの`Link`/`NavLink`のラップ。クリック時に`preventDefault()`→ガード確認→OKなら`navigate()`、同一URLへのクリックは素通りさせる）を追加し、`Layout.tsx`のサイドバーの全ナビゲーション（S-06タブ・S-08タブ・チャンネル/DM一覧・検索アイコン・管理コンソールリンク）と`ChannelSettings.tsx`の「← チャンネルに戻る」リンクをこれに置き換えた。ログアウトボタン（Linkではない）は`onClick`内で`useUnsavedChangesGuard()`を直接呼ぶ形で個別に保護した。5タブそれぞれに、現在のローカル編集状態と「保存済みの値（baseline）」を比較した`isDirty`を計算し`useReportDirty`へ渡す実装を追加し、保存成功時にbaselineを更新する。
  - **実機検証で発見・修正した重大なバグ**: 初回実装ではbaselineを`useRef`で管理し、保存成功時に`baselineRef.current = {...}`と直接代入していたが、**この代入自体はReactの再レンダーを引き起こさない**ため、「保存後に入力欄の値も保存済みの値（trim後）へ揃える」目的で呼んでいた`setName(trimmed)`等が、末尾空白等が無く値が変わらない場合はReactの同値bailoutで再レンダー自体を起こさず、結果として`useReportDirty`側の判定（`ctx.dirtyRef.current`）が保存前の「dirty」のまま更新されない、という不具合を実機（Playwright、実際のPUTリクエストがHTTP 200で成功することまで確認したうえ）で発見した。症状は「保存ボタンを押して保存が成功したにもかかわらず、直後に別タブへ移動しようとすると確認ダイアログが出続ける」というもので、デバッグ用の一時的なconsole.logで実際に「保存後、次に他の理由で再レンダーが起きるまでeffectが発火せずdirtyRef.currentが古い値のまま」であることを直接確認してから特定した。**5タブすべてのbaselineを`useRef`から`useState`へ変更**（`setBaseline({...})`は常に新しいオブジェクト参照を渡すため、値が実質同じでもReactの同値bailoutに引っかからず確実に再レンダーが起きる）することで解消し、修正後は実際のPUT成功→直後のタブ切替で確認ダイアログが出ない（`dialog after save+nav: 0`）ことを再度実機で確認した。あわせて、`UnsavedChangesProvider`の`value={{ dirtyRef }}`が親（`ConfirmProvider`、確認ダイアログの開閉のたびに再レンダーする）の再レンダーに巻き込まれるたびに新しいオブジェクトになり、下位の`useContext`消費側を無駄に再評価させていた点も`useMemo`で参照を固定して解消した（今回のバグの直接原因ではなかったが、同じ根本原因＝ref/オブジェクト参照の扱いに起因する潜在的な不安定要因だったため、発見したついでに直した）。
  - **検証**: ユーザーの実際のローカル開発環境（`start.bat`で起動済みのもの）に対し、Playwrightで実際に(1) キャラクタタブで名前を編集→別タブへの遷移を試みる→確認ダイアログが表示されること、(2)「このまま編集を続ける」→タブに留まり編集内容が保持されること、(3) 再度移動を試み「保存せずに移動する」→実際に遷移しURLが変わること、(4) 遷移先から戻ると編集内容が失われている（保存しなかったため正しい）こと、(5) 編集→保存→直後に別タブへ移動→確認ダイアログが出ないこと（バグ修正の直接確認）、(6) 自動対応範囲タブで新規ルールを追加（未保存）→「← チャンネルに戻る」リンクでも同様に確認ダイアログが出ること、をHTTPレスポンスの直接確認・DOM状態評価の両面で確認した。テストで発生した変更（キャラクタ名・自動対応範囲ルール）はAPI経由で元の状態に戻し確認済み。ブラウザの「戻る」ボタン自体の動作は前述の制約どおり今回のテスト対象外。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-11）。
- 発言に付いた絵文字リアクションのピル（`MessageList.tsx`の`ReactionPills`）に、ホバーで少し拡大・クリック中は少し縮小するマイクロインタラクションを追加した（ユーザーからの明示的な要望「マウスのカーソルを合わせると少しだけ枠が大きくなるように」「クリックしたら一瞬小さくなってクリックした感を出す」）。`hover:scale-110`（ホバー時1.10倍）・`active:scale-90`（マウス押下中〜離すまでの間だけ0.90倍）・`transition-transform duration-150`をピルのボタンに追加しただけの、状態管理を伴わないCSSのみの実装（`active:`疑似クラスがマウスの押下中だけ自動的に適用される性質を利用し、「一瞬小さくなる」を素のCSSで表現した）。`ReactionPills`は`ThreadPanel.tsx`の元発言ヘッダーからも共有importされているため、この1箇所の変更で会話ログ・スレッド元発言の両方に反映される（クイックリアクションボタン・絵文字ピッカーグリッドは「リアクションの絵文字を表示しているところ」という要望の対象外と判断し変更していない）。ローカルで使い捨てのテストメッセージにリアクションを付け、Playwrightで実際に(1) 静止時の枠サイズ（幅約41px）、(2) ホバー時（幅約45px、約1.10倍、`transition-transform duration-150`の待ち時間を空けて計測）、(3) マウス押下中（幅約37px、約0.90倍、`:hover`と`:active`が同時に成り立つ状態からの遷移が完了するまで待ってから計測）を実測し、意図どおりの拡大・縮小になっていることを確認した。押下中の計測ではクリックそのもの（トグルAPI呼び出し）を誤発火させないよう、ピルの外側でmouseupすることでクリックイベントの発生を避け（mousedown/mouseupの対象要素が異なる場合はclickイベントが発生しないブラウザの仕様を利用）、テスト対象のリアクション自体は変化させずに計測した。テストメッセージは削除済み。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（フロントエンドのみの変更でバックエンドAPI・DBは無変更）（2026-09-11）。
- 発言の編集機能を追加した（ユーザーからの明示的な要望「自分が送ったメッセージに限っては、メッセージを送った後でも編集できる機能が欲しい。編集したメッセージに関しては（編集済み）と明記されていることが望ましい」）。要件定義書上のF-xxに対応付けられた新規機能。
  - **権限**: **A-12削除と異なり投稿者本人限定**（adminバイパスは無い）。削除は「消す」だけだが編集は「本文を書き換える」ため、本人以外に許可すると本人が言っていない内容を第三者が作文できてしまうという判断。システム通知（F-43）・AI/BOT発言はsender_user_idを持たないため自然に対象外になる。
  - **スコープ**: 本文のテキストのみを編集対象とし、@メンションの構造化（message_blocks）・添付ファイルは編集の対象外にした。本文の再解析は通知の再送信・AIの再起動等の副作用に発展しうるため、今回は単純な誤字修正用途を想定して本文の書き換えのみに絞った（編集後の本文に新しく「@氏名」等を書いてもクリック可能なメンションとしては扱われず、URLリンク化・太字等の装飾記法のみそのまま解釈される）。編集時間の制限は設けていない（Slackの実際の挙動と同じくいつでも編集できる）。
  - **バックエンド**: 新規`messages.edited_at`列（TIMESTAMPTZ、NULL＝未編集）を追加。新規`PUT /api/messages/{id}`（`routers/messages.py`）を実装し、`UPDATE messages SET body=$2, edited_at=now(), updated_at=now() WHERE id=$1`で更新する（`updated_at`も更新するのは2026-09-04の「AI応答の生成完了がsinceポーリングに反映されない」不具合と同じ教訓——本文以外の更新でも`updated_at`を進めないと、他の参加者の画面に反映されない）。権限判定はA-12削除と同じ「参加していない会話の、他人の発言は存在自体を伏せる」F-34の考え方を踏襲しつつ、投稿後に退出した本人（is_member=false・本人）は編集自体は許可する（削除同様、過去の自分の発言を後から直せる余地を残す設計判断）。`_message_out`（`channels.py`・`dms.py`・`messages.py`の3箇所、既存のGET/POST系エンドポイント全て）に`is_edited`（`edited_at is not None`）を追加した。
  - **フロントエンド**: `MessageList.tsx`の発言ホバー時のツールバーに「編集」ボタンを追加（「返信」の次・「削除」の手前）。クリックすると本文表示がインラインの`<textarea>`に切り替わり（Composer.tsxと同じ規約: Enter＝改行・Ctrl+Enter（Macは⌘+Enter）＝保存・Escape＝取消、加えて明示的な「保存」「キャンセル」ボタンも用意）、保存成功で通常表示に戻り、タイムスタンプの隣に「（編集済み）」を表示する。他のホバーアクション（リアクション・返信・削除）は編集中は非表示にする（`isEditing`のとき丸ごと隠す）。楽観的更新は既存の`onDeleted`/`onReactionToggled`と同じパターンで、新規`onEdited`プロパティ経由で呼び出し元（`useMessages.ts`の新規`updateMessage`・`useThread.ts`の新規`updateReplyMessage`）へその場反映を委ねる（次のポーリングを待たず、保存直後に編集後の本文が表示される）。`ChannelView.tsx`・`DmView.tsx`・`ThreadPanel.tsx`（返信一覧側）の3箇所に配線した。**ThreadPanel.tsxの元発言ヘッダー（MessageListを経由せず個別に描画している箇所）には編集ボタンを追加していない**——このヘッダーは元々「削除」ボタンも持たない設計（2026-09-10の絵文字リアクション追加時、削除・返信は元発言ヘッダーに元から存在しないことが既に確立していた）であり、同じ発言は本体タイムライン側のMessageListに常に表示されているため、編集したい場合はそちらから行える。この既存の非対称性にそのまま合わせた設計判断。
  - **検証**: ローカルで使い捨てのテストメッセージを作成し、curl相当の直接HTTPリクエストで(1) 投稿直後は`is_edited: false`、(2) 本人による編集が200で成功し`is_edited: true`・本文が更新されること、(3) 他人（山田太郎）による編集が403（adminバイパスが無いことも確認——一般ユーザーでの検証だったため、admin自身での追加確認はしていない）、(4) 存在しないメッセージへの編集が404、(5) 空文字の本文が422、を確認した。Playwrightで実際のブラウザ操作も確認：メッセージ投稿→ホバーで「編集」ボタン表示→クリックで本文がテキストエリアに切り替わり元の本文が入っていること→編集してCtrl+Enterで保存→本文が更新され「（編集済み）」ラベルが表示されることをスクリーンショットで確認した。テストメッセージは削除済み。要件定義書・基本設計書・画面モックアップには元々編集機能の記載が無いため改訂は無い（2026-09-11）。
- 発言の編集画面に、通常投稿の投稿欄（Composer.tsx）と同じ書式ボタン・絵文字ボタン・ファイル添付ボタンを追加した（ユーザーからの明示的な要望「メッセージを編集しているときも、通常のメッセージと同じように書式のボタンやファイル・メンション・スタンプのボタンを付けてほしい」）。着手前にメンション・添付ファイルの扱いを確認し、**メンションボタンは編集画面には置かない**（ユーザーの回答「編集の時にはメンションボタン自体をなくしてよいです」）、**添付ファイルは追加・削除の両方に対応する**（ユーザーの回答「ファイルは編集の時に削除する機能も付けたい」）という方針で合意した。
  - **書式ボタン（太字・斜体・下線・取り消し線・コード・箇条書き）**: Composer.tsx内部にあった`wrapSelection`/`wrapCode`/`insertBulletList`（2026-09-10実装、既に2回のバグ修正を経ている——空行での箇条書き挙動等）と、Enterキーでの箇条書き継続処理を、状態を持たない純粋関数として新規`lib/textFormatting.ts`（`wrapSelectionText`・`wrapCodeText`・`insertBulletListText`・`continueBulletOnEnter`）へ切り出し、`MessageList.tsx`の編集用textarea・書式ツールバーから呼び出す形にした。**Composer.tsx自体は変更していない**（既存の動作確認済みコードを触るリスクを避け、意図的にロジックだけを共有モジュールへ切り出す形にした。`force_remove_member`等で採用済みの「重複を許容してでも既存コードは触らない」考え方と同じ）。
  - **絵文字（スタンプ）ボタン**: 会話ログの絵文字リアクション（2026-09-10実装）で使っている`EmojiGridPopover`コンポーネント（anchor/onSelect/onCloseの汎用インターフェース）をそのまま再利用し、選択時はリアクションのトグルではなく編集中のカーソル位置へ絵文字を挿入する。
  - **ファイル添付ボタン**: 編集開始時（`startEdit`）に、その発言の既存添付一覧（`m.attachments`）を`editKeptAttachments`としてコピーし、✕ボタンで（ローカルのstateから）取り除ける（削除確定は保存時）。新規ファイルは既存のA-21アップロード（`uploadAttachment`）を編集画面からも呼べるようにし、アップロード済みのものを`editNewAttachments`として保持、✕で取り消せる（Composer.tsxの添付チップと同じ見た目・挙動）。保存時、「元の添付一覧のうちeditKeptAttachmentsに残っていないもの」のidを`remove_attachment_ids`として、`editNewAttachments`を`new_attachments`として`PUT /api/messages/{id}`に含めて送る。
  - **バックエンド**: `EditMessageRequest`に`remove_attachment_ids: list[str]`・`new_attachments: list[AttachmentInput]`を追加し、`edit_message`（`routers/messages.py`）を本文UPDATE・添付DELETE（`message_id`も条件に含め他の発言への誤爆を防止）・添付INSERT（既存の`attachments.insert_attachments`を再利用）を1つのトランザクションにまとめる形に変更した。@メンションの構造化（message_blocks）は引き続き編集の対象外のまま（今回の合意どおりボタン自体を置かないため、バックエンド側もmentionsパラメータを追加していない）。
  - **実機検証で発見・修正したバグ**: 絵文字ボタンのトグル処理を`setEditEmojiAnchor((v) => (v ? null : e.currentTarget.getBoundingClientRect()))`という、`setState`のupdater関数の中で`e.currentTarget`を参照する実装にしたところ、Playwrightでの実機検証で**クリックした瞬間にReactツリー全体がクラッシュする**（`document.body`のHTMLが103文字まで縮む＝Reactのマウント自体が失敗した状態になる）不具合を発見した。原因はDOM仕様上、`currentTarget`はイベントのディスパッチが終了すると`null`に戻ることがあり、`setState`のupdater関数はReactのスケジューリングによっては同期的なイベント処理の外（`currentTarget`が既にnullになった後）で呼ばれることがあるため、`null.getBoundingClientRect()`で例外が発生していたと判明した。修正は、同期的なイベントハンドラ本体（`currentTarget`がまだ有効な間）で先に`getBoundingClientRect()`を呼んで結果を確定させ、updater関数には確定済みの値だけを渡す形に変更した（既存の正常動作しているリアクション絵文字ピッカーの`onTogglePicker`が既にこの安全なパターンを採用していたため、それと同じ形に揃えた）。
  - **検証**: ローカルで実際に添付ファイル付きの発言を投稿し、Playwrightで実際に(1) 編集画面にメンションボタンが存在しないこと（0件）、(2) 既存の添付ファイルのチップが表示されること、(3) 本文を選択して太字ボタン→`**...**`で囲まれること、(4) 絵文字ボタン→ピッカーが開き選択した絵文字がカーソル位置に挿入されること（前述のクラッシュ修正の直接確認）、(5) 既存添付の✕→一覧から消えること、(6) 新規ファイルを選択→アップロードされチップに追加されること、(7) 保存→最終的な発言が新しい本文（太字・絵文字反映）と新しい添付ファイルのみ（元の添付は消え、新規ファイルだけが残る）で表示され「（編集済み）」ラベルも出ることを確認した。テストメッセージ5件（試行錯誤の過程で作成した分を含む）はすべてAPI経由で削除済み。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-11）。
- **Keirekiからの意図的な相違点**（実装時に必ず踏襲すること。Keirekiのコードを安易にコピーしない）:
  - ロールは`member`/`admin`の2値のみ（Keirekiは`member/viewer/admin`の3値）。chadminは`T-03.is_channel_admin`で別管理し、`users.role`には持たせない。
  - Google再ログイン時、既存ユーザーの`name`/`picture_url`は**上書きしない**（F-39。初回登録時のみ設定、以後は本人がA-62で変更するまで保持）。Keirekiの`callback()`は毎回上書きするが、Kogackではその実装をコピーしない。
  - ログインエラーのコードは`domain_not_allowed`/`account_disabled`/`oauth_failed`の3種（詳細設計書 総論7.2節）。Keirekiの`domain/inactive/forbidden/invalid_request`とは名称が異なる。
  - ポート既定値をKeirekiとずらしている（同時起動時の衝突回避）: `DB_PORT=55433`（Keirekiは55432）、`BACKEND_PORT=8011`（同8010）、`FRONTEND_PORT=5174`（同5173）。
  - 使用するAIモデルは環境変数`AI_MODEL`のみで決定し、Keirekiのような管理画面上でのモデル切替機能・`app_settings`テーブルは設けない（詳細設計書AIサポート10.6節）。コスト単価表（`services/ai_client.py`）も円/1000トークン単位で保持する（Keirekiの`ai_client.py`はUSD/1Mトークン単位）。
  - **ホスティング先はFly.io**（Keireki・LCC CRM踏襲の既定はKoyeb。基本設計書9.6節「設計判断」、ユーザーの意向により2026-09-04に変更）。KoyebのGitブランチ連動自動デプロイに相当する仕組みはFly.io標準には無く、`fly deploy`コマンドを明示実行する運用にする。本番/ステージングの分離もKoyebの1サービス内ブランチ切替ではなく、`kogack`（本番）・`kogack-staging`（ステージング）の2アプリに分ける方式にしている。リポジトリ直下の`Dockerfile`・`.dockerignore`・`fly.toml`もこの前提で新規作成した（Keirekiの`Dockerfile`はKoyeb向けのままで、これをテンプレートにする際はマルチステージビルドの構造自体は流用してよいが、ホスティング先固有の記述はコピーしないこと）。
- 開発用ログイン（`/api/auth/dev-login`・`/api/auth/dev-users`）はKogackの正式な設計書（S-01の仕様）には存在しない、ローカル開発専用の便宜機能（Keirekiの規約を踏襲）。本番では`APP_ENV=production`で常に無効。
- 新しい画面・APIを実装したら、この節と（該当すれば）詳細設計書・基本設計書・画面モックアップを同じコミットで更新すること。

## 開発研修としての位置づけ

新人社員向けAI開発研修の成果物であり、`docs/index.html` の「読み進め方」に研修用の読み順ガイドがある: 要求仕様書 → 要件定義書 → 画面モックアップ → 基本設計書 → 詳細設計書（総論＋4分冊） → 操作マニュアル（未着手）。各ドキュメントはID体系での上流〜下流のトレーサビリティを確認しながら読むことを意図している。
