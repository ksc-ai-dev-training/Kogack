# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 概要

社内向けAIネイティブチャットシステム「Kogack」の設計ドキュメント一式と実装（新人社員向けAI開発研修の成果物）。既存Slackと併存しつつ、チャンネルにAIエージェントが常駐して質問回答・依頼対応・社内システム操作を行う社内Webシステム。設計フェーズ（`docs/`）は完成しており、2026-08-26より**実装フェーズに着手**した（`backend/` `frontend/`）。姉妹プロジェクトの社員経歴書管理システム「Keireki」（`c:\Users\pc-000688\Documents\keireki`、リポジトリ外）を技術構成・実装パターンのテンプレートとして踏襲する（詳細は「実装状況」節）。`docs/index.html` をブラウザで開くと全設計ドキュメントにアクセスできる。

技術構成（既存システム LCC CRM を踏襲、REQ-N-07）: React 19 + TypeScript + Vite + Tailwind CSS + react-router + SWR / FastAPI (Python) + Uvicorn + asyncpg / PostgreSQL (Supabase・全テーブルRLS有効) / Google OAuth 2.0 + セッションJWT（HttpOnly Cookie、許可ドメイン: kogasoftware.com） / OpenAI API（Chat Completions + Function Calling） / Google Drive API（文書参照・権限確認） / リアルタイム配信は3秒間隔ポーリング（WebSocket等は使わない簡易方式。LCC CRMに実装例がないためここだけ基本設計書で個別選定） / Supabase Storage / ホスティングはKoyeb（main=本番 / staging=ステージング、LCC CRMと同様の構成）。

### ロール（3ロール制の実装表現）

グローバルなロールは **member / admin の2値のみ**。「チャンネル管理者（chadmin）」はチャンネル単位で付与されるため、users テーブルの3値カラムでは表現せず `channel_members.is_channel_admin` で表現する2階層構成（設計判断は基本設計書6.2節）。認可判定はサーバー側（`backend/auth_helpers.py` の `require_auth`/`require_roles`）で行い、フロントの表示制御は補助に留める方針（REQ-N-02）。

### 想定規模・現状のリリース計画

想定利用者200名。**2026年9月末に初期リリース**予定で、第0層（チャンネル・スレッド・DM・検索・ファイル共有）と第1層（AIのキャラクタ・スキル・振る舞い・反応設定）はほぼ全て実装、第2層（ドキュメントQ&A・閲覧権限フィルタリング）も核として実装するが、第3層（社内システムへの登録・変更操作）は座席予約システムの**参照のみ**を追加しスマートフォン対応とあわせて対象外とする（要件定義書3.2節）。**自分たちだけでは決めきれない事項は千田氏との別途協議として要件定義書8.2節に明記してあり、この一覧は今のところ2件（AI利用コスト上限到達時の挙動、初期リリースの先行運用対象部署・人数）**。

## ドキュメント構成（変更前に該当ドキュメントを参照すること）

| # | 内容 | ファイル | 状態 |
|---|---|---|---|
| 1 | 要求仕様書 | `docs/01_要求仕様書.html` | 完成 |
| 2 | 要件定義書 | `docs/02_要件定義書.html` | 完成（一部は千田氏との別途協議事項として明記） |
| 3 | 画面モックアップ（S-01, S-03〜S-08 + 補足01〜06） | `docs/画面モックアップ/00_一覧.html` | 完成 |
| 4 | 基本設計書 | `docs/04_基本設計書.html` | 完成 |
| 5 | 詳細設計書（総論＋4分冊） | `docs/05_詳細設計書.html`（総論: 1・2・5・7・9章）+ `docs/05-1_詳細設計書_DB設計.html`（3章）+ `docs/05-2_詳細設計書_API設計.html`（4・6章）+ `docs/05-3_詳細設計書_画面設計.html`（8・11章）+ `docs/05-4_詳細設計書_AIサポート.html`（10章） | 完成 |
| 6 | 操作マニュアル | `docs/06_操作マニュアル.html` | 未着手 |

詳細設計書は2026-08-24に総論＋4分冊へ分割した（章番号は分冊前のまま維持、改訂履歴・版数は総論に一本化。`docs/_sidenav.js` に5ファイルとも登録済み）。

`README.md` の成果物一覧は2026-08-24時点で `docs/index.html`（改訂履歴 Ver 1.6〜4.17）に追従済み。ドキュメントを追加・完成・分割させたときは両方を同時に更新すること（過去に3・4番の完成が反映漏れになっていたため）。

## ID体系（ドキュメント間の相互参照）

REQ-xx（要求）/ F-xx（機能）/ S-xx（画面）/ T-xx（テーブル）/ A-xx（API）。上流の要求IDが下流の設計まで追跡できる（トレーサビリティ）ことを確認しながら読む・書くこと。

## 規約

- **共通スタイル**: 設計書は `docs/assets/style.css` を、画面モックアップは `docs/画面モックアップ/mock.css` を使用。ページ個別のスタイルは作らない。
- **サイドナビ**: 各設計書は `<body>` 直下で `_sidenav.js` を読み込む（サブフォルダからは `../_sidenav.js`）。ドキュメントが完成したら `docs/_sidenav.js` の `DOCS` 配列に1エントリ追加する。**画面モックアップ本体（S-0x・補足0x）は固定サイドバーを持つためサイドナビを読み込まない**（リンクのみ張る）。`DOCS` 配列には一覧ページ（`画面モックアップ/00_一覧.html`）のみを掲載し、個々の画面は掲載しない。
- ドキュメント追加時は `docs/index.html` と `README.md` の成果物一覧も更新する（前述のとおり現状ここが滞っている）。
- **検討資料・計画資料は `検討資料/` へ**: AIが作成する提案・検討・判断根拠のドキュメントは正式な成果物（`docs/`）に混ぜず、リポジトリ直下の `検討資料/` に `YYYYMMDD_タイトル.html` で日付順に置き、`検討資料/README.md` の一覧に登録する。各資料の冒頭にステータス（検討中 / 採用 / 見送り）を明記する。`docs/index.html`・サイドナビには載せない。
- **検討資料は承認後も削除しない**: 内容が `docs/` の該当設計書へ取り込まれたら「採用／設計反映済み」に更新するが、ファイル自体は経緯の記録として残す。判断が後で覆った場合も検討資料自体は当時の記録のまま変更せず、`検討資料/README.md` の一覧側にその旨を追記する（例: 一度「見送り」とした非公開チャンネルを後日採用に変えた際の扱い）。
- **設計判断は基本設計書に callout として残す**: 「なぜその設計にしたか」「代替案との比較」は本文の表に埋め込まず、`<div class="callout">`（新規追加分は `callout diff-new`）に「設計判断（〜理由）:」の形式でまとめる（基本設計書6章に多数の実例あり）。理由を後から読んで判断の妥当性を検証できることを優先する。
- **改訂は複数ドキュメントを横断して同じコミットで一貫更新する**: 要求仕様書・要件定義書・基本設計書・画面モックアップのうち影響する全てを同時に改訂し、変更箇所は `class="diff-new"` で明示する。`docs/index.html` の改訂履歴テーブルに1行（版数・日付・内容・関連検討資料へのリンク）を追記する。
- **見送った機能のID（F-xx / T-xx / A-xx）は欠番のまま再利用しない**: 一度採番した後に対象外へ戻した場合でも、そのIDは空けたままにし、将来復活させる際は同じIDを再利用する（例: F-36個人宛リマインダーを対象外に戻した際、T-20・A-57〜A-60は欠番のまま）。
- 実装は 詳細設計書（総論 `docs/05_詳細設計書.html` ＋4分冊。テーブル定義は05-1、API詳細は05-2、画面項目定義・フロントエンドコンポーネント設計は05-3、AIプロンプト設計は05-4、権限制御は総論）に準拠すること。
- **実装は姉妹プロジェクトKeireki（`c:\Users\pc-000688\Documents\keireki`）のパターンをそのまま流用する**: 技術構成・ディレクトリ構成・`database.py`/`auth_helpers.py`/`google_auth.py`/`lib/api.ts`/`hooks/`/`components/ui/`の作り方は自分で選ばずKeirekiを参照する。ただし「実装状況」節の「Keirekiからの意図的な相違点」は必ず踏襲し、Keirekiのコードを無条件にコピーしないこと。
- **実装を進めたら関連ドキュメントを同じコミットで更新する**: 画面・API・テーブルを新規実装したら、この`実装状況`節に追記する。設計自体を変更した場合（設計書に無い挙動を実装する等）は、詳細設計書・基本設計書・画面モックアップ側もあわせて改訂する（前述の規約どおり）。起動方法・ポート・依存関係を変えたら`README.md`の起動手順も更新する。

## 現状のドキュメントから読み取れる主要な設計判断

- **DM（F-05）は一対一固定ではなくグループDM対応**。参加者は `direct_message_members`（T-17）の中間テーブルで管理し、開始時に固定（開始後の参加者追加・削除は対象外）。
- **非公開チャンネル（F-34）は新規テーブルを追加せず**、既存の `channels.is_public` と `channel_members` の枠組みのまま、API層の認可分岐（公開=誰でも自己参加可、非公開=既存参加者が招待）と RLS（`is_public=false` は参加者以外に存在自体が露出しない）で実現する。非公開チャンネルへのメンバー追加は chadmin 限定にせず参加者全員に開放（運用上のボトルネック回避を優先）。
- **メッセージの送信予約（F-35）・定期投稿（F-36）は専用ジョブキュー（Celery等）を導入せず**、FastAPI内蔵の `asyncio` タスク（`scheduled_dispatcher.py` 相当、30秒間隔ポーリング）で実現する。**単一インスタンス運用が前提**であり、将来Koyebで複数インスタンスに水平スケールする場合は `UPDATE ... WHERE status='pending' RETURNING` によるアトミックな取得に変更するか専用ワーカーへ切り出す必要がある（基本設計書10章に明記）。
- **BOT投稿（`messages.sender_type='bot'`）は本文中の@メンションでAIエージェントを起動しない**。AIエージェント処理（8章）は `sender_type='human'` の投稿のみをトリガー対象とする一貫原則とし、自動応答トリガー（F-38）もこれを踏襲して連鎖起動を防ぐ。
- **定期投稿の宛先はチャンネルのみ**（個人宛て複数可への拡張は一度実装したが方針転換で対象外に戻した。設計自体は妥当だったため次フェーズ再検討用に検討資料・要件定義書3.2節へ記録を残してある）。
- **自動応答トリガー（F-38）は時刻ベースの定期投稿ディスパッチャとは別方式**: `recurring_posts` のスケジュール列と噛み合わないため新規テーブル `trigger_rules`（T-21）に分離し、メッセージ投稿API（A-11）内で同期的に判定する（レイテンシを気にする必要がなく、投稿直後に反応させたほうが体験に合うため）。
- **アイコン画像（個人・チャンネルAI・BOT/トリガー）は汎用アップロードAPI（A-61）1本に集約**し、権限チェックはアップロードAPI自体ではなく反映先（プロフィール更新・チャンネルAI編集・定期投稿編集）の既存の権限チェックに委ねる。アイコンは会話添付ファイルと異なり全認証済み利用者に公開する（発言者表示のため組織内のあらゆる画面に登場しうる性質のものであり、機密情報ではないと判断）。
- **AI発言の根拠カード等（citation / external_system / quote_reference / pending_action）は `message_blocks` テーブル1本にJSONBペイロードでまとめる**（種類ごとにテーブルを分けない）。フロントは `block_type` に応じたコンポーネント（`components/blocks/` 想定）で描画する設計。
- **pending_actions（実行前確認、F-25）は座席予約の登録操作自体が対象外の現時点から先行してスキーマ・APIを用意**しておき、次フェーズで書き込み系連携が増えたときに使い回す。
- 画面「S-06」は当初「チャンネルAI設定」だったが、チャンネル管理者・定期投稿・自動応答トリガーなどAI設定以外の項目が増えたため**「チャンネル設定」に改称**（画面ID・DB/APIの識別子である `channel_ai_settings` 等は変更していない。ファイル名も `S-06_チャンネル設定.html` に追従済み）。
- 画面「S-02（ワークスペース）」は要件定義書の画面定義としては残るが、単独の空状態画面としては起こさず、S-03以降の各画面に共通のサイドバーとして実装済み扱い（個別モックアップファイルはない）。

## 実装状況（縦切り・画面単位で進行中）

Keirekiと同じ流儀（1画面につきDB・API・画面をひととおり作って動かす→次の画面へ。単体・結合テストは省略し、`start.bat`でローカル起動して手動確認する）で進める。デプロイ（Koyeb/Supabase）はこの縦切りフェーズの外。

- **実装済み**:
  - 認証（A-01〜A-04）、S-01 ログイン（開発用ログインを含む）、T-01 users。ディレクトリ構成・`database.py`・`auth_helpers.py`・`google_auth.py`・`lib/api.ts`・`hooks/useMe.ts`・`ToastProvider`/`ConfirmProvider`はKeirekiの該当ファイルをほぼそのまま移植（2026-08-26）。
  - チャンネル一覧・作成・参加・会話（A-05〜A-08, A-10〜A-11）、S-02 サイドバー（チャンネル一覧のみ。検索欄は未実装。予約バッジは後日F-35実装時に追加）、S-03 チャンネル会話（投稿・3秒ポーリング表示のみ。添付・要約・スレッドは未実装）、T-02 channels・T-03 channel_members・T-05 messages（AI/BOT関連カラムは先に用意のみ、未使用）。補足01（チャンネルに参加/作成）は簡易版（非公開チャンネルの招待フローは未実装、公開チャンネルの参加・作成のみ）（2026-08-26）。
  - DM（A-16〜A-19）、A-68 利用者検索（DM相手選択専用）、S-02 サイドバーのDM一覧、DM会話画面（`DmView.tsx`）、補足02 DM相手を選ぶ（`DmPickerModal.tsx`、既存DMタグ・無効化アカウントの選択不可を含む）、T-04 direct_messages・T-17 direct_message_members・T-05 messages（dm_id側）。DM開始（A-17）は呼び出し元を含む参加者集合の完全一致で既存DMを再利用し、グループDM（3名以上）にも対応済み。`useMessages`フックはチャンネル/DM共通のbasePath方式に一般化した（2026-08-26）。
  - S-04 スレッド表示（A-13/A-14。`routers/messages.py`、`require_thread_access`）とA-10/A-18への`thread_reply_count`付与。チャンネル・DMどちらの発言にもぶら下がれる汎用設計（元発言のchannel_id/dm_idに応じて権限判定を分岐）だが、モックアップ・基本設計書の例はチャンネルのみのため、DM側は設計の自然な拡張として実装した位置づけ。スレッド返信は本体タイムライン（A-10/A-18）から`thread_parent_id IS NULL`で除外する（基本設計書8.2節「スレッド内のやり取りは本体タイムラインに流れない」）。フロントは`MessageList`/`Composer`をS-03・S-04共有コンポーネントとして`components/`へ切り出し、`ThreadPanel.tsx`が`?thread=`クエリパラメータ（`useSearchParams`）でChannelView/DmViewの右側に重畳表示する（2026-08-26）。
  - S-05 横断検索（A-20。`routers/search.py`）はメッセージ検索のみを実装。pg_trgm（GINインデックス）でbody部分一致、in/from/with/before/after/on/duringのモディファイアはクエリパラメータとしてバックエンド側は完全対応済みだが、フロントはフリーテキスト入力のみで、in:/from:/with:のオートコンプリートUI（F-42のトークン分解・候補ポップオーバー）は未実装。ファイル検索（A-21/A-22未実装のため`counts.file`は常に0）・ドキュメント根拠検索（第2層AI未実装のため`counts.document`は常に0）も未実装。検索結果はチャンネル・DM両方から横断的に拾い、クリックで該当の会話へ遷移する（メッセージ単体へのアンカー・ジャンプは未対応）。サイドバー上部に検索アイコン（`/search`への導線）を追加。Windowsには`zoneinfo`のtzdataが同梱されないため`tzdata`パッケージを依存に追加した（2026-08-26）。
  - S-06 チャンネル設定（`ChannelSettings.tsx`、`/channels/:channelId/settings`）は10タブ構成のうち「チャンネル管理者」タブ（chadmin追加解除＋公開範囲切替）のみ実装。「定期投稿」「自動応答トリガー」「AI設定」7タブはスケジューラ・AIエンジンが未実装のため対象外（実装したらこの節に追記する）。A-46（参加者一覧）・A-47（chadmin追加）・A-48（chadmin解除。最後の1人は400で拒否）・A-49（公開範囲切替）を`channels.py`に追加し、新規`require_channel_admin`（当該chadminまたはシステムadmin。システムadminは非参加チャンネルでもバイパス、chadminは非公開チャンネルの非参加者に404、参加しているが非chadminには403）で保護。ChannelViewヘッダーにchadmin/adminのみ表示の設定リンクを追加。既知の制約: システムadminが一度も参加していない非公開チャンネルはA-06（`require_channel_member`、admin特例なし）で弾かれるためS-06自体を開けない（今回は着手せず、必要になったタイミングでA-06側の扱いを再検討する）（2026-08-26）。**2026-09-02にA-06・A-46のガードを`require_channel_member_or_admin`へ切り替え、この制約を解消した。後述の該当項を参照**。
  - F-43 チャンネル参加の通知（要件定義書v1.18で新規追加。検討経緯は`検討資料/20260826_チャンネル入室通知の追加.html`）。A-08（`join_channel`）の処理をトランザクション化し、参加者追加と同時にT-05へ`sender_type='bot'`・`bot_display_name='システム通知'`の発言「（氏名）さんが参加しました。」を作成する。新規テーブル・列は追加せずF-36/F-38のBOT発言の枠組みをそのまま流用。`_message_out`（`channels.py`・`dms.py`・`messages.py`の3箇所）はBOT発言時に`sender_name`を`bot_display_name`から補うよう修正（従来は`sender_user_id`が無いBOT発言のsender_nameが常にNULLになる欠陥があったが、今回初めて実際にBOT発言を生成したため顕在化し、あわせて修正した）。フロントの`MessageList`にBOTバッジ表示を追加。チャンネル作成時の自動参加（A-07）は対象外、DMは参加者追加の経路が無いため対象外（2026-08-26）。
  - S-08 管理コンソール（`AdminConsole.tsx`、`/admin`）は4タブ構成のうち「利用者管理」タブのみ実装。「ドキュメント参照範囲」「AI利用状況・コスト」「監査ログ」の3タブはDrive連携・AI呼び出し・監査ログ基盤が未実装のため対象外（実装したらこの節に追記する）。A-36（一覧）・A-37（ロール変更・有効/無効化。自分自身の無効化は400で拒否）を`routers/admin.py`に追加し、`require_roles("admin")`で保護。あわせてT-01に`last_login_at`列を追加し、Google OAuthコールバック・dev-loginの両方でログイン成功時に更新するようにした（05-3画面設計に記載済みだがDB/API側が未反映だった抜けをbackfill。F-42の際のA-68backfillと同種の対応で、検討資料は起こしていない）。chadmin_channelsは参考表示のみ（変更はS-06から行う）。フロントはLayoutサイドバーを`/admin`表示中は利用者管理ナビに差し替え、フッターにadmin限定の「管理コンソール」導線を追加。非adminのアクセスはA-36の403を検知してワークスペースへトースト通知付きでリダイレクト（基本設計書4.2節「設計判断」）（2026-08-26）。
  - S-08利用者管理の管理者ロックアウト防止を自己ロール降格にも拡張（詳細設計書API設計v1.3）。実運用中に唯一の管理者（石井直樹）が自分自身をmemberへ降格し、以後S-08を開けなくなる事象が発生したため、A-37に「自分自身のroleをmemberにする操作は400で拒否」を追加（既存の自己無効化拒否と同じ考え方）。フロントも自分自身の行のロール選択を無効化し、そもそも操作できないようにした（2026-08-26）。
  - A-12 メッセージ削除（論理削除。基本設計書8章にAPI一覧のみ記載で詳細未反映だった抜けをbackfill、詳細設計書API設計v1.4）。`routers/messages.py`に追加し、投稿者本人またはadminのみ許可（参加していない会話の発言は404、参加しているが権限が無い場合は403）。チャンネル・DM・スレッド返信・元発言のいずれも同じエンドポイントで扱う。フロントは`MessageList`にホバー時の「削除」ボタンを追加し（投稿者本人またはadminにのみ表示）、`useConfirm`で確認後に削除する。`useMessages`のsinceポーリングは既存キャッシュ行の消失を検知できないため、`bumpThreadReplyCount`と同じ考え方で`removeMessage`（一覧からその場で除外）・`decrementThreadReplyCount`（スレッド内で返信を削除した際に元発言の件数をその場で-1）を追加した（2026-08-27）。
  - サイドバーの未読バッジ（基本設計書4.2節「サイドバー」に記載済みだったがDB/API設計が未反映だった抜けをbackfill、基本設計書v1.16・詳細設計書DB設計v1.2・API設計v1.5）。新規T-22 read_states（利用者ごとの最終既読時刻のみを保持。メッセージ単位では管理しない設計判断は基本設計書6.2節）、新規API A-69（チャンネル既読化）・A-70（DM既読化）を追加し、A-05のjoined・A-16の各itemに`unread_count`（自分以外が送った・スレッド返信ではない・既読時刻または参加時刻より後の発言数）を追加した。フロントは`Layout.tsx`のサイドバーで未読のチャンネル/DM名を太字にし件数バッジを表示、`ChannelView`/`DmView`がマウント時・新着メッセージ受信時（`messages.length`変化）に既読化APIを呼ぶ。バッジをリアルタイムに追従させるため`useChannels`/`useDms`を3秒間隔ポーリング化した（従来は`useSWR`の既定挙動のみで自動更新されなかった）（2026-08-27）。
  - 実装済み画面（S-01〜S-06, S-08）の見た目を画面モックアップに合わせて統一（機能追加は無し、CSS/Tailwindクラスのみの変更）。`frontend/src/index.css`に`@theme`でモックアップの配色トークン（accent/surface/line/ink/member/chadmin/admin/bot/ok/off/danger の各色）を定義し、以後は`bg-slate-*`等のTailwind既定色ではなくこれらのトークンを使う。`lib/avatarColor.ts`で発言者・DM相手・利用者ごとに色を固定するハッシュ関数を共通化（Layout・MessageList・ThreadPanel・ChannelSettings・AdminConsole・DmPickerModalで共有）。あわせて`h-[calc(100vh-3.5rem)]`という誤った高さ指定（Layoutのmainに実際には無い3.5rem分のトップバー分を引いていた）を`h-full`に修正した（ChannelView/DmView/SearchView/ChannelSettings/AdminConsoleの5箇所）。ブラウザでの見た目は未確認（`tsc -b`・`vite build`のみで検証）（2026-08-27）。
  - 補足03 メンバー一覧（`MembersModal.tsx`）を追加し、S-03ヘッダーの参加人数（`channel.member_count`）をクリックして開けるようにした。chadmin/adminバッジ・無効化アカウント表示のため、A-46（参加者一覧）のレスポンスに`role`・`is_active`を追加。非公開チャンネルのみ「＋メンバーを追加」を表示し、これに伴いA-08（参加）を拡張して**非公開チャンネルの招待フロー（既存参加者がuser_id指定で追加）を実装**した（従来は公開チャンネルの自己参加のみだった欠けを解消）。非参加者からの招待呼び出しは404（存在を伏せる）、招待時の入室通知（F-43）は追加された本人の氏名を使う。管理者（chadmin）の変更はここでは行わずS-06に一本化（詳細設計書API設計v1.6）（2026-08-27）。
  - `MessageList`に画面モックアップと同じ日付区切り（「2026年8月19日」＋左右の罫線）を追加。前のメッセージと日付（現地時間）が変わったタイミングで自動的に挿入する。S-04スレッド返信欄には表示しない（`showDaySeparators={false}`、モックアップに合わせた挙動）（2026-08-27）。
  - F-41 @メンションの入力補助を2点追加（`Composer.tsx`）。(1) 全角「＠」もIME入力時に紛れやすいため半角「@」と同様にメンション候補ポップオーバーの起動文字として扱う。(2) 入力欄下のツールバーに画面モックアップS-03の`mention-btn`に相当するメンションボタンを追加し、押下でカーソル位置に「@」を挿入してポップオーバーを開く（直前が空白でない場合は半角空白を補ってから挿入し、`detectMentionQuery`の起動条件を満たす）。いずれもフロントの入力検知のみの変更で、A-11/A-14のリクエスト形・T-07のデータ形は変わらないためAPI設計書側の改訂は無い（2026-08-27）。
  - F-41 @メンション。新規T-07 message_blocks（block_type='mention'のみ実際に作成。citation/external_system/quote_reference/pending_actionはAIサポート未実装のため対象外）と共通処理`backend/mentions.py`を追加し、A-11（`channels.py`）・A-14（`messages.py`）の両方から呼び出す。投稿と同一トランザクションで、リクエストの`mentions`のうち当該チャンネルの参加者（A-46で判定）であるものだけをT-07へ保存し、参加者でないtarget_user_idは黙って除外する（対象外の指定を理由に投稿自体を拒否しない）。A-10・A-13は`fetch_blocks_grouped`でN+1を避けつつ`blocks`を一括付与。DM発言（元発言がDMのスレッド返信を含む）はメンション候補元（A-46）が存在しないため常に`blocks`空、`mentions`を送っても無視する。フロントは`Composer.tsx`を全面書き換えし、「@」入力でチャンネル参加者（有効アカウントのみ）を絞り込むオートコンプリート（↑↓/Enter/Tab/Escapeのキーボード操作、選択後は本文中の`@氏名`テキストが手動削除されたらメンションとして送らない整合性チェック付き）を実装。`MessageList`の`renderMessageBody`が本文中の`@display_name_snapshot`をハイライト表示し、`target_user_id`をチャンネル参加者一覧で解決した現在の表示名で描画する（A-62プロフィール編集が未実装のため実際には常にsnapshotと現在名は一致する）。AIへのメンション検知・自動応答トリガー判定との連携はAIサポート未実装のため対象外。詳細設計書API設計v1.7（2026-08-27）。
  - プロフィール編集（F-37 画像アップロード・F-39 表示名変更・F-40 プロフィール確認、補足06）。新規A-61（`routers/icons.py`、`POST/GET /api/icons`）・A-62（`PUT /api/users/me`）・A-67（`GET /api/users/{id}`、いずれも`routers/users.py`）を追加。A-61は本番はSupabase Storageを想定する設計（基本設計書2.2節）だが、このスライスはDATABASE_URLと同じ考え方でローカル開発を優先し`backend/uploads/icons`へのディスク保存で代替した（`.gitignore`に追加。Koyeb実配備時は単一コンテナに永続ディスクの保証が無いためSupabase Storageへの置き換えが必要、という制約を明記）。アップロード（multipart/form-data）のパースに`python-multipart`が必要だったため`requirements.txt`に追加（インストール漏れで一時的にuvicorn --reloadが例外を吐いて無応答になったため導入時に判明）。A-62は`role`/`is_active`同様name/picture_urlいずれも省略可の部分更新（A-37と同じ実装パターン）。A-67は所属チャンネル等を含めない（非公開チャンネルの存在が間接的に漏れるのを避けるため。基本設計書5.21節「設計判断」）。フロントは`ProfileEditModal.tsx`（補足06。ヘッダーのユーザーメニューという設計だが、このアプリの実装はサイドバー下部のユーザー情報表示をその代替として開く）とA-61→A-62の順で呼ぶ保存フロー、`ProfileCard.tsx`（F-40。S-03の発言者アイコン・表示名クリックで開くポップオーバー、`useUserProfile`フックでA-67を取得）を新規追加し、`MessageList`・`ThreadPanel`（元発言ヘッダー）に組み込んだ。ProfileCardは設計書上はS-03専用だが、MessageListはS-03・S-04・DM会話の共通コンポーネントでありA-67に閲覧側の絞り込みが無いため、スレッド返信・DM会話でも自然に動作する（スレッドをDM発言にも対応させた際と同じ、設計の自然な拡張という位置づけ）。これによりF-41の「target_user_idを現在の表示名で解決する」ロジックが実際に意味を持つようになった（従来はA-62が無く常にdisplay_name_snapshotと現在名が一致していた）。詳細設計書05-2 API設計・05-3画面設計は既にA-61/A-62/A-67・ProfileCard/ProfileEditModalの仕様を記載済みだったため、ドキュメント側の追記は無い（2026-08-27）。
  - バグ修正: 上記プロフィール画像アップロードを実装した直後、チャンネル会話・スレッド・DMのアイコンがサイドバーとは異なり画像に反映されないことが判明した。原因はA-10/A-13/A-18のレスポンス（`_message_out`、`channels.py`/`messages.py`/`dms.py`）が発言者の`picture_url`を一切含んでおらず、`MessageList`の`Avatar`が常に色付き頭文字の丸アイコンしか描画していなかったこと（画面設計11.6節のAvatarコンポーネント定義は「メッセージ一覧」を含む全画面での画像フォールバック共有を明記しており、この点で実装が設計から漏れていた）。3ルーターの`_MESSAGES_SELECT`にJOIN先の`u.picture_url AS sender_picture_url`を追加し、投稿直後のレスポンス（`post_message`/`post_reply`）にも`user.picture_url`を含めるよう修正。BOT/AI発言は`sender_user_id`が無いためJOIN結果が自然にNULLになり、追加の分岐は不要だった。フロントは`Message`型に`sender_picture_url`を追加し、`MessageList`の`Avatar`が値があれば`<img>`、無ければ従来通り色付き頭文字にフォールバックするよう変更（`ThreadPanel`は同じ`Avatar`を再利用しているため追加対応不要）。curlで新規投稿・A-10一覧取得の両方に`sender_picture_url`が含まれることを確認した（2026-08-27）。
  - バグ修正: サイドバーDM一覧のアイコンも同様に画像が反映されていなかった。こちらはA-16（`GET /api/dms`）のレスポンス自体は元から`members[].picture_url`を含んでいたため、バックエンド側の抜けは無く、`Layout.tsx`のDM一覧描画がその値を使わず常に色付き頭文字を表示していたのが原因（会話ログのAvatarとは別の実装のため、直前の修正では直っていなかった）。画像があれば`<img>`、無ければ従来通り色付き頭文字にフォールバックするよう修正した（2026-08-27）。
  - バグ修正: A-04（`GET /api/auth/me`）が`id`を数値のまま返しており、他の全APIの「idは文字列」規約（総論2.4節）に反していた。フロントの`Me.id`型は`string`と宣言されているためコンパイルは通っていたが、実行時は数値だったため`===`比較が常にfalseになり、`me.id`と文字列idを比較する3箇所が無効化されていた: (1) `MessageList`の削除ボタン表示条件（一般ユーザーは自分の発言でも削除ボタンが出ず、admin判定のみ生きていたため「adminは他人の発言を削除できるが一般ユーザーは自分の発言も削除できない」ように見えていた）、(2) `AdminConsole`の管理者ロックアウト防止（自分自身の行のロール選択を無効化する処理が効いていなかった。バックエンド側のA-37の拒否は`user.id`同士の比較のため無事だったが、UI上は選択できてしまっていた）、(3) `DmPickerModal`のDM相手検索結果からの自分自身の除外。`routers/auth.py`の`me()`で`"id": str(user.id)`に修正し、curlで一般ユーザーの自己削除（200）・他人の発言削除の拒否（403）を再確認した（2026-08-27）。
  - F-35 送信予約（S-03・S-04・DM会話の投稿欄、補足04）。新規T-18 scheduled_messages（05-1 DB設計3.13節）を追加し、A-50（作成）・A-51（自分のpending一覧）・A-52（キャンセル）を新規`routers/scheduled_messages.py`に実装した。channel_id/dm_idはT-05と同じくどちらか一方必須、thread_parent_idは任意（スレッド返信の予約にも対応）。参加者チェックはA-11/A-19と同じ基準（非公開チャンネルの非参加者404・公開チャンネルの非参加者403）だが、channel_id/dm_idがURLパスではなくボディの値のため`require_channel_member`等の依存関数はそのまま使えず、A-12削除APIと同様に手動で再現した。実際の発言化は新規`services/scheduled_dispatcher.py`（FastAPI起動時からのasyncioバックグラウンドタスク、30秒間隔ポーリング）が`status='pending' AND scheduled_at<=now()`を検出し、`UPDATE...WHERE status='pending' RETURNING`で二重発言化を防ぎつつ`messages`へ`sender_type='human'`として直接INSERTする（基本設計書10章の設計判断どおり単一インスタンス運用が前提。`main.py`のlifespanで起動/停止）。**@メンションの構造化（T-07 message_blocks）はこのスライスの対象外**とした（T-18にmentions列を追加していないため、本文に「@氏名」と入力すること自体はできてもプレーンテキストのまま発言化され、ハイライト表示・参照解決はされない。要件定義書3.2節のファイル添付対象外と同種の制約として詳細設計書API設計4.9節に明記。**2026-08-28にmentions列を追加し対応済み、後述のF-35メンション対応の項を参照**）。フロントは`Composer.tsx`に送信日時ポップオーバー（🕐ボタン）を追加し、`scheduleTarget`（channel_id/dm_id/thread_parent_id）を渡した3箇所（ChannelView・DmView・ThreadPanel）すべてで動作する。設計書の「ComposerはchannelId/threadParentIdを直接受け取る」という想定とは異なり、このアプリの既存実装（onSendコールバック方式）に合わせて送信予約だけ`scheduleTarget`経由でComposerが直接A-50を呼ぶ形にした。新規`ScheduledMessagesModal.tsx`（補足04）・`useScheduledMessages`フック（A-51、3秒間隔ポーリング）を追加し、`Layout.tsx`のサイドバー上部に予約中バッジ（🕐、件数）を新設してモーダルを共有した。宛先名（チャンネル名／DM相手名）はA-51のレスポンスに含めず、既存の`useChannels`/`useDms`のキャッシュからフロント側で解決する（他のバッジ・一覧系APIと同じ「軽量API＋フロント側で名前解決」の考え方）。curlで作成・一覧・他人からのキャンセル拒否（404）・本人によるキャンセル（204→再キャンセルは404）・過去日時拒否（400）・channel_id/dm_id同時指定拒否（422）、および実際にディスパッチャが発言化する様子（約40秒後に`sender_type='human'`の通常発言としてA-10に現れ、pending一覧から消える）まで一通り確認した（2026-08-27）。
  - AIサポート基盤の初回スライス（基本設計書8章、詳細設計書AIサポート10章）。**チャンネルAIがメンション時に応答するところまでを実装し、ドキュメントQ&A（層2）・スキル・自動対応範囲分類・実行前確認・座席予約連携・コスト上限通知は次スライス以降に持ち越した**（下記スコープ参照。**スキル・引き継ぎ先は2026-09-01にF-12スキル登録として、自動対応範囲分類は2026-09-02にF-16として実装した。後述の該当項を参照**）。新規T-08 channel_ai_settings・T-13 ai_usage_logsを追加。新規`services/ai_client.py`（OpenAI APIクライアント・コスト計算。姉妹プロジェクトKeirekiの`ai_client.py`と責務は同じだが、モデル切替UIを設けず`AI_MODEL`環境変数のみで決定し、単価表も円/1000トークン単位にする意図的な相違点あり。「Keirekiからの意図的な相違点」参照）・新規`services/ai_agent.py`（応答生成本体）を追加し、新規`routers/ai_settings.py`にA-23（GET一括取得。T-08未作成の既存チャンネルはget-or-createで既定値補完）・A-24（有効/無効切替）・A-25（キャラクタ更新）・A-26（振る舞い定義更新）を実装（`require_channel_admin`で保護）。A-07（チャンネル作成）でT-08既定行を同時作成するようにした。トリガーはA-11（`channels.py`のpost_message）内で本文に「@{persona_name}」の文字列一致を検出したときのみ（AIメンションはF-41と異なりID参照化しない。基本設計書5.22節「設計判断」と同じ考え方）、`asyncio.create_task`で非同期起動しA-11自体は応答を待たずに返す（REQ-N-05・8.7節）。生成中はT-05へ`generation_status='generating'`の仮レコードを作成する方式（詳細設計書10.3節で確定済みの方式をそのまま採用）とし、`bot_display_name`/`bot_icon_url`列をBOT発言と共用してペルソナ名・アイコンをスナップショットする。トークン使用量・概算コストはT-13へ記録するが、T-14上限管理・80%到達通知（F-29後半）は対象外。フロントは`MessageList`のAvatarを「アイコンURL優先→種別ごとの既定表示」の順に並べ替え（AI発言のペルソナアイコン表示に対応、従来はsender_typeで固定表示していた）、生成中は画面モックアップと同じ点滅ドット＋「生成中…」を表示（`index.css`に`ai-typing-blink`キーフレーム追加）。S-06に3タブ（基本設定・キャラクタ・振る舞い定義）を追加し、`Layout.tsx`の設定サイドバー・`ChannelSettings.tsx`のタブ切替を`?tab=`クエリパラメータ方式に変更した（従来はチャンネル管理者タブのみでタブ切替UI自体が無かった）。
    - **このスライスのスコープ外**（`services/ai_agent.py`冒頭コメントに集約）: 反応モードは常にメンション時のみ（`reaction_mode`の編集UI自体が無く、常定'mention_only'のまま。「投稿に自ら反応」は未実装。**2026-09-02にF-15反応モード設定として実装した。後述の該当項を参照**）／Function Calling無し（`search_documents`・`get_seat_availability`とも文書索引・座席予約連携が未実装のため、プレーンな会話応答のみ）／スキル（T-11）・自動対応範囲分類（T-12）・引き継ぎ（F-17）・実行前確認（F-25）は対象外（`out_of_scope_policy`・`fallback_handoff_user_id`は列は存在するがロジックから未参照。**スキル・引き継ぎ先は2026-09-01にF-12スキル登録として配線した。自動対応範囲分類は2026-09-02にF-16として配線した。実行前確認は引き続き対象外**）／スレッド返信（A-14）内の@メンションは対象外（チャンネル本体の投稿のみ）／F-41のメンションピッカーの候補にチャンネルAIを含めていないため、利用者は手入力で「@{persona_name}」と書く必要がある。**2026-09-02にメンション候補一覧へチャンネルAIを追加し解消した。後述の該当項を参照**。
    - **重要: 実際のOpenAI API呼び出しは未検証**（`OPENAI_API_KEY`が開発環境に設定されていないため）。`services/ai_agent.maybe_trigger`が`is_configured()`で早期returnし何もしないこと、A-23〜A-26のCRUD・権限ガード（403/get-or-create）、メンション検知後にAPIキー未設定時は何も起きないこと、まではcurlで確認済み。実際に応答が生成されるところ（OpenAI API呼び出し・`generation_status`の遷移・T-13記録）はローカルの`.env`に`OPENAI_API_KEY`を設定してから確認する必要がある。**2026-09-02に千田氏から交付されたAPIキーを設定し実機検証済み。後述の「AI API実機検証」の項を参照**。
  - S-06「チャンネル管理者」タブにチャンネル名・説明文の編集、チャンネル削除を追加。チャンネル削除（A-09）は基本設計書の権限マトリクスに以前からAPI一覧のみ記載で未実装だった機能をbackfillし、チャンネル名・説明文の編集は要件定義書・基本設計書・API設計・画面設計・画面モックアップのいずれにも存在しなかった新規機能として追加した（新規API A-71 `PUT /api/channels/{id}`、`name`/`topic`いずれも省略可の部分更新、`name`はA-07作成時と同じ一意制約）。ユーザーからの明示的な要望による追加で、既存の「チャンネル管理者」タブに直接ぶら下げるだけの単純なCRUD拡張（設計上の選択肢が実質1つしかない）と判断し、検討資料は起こさず要件定義書（v1.19。権限マトリクスに新規行）・基本設計書（v1.17）・詳細設計書API設計（v1.10）・詳細設計書画面設計（v1.1）・画面モックアップ（S-06、v3.4）を同じコミットで改訂した。A-09（DELETE）は`channels`テーブルへのハード削除のみで、参加者・発言・メッセージブロック・送信予約・AI設定・AI利用ログ・既読状態はすべてON DELETE CASCADEで連動削除される（`database.py`の6つのFKすべてに設定済みだったため新規のCASCADE追加は不要だった）。スレッド返信のように`messages.thread_parent_id`が自己参照するケースも、親・子が同じ`channel_id`を持ち同一のカスケード操作で同時に削除されるため、FK違反は起きないことをcurlで実際に確認した（スレッド返信ありのチャンネルを削除して検証）。フロントは`ChannelSettings.tsx`に`ChannelInfoForm`（名前・説明編集）と危険操作セクション（削除対象のチャンネル名を再入力しない限り「削除する」ボタンが無効のまま。GitHubのリポジトリ削除と同種の確認パターン）を追加した（2026-08-27）。
  - 補足03「メンバー一覧」を「チャンネル情報」「メンバー」の2タブ構成にし、前者に説明・公開範囲・作成者・作成日時の確認（A-06, A-67）とチャンネルからの退出（新規A-72 `DELETE /api/channels/{id}/members/me`）を追加した。これも要件定義書・基本設計書・API設計・画面設計・画面モックアップのいずれにも記載がなかった新規機能で、検討資料は起こさず（前回のチャンネル名編集と同様、既存の補足03モーダルにタブを足すだけの単純な拡張と判断）各ドキュメントを同じコミットで改訂した（要件定義書v1.20・基本設計書v1.18・API設計v1.11・画面設計v1.2・画面モックアップv3.5）。退出はA-48（chadmin解除）と同じ「最後の管理者は操作できない」防止策を流用し、自分が当該チャンネルの最後のchadminの場合は400で拒否する（最後の1人は必ずchadminであるため、この判定だけで参加者ゼロ化も同時に防げることをcurlで確認済み: 非chadminの退出→成功、単独chadminの退出→拒否、もう1人chadminがいる状態での退出→成功、の3パターンを検証）。作成者名の解決には既存のA-67（`useUserProfile`）を再利用し、作成者が退出済みでも氏名を表示できることを確認した。フロントは`MembersModal`に`initialTab`プロパティを追加し、`ChannelView.tsx`のヘッダーでチャンネル名クリックは「チャンネル情報」タブ、参加人数ボタンクリックは「メンバー」タブでモーダルを開くよう使い分けた（`membersOpen`のbooleanステートを`membersModalTab: 'info' | 'members' | null`に置き換え）（2026-08-28）。
  - チャンネル管理者・システム管理者がメンバーをチャンネルから強制退出させる機能を追加（新規API A-73 `DELETE /api/channels/{id}/members/{userId}`）。前回のチャンネル情報タブ・自己退出（A-72）に続く新規機能で、こちらも検討資料は起こさず要件定義書（v1.21）・基本設計書（v1.19）・API設計（v1.12）・画面設計（v1.3）・画面モックアップ（補足03、v3.6）を同じコミットで改訂した。A-72（`/{channel_id}/members/me`）とA-73（`/{channel_id}/members/{userId}`）はパスが被りうる（FastAPIは`{userId}`をintで宣言しても、ルーティング自体は文字列一致でありint変換の失敗はルート未一致ではなく422になるため）。A-72を先に定義することで「me」という文字列がA-73側に誤って一致することを避けている（`channels.py`にコメントで明記）。実際にcurlで「me」宛のリクエストが正しくA-72に届き続けることを、A-73追加後に再確認した。権限guardは`require_channel_admin`（chadmin/admin。A-48・A-71・A-09と同じ）。対象が最後のチャンネル管理者の場合はA-48/A-72と同じ理由で400拒否し、chadmin同士でも実行できる（chadminという理由だけでは除外しない）。curlで4パターン確認: 非chadminによる強制退出の拒否（403）、chadminによる一般メンバーの退出（成功）、最後のchadminを対象にした拒否（400、システム管理者でも拒否）、もう1人chadminがいる状態での退出（成功）。フロントは`MembersModal`の「メンバー」タブの各行に「退出させる」ボタンを追加し、`(channel.is_channel_admin || me?.role === 'admin')`かつ対象が自分自身でない場合のみ表示する（自己退出は「チャンネル情報」タブの専用ボタンに一本化）（2026-08-28）。
  - F-43「チャンネル参加の通知」を「チャンネル参加・退出の通知」に改称し、A-72（自己退出）・A-73（強制退出）のいずれでも参加通知（F-43）と対になる退出通知「（氏名）さんが退出しました。」をT-05へ作成するようにした。参加通知と同じ`sender_type='bot'`・`bot_display_name='システム通知'`の枠組みをそのまま流用（新規テーブル・列は追加なし）。強制退出であっても本文に退出させた側の氏名は含めない（参加通知が招待者を書かないのと同じ一貫性、「誰が誰を退出させたか」という機微な情報を公開しないための設計判断として基本設計書6.2節に追記）。`leave_channel`・`remove_channel_member`とも、削除と通知作成を`conn.transaction()`で1つのトランザクションにまとめた（従来`leave_channel`はトランザクションを使っていなかったため、この対応であわせてトランザクション化した）。curlで自己退出・強制退出の両方でシステム通知（BOTバッジ付き）が会話ログに現れることを確認済み。要件定義書・基本設計書・API設計・画面モックアップ（S-03に退出通知の発言例を追加）を同じコミットで改訂した（2026-08-28）。
  - A-08（`POST /api/channels/{id}/members`）を拡張し、公開チャンネルでも既存の参加者が他の利用者をuser_id指定で招待できるようにした（従来は招待は非公開チャンネルのみで、公開チャンネルは本人の自己参加のみだった）。ユーザーからの明示的な要望による追加で、非公開チャンネルの招待ロジック（A-08の既存の`else`分岐）と全く同じ考え方を公開チャンネル側にも広げるだけの単純な拡張と判断し、検討資料は起こさず要件定義書（v1.23。権限マトリクスの行を「非公開チャンネルへのメンバー追加」から「チャンネルへの他の利用者の追加（招待）」に改称）・基本設計書（5.14節、v1.21）・詳細設計書API設計（v1.14）・詳細設計書画面設計（補足03項目定義、v1.4）・画面モックアップ（補足03、v3.8）を同じコミットで改訂した。`channels.py`の`join_channel`で、公開チャンネル向けの分岐を「target_user_id省略または自分自身→常に自己参加可」「他者のuser_id指定→呼び出し元が既存参加者であることを要求し、そうでなければ403（メッセージ「他の利用者を追加するには、先に自分がこのチャンネルに参加している必要があります」）」の2パターンに分けた。フロントは`MembersModal.tsx`の「＋メンバーを追加」ボタンの表示条件`!channel.is_public`を削除して全チャンネルで表示するようにし、ヒント文言を「公開チャンネルは招待と自己参加の両方が使える」旨に書き換えた（新規UIコンポーネントは追加せず、既存の非公開チャンネル向け検索・追加フローをそのまま再利用）。curlで3パターン確認: 公開チャンネルの既存参加者による他者の招待（201・入室通知は招待された本人の氏名）、非参加者による他者の招待の拒否（403）、自己参加は従来どおり成功（201）（2026-08-28）。
  - S-03・DM会話に「ここから未読メッセージ」区切り線を追加した。ユーザーからの明示的な要望による追加で、既存の未読バッジ（A-05/A-16のunread_count、T-22 read_states）をフロントエンドのみで区切り線の位置に変換して表示する方式とし、新規API・DB列は追加していない（未読バッジ自体が要件定義書上のF-xxに対応付けられていないため、要件定義書の改訂も無い。基本設計書6.2節に設計判断を追記、v1.22）。新規`hooks/useUnreadDivider.ts`を追加し、`conversationId`ごとに会話を開いた瞬間の`unread_count`を1回だけ記録する（React公式の「レンダー中にstateを調整する」パターンを使い、`useEffect`を介さないため`react(set-state-in-effect)`警告を回避）。表示中に既読化ポーリング（A-69/A-70）で`unread_count`が0に戻っても、この記録値は画面を開いている間ずっと固定され、区切り線の位置が動かない（Slack等の「新着メッセージ」区切り線と同じ考え方）。区切り線を出す対象メッセージは、記録した件数だけ`messages`配列の末尾から遡り、`sender_user_id !== me.id`の発言（本体タイムラインのみで元からスレッド返信は含まれない）を数えて特定する。A-10/A-18が直近50件のみを初期読み込みする制約上、未読件数が読み込み済みの範囲を超える場合は読み込み済みの先頭に区切り線を出すフォールバックを入れた。`ChannelView.tsx`は`useChannels()`の`joined`一覧、`DmView.tsx`は`useDms()`の`dms`一覧から該当チャンネル/DMの`unread_count`を引いて渡す（いずれも既存のポーリングキャッシュを流用し、追加のAPI呼び出しは無い）。`MessageList.tsx`に`unreadDividerMessageId`propを追加し、既存の日付区切り線（`DaySeparator`）と同じ描画パターンで`UnreadDivider`（accent色）を表示する。ブラウザでの見た目は未確認（`tsc -b`・`oxlint`・`vite build`のみで検証、フロントエンドのみの変更でバックエンドAPIは無変更のためcurl検証も対象外）（2026-08-28）。
  - F-43のシステム通知（「（氏名）さんが参加/退出しました。」）に対する返信（A-14）・削除（A-12）を対象外にした。ユーザーから「システム通知への返信・削除機能はない方がよいのでは」との指摘を受けての追加で、参加・退出の記録として信頼できる状態を保つ（返信スレッドが付いたり管理者の裁量で消えたりしない）ことを優先する判断に同意し実装した。フロント（`MessageList.tsx`）は`m.sender_type === 'bot' && m.sender_name === 'システム通知'`の行では返信・削除ボタン自体を出さない。REQ-N-02（権限判定はサーバー側で行う）に従い、バックエンド側（`routers/messages.py`）にも同じ判定をbot_display_name基準で追加し、`delete_message`（A-12）・`post_reply`（A-14）とも対象がシステム通知の場合は400（「システム通知は削除できません」「システム通知には返信できません」）で拒否する。F-36定期投稿・F-38自動応答トリガーは内容のあるBOT発言であり返信・削除の需要があるため対象外にしない（`sender_type='bot'`一律ではなく`bot_display_name`で狭く判定する）。curlで4パターン確認: システム通知への返信拒否（400）、adminによるシステム通知削除拒否（400、admin特例も効かない）、通常の人間発言への返信は従来どおり成功（201）、投稿者本人による通常発言の削除は従来どおり成功（200）。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書の改訂は無い。基本設計書（6.2節に設計判断を追記、v1.23）・詳細設計書API設計（A-12・A-14の説明を更新、v1.15）を同じコミットで更新した（2026-08-28）。
  - F-35 送信予約に@メンション（F-41）を対応させた。ユーザーからの明示的な要望（「メンションしながら予約投稿できるようにしたい」）による追加で、T-18 scheduled_messagesに新規`mentions`列（JSONB、`MentionInput`相当の配列）を追加した。参加者検証はA-11/A-14と異なり予約作成時点（`routers/scheduled_messages.py`の`create_scheduled_message`）では行わず、`mentions`をそのままJSONBへ保持するだけにとどめ、実際に発言化するタイミング（`services/scheduled_dispatcher.py`）まで遅らせてから`insert_mention_blocks`（`mentions.py`、既存のF-41共通処理をそのまま再利用）を呼び出す設計にした。理由は、予約から送信までの間に対象者がチャンネルを抜ける可能性があり、A-11/A-14が「投稿（発言化）時点の参加者であること」を基準にするのと同じ考え方を送信時点に揃えるため（基本設計書6.2節「設計判断」に追記）。DM宛ての予約はA-11/A-14と同じくメンション候補元（A-46）が存在しないため`mentions`を送っても無視する（channel_idがNULLの場合はmentions_to_storeを空にしてから保存）。フロントは`Composer.tsx`の`@`オートコンプリート自体は元々スケジュール中の入力でも動作していた（`mentionCandidates`propがテキストエリア共通のため）ため新規UIは追加せず、`confirmSchedule`（A-50呼び出し）にも`activeMentionsIn(text)`（`send()`と共通化した、本文から手動削除されたメンションを除外するフィルタ）で絞り込んだ`mentions`を含めるようにしただけ。curlで3パターン確認: チャンネル宛て予約（mentions付き）が約40秒後に`sender_type='human'`として発言化され、`blocks`に`block_type='mention'`が正しく付与されること（A-10と同じ形）、DM宛て予約はmentionsを指定しても201で受理されつつ発言化後も`blocks`が常に空であること、既存の即時送信（mentions無し・通常のsend()経路）が引き続き正常に動作すること。要件定義書上のF-xxに対応付けられた変更ではない（F-35自体は要件定義書に記載があるが、この拡張は詳細設計書側のみの改訂）。基本設計書（5.15節・6.2節、v1.24）・詳細設計書DB設計（T-18.mentions列、v1.3）・詳細設計書API設計（A-50、v1.16）を同じコミットで更新した（2026-08-28）。
- バグ修正: `start.bat`をエクスプローラーからダブルクリックで起動すると、`.env`で指定した`BACKEND_PORT`/`FRONTEND_PORT`が反映されず、常にハードコードされた既定値（8011/5174）にフォールバックする不具合が発生していた（ユーザーからの報告を受けて調査）。原因は`chcp`未指定のコンソールが日本語Windowsの既定コードページ（932、Shift-JIS）で起動する一方、`start.bat`・`.env`はいずれもUTF-8（日本語コメントを含む）であり、cmd.exeがUTF-8のバイト列をコードページ932として誤読することで、`.env`の`for /f`読み込み（`BACKEND_PORT`/`FRONTEND_PORT`の行）が正しく行われなくなっていたこと。PowerShellから`chcp 932`で同条件を再現し、`start.bat`冒頭（`@echo off`の直後、他のいかなる非ASCII文字より前）に`chcp 65001 >nul`を追加することで解消することを確認した（`.bat`ファイル自体の日本語コメント行も含め、コードページ切替より前に読まれる行はすべて影響を受けるため、切替は非ASCII文字が一切ないと保証できる最初の行に置く必要がある。切替後にコンソールが元々UTF-8だった場合の非回帰も確認済み）。DB_PORT・DEV_AUTH等、`.env`ファイル内で該当行に物理的に近い日本語コメントが少ない変数は本不具合の影響を受けにくく、症状が`BACKEND_PORT`/`FRONTEND_PORT`のみに見えたため原因特定に時間を要した（2026-08-28）。
- バグ修正: ローカルでのGoogle認証（総論2章、A-01/A-02）が`redirect_uri_mismatch`で失敗する不具合を、実際にGoogleへログインを試す前に発見・修正した（ユーザーから千田氏の手順書に基づく設定確認を依頼された際に、実際のcurl検証で発覚）。`google_auth.py`の`redirect_uri_for()`はリクエストの`Host`ヘッダー（`X-Forwarded-Host`優先）からコールバックURLを動的に組み立てる設計だが、ローカルの2段構成（ブラウザ→Vite:FRONTEND_PORT→FastAPI:BACKEND_PORT）では、`vite.config.ts`の`/api`プロキシが文字列省略形（`'/api': 'http://localhost:${backendPort}'`）のままだと、Viteの既定`changeOrigin: true`によりバックエンドへ転送する際に`Host`ヘッダーが転送先（`localhost:{BACKEND_PORT}`）へ書き換えられてしまい、`redirect_uri_for()`が誤って`http://localhost:8010/...`（バックエンドのポート）を組み立てていた。Google Cloud Consoleにはブラウザが実際にアクセスするフロントエンドのポート（`http://localhost:5173/api/auth/callback`）で登録されているため、このままでは一致せず認証が失敗する状態だった。`vite.config.ts`のプロキシ設定をオブジェクト形式に変更し`changeOrigin: false`を明示することで、転送時も元の`Host`ヘッダー（フロントエンドのポート）を維持するよう修正した。`curl`で`/api/auth/login`のLocationヘッダーに含まれる`redirect_uri`パラメータを確認し、修正前は`backend_port`、修正後はアクセスしたフロントエンドのポートが正しく入ることを確認済み（2026-08-28）。**実際のGoogleアカウントでの認証完了（同意画面の操作等）はブラウザでの手動操作が必要なため未検証**。
- バグ修正: ローカルでのGoogle認証で、リダイレクトURI不一致（上記）を解消した後も`Googleでログイン`が「ログインに失敗しました。もう一度お試しください。」で失敗する不具合を発見・修正した。原因はGoogleが発行したIDトークンの`iat`（発行時刻）検証で、PyJWTの既定`leeway=0`がローカルマシンとGoogle側のわずかな時刻のずれ（NTP同期済みでも数秒程度は生じうる）を一切許容せず、`jwt.exceptions.ImmatureSignatureError: The token is not yet valid (iat)`として正当なトークンまで拒否していたこと。原因調査のため`routers/auth.py`のA-02コールバック処理（state照合失敗・トークン交換/検証失敗の各分岐）に恒久的なログ出力を追加し（`oauth_failed`はフロントに詳細を返さない設計のため、原因追跡にはサーバー側ログが必須と判断）、実際にこのログから`ImmatureSignatureError`を特定した。`google_auth.py`の`verify_id_token`（`jwt.decode`呼び出し）に`leeway=60`（60秒の許容）を追加して解消し、実際にGoogleアカウントでのログインが完了することをユーザー自身のブラウザ操作で確認した（2026-08-28）。
- F-36 定期投稿・F-38 自動応答トリガー（S-06「定期投稿」「自動応答トリガー」タブ）。設計自体は基本設計書5.16節・5.19節、詳細設計書DB設計3.14/3.16節・API設計4.10節に既に完成していたため、その仕様どおりに実装した（検討資料は起こしていない）。新規T-19 recurring_posts・T-21 trigger_rulesを追加し、新規`routers/recurring_posts.py`（A-53〜A-56）・`routers/trigger_rules.py`（A-63〜A-66）を実装（いずれも`require_channel_admin`で保護）。定期投稿の発言化は既存の`services/scheduled_dispatcher.py`に合流させた（F-35と同じ30秒間隔ポーリング、`UPDATE...WHERE...RETURNING`によるアトミックな「取得と同時に次の状態へ進める」パターンを流用。recurring_postsには使い捨てのstatus列が無く「削除するまで繰り返し対象であり続ける」性質のため、'once'は`is_active=false`、それ以外は`next_run_at`を次回時刻へ進める形でこのパターンを再現した）。頻度計算（`_next_run_after`）は daily=+1日、weekly=+7日、monthly=起点(anchor_at)の日にちを基準に翌月へ、該当日が無ければ月末に繰り下げ（例: 1/31起点→2/28→3/31、月をまたいでも起点の日にちに戻ることをPythonで直接検証済み）。自動応答トリガーの判定は新規`services/trigger_matcher.py`が担い、A-11（`channels.py`のpost_message）から同期的に呼び出す（DB1件挿入のみで完結しレイテンシを気にする必要がないため、AI応答のような非同期化はしない）。F-41のAIメンショントリガーと同じ理由でA-11のみを判定対象とし、スレッド返信（A-14）は対象外（画面モックアップの「スレッド内であれば同じスレッドへ」という記述は、A-11がthread_parent_idを持たない以上このスライスでは発生しない分岐であることを基本設計書6.2節に設計判断として明記した）。`messages.recurring_post_id`/`trigger_rule_id`（既存列）に`ON DELETE SET NULL`のFKと、両者が排他であることを示すCHECK制約を新たに付与した（ルール削除後も送信済み発言は本文・アイコンとも残り、参照だけが外れる。基本設計書6.2節に設計判断を追記）。あわせて、BOT発言の送り主アイコン（`bot_icon_url`優先、次点で`bot_icon`絵文字）が`_message_out`（`channels.py`・`messages.py`・`dms.py`の3箇所）のレスポンスに含まれておらず常に🔔表示に固定されていた既知の抜け（5.16/5.19節に「アイコン未実装」と明記されていた制約）をbackfillし、`MessageList.tsx`の`Avatar`・`Message`型にも反映した（F-43システム通知は`bot_icon`が常にnullのため表示は変わらない）。フロントは新規`hooks/useRecurringPosts.ts`・`useTriggerRules.ts`と、`ChannelSettings.tsx`に`RecurringPostsTab`・`TriggerRulesTab`（新規作成・編集を同じフォームで扱い、一覧の「編集」ボタンでフォームへ値を読み込む方式）・共有`IconInput`（絵文字直接入力とA-61画像アップロードの併用、画像優先）を追加し、`Layout.tsx`の設定サイドバーに「その他の設定」グループとして2タブを追加した。curlで一通り確認: トリガー作成→一致する発言でBOT自動返信（201・`sender_type='bot'`・`bot_icon`反映）→`is_active=false`で発火しなくなること→削除後は404、定期投稿作成（`frequency='once'`、35秒後）→ディスパッチャが実際に発言化し`is_active`が自動的にfalseになること→更新（頻度変更・再有効化）→削除後もメッセージ本体と`bot_display_name`/`bot_icon`は残り`recurring_post_id`のみNULLになること、チャンネル削除時にCASCADEでrecurring_posts/trigger_rulesとも連動削除されること、過去日時のanchor_atが400で拒否されること。要件定義書上のF-36/F-38は既に記載済みのため改訂は無い。基本設計書（6.2節に設計判断3件を追記、v1.25）・詳細設計書DB設計（T-19・T-21・T-05の列注記、v1.4）・詳細設計書API設計（A-53〜A-56・A-63〜A-66の削除挙動・既定値を明記、v1.17）を同じコミットで更新した（2026-08-28）。
- バグ修正: S-08利用者管理（`AdminConsole.tsx`）の氏名の横のアイコンが、会話ログ・サイドバー等と異なり常に色付き頭文字表示に固定されていた。以前修正した会話ログ・サイドバーDM一覧のアイコン反映漏れ（picture_url未使用）と同種の問題で、この画面は個別に見落としていた。原因はA-36（`GET /api/admin/users`）のSELECTに`u.picture_url`が含まれておらず、レスポンス自体に画像URLが存在しなかったこと（フロント側の描画ロジックの問題ではなかった点が過去2件とは異なる）。`routers/admin.py`のSELECT・レスポンス整形に`picture_url`を追加し、`AdminUser`型にも反映。フロントは画像があれば`<img>`、無ければ従来どおり色付き頭文字にフォールバックするよう`AdminConsole.tsx`を修正した（他画面のAvatarと同じ「画像優先→フォールバック」の考え方）。curlで確認: A-61でアイコンをアップロード済みの利用者は`picture_url`が返り、Google認証でログインした利用者はGoogle側のプロフィール画像URLがそのまま入り、未設定の利用者は`null`のままであることを確認した（2026-08-28）。
- バグ修正: `ProfileCard`（F-40、発言者アイコン・表示名クリックで開くプロフィールカード）が、画面下寄りの発言（特に一番下の発言）でクリックすると投稿欄（Composer）の裏に隠れてしまい見えづらい不具合を修正した。原因は`ProfileCard`が常にクリックした発言行を基準に`absolute left-5 top-9`で下向きに配置される作りだったため、メッセージ一覧の`overflow-y-auto`スクロール領域の下端を越えた分がクリップされ、実質的にComposerの裏に隠れて見えなくなっていたこと。`ProfileCard`に任意の`anchor`（クリックした要素の`getBoundingClientRect()`）propを追加し、指定時は`createPortal`で`document.body`へ描画した上で`position: fixed`により画面座標で配置するよう変更した。`anchor.bottom`から見積もりカード高さ（200px）を足した位置が画面下端を超える場合は自動的に上開き（`bottom`指定）に切り替える。`MessageList.tsx`はアイコン・表示名クリック時のイベントから`anchor`を取得して渡すよう変更し、`ThreadPanel.tsx`の元発言ヘッダー（常に上部にあり同じ問題が起きない）は`anchor`省略のまま従来の相対配置を維持している（`anchor`未指定時は既存の描画パスに変更なし）。フロントエンドのみの変更でバックエンドAPIは無変更。ブラウザでの実際の見た目は未確認（`tsc`・`oxlint`・`vite build`のみで検証）（2026-08-28）。
- F-07 ファイル共有（S-03・S-04・DM会話の投稿欄、A-20横断検索のファイル検索）。要件定義書・基本設計書5.4節・T-06 message_attachments・A-21/A-22は既に表レベルで定義済みだったため、その仕様に沿って実装した（検討資料は起こしていない）。新規`backend/attachments.py`（`AttachmentInput`・`insert_attachments`・`fetch_attachments_grouped`、`mentions.py`と同じ構成）と新規`routers/attachments.py`（A-21アップロード・A-22ダウンロード）を追加。T-06.message_idはNOT NULLのため、A-21はアップロード時点ではDB行を作らずファイル実体を`backend/uploads/attachments`へ保存する（A-61アイコンと同じくSupabase Storageの本番代替。ローカル開発優先の判断は既存踏襲）のみで、`file_name`/`byte_size`/`storage_path`を返す。クライアントはこれを保持し、実際の投稿（A-11 `channels.py`・A-14 `messages.py`・A-19 `dms.py`）で`attachments`として渡すことで、発言作成と同一トランザクション内でT-06へ書き込む（F-41のメンションがT-07へ書き込むのと同じ「確定後に紐づける」パターン）。ダウンロード（A-22）は`require_thread_access`を`Depends()`を介さず直接呼び出して添付先発言の参加者チェックを再利用し（非公開チャンネル非参加者404・公開チャンネル非参加者403・DM非参加者404）、`FileResponse`の`media_type`を`application/octet-stream`に固定してブラウザでのインライン実行を防いだ。A-20（`search.py`）はこれまで`counts.file`を常に0としていたが、`message_attachments`を`messages`にJOINし`file_name`へILIKE判定する形で実装し、in/with/from/日付系の条件組み立て（`_build_conditions`）をメッセージ検索と共有した。フロントは新規`uploadAttachment`（`lib/api.ts`）、`Composer.tsx`に📎ボタン・アップロード中表示・添付チップ（削除可）を追加し、送信予約（`confirmSchedule`）では添付があれば明示的にエラーを出して拒否する（要件定義書3.2節の対象外制約どおり）。`onSend`コールバックの型に`attachments`を追加し、`ChannelView`・`DmView`・`ThreadPanel`の3箇所で送信時に渡すよう更新した。`MessageList.tsx`に`AttachmentList`（📎アイコン＋ファイル名＋サイズのダウンロードリンク、`/api/attachments/{id}`を直接リンクしCookie経由でA-22の認可を通す）を追加。`SearchView.tsx`もメッセージ・ファイルの2セクション表示に対応した。curlとPython httpx（日本語ファイル名はGit-Bashの引数エンコード崩れを避けるため）で一通り確認: アップロード→チャンネル投稿→A-10一覧に反映→ダウンロード成功（RFC5987の`filename*=utf-8''...`）→非参加者はダウンロード403、スレッド返信（A-14）・DM投稿（A-19）でも同様に添付が機能すること、A-20検索でメッセージ・ファイルの両方（日本語ファイル名含む）がヒットしcounts.fileが実件数を返すこと、チャンネル削除時にCASCADEで添付も連動削除されること。基本設計書（5.4節・6.2節に設計判断を追記、v1.26）・詳細設計書DB設計（T-06、v1.5）・詳細設計書API設計（A-21・A-22・A-20ファイル検索、v1.18）・画面モックアップ（S-03に添付ファイルの表示例、v3.10）を同じコミットで更新した（2026-08-28）。
- バグ修正: `MessageList.tsx`の`Avatar`が、BOT発言（F-36定期投稿・F-38トリガー・F-43システム通知）でも画像アップロード済みなら人間・AIと同じ円形（`rounded-full`）で表示していたため、BOTかどうかがアイコンの形からは判別できていなかった（絵文字のみのBOTは`rounded-[9px]`の角丸四角で表示されており、画像有無で見た目の一貫性が崩れていた）。原因は`sender_picture_url`の有無を種別を問わず最初にチェックする分岐順だったこと（BOT発言も`sender_type in ("ai", "bot")`のとき`bot_icon_url`が`sender_picture_url`に入る、`channels.py`等の`_message_out`の既存仕様）。`sender_type === 'bot'`の判定を先頭に移し、その中で画像（`sender_picture_url`）→絵文字（`bot_icon`）→🔔の優先順位はそのまま維持しつつ、どちらも角丸四角の同じ枠内に収めるよう修正した（ユーザーからの明示的な要望）。S-06の定期投稿/トリガータブのプレビュー（`rounded-[7px]`/`rounded-[5px]`）は元々画像・絵文字とも角丸四角で統一されており対象外。バックエンドAPIは無変更のためcurl検証は対象外（`tsc`・`oxlint`・`vite build`のみで検証）（2026-08-28）。
- UX改善: S-06「定期投稿」「自動応答トリガー」タブで、既存項目の編集画面と新規追加画面が同一フォームの使い回し（見出し・ボタン文言のみ「編集」/「追加」で切り替え）だったため、既存作成済みのものを編集しているのか2個目以降を新規作成しているのか見分けにくいというユーザー指摘を受けて改善した（`ChannelSettings.tsx`）。編集は`ProfileEditModal.tsx`等で確立済みのモーダルパターン（`fixed inset-0`のオーバーレイ＋カード、ヘッダーに✕、フッターにキャンセル/更新するのボタン対）を流用した新規`RecurringPostEditModal`/`TriggerRuleEditModal`のポップアップに切り出し、一覧末尾の常設パネルは新規作成専用（`editing`状態を持たず常にPOST、見出し・ボタン文言も固定）にした。フォーム項目のJSX自体は入力欄が多く（送り主表示名・アイコン・本文・頻度/日時、またはトリガー種別/値・本文・送り主表示名・アイコン）作成パネルと編集モーダルの二重管理を避けたかったため、新規`RecurringPostFormFields`/`TriggerRuleFormFields`（状態を持たない、値とonChangeをpropsで受け取るだけの表示専用コンポーネント）に切り出し、既存の共有`IconInput`と同じ考え方で作成パネル・編集モーダルの両方から呼び出す。編集モーダルの初期値は`item`（対象行）から`useState`の初期値として直接設定し（`useEffect`を介さない、`startEdit`関数が担っていた責務をモーダルのマウント自体に置き換えた形）、対象行の削除時（`remove`）は編集モーダルを開いたままにしないよう`editingItem`をクリアするようにした。新規API・DB変更は無く、画面のみの変更のため要件定義書・基本設計書・詳細設計書・画面モックアップの改訂は無い。バックエンドAPIは無変更のためcurl検証は対象外（`tsc -b`・`vite build`のみで検証。oxlintはこのマシンのApplication Control policyがネイティブバインディングの読み込みをブロックし実行不能だったため今回は省略）（2026-08-31）。
- 層2ドキュメントQ&A（F-19〜F-22）の最初のスライス。実装量が大きく、かつGoogle OAuthスコープ拡張（drive.readonly相当。既存ユーザーへの再同意が発生する）とOpenAI Embeddings APIの実通信検証が前提になることから、ユーザーに着手範囲を確認したうえで「Drive同期・索引・AI検索が不要な部分（フォルダ候補の登録・チャンネルへの割当）」に絞って実装した（索引・埋め込み生成・search_documentsツール・引用表示は次スライス）。新規T-09 doc_folders・T-10 channel_doc_foldersを追加（既存の05-1 DB設計3.9節どおりの設計、スキーマ変更は無し）。S-08に新規タブ「ドキュメント参照範囲」（A-38 GET一覧・A-39 POST追加・A-40 DELETE削除）を追加し、`AdminConsole.tsx`を`?tab=`クエリパラメータ方式のタブ構成（従来はタブ切替UI自体が無く利用者管理のみを常時表示していた）に変更した。フォルダ候補の追加はGoogle Picker（画面設計8.2節が想定するDriveのフォルダ選択画面）ではなく、フォルダのURL・IDを手動貼り付けする簡易フォームとした（Drive OAuthスコープ拡張はA-38〜A-41実装時点でもまだ不要にできたが、Picker連携自体は次スライス）。URL形式の場合は`routers/admin.py`の`_extract_folder_id`が正規表現でIDを抽出する。「今すぐ同期」（A-41）はDrive同期・索引化が未実装のため、S-08側でボタンを無効表示にするに留め、バックエンドにもエンドポイントを実装していない。S-06に新規タブ「参照ドキュメント範囲」（既存A-27 PUT doc-scope）を追加し、`DocScopeTab`（`folder_ids`のチェックボックス選択＋`out_of_scope_policy`のラジオ）を実装した。A-27は送信された`folder_ids`の集合でT-10を洗い替え、存在しないfolder_idは黙って無視する（F-41のメンション対象外指定と同じ考え方）。A-23（GET ai-settings）のレスポンスにも`out_of_scope_policy`・`folder_ids`を追加した（従来はドキュメントQ&A未実装のため意図的に省略していた）。**A-38（GET）はS-06でチャンネル管理者（chadminは多くの場合グローバルにはmember）が候補一覧を選択に使う必要があるため、設計時点のadmin限定からrequire_auth（認証済み全員）へ閲覧範囲を広げた**（フォルダ名・登録者名・使用中チャンネル数のみでチャンネル名等の非公開情報を含まないため情報漏えいにはならないと判断。登録・削除（A-39/A-40）は引き続きadmin限定。基本設計書6.2節に設計判断を追記）。curlで一通り確認: 一般member（admin/chadminいずれでもない）によるA-38取得成功（200、閲覧範囲を広げた効果）・同ユーザーによるA-39作成拒否（403、登録は引き続きadmin限定）、admin作成→URL形式からのID抽出・重複409、A-40削除→使用中チャンネルのfolder_idsからON DELETE CASCADEで自動的に外れること、chadmin（member権限のyamadaをA-47で昇格）によるA-27保存→無効なfolder_idの黙殺・out_of_scope_policyの不正値422まで確認した。基本設計書（6.2節に設計判断2件を追記、v1.27）・詳細設計書API設計（A-27・A-38〜A-40、v1.19）・docs/index.html（Ver 4.29）を同じコミットで更新した。要件定義書・画面モックアップは既存のF-19〜F-22・S-06・S-08の記載どおりで変更なし（2026-08-31）。
- F-42 検索条件モディファイアのオートコンプリートUIを実装した（`SearchView.tsx`）。S-05はこれまでフリーテキスト入力のみで、`in:`/`from:`/`with:`/`before:`/`after:`/`on:`/`during:`はバックエンド（`routers/search.py`）側は既に完全対応済みだったがフロント未実装だった抜けを埋めた（バックエンド・API設計6.5節はいずれも無変更）。`detectModifierQuery`はComposer.tsxのF-41 `detectMentionQuery`と全く同じ考え方（トリガー文字列の直前が空白/先頭であること、クエリに空白が入るまでが1トークン）で、`in:`/`from:`/`with:`の入力中に候補ポップオーバーを開く。候補は`in:`が`useChannels()`の参加チャンネル一覧（クライアント側で前方一致フィルタ）、`from:`/`with:`が既存のA-68 `/api/users?q=`（DmPickerModalと同じ200msデバウンス）。候補選択で本文中のトークンが`type:ラベル`の確定形に置き換わり、`resolved`状態（type→{id, label}）に記録される。検索実行時は`buildQuery`が本文中に確定時と同じ`type:ラベル`部分文字列が残っている場合のみそのモディファイアを有効とし（Composer.tsxの`activeMentionsIn`と同じ、選択後に手で消した場合の整合性チェック）、`before:`/`after:`/`on:`/`during:`は候補解決が不要なため正規表現でその場抽出、残りのフリーテキスト（`"..."`のフレーズ含む）を`q`とする。確定済みの条件はチップとして表示し✕で個別に外せる（外すと本文中のトークン文字列も連動して削除）。検索条件はURLのクエリパラメータ（`in`/`in_label`等、ラベルも保持）に反映し、ページ再読み込み・ブラウザの戻る/進むでも検索欄・チップの見た目を復元できるようにした（`composeInputText`/`composeResolved`が`buildQuery`の逆変換）。`useSearch`フックを`q`単一引数から構造化された`SearchQuery`（in/from/with/before/after/on/during）を受け取る形に拡張した。ドキュメント根拠タブ自体が層2未実装のため、F-42の「モディファイアがあるとドキュメント根拠タブを無効化する」挙動（画面設計6.4節）は対象外のまま。純粋な既存設計（詳細設計書API設計6.5節・画面モックアップS-05）どおりのフロントエンド実装であり新規API・DB変更は無いため、要件定義書・基本設計書・詳細設計書・画面モックアップの改訂は無い。バックエンドAPIは無変更のためcurl検証は対象外。パースロジック（`detectModifierQuery`/`buildQuery`/`composeInputText`/`composeResolved`）はNode上に複製した単体スクリプトで9パターン（トークン検出・複数モディファイアの組み立て・手動削除時の整合性・URLとの往復一貫性・フレーズ透過・二重指定時の片方のみ解決）を確認した上で削除し、`tsc -b`・`vite build`で検証した（oxlintはこのマシンのApplication Control policyにより今回も実行不能）（2026-08-31）。
- F-14（やりとりの要約）を実装した。要件定義書・画面モックアップには「要約ボタンを押した時点までの内容を要約する」とのみ記載があり、結果の見せ方（依頼者本人だけに表示するか、チャンネルに投稿するか）が未確定だったため、着手前にユーザーに確認した（チャンネルへのAI発言として投稿を選択）。`services/ai_agent.py`にAIメンション応答（`_generate_and_post`）と全く同じ枠組みを再利用する`start_summary`/`_generate_summary_and_post`を追加し、共通処理（発言者名解決・T-05行→Chat Completions messages変換）を`_resolve_sender_names`/`_rows_to_chat_messages`として両者から呼べるよう切り出した（既存のメンション応答フローもこの共通化に合わせてリファクタ）。生成中プレースホルダ（`generation_status='generating'`）・T-13コスト記録もメンション応答と共通。要約対象は`thread_id`指定時がそのスレッド全体（元発言＋返信、上限なし）、省略時がチャンネル本体の直近100件（`MAX_SUMMARY_CHANNEL_MESSAGES`）で、いずれも`generation_status IS NULL`で絞り込み生成中の行自身を要約対象から除外する。要約用のシステムプロンプトは通常の応答用（`_build_system_prompt`）とは別に`_build_summary_prompt`を新設し、behavior_prompt等ではなく箇条書き・断定回避・創作禁止の要約方針を指示する。新規A-15（`POST /api/channels/{id}/summarize`、`routers/channels.py`）は`require_channel_member`で保護し、`thread_id`が当該チャンネルの発言でなければ404、AI未設定・チャンネルAI無効時は`SummaryUnavailable`例外経由で400を返す（メンション応答の`maybe_trigger`が条件不成立時に黙って何もしないのと異なり、明示的なボタン操作のため理由を返す）。フロントは`ChannelView.tsx`ヘッダー（チャンネル本体、`thread_id`省略）と`ThreadPanel.tsx`ヘッダー（スレッド全体、`thread_id`指定。`parentMessage.channel_id`がある＝チャンネルのスレッドのときのみボタン表示、DMのスレッドには出さない）の2箇所に「📝 要約」ボタンを追加した。押下後は投稿完了を待たずボタンをすぐ元に戻し（`mutateMessages`/`mutateReplies`で生成中プレースホルダを早めに表示させるのみ）、実際の生成結果は既存の3秒ポーリング・`MessageList`の「生成中」表示にそのまま乗る。curlで一通り確認: AI未設定時の400、`thread_id`が数値でない場合の422、他チャンネルの発言idを指定した場合の404、非参加者の403、`_fetch_summary_source_rows`のSQL（チャンネル本体・スレッド全体の両方）が期待どおりの行を返すこと、既存のAIメンション応答経路がリファクタ後も従来どおり動作すること（`@AI`宛て投稿がAPIキー未設定時に静かに無視されること）を確認した。**実際のOpenAI API呼び出し（要約文の生成）はチャットAI機能と同様に未検証**（`OPENAI_API_KEY`が開発環境に設定されていないため）。**2026-09-02に実機検証済み。後述の「AI API実機検証」の項を参照**。基本設計書（6.2節に設計判断を追記、v1.28）・詳細設計書API設計（A-15、v1.20）・docs/index.html（Ver 4.30）を同じコミットで更新した。要件定義書・画面モックアップ（S-03/S-04の要約ボタン）は既存の記載どおりで変更なし（2026-08-31）。
- F-29 AI利用コストの管理のうち、S-08「AI利用状況・コスト」タブ（A-42/A-43）を実装した。新規T-14 ai_usage_limitsを05-1 DB設計3.11節に定義済みのカラム構成のまま実テーブル化し、`scope='global'`は`scope`列の部分ユニークインデックスで最大1行、`scope='channel'`は`channel_id`列の部分ユニークインデックスでchannel_idごとに最大1行へ制約した（`routers/admin.py`のA-43はこれらをON CONFLICTの対象にして洗い替える。同じscope+channel_idへの複数回PUTで行が増えないことをSQLで確認済み）。A-42（`GET /api/admin/usage?month=YYYY-MM`、month省略時はJSTの当月）はT-13 ai_usage_logsを`total`・`by_channel`・`by_user`の3通りに集計する。旧いレスポンス例はby_channelのみだったが、基本設計書8.6節「月次・チャンネル別・利用者別に集計して表示する」・05-3画面設計「利用者別テーブル」の記載に合わせてby_userを追加した（backfill）。`limits.global`・`limits.channels[]`にそれぞれの`used_pct`（該当月のcost_yen÷monthly_limit_yen×100）を含める。**しきい値（notify_threshold_pct）到達時の通知メール送信・上限到達時の応答停止は実装していない**（要件定義書8.2節のとおり上限到達時の挙動は千田氏との別途協議事項のため、このスライスは設定の保存とused_pct表示のみ行う。notify_emailは保存するだけで実際に送信はしない）。フロントは新規`useUsageStats(month)`フックと`AdminConsole.tsx`の3つ目のタブ`UsageTab`（月選択・概算コスト合計・チャンネル別/利用者別の内訳テーブル・全体上限の編集フォーム`GlobalLimitForm`）を追加した。`GlobalLimitForm`は`ChannelSettings.tsx`のtabコンポーネント群と同じ「非同期データが読み込まれてから条件付きレンダーし、propsからuseStateの初期値を直接設定する」パターン（`{!usage ? <p>読み込み中...</p> : <UsageTabBody usage={usage} .../>}`）でuseEffectを使わずに済ませた。**チャンネル別上限（scope='channel'）の作成・編集UIはこのスライスでは対象外**とした（バックエンドは対応済みで、既存の値があれば一覧に参考表示するのみ。追加するにはAPIを直接呼ぶ必要がある）。curlで一通り確認: 集計値の正確性（3件のテスト用ai_usage_logsを直接INSERTしてcall_count/tokens/cost_yenの合計・チャンネル別・利用者別内訳が一致すること）、A-43の上書き（同じscopeへの2回目のPUTで行が増えないこと）、used_pctの計算、バリデーション（不正なscope・channel_id未指定・monthly_limit_yen≤0がいずれも422）、無関係な月を指定した場合の0件応答、非adminの403を確認した。基本設計書（v1.29）・詳細設計書DB設計（T-14、v1.6）・API設計（A-42・A-43、v1.21）・docs/index.html（Ver 4.31）を同じコミットで更新した。要件定義書・画面モックアップは既存のF-29・S-08の記載どおりで変更なし（2026-08-31）。
- S-08「監査ログ」タブ（A-44、T-16）を実装し、S-08の4タブすべてが揃った。新規`backend/audit_log.py`（`record(conn, event_type, actor_user_id, summary, target_channel_id=None, target_field=None)`のみの薄い共通処理、`mentions.py`等と同じ構成）を追加し、`routers/auth.py`のA-02（OAuthコールバック）・dev-loginの成功時に`event_type='login'`を、`routers/ai_settings.py`のA-24〜A-27の成功時に`event_type='channel_ai_setting_change'`を記録する（A-45 handoffは当時未実装のため対象外だった。**2026-09-01にA-28〜A-30・A-45を実装し、いずれも同じevent_typeで記録するようになった。後述のF-12スキル登録の項を参照**）。`summary`は「どの項目が変更されたか」の説明のみで、変更後の実際の値・差分は含めない（例: A-26は「振る舞い定義を更新しました」であって本文は含めない、05-1 DB設計3.12節「本文差分は保持しない」）。**T-16.target_channel_idはON DELETE CASCADEではなくSET NULLにした**（他の多くのFKはCASCADEだが、監査ログはチャンネル削除後も「いつ・誰が・どの項目を変更したか」の記録自体を残す目的のテーブルであり、CASCADEにすると最も確認したい記録が消えてしまうため。`messages.recurring_post_id`等と同じ「履歴は消さない」設計判断。基本設計書6.2節に追記）。新規A-44（`GET /api/admin/audit-logs`、`routers/admin.py`）は`event_type`/`channel_id`/`actor_user_id`/`after`/`before`で絞り込み、新しい順に1ページ50件返す。フロントは新規`useAuditLogs(filters)`フックと`AdminConsole.tsx`の4つ目のタブ`AuditLogTab`（種別セレクト・実行者セレクト（既存`useAdminUsers()`を再利用）・期間の2つの日付入力）を追加した。対象チャンネルでの絞り込みUIは対象外とし（全チャンネル一覧を返す管理者向けAPIが無いため）、一覧の「対象」列に表示するのみに留めた。**実装中にバグを1件発見・修正**: `before`フィルタの初期実装が`+timedelta(days=1)`していたため「指定日を含まない」という自分で書いたdocstringの意図と逆に「指定日を含む」動作になっていた（`routers/search.py`の`before`と同じ「含まない」規約に合わせて修正）。curlで一通り確認: ログイン2件・チャンネルAI設定変更4件（A-24〜A-27）の記録、`event_type`/`channel_id`/`actor_user_id`/`before`/`after`の各絞り込み、修正後の`before`/`after`の境界値が正しいこと、チャンネル削除後も監査ログ行がtarget_channel_id=NULLで残ること（CASCADEではなくSET NULLが効いていることの直接確認）、不正なevent_type・日付形式が422、非adminが403を確認した。基本設計書（6.2節に設計判断を追記、v1.30）・詳細設計書DB設計（T-16、v1.7）・API設計（A-44、v1.22）・docs/index.html（Ver 4.32）を同じコミットで更新した。要件定義書・画面モックアップは既存のF-2x・S-08の記載どおりで変更なし（2026-09-01）。
- S-05検索結果のタブ切替UIを実装した（F-06、詳細設計書API設計6.4節）。従来「メッセージ」「ファイル」を縦に並べているだけだった表示を、実際にクリックで切り替わるタブ（すべて/メッセージ/ファイル、件数バッジ付き）にした。設計どおり「初回検索でtype=allとして3種すべてを取得し、タブ切替のたびに再検索はしない」方式とし、`SearchResultsTabs`が`result.items`をクライアント側で`type`によって絞り込むだけに留めた（1タブ最大20件の「もっと見る」追加取得は今回は対象外）。ドキュメント根拠タブは層2検索が未実装のため常に無効表示（グレー・カーソル`not-allowed`・ツールチップ）とした。新しい検索を実行するたびに`activeTab`を「すべて」へ戻すため、`SearchResultsTabs`を`key={searchParams.toString()}`で新しい検索ごとに再マウントする方式にし、useEffectでの同期を避けた（Reactの「keyで状態をリセットする」公式パターン）。**このタスクでは初めてPlaywrightで実際にブラウザを操作して検証した**（`run`スキル経由。chromium-cliが使えない環境だったため生Playwrightスクリプトで代替、`playwright`パッケージと対応Chromiumバイナリをこのマシンに導入）。curlでチャンネル・メッセージ2件・添付ファイル1件（同じキーワードを含む）を作成し、そのセッションCookieをPlaywrightのブラウザコンテキストへ注入して`/search`にアクセス、実際に「メッセージ」「ファイル」タブをクリックして表示内容が正しく絞り込まれること（メッセージタブにファイル名が出ない、ファイルタブに本文が出ない等）、「すべて」に戻すと両方表示されること、ドキュメント根拠タブが無効化されていること、タブ切替時に`/api/search`への新規リクエストが発生しないこと（Playwrightのnetworkイベントで0件を確認）、consoleエラーが0件であることをスクリーンショット付きで確認した。純粋な既存設計（詳細設計書API設計6.4節・05-3画面設計）どおりのフロントエンド実装であり新規API・DB変更は無いため、設計文書側の改訂は無い。検証用に作成したテストデータ・スクリプトはすべて削除済み（2026-09-01）。
- S-08のチャンネル別上限編集UI（A-43 scope='channel'）と監査ログの対象チャンネル絞り込みUI（A-44 channel_id）を実装した。いずれもバックエンドは既に実装済みで、フロントにUIを足すだけの追加。**チャンネル別上限**: `ChannelLimitsSection`（`AdminConsole.tsx`）が既存の上限一覧（編集ボタン付き）と新規追加パネルを表示する。編集はモーダル（`ChannelLimitEditModal`）、追加は常設パネル（`ChannelLimitAddPanel`）で画面を分け、S-06定期投稿/トリガーで一度指摘を受けた「新規作成と編集の見分けにくさ」と同じ問題を最初から避けた。追加候補は当月`usage.by_channel`のうち未設定のチャンネルに限定（全チャンネル一覧を返す管理者向けAPIが無いため。月を変えれば候補も変わる）。月次上限額・通知しきい値・通知先の3項目入力欄は`UsageLimitFormFields`として切り出し、`GlobalLimitForm`・`ChannelLimitAddPanel`・`ChannelLimitEditModal`の3箇所で共有した（`GlobalLimitForm`もこの切り出しにあわせてリファクタ）。**監査ログの対象チャンネル絞り込み**: `useAuditLogs({})`（フィルタ無し）を追加でもう1回呼び、そこに含まれる`target_channel_id`から絞り込み用`<select>`の選択肢を作る（チャンネル一覧を返す管理者向けAPIが無いための代替。channel_idで絞り込み中でも選択肢自体は消えないよう、表示用の一覧とは別に取得する設計）。**このタスクもPlaywrightで実際にブラウザ操作して検証した**: curlでテスト用チャンネル・当月AI利用ログ・監査ログ1件（A-24呼び出し）を作成し、そのセッションをブラウザに注入して(1)チャンネル別上限の追加→一覧に反映→編集モーダルで金額変更→一覧に反映、(2)監査ログの対象チャンネルセレクトにそのチャンネルが選択肢として現れる→選択すると該当行のみに絞り込まれる（8件→1件）、をスクリーンショット付きで確認し、途中経過も含めてDBに正しく永続化されていることを直接SQLで確認した。consoleエラー0件。テストデータ・検証スクリプトはすべて削除済み。新規API・DB変更は無いため設計文書側の改訂は無い（2026-09-01）。
- S-05の「もっと見る」（1タブ20件超の追加取得）を実装した（F-06、詳細設計書API設計6.4節「超過分は「もっと見る」でtype指定の追加リクエスト（type=message&page=2等）を行う」）。`useSearch.ts`から`searchParamsFor(query, extra?)`を切り出し（`SearchQuery`→`URLSearchParams`変換）、初回検索（`useSearch`本体）と「もっと見る」の追加リクエストの両方で共有した。`SearchResultsTabs`に`extraItems`（もっと見るで積み増した行）・`loadedPages`（type別の既読ページ数）を追加し、`allItems = [...result.items, ...extraItems]`をtypeで絞り込んで描画する。ボタンは`もっと見る（残りN件）`で残り件数を明示し、`type=message`/`type=file`をそれぞれ独立に追跡するため、片方だけ全部読み込んでももう片方はまだボタンが出る。`key={searchParams.toString()}`での再マウント（既存のタブ切替リセットと同じ仕組み）により、新しい検索のたびに積み増し状態も自動的に初期化される。Playwrightで実際に検証: 25件のテストメッセージ（SQLで直接INSERT）を用意し、初回20件表示→「もっと見る（残り5件）」クリック→`type=message&page=2`が1回だけ発火→25件全件表示・ボタン消失→タブ切替後も25件維持、をスクリーンショット・ネットワークログで確認した。consoleエラー0件。純粋な既存設計どおりのフロントエンド実装であり新規API・DB変更は無いため、設計文書側の改訂は無い。テストデータ・検証スクリプトはすべて削除済み（2026-09-01）。
- F-12 スキル登録・F-17 引き継ぎ先設定を実装した（同じS-06「スキル」タブに同居させる。基本設計書5.9節「スキルにない業務依頼の引き継ぎ先も設定する」の記載どおり）。新規T-11 channel_skills（channel_id・title・instructions）を追加し、新規A-28（POST）・A-29（PUT）・A-30（DELETE、いずれも`routers/ai_settings.py`、`require_channel_admin`で保護）を実装、A-23の一括取得レスポンスに`skills`配列を追加した。新規A-45（`PUT /api/channels/{id}/ai-settings/handoff`）は`fallback_handoff_user_id`を更新し、指定時は当該チャンネルの参加者であることをAPI側で検証する（基本設計書8.3節に既に明記されていた要件）。**指定した人物が退出・無効化された場合はNULL（既定のチャンネル管理者へ引き継ぎ）に自動的に戻す**設計判断（基本設計書8.3節）を、`channels.py`の`leave_channel`（A-72）・`remove_channel_member`（A-73）と`routers/admin.py`の`update_user`（A-37、`is_active=false`時）の3箇所に同じ`UPDATE channel_ai_settings SET fallback_handoff_user_id = NULL WHERE ...`パターンで実装し、curlで3経路とも実際にNULLへ戻ることを確認した（自己退出はA-73と同じコードパスのため個別確認は省略）。`services/ai_agent.py`の`_build_system_prompt`を拡張し、新規`_build_skills_section`がT-11の内容を「# あなたのスキル」節として列挙し、どのスキルにも当てはまらない依頼を受けた場合は引き継ぎ先（未指定時は「このチャンネルの管理者」という汎用ラベル）へ相談するよう案内する指示を追記する（スキルが0件のチャンネルではこの節自体を省略）。**out_of_scope_policyは層2ドキュメントQ&A関連のため今回もプロンプトへは配線していない**（AIサポート基盤の「スコープ外」に残る唯一の項目）。AI応答本文に含まれる「@氏名」は参考情報としてのテキストに留まり、F-41のようなクリック可能なメンション（message_blocks）には未対応（AI応答経路には`insert_mention_blocks`を配線していないため）。フロントは`ChannelSettings.tsx`に定期投稿・トリガーと同じ「新規作成パネル＋編集モーダル」構成の`SkillsTab`（`SkillFormFields`共有）を追加し、引き継ぎ先は`useChannelMembers`から取れる有効な参加者一覧のセレクトで即保存する。`Layout.tsx`のS-06サイドバーに8番目のタブとして追加した。curlで一通り確認（作成・更新・存在しないskill_idへのPUT/DELETEが404・非chadmin/非adminからの403・非参加者user_idを指定した引き継ぎ先設定の400）に加え、`_build_skills_section`を実際のDB行に対して直接呼び出し、スキル節・引き継ぎ先名の解決・引き継ぎ先未指定時の既定ラベルの3パターンが期待どおりの文字列になることを確認した。**実際のOpenAI API呼び出し（AIがこのスキル指示に実際に従うか）はチャットAI機能と同様に未検証**（`OPENAI_API_KEY`が開発環境に設定されていないため）。**2026-09-02に実機検証済み。後述の「AI API実機検証」の項を参照**。Playwrightで実際のブラウザ操作も確認: スキルの追加→一覧反映→編集→削除の一連の操作、引き継ぎ先セレクトの選択肢（参加者一覧）と保存、consoleエラー0件。テストデータ・検証スクリプトはすべて削除済み。基本設計書（v1.31）・詳細設計書API設計（A-28〜A-30・A-45、v1.23）・docs/index.html（Ver 4.33）を同じコミットで更新した。要件定義書・画面モックアップは既存のF-12・F-17・S-06の記載どおりで変更なし（2026-09-01）。
- F-15 反応モード設定を実装した（S-06「反応モード」タブ）。既存A-24（`PUT /api/channels/{id}/ai-settings/general`）のリクエストに`reaction_mode`（`'mention_only'`/`'proactive'`、必須、それ以外は422）を追加し、新規エンドポイントは作らなかった（基本設計書・詳細設計書API設計とも元々A-24を対応APIとして明記済み）。基本設定タブ（`GeneralTab`、`is_ai_enabled`を変更）と反応モードタブ（新規`ReactionTab`、`reaction_mode`を変更）はいずれもこのA-24を共用し、自分が変更しない側のフィールドも現在値のまま一緒に送る（A-27参照ドキュメント範囲がfolder_ids・out_of_scope_policyを束ねて送るのと同じ考え方）。**「投稿に自ら反応」モードでAIがどの投稿に反応するかの判定ロジックは設計書が規定していなかった（グレー）ため、着手前にユーザーに確認した**（案1: 投稿ごとに軽量な事前判定をLLMに1回追加で呼んでから反応要否を決める2段階方式、案2: 人間の投稿には関連性判定を挟まず常に応答するシンプルな方式、案3: 「？」や疑問系キーワードでの文字列ヒューリスティック、の3案を提示し、コストを増やさない案2を選択）。`services/ai_agent.py`の`maybe_trigger`に`settings["reaction_mode"] != "proactive" and not detect_mention(...)`という条件分岐を1行追加するだけで実現でき（proactiveのときはメンション判定自体をスキップし、`sender_type='human'`の投稿であれば常に起動する）、判定用の追加LLM呼び出しは行わない。この設計判断は基本設計書8.1節に`callout diff-new`として追記した（要件定義書F-15自体の記載は変更なし）。フロントは画面モックアップのカード型ラジオ（クリックで即選択・即保存、選択中はaccent色の枠とfillされたドットで表示）をそのまま再現した`ReactionTab`を追加し、`Layout.tsx`のS-06サイドバーに「スキル」の次・「定期投稿」の前として追加した（画面モックアップのタブ順どおり）。curlで一通り確認（不正なreaction_modeの422、reaction_mode省略時の422、更新後の監査ログ記録）に加え、DB直結の単体テストで`maybe_trigger`のゲート条件3パターン（mention_only×メンション無し→ブロック、mention_only×メンション有り→通過、proactive×メンション無し→通過）が期待どおりであることを確認した。Playwrightで実際のブラウザ操作も確認: カード選択→トースト表示→リロード後も選択状態が保持されること、consoleエラー0件。**実際のOpenAI API呼び出し（proactiveモードで実際に毎回応答が生成されるか）はチャットAI機能と同様に未検証**（`OPENAI_API_KEY`が開発環境に設定されていないため）。**2026-09-02に実機検証済み。後述の「AI API実機検証」の項を参照**。テストデータ・検証スクリプトはすべて削除済み。基本設計書（8.1節に設計判断を追記、v1.32）・詳細設計書API設計（A-24、v1.24）を同じコミットで更新した。要件定義書・画面モックアップは既存のF-15・S-06の記載どおりで変更なし（2026-09-02）。
- F-16 自動対応範囲の設定を実装した（S-06「自動対応範囲」タブ）。新規T-12 channel_auto_response_rules（channel_id・request_category・response_level。UNIQUE(channel_id, request_category)、channel_idはON DELETE CASCADE）を追加し、新規A-31（`PUT /api/channels/{id}/ai-settings/auto-response`）を`routers/ai_settings.py`に実装した。A-27参照ドキュメント範囲・A-31とも同じ「洗い替え」方式（送信されたルール集合でT-12をDELETE→INSERT、差分計算はしない）を採用し、新規エンドポインは1本のみ追加した。同じrequest_categoryが複数送られた場合はPython側のdictで最後の指定を採用し（DBのUNIQUE制約違反を避ける）、response_levelは'auto'/'confirm'/'human'以外422。**request_category（依頼内容カテゴリ）をチャンネル管理者が自由に追加・削除できる仕様にするか、画面モックアップが示す6例の固定候補にするかは着手前にユーザーに確認した**（要件定義書REQ-F-15が「担当部署が自ら決められる」ことを求めており、モックアップの6例は記入例として扱う自由編集案を選択）。フロントは`ChannelSettings.tsx`に`AutoResponseTab`を追加し、DocScopeTabと同じ「ローカルで編集→まとめて1回のPUTで保存」方式（行の追加・削除・区分（自動対応可／確認のうえ対応／人が対応）変更のたびに個別リクエストは発生させない）とした。区分の見た目（ピル）は画面モックアップのok/warn/human配色と一致する既存トークン（ok/bot/member）をそのまま再利用し、新規CSS変数は追加していない。**「人が対応」に区分される依頼をAIがどう検知するかも設計書が規定していなかったため、着手前にユーザーに確認した**（案1: 区分一覧をシステムプロンプトに含めてAI自身に1回の応答内で判断・引き継ぎ案内させる、案2: 投稿ごとに軽量な事前分類LLM呼び出しを追加してから本応答を生成する2段階方式、案3: request_category名の部分一致による機械的フィルタ、の3案を提示し、F-15反応モードと同じくコストを増やさない案1を選択）。`services/ai_agent.py`に新規`_build_auto_response_section`（T-12を「# あなたが対応してよい依頼の目安」節として列挙し、「人が対応」該当時は引き継ぎ案内を追記）を追加し、`_build_system_prompt`へ配線した（設計書どおりの節順で、スキル節より前に配置）。引き継ぎ先の名前解決ロジックは`_build_skills_section`と重複していたため、新規`_resolve_handoff_label`共通ヘルパーへ切り出した（両関数から共有）。'confirm'（確認のうえ対応）は実行前確認（F-25・pending_actions）が未実装で書き込み系ツール自体が存在しないため、現状は'auto'と同じ「通常どおり応答してよい」扱いとし、システムプロンプト内の説明文でその旨を明記するに留めた。ルールが1件も登録されていないチャンネルでは節自体を省略する（スキル節と同じ挙動）。curlで一通り確認（response_level不正値の422、洗い替えの動作確認、同一カテゴリ重複送信時の最後勝ち、非chadminからの403、監査ログ記録）に加え、`_build_auto_response_section`を実際のDB行に対して直接呼び出し、区分ラベルの列挙・引き継ぎ先名の解決・ルール0件時の節省略の3パターンが期待どおりの文字列になることを確認した。**実際のOpenAI API呼び出し（AIが実際に「人が対応」区分を判別できるか）はチャットAI機能と同様に未検証**（`OPENAI_API_KEY`が開発環境に設定されていないため）。**2026-09-02に実機検証済み。後述の「AI API実機検証」の項を参照**。Playwrightで実際のブラウザ操作も確認: 3カテゴリの追加→区分変更（ピルクリック）→保存→リロード後の永続化確認→1件削除→再保存→件数確認、ピルクリックの選択クラス切り替えとAPIレスポンスの一致をDOM属性＋直接API呼び出しの両面で確認、consoleエラー0件。テストデータ・検証スクリプトはすべて削除済み。基本設計書（8.1節に設計判断を追記、v1.33）・詳細設計書DB設計（T-12、v1.9）・API設計（A-31、v1.25）・AIサポート（10.2節、v1.2）・docs/index.html（Ver 4.35）を同じコミットで更新した。要件定義書・画面モックアップは既存のF-16・S-06の記載どおりで変更なし。**ユーザーから2点フィードバックを受け同じコミットで修正した**: (1) 「人が対応」ピルの選択色が`member`トークン（薄いグレー、#f1f5f9/#475569）で他の2区分（ok=緑、bot=amber）と比べて視認しにくかったため、既存の`danger`トークン（赤系、bg-danger-bg/text-danger-text/border-danger-border）に差し替えた。(2) 依頼内容（request_category）を一度追加した後に文言を修正する手段が削除→再追加しかなかったため、行ごとに「編集」ボタン（インライン`<input>`に切り替え、確定/キャンセルボタンで確定する。Enter/Escapeキーにも対応）を追加した。いずれもフロントエンドのみの変更でバックエンドAPI・DBは無変更のため設計文書側の改訂は無い（2026-09-02）。
- S-06「参照ドキュメント範囲」タブの「参照範囲外の質問への対応」を画面モックアップと同じカード型ラジオ（タイトル＋説明文＋選択時に塗りつぶされるドット）に作り直した。従来は素の`<input type="radio">`＋ラベルの簡易実装で、F-15反応モードタブで導入したカード型ラジオ（`ReactionTab`）と見た目が揃っていなかった。両タブで見た目が完全に同じだったため、共通の`RadioCard`コンポーネント（タイトル・説明文・選択状態・onClick・disabledのみを受け取る表示専用）に切り出し、`DocScopeTab`・`ReactionTab`の両方から使うようにした（クリック時の挙動はコンポーネント外：`DocScopeTab`はローカル状態を更新するだけで下部の「保存」ボタンでまとめて送信、`ReactionTab`は選択時に即A-24を呼んで保存する、という既存の違いはそのまま維持）。選択肢の文言も画面モックアップの実際のコピー（「厳格に制限」「一般回答を許可」、各説明文）に合わせて更新した。あわせて、直近のF-16自動対応範囲タブ追加時にEditツールの文字列一致の都合で発生していた副作用（`SkillFormFields`の説明コメントが誤って`ReactionTab`の直前に迷い込んでいた）に気づき、本来の位置（`SkillFormFields`の直前）に戻した。新規API・DB変更は無いフロントエンドのみの変更のため、設計文書側の改訂は無い。Playwrightで実際のブラウザ操作を確認: カードクリックでの選択→保存→リロード後の値の一致（`DocScopeTab`）、リファクタ後も`ReactionTab`の選択即保存が壊れていないことの回帰確認、consoleエラー0件。テストデータ・検証スクリプトはすべて削除済み（2026-09-02）。
- F-40 プロフィールカードに「DMを送る」ボタンを追加した。ユーザーからの明示的な要望による追加で、要件定義書・画面モックアップのいずれにも記載がなかった新規機能。既存のS-06系タブ拡張（チャンネル名編集・自己退出等）と同様、既存の仕組み（A-16参加中DM一覧・A-17 DM開始、いずれもF-05で実装済み）にボタンを1つ足すだけの単純な拡張で設計上の選択肢が実質1つしかないと判断し、検討資料は起こさず要件定義書（F-40の記載を拡張、v1.24）・基本設計書（5.21節、v1.34）・詳細設計書画面設計（S-03項目表・ProfileCardコンポーネント定義、v1.5）・画面モックアップ（S-03、プロフィールポップオーバーに「💬 DMを送る」ボタンを追加）を同じコミットで改訂した。新規API・DB変更は無い。`ProfileCard.tsx`に`useDms()`（サイドバーと同じSWRキャッシュを共有）・`useMe()`を追加し、`userId !== me.id`のときのみボタンを表示する。押下時はまず`useDms()`のキャッシュから一対一DM（`members.length === 1 && members[0].id === profile.id`）を探し、見つかればAPI呼び出し無しで`/dms/{id}`へ即遷移、無ければ`POST /api/dms`（A-17）で新規開始してから遷移する（`DmPickerModal.tsx`の`existingDmWith`・`start`と同じロジック）。カードの高さ見積もり（`CARD_HEIGHT_ESTIMATE`、下開き/上開きの判定にのみ使う）をボタン追加分だけ200→240に調整した。Playwrightで実際のブラウザ操作を確認: 他者の発言者名クリック→カードに「DMを送る」表示→押下→`/dms/{id}`へ遷移、自分自身の発言者名クリック→「DMを送る」ボタンが表示されないこと（0件）、consoleエラー0件。テストデータ・検証スクリプトはすべて削除済み（2026-09-02）。
- F-05に自分専用DM（メモ・下書き・To-do・リンクやファイルの保管用途、Slackの「Myself」相当）を追加した。ユーザーからの明示的な要望による追加で、要件定義書・基本設計書・詳細設計書・画面モックアップのいずれにも記載がなかった新規機能。着手前に実装可否・影響範囲を調査したところ、T-17 direct_message_membersのPRIMARY KEY (dm_id, user_id)には最小人数の制約が無く自分1行のみのDMを作ること自体はDB上可能である一方、A-17（DM開始）の`other_ids.discard(user.id); if not other_ids: raise 422`という既存バリデーションが「自分以外を指定しなかった場合」を常にエラー扱いしており、また`_dm_out`の`members`解決クエリ（`u.id <> $2`で常に自分を除外）が自分専用DMでは`members`を空にしてしまう、という2箇所が実際のブロッカーだと判明した。新規テーブル・新規APIは追加せず、この2箇所を修正する方式で対応した：(1) `routers/dms.py`の`create_dm`から早期404チェックを削除し、`member_user_ids`に自分のidのみを指定した場合（Pydantic`Field(min_length=1)`により少なくとも1件は送られている前提のため、除外後に空集合になるのは「自分だけを明示的に指定した」場合に限られる）を自分専用DMの開始として扱うようにした。(2) `_dm_out`が全参加者idを取得したうえで、除外後に空になる（＝自分専用DM）場合だけ自分自身を`members`に含め、判別用の新規フィールド`is_self`をレスポンスに追加した（`members`が「自分以外の相手」を返すという既存の意味づけ自体は変えていない）。フロントは`types/index.ts`の`Dm`に`is_self`を追加し、`DmPickerModal.tsx`に検索結果とは独立した「自分」の固定枠（常時一覧の先頭に表示、他の相手との組み合わせ選択は不可な排他的選択）を追加、`DmView.tsx`・`Layout.tsx`は`is_self`のときだけタイトル・サイドバー表示名を「自分（メモ）」固定表記にし📝アイコンを添える（それ以外の投稿・スレッド・既読化・検索等の挙動は通常のDMと完全に共通のため変更なし）。curlで一通り確認（自分専用DMの新規作成→`is_self:true`・`members`に自分自身、再取得で`created:false`となる再利用、通常の1:1/グループDM作成が引き続き正常動作すること、A-16一覧に混在して正しく表示されること、メッセージ投稿・取得）に加え、Playwrightで実際のブラウザ操作（DM相手選択モーダルの「自分」行選択→開始→サイドバーに📝「自分（メモ）」表示→メッセージ送信→再度モーダルを開くと「DM済み」タグが表示されることの一連の流れ）を確認した。consoleエラー0件。テストデータ・検証スクリプトはすべて削除済み。基本設計書（5.3節に設計判断を追記、v1.35）・詳細設計書DB設計（T-17、v1.10）・API設計（A-16・A-17、v1.26。あわせて実装と食い違っていた旧いリクエスト例のbackfillも実施）を同じコミットで更新した。要件定義書（F-05、v1.25）・画面モックアップ（補足02、「自分」固定枠の追加）も同じコミットで更新した（2026-09-02）。
- システム管理者が一度も参加していない非公開チャンネルでもS-06チャンネル設定を開けるようにした（2026-08-26付の既知の制約の解消）。ユーザーから「システム管理者は他人の非公開チャンネルに未参加でも入れたり中身を閲覧できる方がよいか、既に何か定義されているか」と質問を受け、調べた結果を回答した：要件定義書の権限マトリクスは「チャンネルの削除」「チャンネル名・説明文の編集」「チャンネルの公開／非公開の切り替え」「担当外チャンネルのAI設定編集」をいずれもadmin「○（全チャンネル）」とする一方、「非公開チャンネルの一覧表示・検索・閲覧」（＝会話内容）はadminも含め参加者限定のままと明確に区別しており（「参加していない非公開チャンネルの一覧・内容が既定で見えるわけではない点はmember・chadminと同様」の注記あり）、この区別は既に設計判断として下されていた。前者（管理系操作）はA-09・A-23〜A-31・A-45・A-47〜A-56・A-63〜A-66が既に`require_channel_admin`でシステムadminバイパスを持っており矛盾は無かったが、実際にはChannelSettings.tsx（S-06）がヘッダー表示・チャンネル管理者タブのためにA-06（チャンネル詳細）・A-46（参加者一覧）も呼んでおり、この2つは`require_channel_member`のままバイパスが無かったため、非参加の非公開チャンネルではS-06自体を開けなかった（実際に該当の状態を再現し、S-06が完全に開けることをスクリーンショットで確認した上で着手の可否をユーザーに確認）。`auth_helpers.py`に新規`require_channel_member_or_admin`（`require_channel_member`と同じ判定に`require_channel_admin`と同じ`if user.role == "admin": return user`バイパスを加えたもの）を追加し、A-06・A-46のガードをこれに切り替えた。この2つは発言本文を一切含まないメタデータ専用エンドポイントであるため、要件定義書が明記する「会話内容は参加者限定」という区別は崩さない。発言本文を扱うA-10・A-11・A-14・A-15・A-18・A-19・A-69・A-72等は変更せず`require_channel_member`のまま（adminバイパス無し）とした。ただし、A-06が非参加adminにも200を返すようになった副作用として、S-03（会話画面）の既存ガード（`ChannelView.tsx`がA-06のエラーだけを見て404/403ならワークスペースへ戻す仕組み）が非参加adminを締め出せなくなる問題に気づき、A-06のレスポンスに新規`is_member`（呼び出し元が実際の参加者か）を追加し、`ChannelView.tsx`に`channel && !channel.is_member`のときも同じくワークスペースへ戻す分岐を追加した（S-06側は元々`is_channel_admin || me.role==='admin'`でゲートしており`is_member`を見る必要が無いため変更不要）。curlで一通り確認（非参加adminのA-06/A-46が200で`is_member:false`、非参加adminのA-10/A-11が引き続き404、非管理者の一般利用者は従来どおり404、実際の参加者は`is_member:true`）に加え、Playwrightで実際のブラウザ操作を確認：非参加adminが対象チャンネルのS-06を開けチャンネル管理者タブ（名称編集・chadmin一覧）が正しく表示されること、同じチャンネルのS-03を直接開くとメインペインが空白のままリダイレクト相当の挙動になり会話内容が一切見えないこと、consoleエラー0件。テストデータ・検証スクリプトはすべて削除済み。詳細設計書（総論5.1・5.2・5.9節に設計判断を追記、v1.5）・API設計（A-46、v1.27）を同じコミットで更新した。要件定義書・基本設計書は元々「チャンネル管理系操作はadmin全チャンネル可」としていた記載どおりで変更なし。**この直後、ユーザーから「実際にこの機能を使うには石井直樹119はアプリ上でどう操作すればその設定画面にたどり着けるのか」と聞かれ、調査した結果、S-06自体は開けるようになったが「そのチャンネルを見つけて開く」導線が存在しないこと（サイドバーの＋・横断検索はいずれも自分の参加チャンネルの範囲に限定、S-08の各タブもチャンネル名をプレーンテキストで表示するのみでクリックできない）が判明した。ユーザーの了承のもと、最小の追加としてS-08「監査ログ」タブの「対象」列（`AdminConsole.tsx`のAuditLogTab）を、そのチャンネルのS-06へのリンク（`target_channel_id`があるときだけ`<Link to={`/channels/${id}/settings`}>`、無いときは従来どおり`—`表示）に変更した。この列はchadminがそのチャンネルのAI設定を変更した記録があるときのみ埋まるため万能の発見手段ではないが、既存の画面に手を加えるだけで実現できる最小の一手として選んだ（S-08に全チャンネル一覧を返す新規APIを追加する等のより網羅的な対応は今回は見送り）。Playwrightで実際に確認：chadminが非公開チャンネルのAI設定を変更→監査ログに記録→システムadmin（非参加）が監査ログタブでそのチャンネル名のリンクをクリック→実際にそのチャンネルのS-06（チャンネル管理者タブ）へ遷移できること、consoleエラー0件。新規API・DB変更・設計文書側の改訂は無い（既存のリンク先S-06の権限自体は直前の変更で対応済みのため）。**さらにユーザーから「利用者管理タブのチャンネル管理者列と、AI利用状況・コストタブのチャンネル別上限からもS-06へ飛べるようにしたい」との追加依頼を受け、同様にリンク化した。「利用者管理」タブの`chadmin_channels`（そのユーザーがchadminを務める全チャンネル、非公開含む）は従来チャンネル名の文字列配列のみでリンクを組み立てるためのidが無かったため、A-36（`routers/admin.py`）のSQLを`array_agg(c.name ...)`1本から`array_agg(c.id ...)`・`array_agg(c.name ...)`の2本（いずれも同じ`ORDER BY c.name`で順序を揃える）に変更し、`{id, name}`オブジェクトの配列としてzipして返すようにレスポンス形状を変更した（フロントの`AdminUser.chadmin_channels`型・`types/index.ts`も追従）。「AI利用状況・コスト」タブのチャンネル別上限（`ChannelLimitsSection`）は元々`UsageChannelLimit`が`channel_id`・`channel_name`の両方を持っていたため、バックエンド変更は不要でフロントの表示（`# {channel_name}`のテキスト）を`<Link to={`/channels/${channel_id}/settings`}>`に置き換えるだけで済んだ。curlでA-36の新しいレスポンス形状を確認し、Playwrightで両タブから実際にリンクをクリックしてS-06へ遷移できることを確認した（利用者管理タブは非公開チャンネル「非公開」を含む複数チャンネルのリンクが正しいidで生成されること、AI利用状況タブは新規に設定したチャンネル別上限のリンクが遷移すること）。consoleエラー0件。テストデータ・検証スクリプトはすべて削除済み。詳細設計書API設計（A-36、v1.28）・docs/index.html（Ver 4.39）を同じコミットで更新した。**続けてユーザーから「管理者がS-08のリンクから非公開チャンネルのS-06を開いたあとS-03（会話画面）へ移行しようとすると、サイドバー以外真っ白な画面になり何が起きたか分かりづらい。参加していない旨のメッセージを出してほしい」との指摘を受けた。`ChannelView.tsx`の`channel && !channel.is_member`分岐（1.5版で追加した非参加admin向けの早期return）を、無言の`return null`から「🔒 {チャンネル名} には参加していません」という見出し・説明文・「⚙ チャンネル設定を開く」という戻り導線（`/channels/{id}/settings`へのLink）を表示するUIに差し替えた。この変更は`channel.is_member=false`の分岐だけが対象で、その1つ上にある`channelError`（404/403、F-34「存在を伏せる」対象の未参加者向け無言リダイレクト）には手を加えていない——この分岐に来る時点でA-06自体は既に成功しておりチャンネルの存在・名称はシステムadminへ開示済み（S-08のリンクを踏んだ以上、伏せる理由が無い）なので、両者を区別する判断基準は前回（1.5版）の設計判断のとおり。Playwrightで実際に、S-06を開いてから「← {チャンネル名} に戻る」リンクでS-03へ遷移するという指摘どおりの操作手順を再現し、メッセージが表示されること、「チャンネル設定を開く」リンクでS-06へ戻れること、consoleエラー0件（発言一覧等の背景取得が404になる既存の挙動は変更前と同じ）を確認した。テストデータ・検証スクリプトはすべて削除済み。詳細設計書（総論5.9節、v1.6）・docs/index.html（Ver 4.40）を同じコミットで更新した（2026-09-02）。**
- **AI API実機検証**: 千田氏から交付されたOpenAI APIキーをローカル`.env`に設定し（`OPENAI_API_KEY`は本人が直接編集、チャットへの貼り付けはさせていない。`.gitignore`済み・`git log --all --full-history -- .env`でコミット履歴に一度も含まれていないことを確認済み）、これまで「実際のOpenAI API呼び出しは未検証」としていたAIサポート機能（チャットAIメンション応答、F-14要約、F-12スキル、F-15反応モード、F-16自動対応範囲）を初めて実機で検証した。**`AI_MODEL`は`gpt-5-nano`を採用**（千田氏から指定モデルは無いとの回答だったため、2026-09-02時点でOpenAI公式単価ページ（developers.openai.com/api/docs/pricing）で確認できる最安のテキスト生成モデルをローカル動作確認用に選定。$0.05/$0.40（1Mトークン、入力/出力）。本番投入前に品質面で必要なら`gpt-4.1-nano`や`gpt-4o-mini`へ変更する前提でユーザー了承済み）。`services/ai_client.py`の`MODEL_COSTS`に`gpt-5-nano`の単価行を追加した（既存の`gpt-4o-mini`行と同じ円/USD換算レートで円/1000トークンへ変換。未知モデルは既定モデルの単価で概算する既存仕様のため、単価行の追加を忘れると誤ったコストが表示され続ける）。**実機検証で初めて顕在化する実装バグを2件発見・修正した**（いずれもAPIキー未設定時のcurl検証では検出できない性質のバグ）: (1) `ai_agent.py`が全チャット補完呼び出しにハードコードした`TEMPERATURE = 0.5`を渡していたが、`gpt-5-nano`のようなreasoning系モデルは既定値(1)以外の`temperature`を一切サポートせず`openai.BadRequestError: Unsupported value`になることが判明したため、`temperature`パラメータ自体を削除しAPIの既定値に委ねるよう修正した（`AI_MODEL`を環境変数だけで自由に差し替えられる設計上、モデルごとに対応が割れるパラメータは指定しないほうが頑健という判断）。(2) reasoning系モデルは`reasoning_effort`を指定しないと内部の思考に`max_completion_tokens`を使い切ってしまい、応答本文が空のまま`finish_reason='length'`で返る（`reasoning_tokens`として消費済みのため課金は発生する）ことを、生のAPIレスポンスを直接調査して特定した。`reasoning_effort='minimal'`の指定で解決する一方、非reasoningモデル（`gpt-4o-mini`で確認）に同じパラメータを渡すと逆に「Unrecognized request argument」で400エラーになるため、新規`ai_client.REASONING_MODELS`／`is_reasoning_model()`と、`ai_agent._completion_extra_kwargs()`（reasoning系モデルのときだけ`reasoning_effort='minimal'`を付与する小さなヘルパー、`_generate_and_post`・`_generate_summary_and_post`の2箇所で共有）を追加し、モデル種別に応じて条件付きで付与するようにした。修正後、使い捨てのテストチャンネル（`ai-verify-test`）上で以下5点を実際のOpenAI API呼び出しで確認した: チャットAIメンション応答（応答本文の生成・`generation_status`の`generating`→解除・T-13への使用量/コスト記録）、F-14要約（チャンネル本体の直近発言を要点箇条書きで要約）、F-12スキル遵守（登録した指示文の必須文言を応答に一字一句含める）、F-16自動対応範囲（`response_level='human'`のカテゴリに一致する依頼には直接回答せず、設定した引き継ぎ先の氏名を案内する）、F-15反応モード`proactive`（「@AI」の明示的な言及が無い発言にも自発的に応答する）。検証後、テストチャンネルごと削除した（CASCADEでスキル・発言・T-13記録も連動削除）。層2ドキュメントQ&A（OpenAI Embeddings API）はこのスライスの対象外のまま（Drive OAuthスコープ拡張が別途必要なため）。今回の修正はモデル対応パラメータの実装バグ修正のみで設計判断・API仕様に変更は無いため、設計文書の改訂は無い（2026-09-02）。
- チャットAIメンション応答（`ai_agent.py`の`_generate_and_post`）が参照する会話履歴の件数を`MAX_HISTORY_MESSAGES`で200→20件に縮小した。ユーザーから、共有された「AI API編」手順書の「直近だけ送る：『最新の10往復まで』と決めて、古い分は送らない。いちばん簡単で、まずはこれで十分」という指針との乖離を指摘され、この基準（human+aiあわせて概ね20件。bot発言混在のため厳密な往復数ではない）に合わせて縮小した。直前のAI API実機検証でreasoning系モデル（`gpt-5-nano`）が`reasoning_tokens`を消費しやすいことが判明したばかりで、送信するトークン量自体を抑える意義も大きいと判断し即対応した。F-14要約（`MAX_SUMMARY_CHANNEL_MESSAGES = 100`）はチャンネル全体を俯瞰する別用途のため対象外（`start_summary`は明示的なボタン操作であり毎回発生する会話履歴とは性質が異なる）。設計判断・API仕様に変更は無いため設計文書の改訂は無い（2026-09-02）。
- AIメンション（本文中の「@ペルソナ名」）をF-41の人間宛メンションと同じ見た目（青背景・太字、画面モックアップS-03の`.mention`）でハイライト表示するようにした。ユーザーからの明示的な要望による追加。AIメンションはF-41と異なりID参照化されずmessage_blocks（T-07）に残らない（基本設計書5.22節の設計判断、`services/ai_agent.detect_mention`と同じ本文中の文字列一致のみ）ため、`MessageList.tsx`の`renderMessageBody`にAIメンション検出を追加した: 構造化された`blocks`由来のマッチに加えて、新規引数`aiPersonaName`が渡されていれば本文中の「@{aiPersonaName}」を直接検索し、同じ`bg-accent-100 text-accent-700`のスタイルで両者をマージして描画する。`aiPersonaName`はチャンネルAIのペルソナ名（既定"AI"）で、`ChannelView.tsx`→`MessageList`・`ThreadPanel`（チャンネルのスレッドのみ、DMのスレッドでは渡さない）の2箇所に配線した。**取得元はA-23ではなく新規にA-06（`GET /api/channels/{id}`）のレスポンスへ`ai_persona_name`を追加する形にした**（A-23は`require_channel_admin`でchadmin/admin限定のため、会話画面を開く全参加者からは呼べない。A-06は元々`require_channel_member_or_admin`で参加者全員が呼べる上、`channel_ai_settings`は必ずA-07作成時に既定行を持つため`persona_name`を単純にSELECTするだけで済んだ）。DM会話（DmView.tsx）は元々AIが存在しない（AI応答はチャンネルのみ対応）ため`aiPersonaName`を渡していない。**実装中に別の既存バグを発見・修正した**: `_message_out`（`channels.py`・`messages.py`・`dms.py`の3箇所）の`sender_name`解決が`sender_type == "bot"`のときだけ`bot_display_name`を使う分岐になっており、AI発言（`sender_type == "ai"`）が抜けていたため、AI発言のヘッダー表示名が常にnull（フロントは「(不明)」にフォールバック）になっていた。AI発言も同じ`bot_display_name`列にペルソナ名をスナップショットしているため（`services/ai_agent.py`）、3箇所とも`row["sender_type"] in ("bot", "ai")`に修正した。curlで、A-06が`ai_persona_name`を一般memberからも取得できること、AI発言の`sender_name`が正しくペルソナ名（既定"AI"）になることを確認したうえで、Playwrightで実際にブラウザ操作を確認: 「@AI」を含む発言を投稿→本文中の「@AI」が青背景でハイライトされること、AIの返信ヘッダーが「(不明)」ではなく「AI」と表示されること、をスクリーンショットで確認した（consoleエラーは無関係な既存のfavicon.ico 404のみ、`vite`開発サーバーの既知の無害な挙動で今回の変更とは無関係）。テストチャンネル・検証スクリプトはすべて削除済み。新規API・DB変更は無い（既存のA-06レスポンスへのフィールド追加のみ）ため設計文書側の改訂は無い（2026-09-02）。
- チャンネルAIの既定のペルソナ名（T-08 persona_name）を「AI」から「Kogack AI」に変更した。ユーザーからの明示的な要望による追加。画面モックアップ（S-06「キャラクタ」タブの名前欄、S-03の発言例）は元々`value="Kogack AI"`・`@Kogack AI`を使っており、DB既定値・バックエンドのフォールバック文字列側がこれに追従していなかった抜けだった（画面モックアップ自体の変更は無い）。`database.py`の`CREATE TABLE`の`DEFAULT`を変更し、既存DBにも反映されるよう`ALTER TABLE channel_ai_settings ALTER COLUMN persona_name SET DEFAULT 'Kogack AI'`を追記（起動時に毎回実行される安全なスキーマ変更。既存行のデータ自体は書き換えない）。既存行（当時10チャンネル、いずれも旧既定値のまま未カスタマイズ）は一度きりの手動SQL（`UPDATE channel_ai_settings SET persona_name = 'Kogack AI' WHERE persona_name = 'AI'`）でバックフィルし、database.py側には埋め込まなかった（スキーマのADD COLUMN IF NOT EXISTSと異なり、値の上書きを起動のたびに繰り返すとチャンネル管理者が将来"AI"という文字列に意図的に戻した場合を巻き戻してしまうため）。`services/ai_agent.py`の5箇所・`routers/channels.py`のA-06フォールバック・`ChannelSettings.tsx`の2箇所のフォールバック文字列も同様に変更した。アイコン未設定時の2文字アバター（Avatar/`char-avatar`の"AI"表示）は persona_name とは独立した固定の略称表示のため対象外（画面モックアップ・実装とも変更なし）。curlで、既存チャンネルのA-06が`ai_persona_name: "Kogack AI"`を返すこと・新規チャンネル作成でも同じ既定値が入ることを確認し、Playwrightで実際に「@Kogack AI」を含む発言を投稿→本文中の該当箇所が青背景でハイライトされること・AIの返信ヘッダーが「Kogack AI」と表示されること・実際に応答が生成されることをスクリーンショットで確認した。テストチャンネル・検証スクリプトはすべて削除済み。詳細設計書DB設計（T-08、v1.11）・API設計（A-23レスポンス例、v1.29）・docs/index.html（Ver 4.41）を同じコミットで更新した。要件定義書・基本設計書・画面モックアップは変更なし（2026-09-02）。
- F-41のメンション候補一覧（「@」ボタン押下・本文への直接入力の両方）にチャンネルAIを含めるようにした。ユーザーからの明示的な要望による追加で、AIサポート基盤スライスから既知のスコープ外だった「F-41のメンションピッカーの候補にチャンネルAIを含めていない」を解消した。画面モックアップS-03のメンションポップオーバーは元々候補の先頭に専用アイコン付きで「Kogack AI」を表示しており（`mp-hint`「当該チャンネルの参加者＋チャンネルAI」）、これに合わせた。A-06（`GET /api/channels/{id}`）のレスポンスへ新規`ai_is_enabled`を追加した（前回追加した`ai_persona_name`と同様、A-23と異なり参加者全員が呼べるA-06から軽量に取得する）。`ChannelView.tsx`で`channel.ai_is_enabled`のときだけ`mentionCandidatesWithAi`の先頭にAIの合成候補（`{id: 'ai', name: persona_name, isAi: true}`）を追加してComposerへ渡す（チャンネルAIが無効なチャンネルでは「@ペルソナ名」と書いてもAIは応答しないため候補にも出さない）。`Composer.tsx`の`MentionCandidate`に`isAi`フラグを追加し、(1) 候補一覧の描画でAI行だけ`MessageList`のAvatarと同じ角丸四角のグラデーションアイコンにする、(2) `selectCandidate`でAI選択時は本文への「@ペルソナ名 」挿入のみ行いmentions配列（T-07構造化メンション）には追加しない（基本設計書5.22節の設計判断どおり、AIメンションはID参照化しない）、の2点を分岐した。スレッド返信（ThreadPanel経由のComposer）には配線していない（A-14内の@AI検知は既存スコープ外のまま、基本設計書8章）。curlでA-06の`ai_is_enabled`がAI有効/無効の両チャンネルで正しく返ることを確認し、Playwrightで実際のブラウザ操作を確認: 「@」ボタン押下・本文への直接入力のいずれでも候補先頭にAI専用アイコン付きで表示されること、選択して送信した発言の`blocks`が空（構造化メンションを作らない）でありながら実際にAIが応答すること、AI無効チャンネルでは候補に出ないこと（0件）。テストチャンネル・検証スクリプトはすべて削除済み。新規API・DB変更は無い（既存A-06への1フィールド追加のみ）ため設計文書側の改訂は無い（2026-09-02）。
- 投稿欄（Composer）で入力中の本文も、選択済みメンション（人間・チャンネルAIいずれも）を青くハイライト表示するようにした。ユーザーからの明示的な要望による追加。`<textarea>`自体は部分的な文字装飾ができないため、`MessageList`/`renderMessageBody`同様の「投稿後の表示」ではなく、業界で広く使われる透明textarea＋背後オーバーレイ方式（react-mentions等と同じ技法）を採用した: `Composer.tsx`のtextareaを`bg-transparent text-transparent caret-ink`にして文字自体を不可視化し、同じ位置に`position:absolute inset-0`で重ねた`aria-hidden`なdiv（`pointer-events-none`）に同じ本文をハイライト付きで描画する。**両者のフォントサイズ・行間・パディング・折り返し（`whitespace-pre-wrap break-words`）を完全に一致させないと文字がずれるため、ハイライト部分にも背景色・文字色以外（padding・font-weightなど、グリフの幅や位置に影響する装飾）は一切加えていない**（投稿後の表示side chip（`rounded px-1 font-semibold`）とはあえて意匠を分けた）。新規`findMentionHighlights(text, mentions, aiPersonaName)`が、選択済みmentions state（`display_name_snapshot`）とAIメンション（`@{aiPersonaName}`の文字列一致、複数出現にも対応）の両方から本文中の該当範囲を検出する（本文から手で消された分は`indexOf`が見つからず自然にハイライトが外れる、既存の`activeMentionsIn`と同じ考え方）。オートリサイズで動的に高さが変わる既存のuseEffect・スクロール（10行超で内部スクロールする既存挙動）にも対応するため、textareaの`onScroll`でオーバーレイ側の`scrollTop`を同期させている。新規`aiPersonaName`propを`Composer`に追加し、`ChannelView.tsx`・`ThreadPanel.tsx`（スレッド返信、AIは実際には応答しないが表示の一貫性のため同じくハイライトする）から`channel.ai_persona_name`を渡した（DM会話は元々AIが無いため対象外）。Playwrightで実際のブラウザ操作を確認: 「@」ボタン・本文への直接入力いずれからも人間・AIそれぞれの選択後に即座に青くハイライトされること、本文を選択し直して置き換える（＝メンション文字列を削除する）とハイライトが消えること、10行相当の長文・複数回出現するAIメンション・実際にtextareaが内部スクロールする状態（画面外の行を含む）のいずれでもハイライト位置が実際の文字位置とずれないこと、をスクリーンショットで確認した。検証中に誤って実チャンネル（雑談）へテスト用の発言を複数回送信してしまった（Playwrightの`.type()`が本文中の改行文字をEnterキー入力として送出し、このアプリの「Shift無しEnter＝即送信」という既存仕様どおり毎回送信されてしまったため）が、直後にAPIで全件削除して原状回復した（AI応答分はsender_user_idを持たないため投稿者本人判定に一致せず、削除にはadmin権限が必要だったことも確認した）。新規API・DB変更は無いフロントエンドのみの変更のため、設計文書側の改訂は無い（2026-09-02）。
- Composer.tsxの`detectMentionQuery`（F-41）から「直前の文字が空白または本文の先頭でなければメンション開始とみなさない」という制限を外した。ユーザーからの明示的な要望で、「こんにちは@」のように単語の途中・直前に空白が無い位置で「@」を入力した場合でも、その場でメンション候補一覧を表示するようにした（従来は直前に半角/全角スペースを挟むか本文冒頭でなければ候補が出なかった）。「@」直後からカーソルまでに空白・改行を含む場合は候補を閉じる既存の判定はそのまま残しており、クエリの区切り方自体は変更していない。挿入・ハイライト（`selectCandidate`・`findMentionHighlights`）はいずれも「@」の直前文字に依存しない実装だったため変更不要だった。横断検索（S-05）のモディファイア入力補助（`SearchView.tsx`の`detectModifierQuery`）はF-41と同じ考え方で作られた別実装だが、今回のユーザーからの要望はメッセージ投稿欄限定のため対象外とした（検索欄の`in:`/`from:`等はそもそも文中に埋め込む用途を想定しておらず、様子は異なる）。Playwrightで実際のブラウザ操作を確認: 「こんにちは@」の直後に候補が表示されること、選択すると「こんにちは@Kogack AI 」に正しく挿入されハイライトも即座に効くこと、consoleエラーは無関係な既存のfavicon.ico 404のみ。新規API・DB変更は無いフロントエンドのみの変更のため、設計文書側の改訂は無い（2026-09-02）。
- バグ修正: 横断検索（S-05、A-20）でAI・BOT発言がヒットした際、発言者名が常に「(不明)」と表示されていた。会話ログ・DM一覧のアイコン反映漏れ（2026-08-27）・S-08利用者管理のアイコン反映漏れ（2026-08-28）と同種の「AI/BOT発言はsender_user_idが無いため素朴なJOINだけでは発言者名が解決できない」問題で、`_message_out`（channels.py等）は既に`sender_type in ("bot","ai")`のとき`bot_display_name`を使う分岐を持っていたが、`routers/search.py`のメッセージ検索・ファイル検索の2つのSELECTには元々この分岐が無く、常に`u.name`（sender_user_idへのJOIN、AI/BOT発言では必ずNULL）をそのまま返していたのが原因。両SELECTに`m.sender_type, m.bot_display_name`を追加し、レスポンス整形時に`_message_out`と同じ`bot_display_name if sender_type in ("bot","ai") else sender_name`の分岐を適用した（ファイル検索側は実際には現状BOT/AI発言に添付ファイルが付くことは無いが、他と分岐を揃えるため同様に修正）。テスト用チャンネルで実際にAIへメンション→応答を待ってから検索し、`sender_display_name`が「Kogack AI」になることをcurl・Playwrightの両方で確認した。テストデータ・検証スクリプトはすべて削除済み。ユーザーからの報告を受けての修正で、要件定義書上のF-xxに対応付けられた変更ではないため要件定義書の改訂は無い。新規API・DB変更も無い（2026-09-02）。
- 横断検索（S-05、A-20）の結果一覧で、ヒットした検索語をオレンジ色でハイライト表示するようにした。ユーザーからの明示的な要望による追加。フロント側は`routers/search.py`の`_parse_terms`と同じ規則（`"…"`はフレーズのまま1語、それ以外は空白区切り）をミラーした新規`parseHighlightTerms`で`query.q`（検索欄のフリーテキスト部分）を語に分解し、新規`highlightTerms`がexcerpt・file_nameの両方から大文字小文字を区別しない一致で該当範囲を`<mark>`要素（既存の`bot-bg`/`bot-text`トークンを流用、新規CSS変数は追加せず。@メンションハイライトが`accent`トークンを流用しているのと同じ考え方）で囲む。複数の検索語が重なってヒットする場合は範囲をマージし二重に囲まないようにした。**実装中に、バックエンドのexcerpt生成（`_excerpt`、80文字の抜粋）に既存のバグを発見・修正した**: 検索語の一致開始位置が80文字以内でも、語自体が80文字目をまたいで終わる場合、従来の単純な先頭80文字切り出しでは語の途中で切れてしまい（例:「zzhighlightprobe」が「zz」で切れる）、この状態のままだとハイライトも中途半端にしか効かなかった。`_excerpt`に`terms`引数を追加し、素朴な先頭切り出しで検索語の終端まで収まるかを判定するよう修正（`match_start`だけでなく`match_end`もlimit以内かを見る）、収まらない場合は最初にヒットした検索語を中心に前後を「…」で省略したスニペットを切り出すようにした（Google検索結果のスニペットと同じ発想）。curl・Playwrightで実際に確認: 短いメッセージ・80文字を超える長いメッセージ（検索語がちょうど末尾付近にまたがるケースを含む）・添付ファイル名のいずれでも検索語が省略されずオレンジでハイライトされること、複数件ヒットした場合もそれぞれ正しくハイライトされること。テストデータ・検証スクリプトはすべて削除済み。新規API・DB変更は無い（既存の`excerpt`文字列の生成ロジック改善のみ）ため設計文書側の改訂は無い（2026-09-02）。
- バグ修正: F-41メンション候補一覧（Composer.tsx）のアイコンが、常に色付き頭文字（人間）またはAIグラデーション表示（チャンネルAI）に固定されており、実際に発言したときのアイコン（プロフィール画像・AIペルソナアイコン）と一致していなかった。ユーザーからの報告を受けて修正。会話ログ・DM一覧・S-08利用者管理で過去に繰り返し発生したのと同種の「画像優先→フォールバック」の分岐漏れで、`MentionCandidate`型に`picture_url`が無く候補一覧側の描画がこれを一切見ていなかったのが原因。`MentionCandidate`に`picture_url`を追加し、`MessageList.tsx`の`Avatar`と同じ優先順位（画像があれば`<img>`、無ければ人間は色付き頭文字・AIはグラデーション「AI」表示、AIの画像はAvatarと同じ角丸四角・人間の画像は円形）で描画するよう修正した。チャンネル参加者（`ChannelMember`）は元々`picture_url`を持っていたため`ChannelView.tsx`側の変更は不要だったが、チャンネルAIの合成候補（`{id:'ai', ...}`）にはアイコン情報が無かったため、新規A-06レスポンスフィールド`ai_persona_icon_url`（`channel_ai_settings.persona_icon_url`、A-23と異なり参加者全員が取得できる）を追加し、`mentionCandidatesWithAi`から渡すようにした。curlでA-06が`ai_persona_icon_url`を返すことを確認し、Playwrightで実際のブラウザ操作を確認: 「@」入力で開く候補一覧の各行の`<img>`が、会話ログのAvatarと同じURL（ローカルアップロード画像・Googleプロフィール画像の両方）を指し、`naturalWidth`から実際に画像として読み込めていることをDOM直接評価で確認した。テストデータ・検証スクリプトはすべて削除済み。新規API・DB変更は無い（既存A-06レスポンスへのフィールド追加のみ）ため設計文書側の改訂は無い（2026-09-02）。
- 開発用ログイン（dev-login/dev-users）のユーザーを1名追加した。ユーザーからの明示的な要望による追加。`backend/seed.py`は「無ければ作る」形の追記式（既存3ユーザーと同じパターン）のため、既存コードと同じ形で`takahashi@kogasoftware.com`（高橋 健太、role='member'）を追記した。dev-users一覧（`GET /api/auth/dev-users`）はDBの`users`テーブルを`is_active=true`で単純にSELECTするだけの実装（ハードコードされた候補リストではない）ため、`seed.py`にユーザーを追記するのがこの機能を拡張する唯一の方法（新規APIやハードコードリストは不要）。ローカルの稼働中DBに対して実際に`seed.py`を実行し、dev-login・`/api/auth/me`の両方で実際にログインできることをcurlで確認し、Playwrightで実際にS-01ログイン画面の開発用ログイン一覧に「高橋 健太」が表示されることを確認した。新規API・DB変更は無い（既存の`users`テーブルへの1行追加のみ）ため設計文書側の改訂は無い（`start.bat`経由で`seed.py`が自動実行されるため、次回以降の新規DB作成時にも自動的に投入される）（2026-09-02）。
- バグ修正: アイコン画像未設定ユーザーの色付き頭文字フォールバックが、画面によって色がバラバラだった。ユーザーからの報告を受けて調査したところ、発言者（`MessageList.tsx`）・メンション候補（`Composer.tsx`）・管理コンソール（`AdminConsole.tsx`）等9箇所は既に共通の`avatarColorFor(id)`（`lib/avatarColor.ts`、idのハッシュ値から固定色を決める）で統一されていたが、サイドバー左下の自分のプロフィール表示（`Layout.tsx`、クリックで「プロフィールを編集」モーダルを開くボタン）とそのモーダル自身の大アイコン（`ProfileEditModal.tsx`）の2箇所だけが、`avatarColorFor`を使わず全ユーザー共通の固定色（`bg-[#cbd5f5]`＋`text-accent-700`）にハードコードされていたのが原因。この2箇所を他と同じ`avatarColorFor(me.id)`＋`text-white`に統一した。アイコン画像未設定のテストユーザー（`takahashi@kogasoftware.com`）で実際に確認: サイドバー左下・プロフィール編集モーダル・発言時のアイコン・別ユーザーから見たメンション候補一覧・管理コンソールの利用者管理タブの5箇所すべてで同じ色（黄色、`avatarColorFor('5')`の結果）になることをスクリーンショット・`getComputedStyle`の直接評価で確認した。テストデータ（投稿した確認用発言・チャンネル参加）はすべて削除済み。フロントエンドのみの変更でバックエンドAPI・DBは無変更のため設計文書側の改訂は無い（2026-09-03）。
- `MessageList.tsx`の`Avatar`が、AIペルソナアイコン画像を設定済みのチャンネルAI発言でも人間と同じ円形（`rounded-full`）で表示しており、実在の人物と見分けがつかないというユーザーからの指摘を受けて修正した。BOT発言（F-36定期投稿・F-38トリガー・F-43システム通知）は画像有無に関わらず常に角丸四角（`rounded-[9px]`）で人間の円形と区別しているのに対し、AI発言は「画像があれば`sender_picture_url`優先で円形描画」という種別を問わない共通分岐に先に引っかかっていたため、未設定時のみ角丸四角の「AI」グラデーション表示にフォールバックする構造になっていた（当初の設計判断は基本設計書5.22節「AIメンションはID参照化しない」とは無関係の、このAvatarコンポーネント固有の実装順序の問題）。BOT分岐の直後に`sender_type === 'ai'`の専用分岐を追加し、BOTと同じ「角丸四角の枠内で画像→フォールバック表示の優先順位」構造（画像があれば`<img>`、無ければ「AI」のグラデーション表示）に変更した。これにより人間の円形とBOT・AIの角丸四角という2形状に整理され、画像設定の有無に関わらずAI・BOT発言だと一目で判別できるようになった。`ThreadPanel.tsx`は同じ`Avatar`を再利用しているため追加対応不要。`Composer.tsx`のメンション候補一覧は2026-09-02の時点で既にAI候補を角丸四角（`rounded-[8px]`）で描画済みだったため対象外。A-61でアップロードしたテスト画像をチャンネルAIのペルソナアイコンに設定し、実際にメンション応答を生成させて`getComputedStyle`で角丸四角（`border-radius: 9px`）になっていることを直接確認した。テストチャンネル・アイコンはすべて削除済み。フロントエンドのみの変更でバックエンドAPI・DBは無変更のため設計文書側の改訂は無い（2026-09-03）。
- F-14要約（要約ボタン）で生成された発言が、会話上で要約機能により作成されたものだと分かるようにした。ユーザーからの明示的な要望による追加。通常のAIメンション応答と要約結果はいずれも`sender_type='ai'`・`thread_parent_id`もチャンネル本体では同じくNULLになるため、既存の列だけでは構造上区別できない（チャンネル本体の投稿として要約するケース）ことが分かったため、新規`messages.is_summary`列（BOOLEAN、既定false）を追加した。`services/ai_agent.start_summary`の生成中プレースホルダINSERT時のみ`true`を設定する（実際の要約文が確定する`_generate_summary_and_post`のUPDATEは`body`/`generation_status`のみ更新するため、この列の値はそのまま引き継がれる）。`channels.py`・`messages.py`・`dms.py`の3つの`_message_out`すべてにレスポンスフィールドとして追加した（DMはF-14がチャンネル専用APIのため実際には常にfalseだが、他2ルーターと同じ分岐に揃えた）。フロントは`Message`型に`is_summary?: boolean`を追加し、`MessageList.tsx`の「AI」バッジの隣に「📝 要約」バッジ（ChannelView/ThreadPanelの要約ボタンと同じ絵文字・配色）を条件表示するようにした。curlで実際に要約を生成させ`is_summary: true`が返ることを確認し、Playwrightで実際に「📝 要約」バッジが会話ログに表示されることを確認した。テストデータはすべて削除済み。詳細設計書DB設計（T-05、v1.12）・docs/index.html（Ver 4.42）を同じコミットで更新した。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書・基本設計書・画面モックアップの改訂は無い（2026-09-03）。
- バグ修正: Googleの同意画面で「キャンセル」を選択すると、理由不明のまま何度試してもログインできなくなる不具合を修正した。層2ドキュメントQ&Aに向けたDrive OAuthスコープ拡張の実機検証（一時的にスコープへ`drive.readonly`を追加して実際にログインフローを試した）を進める中でユーザーから提起された懸念で、実際にコードを確認したところ現実に存在する不具合と判明した。従来`routers/auth.py`のA-02コールバックは、Googleからの`code`が無い場合を一律`state`不一致と同じ扱いにして`oauth_failed`（「ログインに失敗しました。もう一度お試しください。」）へ誘導していたが、Googleは同意画面拒否時に`code`の代わりに`error=access_denied`を返すため、この場合も理由を伝えないまま同じ汎用エラーになっていた。スコープにDrive等の追加権限を含めると実際に同意画面で「キャンセル」を押す選択肢が生まれるため、拒否した利用者はチャット機能自体にもログインできないまま原因不明で詰まる状態になる（ログイン権限とDrive権限が同一リクエストにまとめられているため、部分的な許可という選択肢が無い）。コールバックに`error`クエリパラメータを新たに受け取り、`error=access_denied`かつ`state`がCookie値と一致した場合（改ざんされた偽のエラー遷移を弾くため、一致しない場合は従来どおり`oauth_failed`のまま）のみ、新規エラー種別`consent_denied`（「Googleへのアクセス許可が完了しなかったため、ログインできませんでした。もう一度『Googleでログイン』を押し、表示された内容を『許可』してください。」）へ誘導するようにした。curlでGoogleからの3種類の応答（`error=access_denied`＋state一致→`consent_denied`、`error=access_denied`＋state不一致のなりすまし試行→従来どおり`oauth_failed`、`error=invalid_scope`等の他のエラー値→`oauth_failed`）を再現して確認し、Playwrightで実際に表示されるメッセージも確認した。要件定義書上のF-xxに対応付けられた変更ではないため要件定義書の改訂は無い。詳細設計書（総論7.2節、v1.7）・docs/index.html（Ver 4.43）を同じコミットで更新した（2026-09-03）。
- **未実装**: 層2ドキュメントQ&Aの索引・AI検索（Drive同期・埋め込みベクトル生成・search_documentsツール・回答根拠の引用表示。Drive OAuthスコープ拡張とOpenAI Embeddings APIの実通信検証が前提）、実行前確認・座席予約連携（上記AIサポート基盤の「スコープ外」参照。スキル・反応モード・自動対応範囲分類はF-12・F-15・F-16実装により対象外リストから外れた）、AI利用コストの80%到達時の通知メール送信・上限到達時の応答停止（上限到達時の挙動は千田氏との別途協議事項）、Docker/Koyebデプロイ設定。主要画面（S-01〜S-06, S-08）はひととおり縦切りを終え、S-05・S-08はいずれも設計どおりの機能をひととおり実装済み。AIサポート・F-36/F-38・F-07・層2の第一歩・F-42・F-14・F-29・F-12・F-15・F-16にも着手したため、次は層2の索引・AI検索本体（Drive OAuthスコープ拡張・OpenAI Embeddings APIの実通信検証が前提）に取り組むか、実行前確認（F-25）・座席予約参照連携に着手するかをユーザーと相談する。
- **Keirekiからの意図的な相違点**（実装時に必ず踏襲すること。Keirekiのコードを安易にコピーしない）:
  - ロールは`member`/`admin`の2値のみ（Keirekiは`member/viewer/admin`の3値）。chadminは`T-03.is_channel_admin`で別管理し、`users.role`には持たせない。
  - Google再ログイン時、既存ユーザーの`name`/`picture_url`は**上書きしない**（F-39。初回登録時のみ設定、以後は本人がA-62で変更するまで保持）。Keirekiの`callback()`は毎回上書きするが、Kogackではその実装をコピーしない。
  - ログインエラーのコードは`domain_not_allowed`/`account_disabled`/`oauth_failed`の3種（詳細設計書 総論7.2節）。Keirekiの`domain/inactive/forbidden/invalid_request`とは名称が異なる。
  - ポート既定値をKeirekiとずらしている（同時起動時の衝突回避）: `DB_PORT=55433`（Keirekiは55432）、`BACKEND_PORT=8011`（同8010）、`FRONTEND_PORT=5174`（同5173）。
  - 使用するAIモデルは環境変数`AI_MODEL`のみで決定し、Keirekiのような管理画面上でのモデル切替機能・`app_settings`テーブルは設けない（詳細設計書AIサポート10.6節）。コスト単価表（`services/ai_client.py`）も円/1000トークン単位で保持する（Keirekiの`ai_client.py`はUSD/1Mトークン単位）。
- 開発用ログイン（`/api/auth/dev-login`・`/api/auth/dev-users`）はKogackの正式な設計書（S-01の仕様）には存在しない、ローカル開発専用の便宜機能（Keirekiの規約を踏襲）。本番では`APP_ENV=production`で常に無効。
- 新しい画面・APIを実装したら、この節と（該当すれば）詳細設計書・基本設計書・画面モックアップを同じコミットで更新すること。

## 開発研修としての位置づけ

新人社員向けAI開発研修の成果物であり、`docs/index.html` の「読み進め方」に研修用の読み順ガイドがある: 要求仕様書 → 要件定義書 → 画面モックアップ → 基本設計書 → 詳細設計書（総論＋4分冊） → 操作マニュアル（未着手）。各ドキュメントはID体系での上流〜下流のトレーサビリティを確認しながら読むことを意図している。
